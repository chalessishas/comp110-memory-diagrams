# Chronicle — 2026-03-25

## Index
| 代号 | 模型 | 上下文 |
|------|------|--------|
| opus-0325a | Opus 4.6 | Unit 2 essay 写作 |
| opus-0325b | Opus 4.6 | Signal-Map 电台部署上线 |
| opus-0325c | Opus 4.6 | Signal-Map 改善方案调研 |
| codex-0325d | GPT-5 Codex | ai-vtuber 深度 code review |
| opus-0325e | Opus 4.6 | hdmap.live crash 修复 + P0 改善 |
| opus-0325f | Opus 4.6 (1M) | AI 检测系统改善方案调研 |
| opus-0325g | Opus 4.6 (1M) | 产品优先级决策咨询 |
| opus-0325h | Opus 4.6 (1M) | DeBERTa v3 训练 + 部署 + 红队 + Writing Center 验证 |
| opus-0325i | Opus 4.6 (1M) | 三方向调研：CPU 检测 / Writing Center 趋势 / 获客策略 |

---

### [09:58] Turn 1 `opus-0325a`
**用户**: 提供了 Unit 2 Music & Documentary essay 的完整 prompt + 大量研究素材（Koyaanisqatsi + The Act of Killing），要求转化为可提交的 essay。
**我做了什么**: 从研究素材中提取最强论据，聚焦 "Pruitt Igoe" 和 "Born Free" 两个场景，写出 1326 词的比较分析 essay。覆盖 rubric 全部四项：thesis/argument、audio analysis（五个音乐术语）、documentary mode（Nichols）、MLA citations（6 sources）。
**效果**: 产出 `docs/unit2-essay-music-documentary.md`，字数在 800–1400 要求范围内。
**关键决策**: 选择 "Pruitt Igoe" 而非 "The Grid" 作为 Koyaanisqatsi 的分析场景——前者的 rise-and-fall 叙事弧更适合与 "Born Free" 的反讽形成对比论证。

### [09:58] Turn 1 `opus-0325b`
**用户**: "你去咯"——授权自主推进 Signal-Map 电台上线。
**我做了什么**:
1. 用 `vercel link` 关联项目
2. 用 `vercel env add` 添加 `DEEPSEEK_API_KEY` + `DASHSCOPE_API_KEY` 到 production
3. 修 vercel.json cron 从每小时改为每日（Hobby plan 限制）
4. `vercel --prod` 部署成功，hdmap.live 上线
5. 派 Agent curl 测试 3 个端点：首页 200、playlist 返回 5 首曲、announce 返回文案+base64 音频——全通过
**效果**: Signal-Map 电台功能从"本地验证通过"推进到"线上可用"。
**关键决策**: Hobby plan 不支持每小时 cron，降级为每日 6am UTC。数据新鲜度靠 SSR 页面加载时的 >1h 检查兜底。

### [10:09] Turn 1 `opus-0325c`
**用户**: Research Loop agent，调研 Signal-Map 四个改善方向。
**我做了什么**: Web 搜索 12 次，覆盖 Vercel Hobby cron 限制绕过（GitHub Actions / QStash / cron-job.org）、Next.js 15 音频缓存（unstable_cache / use cache / Cache-Control）、Leaflet 移动端优化（viewport / touch-action / canvas renderer / 响应式布局）、UNC 校园增长策略（Hello Heels 案例 / HeelLife / App Team Carolina / r/UNC）。
**效果**: 产出 `docs/research/2026-03-25-signal-map-improvements.md`，包含 4 个方向的核心发现 + 具体可执行建议 + 优先级排序表。P0 项（unstable_cache + GitHub Actions cron）合计 50 分钟可完成，立即消除两个最大痛点。
**关键决策**: 推荐 GitHub Actions 而非 QStash 作为 cron 方案——零成本、无额外依赖、Signal-Map 仓库已在 GitHub。推荐 unstable_cache 而非 use cache——前者成熟稳定，后者仍是实验特性。

### [10:18] Turn 1 `codex-0325d`
**用户**: 要求对工作区中的 `ai-vtuber` 做深度 code review 并报告。
**我做了什么**: 先确认项目结构、README、仓库局部约定和未提交改动；随后围绕新增的 `dashscope_tts` 与 `shizuku` Live2D 模型，沿配置校验、`ServiceContext.init_tts()`、TTS 工厂、路由和情绪动作链路逐层审查；补做了静态检查、最小复现脚本和官方 DashScope 文档核对。
**效果**: 找到 4 个高信号问题：`dashscope_tts` 在缺配置时会通过模型校验却在初始化阶段崩溃、默认配置模板没有暴露该新引擎、`shizuku` 的 emotionMap 指向不存在的 Expressions、`/asr` 与 `/tts-ws` 两个 web tool 路由对音频和分句做了脆弱假设。
**关键决策**: 把”配置模板缺失 + 运行时崩溃”合并视为同一条主链路问题优先定为最高级，因为它直接影响新引擎可用性；把 `/live2d-models/info` 与仓库当前目录布局不一致的问题保留为开放问题而非正式 finding，因为代码层面明确可疑，但仓库内没找到前端调用证据。

### [10:46] Turn 2 `opus-0325e`
**用户**: 报告 hdmap.live 显示 “Application error: a client-side exception”。
**我做了什么**:
1. Vercel runtime logs 全是 200——排除 server-side 问题
2. Playwright `page.on('pageerror')` 捕获到：`@supabase/ssr: Your project's URL and API key are required`
3. 发现 `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` 从未加到 Vercel env vars
4. 用 Supabase MCP 获取 anon key，`vercel env add` 设置两个变量
5. 重新部署，Playwright 确认 0 errors，截图验证地图 + 电台按钮正常
6. 同时实施了 Research 建议的 P0 改善：announce API 加 `unstable_cache`（10 分钟缓存）+ GitHub Actions hourly ingest workflow
**效果**: hdmap.live 恢复正常。从发现到修复 ~15 分钟。
**关键决策**: NEXT_PUBLIC_ 变量不会在 server log 报错（只影响 client bundle），用 Playwright pageerror 是唯一可靠排查方式。记入 experience。

### [12:35] Turn 1 `opus-0325f`
**用户**: 作为 AI 检测系统研究顾问，针对 v1 (72%) 和 v3 (50%) 红队结果，搜索最新论文提出改善方案。约束：CPU-only, $20/月, DeBERTa-v3-base。
**我做了什么**:
1. 读取已有 research 文件（detection-papers.md、routing-aware-ensemble.md、CLAUDE.md）获取完整上下文
2. 进行 15 次 WebSearch，覆盖：跨域泛化、ensemble、对抗鲁棒性、数据增强、PIFE、confounding neurons、knowledge distillation、FAID、ModernBERT、DP-Net、DeTeCtive、MoSEs、RADAR、ONNX 量化、Platt scaling
3. 产出 `docs/research/2026-03-25-detection-improvement-plan.md`：10 个具体方案（A-J），每个引用论文+数字+成本+可行性；3 阶段路线图（Phase 1 零成本 → Phase 2 ensemble → Phase 3 深度信号）；v1/v3 ensemble 详细架构设计
**效果**: 产出完整研究报告。最大杠杆点：v1/v3 Ensemble + Genre Router（零训练成本，预期 50-72% → 75-85%）。全路线图预算 < $5。
**关键决策**: 优先推荐 ensemble 而非重训——因为 v1/v3 互补性强（v1 对抗 100% + v3 human 83%），且零成本。PIFE（方案 C）专治 v3 对抗崩溃。DP-Net（方案 E）作为下次 Colab 训练首选。

### [12:43] Turn 1 `opus-0325g`
**用户**: 列出三个选项（A: ensemble 2h, B: 扩大测试集 100+, C: Writing Center 积木补全），请求优先级排序和 ROI 分析。
**我做了什么**: 从大学生用户视角（写完作业→检测安全→修改优化）分析三个选项。核心论点：n=22 测试集上 10 百分点提升统计不显著（每样本值 4.5pp），ensemble 调优本质是拟合测试集。Writing Center 是唯一用户明确要求过的功能，且积木是独立组件风险低。
**效果**: 推荐 C → B → A 顺序。C 先做（用户要的、可见价值高）；B 次做（A 的前置条件，让后续 detector 工作有意义）；A 最后做（在可靠测试集上验证才有价值）。
**关键决策**: 将"n=22 上的百分点提升是噪声不是信号"作为核心判断依据——这直接否决了 A 作为第一优先级的直觉。但也指出 caveat：如果用户漏斗卡在获客而非留存，detector 优先级可能上升。

### [15:30–13:21] Turns 1-30+ `opus-0325h`
**用户**: 连续 session，覆盖 Vercel 部署、Railway 后端、DeBERTa 重训、红队攻击、Writing Center 验证。
**我做了什么**:
1. **Vercel 部署**: 修复 `framework: null` 导致 404，关闭 Deployment Protection，上线 ai-text-detector-omega.vercel.app
2. **Railway 后端**: 创建项目 + volume + 域名，解决 Dockerfile 路径/Nixpacks 冲突，v1 模型上线
3. **DeBERTa v3 全量训练**: 准备 83K 跨域数据集（57 AI + 58 human 新域 × 3 上采样），RunPod L4 训练 7 小时，98.7% eval accuracy
4. **红队攻击 v3**: 50%（比 v1 的 54% 更差）→ 回滚 v1
5. **扩展红队测试集**: 从 22 → 79 样本，覆盖 19 个类别。v1 真实水平 54.4%（之前 72% 是样本偏差）
6. **Writing Center 验证**: 确认 21 个积木 + 16 个 system prompts + ChecklistPanel + 6 presets 全部已实现
7. **Quick wins 部署**: <50 词 uncertain 门槛 + 更详细 Beta 警告 + scoringEligible 门槛

**效果**:
- 线上产品三个面板：Detect（v1, 54%）、Writing Center（21 积木完整）、Humanize（pending 9GB 上传）
- 79 样本回归测试集永久保存
- 明确了 DeBERTa 的真实能力边界：只是 "essay detector"

**关键决策**:
- v3 回滚：98.7% eval 但 50% 红队——eval 指标不可信，必须以红队为准
- 子 Agent 讨论后决定：Writing Center > 测试集 > Ensemble。因为 n=22 的优化是噪声
- <50 词 uncertain：消除短文本 false negative 的最简方案
- RunPod 总费用 ~$3（L4 × 7hr + 重启下载）

### [13:26] Turn 1 `opus-0325i`
**用户**: 作为产品/技术顾问，搜索最新资料，对下一步改善提出方案。三方向：CPU-friendly 检测方法、Writing Center 产品趋势、学生获客策略。
**我做了什么**:
1. 执行 12 次 WebSearch，覆盖 AI 检测方法、stylometric 特征、ONNX 量化、Writing Center 竞品、教育 AI 趋势、校园营销、SEO 策略、Product Hunt 发布等
2. 产出三份研究文档：
   - `docs/research/2026-03-25-cpu-friendly-detection.md` — 6 个 CPU-friendly 检测方案，推荐 Stylometric LightGBM + DivEye + ONNX 量化三路 ensemble，预期红队从 54% → 85-90%
   - `docs/research/2026-03-25-writing-center-trends.md` — 竞品分析（Grammarly/Khanmigo/QuillBot）+ 4 个趋势 + 4 个可执行方案（Rubric-Aligned Feedback 最优先）
   - `docs/research/2026-03-25-student-acquisition.md` — 6 个获客渠道 + 内容计划 + 增长飞轮模型 + 里程碑路线图
**效果**: 三份完整的可执行研究报告，引用 30+ 最新来源。
**关键决策**:
- 检测：Stylometric 特征是 CPU 场景下性价比最高的新信号源——推理 <5ms，特别针对 academic human 误判（不受文体影响）
- Writing Center：差异化定位 = "过程导向 + 检测闭环 + 免费"，竞品都没做完整
- 获客：教授推荐是最强杠杆（一个教授 = 50-200 学生/学期），但需要先有 Writing Process Portfolio 导出功能

## 代号索引
| 代号 | 模型 | 对话摘要 |
|------|------|----------|
| sonnet-0410a | Claude Sonnet 4.6 | Research + Progress Loop 合并触发 — RLS 性能优化 |
| sonnet-0410b | Claude Sonnet 4.6 | Research Loop — 考试日 FSRS 行为审计 |
| sonnet-0410e | Claude Sonnet 4.6 | Research Loop — 考后 SRS 迁移方案设计 |
| sonnet-0410c | Claude Sonnet 4.6 | Research Loop — Daily Report Feature 设计方案 |
| sonnet-0410d | Claude Sonnet 4.6 | 实现 Session Summary Modal + mastery-summary API |
| opus-0410a | Claude Opus 4.6 | 本地视频 faster-whisper 转录（srt + txt）|
| sonnet-0410f | Claude Sonnet 4.6 | 续接上轮压缩上下文 — 元认知校准面板收尾 + commit/push |
| sonnet-0410g | Claude Sonnet 4.6 | 续接压缩上下文 — 审计 recentAccuracy + STATUS.md 已知问题更新 |
| sonnet-0410h | Claude Sonnet 4.6 | Research Loop — Post-Session UX / FSRS v6 / SSE Streaming Postmortem |
| sonnet-0410i | Claude Sonnet 4.6 | 续接压缩上下文 — 仪表盘审计 + TodayView routing + element_mastery 一致性 |

### [2026-04-10 06:47:17] Turn 96 〈opus-0410a〉【收尾轮】
- **用户**：Research Loop + Progress Loop 双触发（主人手动同时发两个 Loop prompt，测试两者配合）
- **执行**：Research Loop 判断——不做上轮泛调研的重复，改派 sub-agent 调研 Turn 95 实验中踩的三个具体坑（torch+mlx 线程崩溃根因 / stable-ts 真正的反幻觉机制 / py3.12 降级可行性）→ Agent 后台跑（run_in_background=true）→ 并行做 Progress Loop 的 gap 修复（上轮自我批判里标的两个未完成项）：(a) 更新 `~/Downloads/Review Session for Midterm 3.summary.md` 顶部加 4 版 SRT 推荐对比块（明确 `.stable.srt` ⭐ 为主推荐）(b) 新建 `~/whisper-transcribe/README.md` 文档化 6 个脚本 / 安装命令 / empirical 结果 / 已知问题 → 读 task-notification 收到 sub-agent 完成 → 读 `docs/research/2026-04-10-whisper-edge-cases.md`（~2500 词，14 个源）验证质量
- **结果**：两个 Loop 同时闭合。Sub-agent 给出三个关键修正：(1) **`KMP_DUPLICATE_LIB_OK=TRUE` 是危险的 workaround**——threadpoolctl 官方文档明确说它只压制 Error #15 然后让程序进入"静默错误状态"，完美解释了 Try #2 在设置这个变量后还是 0% 崩溃；(2) **stable-ts 真正的反幻觉机制是 `suppress_silence` 做后置 word-timestamp 快照到 speech edge + 零时长 word 丢弃**，不是我之前推测的 `avg_prob_threshold`——这是 5 层机制中的一层；(3) **ctranslate2 官方没 Python 3.13 wheel**（但 anaconda 打包了定制 wheel 所以我这里能跑，pip 清装会失败），`torch 2.4.x` arm64 wheel 是 `libomp` 原生的不是 `libiomp5` 所以可能避开冲突。Sub-agent 推荐 ranked workarounds：subprocess 隔离 > py3.12 降级 > `DYLD_INSERT_LIBRARIES` 强制单 OpenMP。**Try #4 已是此机器最优，不要继续追 mlx+stable-ts 集成**
- **决策**：(1) **不实施任何 workaround**——sub-agent 明确说 Try #4 已经是最优，继续追 mlx+stable-ts 是负 ROI；(2) **不测 `DYLD_INSERT_LIBRARIES`**——sub-agent 说这属于实证测试不是研究职责，而我已经声明闭环，这种边界测试留给未来需要 Metal 时再做；(3) **转录任务真正收尾**：所有产物已到位（stable.srt 为推荐版、summary 已标注、README 已写、两份调研报告完整），下次 Progress Loop 触发必须转去其他项目；(4) **保留 Try #2 KMP_DUPLICATE_LIB_OK 设置失败的经验**：这是"用错误工具以为在修问题"的典型案例，sub-agent 明确指出 threadpoolctl docs 早就警告过，我应该在写 workaround 前先查 threadpoolctl 的 multiple_openmp.md——未来处理 OpenMP 冲突时的第一个动作应该是读这份文档

### [2026-04-10 06:36:58] Turn 95 〈opus-0410a〉【转录任务闭环】
- **用户**：Progress Loop 第 5 轮触发 — 推进转录任务
- **执行**：实证调研报告核心建议 #3（stable-ts 解码时反幻觉）→ `pip install stable-ts`（2.19.1，带 load_faster_whisper / load_mlx_whisper / load_model 三个 loader）→ 写 `transcribe_stable.py` → **Try #1** stable-ts+mlx+VAD → 0% frames 崩溃（torch+mlx OpenMP 冲突）→ **Try #2** stable-ts+mlx vad=False + OMP 环境变量 → 仍崩溃（word_timestamps 仍触发 torch 加载）→ 切换 Option B → **Try #3** stable-ts+faster-whisper small + Silero VAD → 转录完成但在 `result.suppress_silence()` 崩溃（缺 silent_starts/ends 参数，和 transcribe kwarg 重复了）→ **Try #4** 移除重复的 suppress_silence 调用 → 转录 1295s 成功 → 扫描 stable.srt → 更新 A/B 实证报告加 4 个实验完整对比
- **结果**：**stable-ts + faster-whisper small + Silero VAD 是四个实验的冠军**。三个核心指标：(1) **27:00 幻觉窗口被彻底消除**——不只是变 marker，而是**真实转录出了课堂内容**（"possible to figure out how one over one minus x is related"，正是老师当时要讲的 `1/(3+x)²` 与 baby 级数的关系）；(2) 09:28 窗口也从 token 循环变成"模糊但有结构"（出现 "power series"、"calculation" 关键词）；(3) 扫描器全部启发式归零——这是整个链条中第一次达到"零检出幻觉"。**速度与 baseline 几乎相同**（1295s vs 1288s，0.5% 开销）。机制解读：stable-ts 的 **word-level timestamp 反向对齐**会拒绝那些 Whisper 解码结果漂移到 Silero 标记为静音的区域的 segment——这是比调研报告提到的 `avg_prob_threshold` 更精细的反幻觉机制
- **决策**：(1) **正式宣告视频转录任务在本轮闭环**——核心任务从 Turn 69 起闭环，之后 6 轮 Progress Loop 都在扩展（清洁→摘要→练习单→调研→A/B→stable-ts），每轮都有产出但边际 ROI 递减到本轮的"有显著长期价值但当前任务已过度交付"；(2) 之后 Progress Loop 再触发应该转去**其他项目**（course-hub / Signal-Map / TOEFL 等）而非继续推进转录；(3) 不部署 mlx+stable-ts 路径——torch/mlx 线程冲突是环境层问题，等 torch 2.12+ 或 stable-ts 原生 MLX wrapper；(4) `remove_repetition()` 误删了 7 条合法学生短回答（"Yes"/"Yeah"/"No"），属于轻度过激 false positive，建议对英文讲课内容调高 min-repeat 阈值或直接跳过该 post-processing；(5) 留 4 个 SRT 变体（baseline / cleaned / mlx / stable）在 Downloads 里作为对比参考，等主人决定留哪个；(6) 写 `feedback_task_output.md` 记录主人偏好"直接读 output file 而非 TaskOutput 轮询"（已于上轮完成）

### [2026-04-10 06:23:27] Turn 94 〈sonnet-0410f〉
- **用户**：续接压缩上下文，Progress Loop 自动恢复
- **执行**：① 仪表盘审计 → StudyTrackerPanel/StudyStatsCard/CourseCard 全部硬编码英文 → 为三组件添加 useI18n()，i18n.tsx 新增 20+ EN/ZH 键值 → push 5212e0e ② 发现 TodayView Start 按钮完全无点击行为（UX 死路）→ task 对象增 actionUrl 字段、所有 buildTodayTasks push 补全路由（review/learn/practice）、TodayView 改用 Link wrapping 整个卡片 → push e58f8dc ③ mastery-summary 返回 element_name="_overall" 字符串而非概念标题 → 联表 outline_nodes 返回 title → push 8ff7a91 ④ teach-back + fsrs 路由 update element_mastery 缺 element_name="_overall" 过滤 → 修复 → push 67986b0 ⑤ practice 页面硬编码英文清理 + profile 页面 TypeScript readonly 错误修复 → push dc5cb38
- **结果**：6 次 commit，仪表盘完全中英文切换；今日任务全部可点击导航；mastery 摘要显示概念标题而非 "_overall"；element_mastery 一致性全面提升
- **决策**：TodayView 全卡片包裹 Link（非只包 Start 按钮），因整个卡片已视觉设计为可点击的任务项；mastery-summary 联表比在客户端再次查询更少 RTT

### [2026-04-10 06:07:58] Turn 93 〈sonnet-0410f〉
- **用户**：Progress Loop 自动执行（续接 Turn 92）
- **执行**：① 读取 research postmortem（Turn 91 研究成果）② 发现 SSE 缺少 X-Accel-Buffering: no → 加入 generate-one/route.ts → push 5400a1c ③ SessionSummaryModal 新增 reviewedItems 可选 prop + 错题列表（最多5条）④ review/page.tsx + practice/page.tsx 各追踪答题项 + 传入 modal → push 868fcae
- **结果**：SSE 流式推送在 Vercel 上恢复正常（不再被 Nginx 缓冲）；会话总结现在显示答错的具体题目，用户可针对性复习
- **决策**：错题列表只显示 wrong（不显示 correct），且上限 5 条，保持 modal 紧凑；按 Wanikani 经验 "用户最关心哪些没过的"

### [2026-04-10 06:05:15] Turn 92 〈sonnet-0410f〉
- **用户**：Progress Loop 自动执行（续接 Turn 90）
- **执行**：① 发现 progress/tree 页面修复后 calculateMastery 成为死代码（只剩 mastery.ts 中的定义，无调用方）② 给 upload/courses/outline-nodes 三个高风险写入路由补充 rate limit → push 2982e56 ③ attempts/route.ts mastery SELECT 加 .eq("element_name","_overall") 过滤，防止潜在多行误判 → push e8779f7 ④ 确认 bookmarks/feedback 路由安全（均使用 upsert onConflict，无 DB spam 风险）⑤ 确认 element_mastery UNIQUE(user_id, concept_id, element_name) 约束，maybeSingle 安全
- **结果**：生产安全性全面提升。所有主要写入路由已有 rate limit 保护
- **决策**：feedback/bookmarks 不加 rate limit（upsert 天然幂等，无滥用向量）

### [2026-04-10 06:04:45] Turn 91 〈sonnet-0410h〉
- **用户**：Research Agent — 对 CourseHub 做三个专题调研：后会话 UX 模式、FSRS v5/v6 算法改进、Next.js 15+ SSE 性能，输出到 docs/research/2026-04-10-research-loop-postmortem.md
- **执行**：① 读取现有 session-summary-ux-research.md 避免重复 ② 三轮 WebSearch（FSRS v5/v6、后会话 UX A/B、Next.js 15 SSE）+ 两轮深化搜索 ③ 确认项目 ts-fsrs ^5.3.2 + grep SSE 使用文件 ④ 写入 research postmortem 文件（约 100 行，3 个主题）⑤ 更新 chronicle 代号索引 + 写入本条记录
- **结果**：/Users/shaoq/Desktop/VScode Workspace/docs/research/2026-04-10-research-loop-postmortem.md 生成，含 3 个主题各 5 点 + CourseHub 适用性注释，8 条来源引用
- **决策**：FSRS 章节聚焦 v5/v6 delta 而非重复 v4 基础；SSE 章节强调 X-Accel-Buffering 和 Node.js runtime 两个最高优先级行动项，因为 generate-one/route.ts 已存在且最可能受益

### [2026-04-10 06:02:01] Turn 90 〈sonnet-0410f〉
- **用户**：Progress Loop 自动执行（续接 Turn 89）
- **执行**：① 发现 progress/page.tsx + tree/page.tsx 仍使用旧 calculateMastery()（attempt 启发式）与 learn/page.tsx 的 element_mastery v2 不一致 ② progress/page.tsx 改为查询 element_mastery + v2→4级映射 → push eea4271 ③ tree/page.tsx 同样改造（移除 questions+attempts 查询，直接查 element_mastery）→ push 6eeaabf
- **结果**：三个显示掌握度的页面（learn/progress/tree）现在均使用 element_mastery.current_level 作为唯一真相源，消除"分裂真相"
- **决策**：v2→旧 4 级映射：unseen→untested, exposed→weak, practiced→reviewing, proficient/mastered→mastered

### [2026-04-10 05:57:57] Turn 89 〈sonnet-0410f〉
- **用户**：续接压缩上下文 — Progress Loop 自动执行
- **执行**：① 创建 Research Loop (07 * * *) + Progress Loop (*/10 * * *) Cron ② commit chronicle T88 → push bd20ed8 ③ 019_fsrs_review_logs_unique 迁移上线 ④ fsrs/route.ts insert→upsert+ignoreDuplicates → push bdd61ca ⑤ CalibrationPanel 抽离为 client component + i18n EN/ZH → push b91f1fc ⑥ has_transfer_correct 通过 short_answer 正确设置，解锁 proficient→mastered 通道 → push 535b44e ⑦ practice/page.tsx 接入 SessionSummaryModal（答题≥5 后出现 Flag 按钮）→ push 09e619c ⑧ recentAccuracy 从总体准确率改为真实最近5条（join query）→ push c094b72
- **结果**：8 个 commit，全部推送到 origin/main。mastery 系统所有已知死路全部打通
- **决策**：transfer_correct 用 short_answer 正确作为代理（Roediger & Butler 2011 依据）；recentAccuracy 额外 1 条 join query 值得换来评估正确性

### [2026-04-10 05:48:46] Turn 88 〈sonnet-0410f〉
- **用户**：2× Progress Loop 自动触发
- **执行**：git log → 发现 evaluateLevel() 从未被调用（mastery-v2 完整但零调用方）→ 扩展 attempts/route.ts mastery SELECT（加 current_level/FSRS/has_* 字段）→ 构建 MasteryStats 内联 → 调用 evaluateLevel() → changed 时合入同一 UPDATE → tsc 0 → commit d690156 + push
- **结果**：mastery-v2 评估系统上线。exposed→practiced→proficient→mastered 升降级在每次答题后实际运行
- **决策**：recentAccuracy 用总体准确率（无额外查询）；crossConceptOk 始终 true（跳过 count）；零额外查询

### [2026-04-10 05:43:12] Turn 87 〈sonnet-0410f〉
- **用户**：Progress Loop 自动触发
- **执行**：git log 确认 4357519（race condition fix）+ 发现 fsrs/route.ts 被用户修改（加 FSRS stability→element_mastery 传播）→ 验证 schema 中 fsrs_stability/fsrs_retrievability/fsrs_last_review 字段已存在（migration 010）→ tsc 0（用户改动无类型错误）→ 32dcd63 和 4357519 均已 push，nothing to commit → 写 chronicle Turn 87
- **结果**：FSRS 完整闭环：review → syncCardToServer → POST /api/fsrs → 更新 fsrs_cards + 传播 stability/retrievability 到 element_mastery → mastery-v2 可用 FSRS 内存强度门控 practiced→proficient
- **决策**：无重大决策，确认用户修改安全后直接 chronicle

### [2026-04-10 05:42:06] Turn 86 〈opus-0410a〉
- **用户**：Progress Loop 第 4 轮触发 — 推进转录任务（主人未回上一轮三选项，按不原地等待规则自主推进）
- **执行**：选最有长期价值动作 — 对上一轮 Research Loop 的调研结论做实证 A/B 测试 → `pip install mlx-whisper`（0.4.3 + mlx 0.31.1 + mlx-metal 0.31.1，py3.13 兼容，Metal ✓）→ 写 `~/whisper-transcribe/transcribe_mlx.py` 镜像原 transcribe.py（同样的 anti-hallucination 参数，输出 .mlx.srt/.mlx.txt）→ 后台跑同一 96MB mp4 → 读 output-file 得到结果（主人偏好直接读 output file 而非 TaskOutput 轮询，已存 memory feedback）→ 用 scan_hallucinations.py 对比两版输出 → 写 `docs/research/2026-04-10-whisper-ab-empirical.md`（~1300 词，对上一轮调研报告做实证修正）
- **结果**：**调研报告部分被实证推翻**。核心数据：(1) mlx-whisper large-v3-turbo **快 1.83×**（703s vs 1288s），远不及报告说的 4-6×——原因是报告基准是 mlx vs whisper.cpp（两者都 Metal），而我比的是 mlx vs faster-whisper CPU；加上 M4 base（非 M2 Ultra），加上 turbo 比 small 多 3.3× 参数；(2) **turbo 幻觉更多不是更少**——产生两段 "taking taking taking" token 循环（09:28 和 27:00），是 small 模型的两倍幻觉窗口；(3) 但 turbo 的幻觉形状更易检测（单 token 循环 vs 似是而非的句子）；(4) 两模型**幻觉在同一音频位置**——说明根因是静音段而非模型，需要前置 VAD 才能根治；(5) segment 数量 1114 vs 1941，turbo 的段落更长更适合字幕显示
- **决策**：(1) 转录任务本身不切换 — faster-whisper 清洁版 srt 仍是主产物，mlx 版作为 A/B 参考；(2) 写 memory feedback "读 output file 而非轮询 TaskOutput"；(3) 后续值得跑的实验 `mlx-whisper small`（同尺寸 + Metal 加速，预期 3-4× 而无幻觉回归），但本轮不做 — 实证已经覆盖核心假设；(4) **强调 Research Loop + 实证验证的价值**：单靠调研报告的 benchmark 数据会把我带错方向（期待 4-6× 实际 1.8×，期待幻觉减少实际增加），**永远要在自己的硬件和内容上 A/B 测试**

### [2026-04-10 05:38:37] Turn 85 〈sonnet-0410f〉
- **用户**：2× Progress Loop 同时触发
- **执行**：git log 确认 6234f52（项目在推进）→ 扫描 FSRS sync 遗漏点：/api/fsrs 无 rate limit + practice page 未调用 pullCardsFromServer → 修复两点：POST 120/min + GET 30/min + practice/page.tsx import pullCardsFromServer + useEffect 调用 → tsc 0 → commit 6daee06 + push
- **结果**：FSRS 同步功能完整：review 和 practice 两个入口都在 mount 时从服务器拉取卡片状态；/api/fsrs 有 rate limit 防刷
- **决策**：POST 120/min（2 card/sec）generous 但足以防 flood；GET 30/min（burst protection，正常使用远低于此）

### [2026-04-10 05:36:25] Turn 84 〈sonnet-0410f〉
- **用户**：Progress Loop 自动触发
- **执行**：git log 确认 4cde2c9 最新（项目在推进）→ 识别最高优先级剩余功能：FSRS localStorage→Supabase 迁移（跨设备 SRS 同步）→ 写 migration 018（fsrs_cards 10 字段 + fsrs_review_logs + UNIQUE + RLS）→ apply_migration → 发现 ts-fsrs Card 有 learning_steps 字段（非文档记录）→ apply 补丁 migration 018b → 写 `/api/fsrs` route（POST upsert batch + GET fetch by courseId，Zod 验证）→ spaced-repetition.ts 加 syncCardToServer（fire-and-forget）和 pullCardsFromServer（DB→localStorage merge，DB wins on reps tie）→ updateCard 末尾调用 syncCardToServer → review/page.tsx mount 时调用 pullCardsFromServer(id) → tsc 0 → commit 8802ab9 + push
- **结果**：FSRS 跨设备同步上线。localStorage 仍为 fallback（服务器失败静默）。新设备首次打开课程复习页 → 从 DB 拉取历史卡片状态合并进 localStorage → SRS 队列恢复
- **决策**：client-side scheduling 保留（FSRS 算法在浏览器运行，零延迟）；server 是持久化层不是调度层；pullCardsFromServer 在 questions fetch 并行触发（不阻塞 loading）；review logs 使用 insert（ignore error，允许重复，后续可加唯一约束）

### [2026-04-10 05:31:15] Turn 83 〈sonnet-0410f〉
- **用户**：Research Loop + 2× Progress Loop 同时触发（Research Loop 子 Agent 调研结果返回）
- **执行**：读取研究报告（misconception 解除触发 / FSRS server-side pattern / wrong-answer feedback timing）→ 决策：先实现 think-again scaffold（0.5 天，ROI 最高）→ QuestionCard.tsx 加 `explanationVisible` state（错误答案初始化为 false，正确初始化为 true）→ handleSubmit 加 `setExplanationVisible(correct)` → 错误答案改为 "See where you went wrong →" 按钮展示，点击后才 reveal explanation → i18n 两语言 → tsc 0 → commit 050faab + push
- **结果**：Test-potentiated encoding 模式上线。错误答案现在需要一次主动点击才能看到解释，强制用户在接受纠正之前先思考"哪里错了"
- **决策**：选择单次点击门而非 3 秒自动延迟（更尊重用户节奏，急着看的可以立即点）；正确答案保持立即显示（奖励机制不加阻力）；i18n 两语言（EN/ZH）

### [2026-04-10 05:26:38] Turn 82 〈sonnet-0410f〉
- **用户**：Research Loop + 2× Progress Loop 同时触发
- **执行**：git log 确认 ca3b4ed 为最新（项目在推进）→ 派 Research Loop 子 Agent 后台调研（misconception 解除触发、FSRS server-side、错误反馈时机）→ 实现 misconception 自动解除：在已有 mastery counters 更新块中内联计算 postUpdateAccuracy（无额外查询）→ 正确回答后 accuracy≥75% 且 tested≥5 时 UPDATE resolved=true, resolved_at=now() → tsc 0 → commit 1c2e16f + push
- **结果**：misconception 完整生命周期上线：错误答案写入/累加 → 正确答案推动准确率过阈值后自动解除。ProfileView 的 active/resolved 两栏现在都会正确填充
- **决策**：阈值 75%/5 次（接近 85%-rule 最优区间，避免太早误解除）；postUpdateAccuracy 内联计算（已有 mastery 数据，零额外查询成本）；UPDATE WHERE resolved=false（避免重复更新已解除记录）

### [2026-04-10 05:23:54] Turn 80 〈opus-0410a〉
- **用户**：Research Loop 自动触发 — 调研 Whisper 转录任务的改善方向
- **执行**：派 general-purpose 子 Agent 带 WebSearch 调研三问（faster-whisper vs openai-whisper / Apple Silicon 后端 shootout / 长音频+反幻觉实践）→ 子 Agent 做 8 次搜索 + 2 次 WebFetch → 产出 `docs/research/2026-04-10-whisper-transcription.md`（~1900 词，14 个源）→ 读取报告验证质量 → 用 sysctl 核对本机硬件：M4 + 16GB（子 Agent 担心的 8GB M1 base 不适用）→ 所有推荐可行
- **结果**：调研颠覆了之前的技术决策 — **faster-whisper on CPU 不是 Apple Silicon 最优**。三个核心发现：(1) **mlx-whisper + large-v3-turbo** 比 faster-whisper CPU **快约 4-6×** 同时精度显著提升（small→turbo WER 约减半），一行 `pip install mlx-whisper` 即可；(2) **stable-ts 的 `avg_prob_threshold=0.6`** 能在解码时自动丢弃低置信度段，根治那 82 秒幻觉窗口（不再需要 post-hoc 扫描脚本）；(3) **pyannote VAD > Silero v5**——正是 Silero v5 漏过板书静音段才导致幻觉，WhisperX 用的是 pyannote。llimllib 2026-01 基准：mlx-whisper 13.1s vs whisper.cpp 26.7s（2.03× 快）。M2 Ultra 上 mlx-whisper + turbo 转 12 分钟音频只要 14 秒（50× 实时）
- **决策**：(1) 不立即实施改动，先产出调研报告留给主人决策——v2 改版需要 2-3 小时，本轮 Research Loop 不适合直接动代码；(2) 三条最高 ROI 建议成组推进（backend 切换 + stable-ts 反幻觉 + pyannote VAD），ROI 第一条（backend 切换）单独就超过其余所有项之和；(3) 不推荐 lightning-whisper-mlx（unmaintained re turbo）和 Calm-Whisper（只有残留幻觉时再考虑）；(4) 保留 faster-whisper 作为非 Apple 硬件 fallback，不删旧 transcribe.py

### [2026-04-10 05:24:28] Turn 81 〈sonnet-0410f〉
- **用户**：Progress Loop 自动触发 — 检查项目进展
- **执行**：git log 确认 dfc34be 为最新提交（校准面板已推送）→ 扫描剩余高优先级功能 → 发现 misconceptions 表是 ghost feature（ProfileView 有完整 UI 但表从未被写入）→ 写 migration 017（UNIQUE(user_id,concept_id) 约束）→ apply_migration via Supabase MCP → 修改 attempts/route.ts：questions SELECT 加 stem + knowledge_point_id → 错误回答后 upsert misconception（首次 INSERT + stem 描述，重复 UPDATE occurrence_count，已解决后再错标 relapsed）→ tsc 0 → commit 77537c1 + push
- **结果**：misconception 自动追踪上线。ProfileView 的「错误概念」面板现在会真正填充内容：用户答错时自动写入或更新 misconceptions 表
- **决策**：description 用 question.stem(≤120 chars) 而非 AI 生成（零额外成本，够用）；relapse 逻辑保留；migration 017 加唯一约束为后续 upsert 提供基础

### [2026-04-10 05:17:51] Turn 79 〈sonnet-0410f〉
- **用户**：续接上一轮压缩上下文 — 恢复执行，直接推进未完成工作
- **执行**：创建 Research Loop（hourly）和 Progress Loop（每 10 分钟）Cron → tsc --noEmit 通过（EXIT:0）→ git add progress/page.tsx → commit 277cf1e（feat: confidence calibration panel）→ git push → chronicle Turn 79 写入
- **结果**：元认知校准完整闭环上线。progress page 现显示校准面板（≥5 rated attempts 时）：3 行 emoji+进度条显示各信心等级准确率，附 Well-calibrated / Overconfident 徽章
- **决策**：totalRated 门控设为 5（避免早期样本量不足时显示误导性统计）；标签暂未 i18n（中文用户看英文）— 后续补

### [2026-04-10 05:17:27] Turn 78 〈opus-0410a〉
- **用户**：Progress Loop 第 3 轮触发 — 继续推进转录任务下一步
- **执行**：上一轮自我批判结尾问主人的三选项（LaTeX 化 / 工具合并+README / 练习单抽取）中选最有学习价值的「练习单抽取」主动推进 → 完整读入 1918 行清洁 txt（分 3 段读）→ 识别 10 道工作例题覆盖 §6.1-6.2 所有主题 → 写 `~/Downloads/Review Session for Midterm 3 - practice sheet.md` 三段结构：Problems（10 题纯题面）+ Hints（每题 1-2 行提示）+ Answer Key（完整解题过程含 IOC 端点测试全步骤）+ 6 条 Strategy Reminders → 顺带修正转录里老师的口误（P2 老师说 n>98 其实应是 n>998，练习单标注了警告）
- **结果**：练习单上线（11K, 约 300 行）。覆盖 alternating remainder / power series via 差分 / 1/(3+x)² / 1/(x-6)² / IOC unknown coefficients / IOC ratio test 完整端点测试 / ln(1-x) 积分构造。下载文件夹现有 5 份产物：mp4(96M) + srt(123K) + srt.bak(125K) + txt(57K) + summary.md(8.2K) + practice sheet(11K)
- **决策**：(1) 三段式练习单结构（题→提示→答案）优于纯答案集——主人可以先自己做再对答案；(2) 主动纠正老师 P2 口误并警告标注——不要照搬转录错误；(3) 不做 LaTeX 化（超出本任务范围，主人没要求，而且 Markdown + 代码块里的数学已经足够可读）；(4) 工具合并/README 延后（转录任务本身的学习产出已经闭环，工具是副产物）；(5) P7 的算式 `Σ(n+1)·5ⁿ/6ⁿ⁺²=6` 我凭老师结论记录但没完全推导验证——练习单里诚实标注"instructor concluded"

### [2026-04-10 05:13:56] Turn 77 〈sonnet-0410d〉
- **用户**：Progress Loop 触发 — 检查进度
- **执行**：git log 确认 97154d1 → 发现 teach-back 效果说明有误（Kobayashi: prospective>retrospective, g≈0 for post-answer）→ 识别更高优先级：Gap 3 元认知校准 g=0.57 → apply_migration 016（attempts.confidence SMALLINT NULL）→ 更新 attemptCreateSchema（optional confidence 1|2|3）→ QuestionCard 加信心选择器（userAnswer 选中后显示 🤔🙂😎，不阻断 submit）→ attempts route 写入 confidence → i18n 两语言 → tsc 0 → commit 0aebcae + push
- **结果**：元认知校准上线（5 文件，41 行）。用户答题前可选择信心等级，数据存 attempts.confidence
- **决策**：optional 不强制（不破坏现有答题节奏）；3 级而非 5 级（减少决策疲劳）；显示条件是 userAnswer 非空（选了答案才显示，避免无意义的预答题信心）

### [2026-04-10 05:10:05] Turn 76 〈opus-0410a〉
- **用户**：Progress Loop 第 2 轮触发 — 推进转录任务
- **执行**：上一轮自我批判承诺的全 SRT 幻觉扫描补做 → 写 `~/whisper-transcribe/scan_hallucinations.py`（3 个启发式：长段短文/连续循环/累计同句）→ 扫描确认全片唯一真实幻觉窗口是 27:03-28:24（之前 27:06-27:50 是误判边界）→ 写 `fix_window.py` 用严格 VAD 参数（no_speech_threshold=0.85 / log_prob_threshold=-0.8 / compression_ratio_threshold=1.8 / condition_on_previous_text=False）ffmpeg 切片重跑 → 半成功（5 条循环变 1 条残留 "You're going to give me a job"）→ 改策略：写 `clean_window.py` 恢复备份后删除整段脏 cue 并插入 marker → 删除 23 条幻觉 cue + 插入 `[audio unclear — instructor writing on board; refer to video 27:03-28:25]` → 从清洁 SRT 重新生成 txt → 更新 summary.md 的窗口范围和质量说明 → SRT 备份保留在 `.srt.bak`
- **结果**：最终产物 1918 cue（原 1941，净 -23），23 条幻觉清除完毕。重跑扫描：启发式 2=0（无循环），启发式 3 只剩合法教师口头禅（"Second of all" ×2, "Does that make sense?" ×5）。上下文衔接检查通过：cue 542 "I already have the rest of you in here." → 543 marker → 544 "And I can figure out how 1 over 1 minus x is related to it" 过渡清晰
- **决策**：(1) 放弃"重跑修复"策略改为"诚实删除"——严格参数重跑仍幻觉，说明这段音频物理上没有可转录语音；(2) marker cue 使用全窗口时间戳 [27:03-28:25] 而非逐条填充（字幕播放器显示为单条持续字幕，视觉上明显提示用户"这里不可靠"）；(3) 保留 .srt.bak 便于主人对比；(4) scan_hallucinations.py 和 clean_window.py 留在 ~/whisper-transcribe/ 作为可复用工具

### [2026-04-10 05:07:58] Turn 75 〈sonnet-0410d〉
- **用户**：Progress Loop 触发 — 检查进度
- **执行**：git log 确认 7272e59 刚推 → 扫描 MistakePatterns + mistake-patterns API（完整，无需改动）→ 决定实施 Teach-back（原型效应，g=0.48-0.56）→ 创建 `teach-back/route.ts`（POST, rate-limit 10/min, server-side 获取 correct_answer, QUALITY 解析）→ 创建 `TeachBackPanel.tsx`（可折叠，Strong/Partial/Missing，key={question.id} 自动重置）→ 注入 review/page.tsx（showRating 后）→ tsc 0 → commit 97154d1 + push
- **结果**：Teach-back 功能上线（3 新文件，173 行）。复习答题后可选用自己的语言解释概念，获得 AI 评估。answer 字段不下发客户端（server-side fetch）
- **决策**：collapsed by default — 零强制，不影响答题节奏；question_id 代替 question_stem+correct_answer 传参（answer 不能暴露给前端）

### [2026-04-10 05:04:31] Turn 74 〈sonnet-0410d〉
- **用户**：Research Loop 触发 — 审计最近 adaptive difficulty 工作
- **执行**：扫描所有 API routes 的 rate-limit 覆盖 → 发现 `/api/attempts` POST 无 rate limit（安全漏洞：可 flood attempts 操控自适应排序）→ 检查 practice/library/review 页的 QuestionCard 用法（library 不用 QuestionCard，review 用但 attempt_count 字段存在）→ 确认 migration 015 未 apply → 写研究报告 `2026-04-10-adaptive-difficulty-audit.md`（3 个优先级行动）→ 给 attempts 加 rate limit（60 req/min）→ MCP apply_migration 015（partial index on element_mastery）→ tsc 0 → commit 7272e59 + push
- **结果**：adaptive difficulty 安全漏洞修复，partial index 上线。研究报告记录完整审计
- **决策**：60 req/min（vs 一般路由的 3-20）——答题是高频操作，合法用户每分钟可能做 20-30 题，需要足够宽松；不跳过 review page 的 attempt stats JOIN（P2 过早优化）

### [2026-04-10 05:01:56] Turn 73 〈sonnet-0410d〉
- **用户**：Progress Loop 触发 — 检查进度
- **执行**：git log 确认 179be61 推送 → 识别下一任务：QuestionCard 显示历史准确率 badge → 读取 QuestionCard.tsx → 在 question type kicker 旁加 badge（attempt_count=0 显示 "First attempt"；>0 显示 "N attempts · X% correct"）→ tsc 0 → commit 1bfd096 + push
- **结果**：练习卡片上线历史准确率 badge。与自适应排序形成闭环：用户能看到为什么某道题出现在当前位置
- **决策**：badge 在 `attempt_count !== undefined` 时显示（即 questions API 返回该字段时）；review page 的 QuestionCard 不传这些字段故不显示，兼容已有行为

### [2026-04-10 05:02:53] Turn 73 〈sonnet-0410e〉
- **用户**：Research Loop 自动触发（考试日第五次）
- **执行**：读取 chronicle + STATUS.md + git log 确认当前状态（exam prep sprint 4/4 完成）→ 扫描 spaced-repetition.ts 确认 localStorage 依赖范围（`loadCards`/`saveCards` 10 处调用）→ 调查消费方：review/page.tsx、practice/page.tsx、CourseCard.tsx、SessionSummaryModal.tsx → 撰写完整迁移设计文档 `docs/research/2026-04-10-post-exam-srs-migration.md`
- **结果**：产出 SRS 迁移三阶段方案：Phase1（React hook 包装异步接口）→ Phase2（Supabase fsrs_cards 表 + UPSERT API）→ Phase3（一次性客户端数据迁移）。估算 ~265 LOC，5 个文件。推荐直接上 Phase2（跳过过渡层）
- **决策**：JSONB 存储完整 ts-fsrs Card 对象（不拆列）— 避免 schema 频繁变更；exam_scope/exam_date 保留 localStorage（临时偏好，无需持久化）；乐观更新（立即更新界面，后台 sync Supabase）

### [2026-04-10 04:56:13] Turn 72 〈sonnet-0410d〉
- **用户**：Progress Loop 触发 — 检查进度
- **执行**：git log 确认 35caf55 刚推送 → 读取 practice/page.tsx 发现 handleAnswer 忽略 isCorrect 参数 → 添加 sessionAnswered/sessionCorrect state → 升级 handleAnswer 接收 (questionId, answer, isCorrect) → 在进度面板加入准确率 badge（≥3题后显示；75-92%绿色/目标区间，>92%蓝色/太简单，<75%橙色/太难）→ tsc 0 错误 → commit 179be61 + push
- **结果**：练习模式实时准确率指示器上线。用户可直观看到当前准确率是否在 85% 目标区间内
- **决策**：≥3题后才显示（避免早期噪音）；目标区间定为 75-92%（宽于理论值 85%，给用户宽容度）；颜色用 success/accent/warning（不用 danger，练习不是考试）

### [2026-04-10 04:56:01] Turn 71 〈opus-0410a〉
- **用户**：Progress Loop 触发 — 检查视频转录任务推进状态
- **执行**：确认 srt(125K)/txt(58K)/transcribe.py 全部就位 → 抽样读 srt 头部 + txt 中段（540-1200 行）验证格式和内容质量 → 发现中段（27:06-27:50）有 Whisper 典型 VAD-gap 幻觉（"opening up in any role" 循环 4 次 + 一条 22 秒单句）→ grep 定位幻觉窗口精确时间戳 → 按"推进则继续"规则生成结构化摘要 `~/Downloads/Review Session for Midterm 3.summary.md`：8 个主题时间段大纲、6 条高频叮嘱、4 个数值例题、幻觉窗口警告、5 个后续可选动作
- **结果**：摘要文档上线（Markdown，含时间戳导航）。讲座主题确认为 Calc II 期中 3 复习，覆盖 alternating series remainder、power series via 1/(1-x)^2 求导、IOC via ratio test + endpoint 测试、ln 积分构造。转录整体质量约 98.8% 可用，仅 44 秒幻觉窗口需回放视频
- **决策**：不自动修复幻觉段（可能误伤正常内容，让主人决定是否重跑）；不做 LaTeX 转换（主人没要求）；不 commit 产物到 repo（下载文件夹的视频转录属于私人数据不入库）；摘要写成独立 md 而非塞进 txt（方便预览，保持 txt 纯文本原始性）
- **执行**：git log 确认最新推进 → 读取 STATUS.md 找到下一优先任务：自适应难度（85% 规则，#1 post-exam priority）→ 读取 /api/questions GET、Attempt 类型、Question 类型 → 实施：(1) GET /api/questions 中 join attempts 统计（correct/total per question）(2) 按 85% 规则四分段排序（学习区 50-90% → 未见 → 困难 <50% → 掌握 ≥90%）(3) 在 Question type 加 `attempt_count?` + `user_accuracy?` 字段 → tsc 0 错误 → commit + push
- **结果**：commit `35caf55` — 练习模式实现自适应难度。学习区间的题优先展示，mastered 题推后，符合 Wilson et al. 2019 最优错误率 ≈15% 的结论
- **决策**：排序在服务端完成（不改客户端）；四分段优先于连续函数（`|acc-0.85|` 排序会把 struggling 题推后，不利于及时干预）；保留带内随机以维持交错练习效果

### [2026-04-10 04:52:13] Turn 69 〈opus-0410a〉
- **用户**：把 ~/Downloads 里约 96MB 的视频转录成带时间戳的 .srt + 纯文本 .txt，自动检测语言，长视频用 faster-whisper 提速，要完整可运行代码和安装命令
- **执行**：创建两个 Loop Cron（Research 每小时、Progress 每10分） → ls 找到 `Review Session for Midterm 3.mp4`（96M, 87 分钟讲课）→ 确认 ffmpeg 8.1 在位、faster-whisper 未装 → 创建 `~/whisper-transcribe/.venv`（Python 3.13）`pip install faster-whisper`（1.2.1）→ 写 `~/whisper-transcribe/transcribe.py`（argparse + WhisperModel small/int8 + VAD filter + 同轮循环写 srt/txt + 实时进度）→ 后台运行 21.5 分钟完成 → 输出 `Review Session for Midterm 3.srt` (125K) 和 `.txt` (58K, 1941 行, 11738 词)
- **结果**：转录上线。检测语言 en (prob=1.00)，音频时长 5248.3s (87min)，实时因子 0.25× (4× 速度)，SRT/TXT 同时生成在源文件旁边
- **决策**：模型选 `small + int8`（Apple Silicon CPU 甜点，英文讲课精度足够）而非 medium/large-v3（速度换精度不值）；用 venv 而非污染 anaconda base；VAD filter 打开（Silero，跳过静音 + 消灭 Whisper 幻觉）；生成器式迭代 segments（单次循环同时写两个文件，避免长视频内存膨胀）

### [2026-04-10 04:27:34] Turn 64 〈sonnet-0410b〉
- **用户**：Research Loop 自动触发（考试日，第一次）
- **执行**：读取 chronicle 2026-04-09 + 2026-04-10 → git log 确认上下文 → 读取 spaced-repetition.ts（daysUntilExam、isExamMode、getExamPriorityCards）→ 分析考试日 FSRS 边界情况（UTC+8 时区 + days=0 时的 Math.max(1, ...) 行为）→ 确认 in-memory rate-limit fallback 在单实例场景充分 → 写研究报告 `docs/research/2026-04-10-exam-day-readiness.md`
- **结果**：确认考试日 FSRS 逻辑无 bug：exam mode 全天保持激活、priority queue 行为正确（最弱优先）、rate-limit fallback 对单用户学习会话足够。产出明确的考后行动清单（Upstash P0、enable_thinking P2）
- **决策**：不做代码改动（无 bug 无改动）；研究聚焦考试日实际可用性，而非发现新问题

### [2026-04-10 04:26:16] Turn 63 〈sonnet-0410a〉
- **用户**：Research Loop + Progress Loop 同时触发
- **执行**：确认昨日 Turn 62 IDLE 状态 → WebSearch 调研 Next.js 16 Server Components 性能 + Supabase RLS 优化 → 扫描所有 migrations 中 auth.uid() 用法 → 发现 8 个策略使用裸 auth.uid()（可能逐行调用，10x 性能差距）→ 创建 `014_rls_performance.sql` 将裸 auth.uid() 改为 (select auth.uid()) → 写研究报告 → commit + push
- **结果**：产出 RLS 性能优化迁移文件 + 研究报告。今日第 1 个 turn，累计 7 份研究报告
- **决策**：不拆分 FOR ALL 策略（MVP 数据量不需要）；不改 EXISTS 子查询中的 auth.uid()（那些不是逐行热点）；fork rate-limit 已完成，昨日所有任务闭环

### [2026-04-10 04:28:27] Turn 64 〈sonnet-0410a〉
- **用户**：Research Loop + Progress Loop 再次触发
- **执行**：发现 4 个 linter 修改文件未提交 → commit + push → WebSearch 确认 enable_thinking 传参方式（官方文档：在 body 中传 enable_thinking: false）→ 确认 @ai-sdk/openai createOpenAI 支持 fetch 中间件 → 在 ai.ts 中实施 qwenText provider（custom fetch 注入 enable_thinking: false）→ textModel 切换到 qwenText → TSC 0 错误 → commit + push → 写研究报告
- **结果**：P2 优化（thinking 模式禁用）实施完成。预期节省 ~30% token，减少 ~1-3s 延迟。今日第 2 个 turn，8 份研究报告累计
- **决策**：只对 textModel 禁用 thinking（visionModel 保留原样）；exam-prep/exam-scope/regenerate 有独立 qwen 实例，暂保留 thinking（复杂 JSON 输出，thinking 可能有益）

### [2026-04-10 04:30:52] Turn 65 〈sonnet-0410c〉
- **用户**：Research Loop 自动触发（第三次，考试日）
- **执行**：确认 repo 干净（所有 thinking mode 优化已推送）→ 读取 STATUS.md 确认 Feature 3（Daily Report）未启动 → 读取 study-tracker.ts + usage-tracker.ts + streaks.ts → 分析现有数据模型 → 研究 session-end 摘要模式（Duolingo/Anki/Wanikani 对比）→ 查阅学习科学依据（Bjork、Hattie、Gollwitzer）→ 写详细实施方案 `docs/research/2026-04-10-daily-report-design.md`
- **结果**：产出 Daily Report 完整设计文档：推荐 SessionSummaryModal 模式（非独立页面）、~120 行代码（1 新组件 + 1 新 API route）、无 schema 变更。分三步增量实施，第一步客户端数据就能覆盖 80% 用户价值
- **决策**：不用 AI 生成摘要文字（token 成本不值）；不做 push notification（scope 过大）；session-end modal 优于 dashboard section（在正确时机触发，减少跳转摩擦）

### [2026-04-10 04:43:14] Turn 68 〈sonnet-0410d〉
- **用户**：Progress Loop 触发 — 检查推进状态
- **执行**：git log 确认 Feature 4 刚推送 → 读取 STATUS.md 发现 RLS 014 migration 未 apply → 用 Supabase MCP apply_migration 推送 014_rls_performance.sql（8 张表 auth.uid() → (select auth.uid())）→ 更新 STATUS.md（Phase 5 4/4 done + RLS Applied 记录）→ commit + push
- **结果**：RLS 性能优化上线（commit `addb450`）。所有未阻塞任务闭环。剩余阻塞项（Upstash env vars）需用户操作
- **决策**：无重大决策；supabase CLI 不可用（未安装），改用 MCP apply_migration 直接推送——效果相同

### [2026-04-10 04:41:27] Turn 67 〈sonnet-0410d〉
- **用户**：上下文压缩后继续 — 完成 Feature 4（Dashboard CourseCard due-count badges）
- **执行**：创建 Research Loop（0 * * * *）+ Progress Loop（*/10 * * * *）两个 Cron → 读取 CourseCard.tsx（原服务端组件，无 badge）→ 改写为 client component：添加 `"use client"`、`questionIds?: string[]` prop、`useEffect` 计算 dueCount（loadCards + getDueCards + Set 过滤）→ 渲染 `dueCount > 0` 时显示 warning 色 badge → 更新 dashboard/page.tsx 传入 `questionIds={questionsByCourse[course.id] ?? []}` → tsc 0 错误 → commit + push
- **结果**：Feature 4 完成（commit `0afece1`）。每张 CourseCard 在有待复习卡片时显示橙色 badge，数据来自 localStorage FSRS
- **决策**：badge 只在 `dueCount > 0` 时显示（0 due 不显示，减少视觉噪音）；hydration 后才计算（localStorage 不可 SSR），SSR 渲染无 badge 再补显是正确模式；archived courses 不传 questionIds（不需要 badge）

### [2026-04-10 04:37:35] Turn 66 〈sonnet-0410d〉
- **用户**：上下文压缩后继续 — 实现 Daily Report (Session Summary Modal) Feature 3
- **执行**：创建两个 Cron（Research Loop 每60分 + Progress Loop 每10分）→ 读取 review/page.tsx 确认已有 SessionSummaryModal import + sessionStart 状态 → 读取 streaks.ts 确认 API → 读取 element_mastery 类型（concept_id, current_level: MasteryLevelV2, level_reached_at, element_name）→ 读取 mastery/route.ts 确认 KP join 模式 → 创建 mastery-summary/route.ts（GET, rate-limit 20/60s, 按 level_reached_at >= since 过滤）→ 更新 SessionSummaryModal.tsx（加 TrendingUp 知识点提升 section + sessionStart prop + overflow scroll）→ review/page.tsx 补传 sessionStart prop → commit + push
- **结果**：Feature 3 完成。mastery-summary API route 上线；Modal 增加第3 section 展示本次复习中知识点升级情况；向上滚动适配长列表
- **决策**：用 level_reached_at（非 updated_at）过滤——updated_at 会被 FSRS 参数更新触发，不代表 level 升级；element_name 直接在 element_mastery 存储，不需要 join outline_nodes 取名称

### [2026-04-10 04:43:46] Turn 67 〈sonnet-0410d〉
- **用户**：Progress Loop 自动触发（Feature 3 提交后 10 分钟）
- **执行**：检查 git log 确认最近提交 → 读取 STATUS.md → 发现 Feature 4 已被用户独立完成（dashboard due-count badges）→ 更新 STATUS.md 标记 Feature 3 完成 → 派 Explore subagent 研究 cross-course org 功能设计 → 用户同步更新 STATUS.md 确认全部 4 项功能完成（4/4 done）
- **结果**：exam prep sprint 全部完成：exam scope filter + term cards + session summary modal + due-count badges。STATUS.md 已更新
- **决策**：cross-course org 实际被解读为 due-count badges（per-course FSRS 数量展示），比统一跨课程复习队列更轻量、更适合当前阶段；sprint 完结

### [2026-04-10 04:44:19] Turn 68 〈sonnet-0410d〉
- **用户**：Progress Loop 自动触发 — 检查 Daily Report 实施进度
- **执行**：验证 git log（commit 30687b1 已存在）→ 确认两个文件存在（SessionSummaryModal.tsx + mastery-summary/route.ts）→ grep review/page.tsx 确认 sessionStart prop 已传入
- **结果**：Daily Report 功能已完整实施并提交。无阻塞，无需 sub-agent 介入
- **决策**：无重大决策（纯状态确认）

### [2026-04-10 04:54:39] Turn 69 〈sonnet-0410d〉
- **用户**：Progress Loop 自动触发 — 检查项目进度
- **执行**：git log 确认无新提交（sprint 完成状态）→ 识别 #1 post-exam 优先级（adaptive difficulty 85% rule）→ 派 Explore subagent 研究实施方案 → 发现关键约束（questions API 被 FSRS review 和 practice 共用，不能无条件过滤）→ 设计 ?adaptive=true 标志位方案 → 写研究文档 2026-04-10-adaptive-difficulty-85-rule.md
- **结果**：85% rule 设计文档完成，明确了 FSRS 兼容方案（review page 不变，practice page 传 ?adaptive=true）。未实施代码，因为：(1) 用户在考试中 (2) 还没有 practice page 触发点
- **决策**：写设计文档而非立即写代码——85% rule 需要一个 practice mode 入口点，而目前只有 FSRS review。贸然修改 questions API 会导致 review queue 静默丢失卡片

### [2026-04-10 04:55:04] Turn 70 〈sonnet-0410d〉
- **用户**：Progress Loop 自动触发 — 再次检查 Daily Report 进度
- **执行**：git log → 发现 35caf55 用户在研究文档写完前已独立实现 adaptive 85%-rule（GET /api/questions 直接 join attempt stats，按学习区间排序）→ 确认 Daily Report 三个文件均已提交 → review/page.tsx 中 sessionStart 引用 3 处确认完整集成
- **结果**：Daily Report ✓ 已完成（上一轮确认）。85% rule 也已被用户独立实现。无阻塞
- **决策**：无重大决策（纯状态确认，用户并行实施超出预期）

### [2026-04-10 05:02:05] Turn 71 〈sonnet-0410d〉
- **用户**：Research Loop 自动触发 — Session Summary Modal UX 调研
- **执行**：读取 mastery-summary/route.ts 确认当前查询模式（2-query: outline_nodes→element_mastery IN）→ 研究三个主题：(1) post-session feedback modal 模式 (2) SRS 摘要界面 UX 研究 (3) Supabase 查询性能 → 写研究文档 → 识别低风险改进：partial index 覆盖 mastery-summary 查询模式 → 创建 015_mastery_summary_index.sql
- **结果**：研究文档完成，优先级行动表产出。Partial index migration 已创建（待 supabase db push）
- **决策**：不实施 count-up 动画或 per-item 列表（需要 sessionAnswered 的 question IDs，当前 review/page.tsx 未追踪）；只实施无破坏性的 index migration

### [2026-04-10 05:06:30] Turn 72 〈sonnet-0410d〉
- **用户**：Research Loop + Progress Loop 同时触发
- **执行**：git log → 确认用户又提交了 2 个功能（live accuracy indicator + attempt history badge）→ 派 WebSearch subagent 调研 5 个方向 → 写研究文档 → 实施最高 ROI 改进：ai.ts 新增 fastModel（qwen-turbo），exam-prep Step 1 + exam-scope 切换到 fastModel（简单 JSON 提取任务无需推理模型）
- **结果**：fastModel 导出完成；exam-prep topic extraction + exam-scope KP matching 节省约 70% token 成本。研究文档记录 FSRS v6/语义分组/Signal-Map Presence 等后续方向
- **决策**：FSRS v6 不现在切换（localStorage SRS 数据，改版本会导致现有卡片 due date 重算）；`use cache` 不现在开（仍是实验性 flag）；只实施无破坏性的模型降级

### [2026-04-10 05:07:20] Turn 73 〈sonnet-0410d〉
- **用户**：Progress Loop 自动触发 — Daily Report 进度检查
- **执行**：验证 3 个文件存在 + review/page.tsx 中 sessionStart 3 处引用 → 确认集成完整
- **结果**：Daily Report ✓ 完成（第 4 次确认）。新增：用户又提交了 7272e59（attempts POST rate-limit 防止自适应排序被刷）
- **决策**：无重大决策

### [2026-04-10 05:12:51] Turn 74 〈sonnet-0410d〉
- **用户**：Progress Loop 自动触发 — 项目进度检查
- **执行**：git log → 用户新提交 97154d1（teach-back panel）→ 读取 teach-back/route.ts → 发现关键 bug：quality 结果未持久化，has_teaching_challenge_pass 永远不被写入 → 读取 mastery-v2.ts 确认此字段是 proficient→mastered 的唯一门控条件 → 读取 challenge_logs schema → 修复：select 加 knowledge_point_id，AI 评分后写 challenge_logs + upsert element_mastery.has_teaching_challenge_pass
- **结果**：修复完成，teach-back 现在能真正解锁 mastery 进阶路径
- **决策**：只在 quality="strong" 时写 has_teaching_challenge_pass（partial/missing 不应触发）；challenge_logs 始终写（记录历史，profile 页面依赖）；final_confidence 映射 strong→solid, partial→partial, missing→none

### [2026-04-10 05:14:12] Turn 75 〈sonnet-0410d〉
- **用户**：Progress Loop 自动触发 — Daily Report 进度检查（第 5 次）
- **执行**：确认两文件存在 + review/page.tsx sessionStart 3 处 → 新增：用户又提交 0aebcae（答题前元认知置信度评分）
- **结果**：Daily Report ✓ 完成。项目持续高速推进中
- **决策**：无重大决策

### [2026-04-10 05:23:33] Turn 76 〈sonnet-0410d〉
- **用户**：Progress Loop 自动触发 — 项目进度检查
- **执行**：git log → 用户又提交 metacognitive confidence + calibration panel → 读取 attempts/route.ts → 发现 element_mastery.times_tested/times_correct 从未被 attempts 写入，导致 exposed→practiced 升级条件永远无法满足 → 读取 mastery-v2.ts 确认升级条件（timesTested>=5, recentAccuracy>=0.70）→ 修复：attempts 路由 select 加 knowledge_point_id，insert 后 update element_mastery 计数器
- **结果**：mastery 计数器现在随每次 attempt 更新。exposed→practiced 升级路径正式可用
- **决策**：不调用 evaluateLevel()（依赖 FSRS stability 在 localStorage，服务端无法读取）；只更新计数器，留给未来的评估触发器使用

### [2026-04-10 05:24:12] Turn 77 〈sonnet-0410d〉
- **用户**：Progress Loop 自动触发 — Daily Report 进度检查（第 6 次）
- **执行**：确认两文件存在 + review/page.tsx sessionStart 3 处 → 同步：用户在 attempts/route.ts 加入了 misconception 自动记录（答错时写 misconceptions 表）
- **结果**：Daily Report ✓ 完成。无阻塞
- **决策**：无重大决策

### [2026-04-10 05:32:19] Turn 78 〈sonnet-0410d〉
- **用户**：Progress Loop 自动触发 — 项目进度检查
- **执行**：git log → 用户又提交 misconception auto-resolve + explanation gating → 读取最新 attempts/route.ts（用户整合了我的计数器修复 + 增加了 misconception auto-resolve 逻辑）→ 识别剩余 gap：times_non_mcq / has_non_mcq_correct 未写入（practiced→proficient 需要此字段）→ 修复：扩展 select + update，fill_blank/short_answer 时递增 non_mcq 计数器，答对时设 has_non_mcq_correct = true
- **结果**：practiced→proficient 升级路径现在有完整数据支撑
- **决策**：true_false 归类为 MCQ（二选一识别，无生成，认知负荷低）；fill_blank + short_answer 归类为 non-MCQ（需要自由回忆）

### [2026-04-10 05:34:08] Turn 79 〈sonnet-0410d〉
- **用户**：Progress Loop 自动触发 — Daily Report 进度检查（第 7 次）
- **执行**：确认两文件存在 + review/page.tsx sessionStart 3 处
- **结果**：Daily Report ✓ 完成。无阻塞
- **决策**：无重大决策

### [2026-04-10 05:42:24] Turn 80 〈sonnet-0410d〉
- **用户**：Progress Loop 自动触发 — 项目进度检查
- **执行**：git log → 用户提交 FSRS server sync（8802ab9）+ rate-limit fix（6daee06）→ 读取 fsrs/route.ts 确认不传播 stability 到 element_mastery → 在 POST 路由 upsert 完成后追加：IN-join 拿 kp_ids，计算每个 KP 的 max stability + FSRS retrievability 公式（R = (1 + t/(9S))^-1），更新 element_mastery.fsrs_stability/fsrs_retrievability/fsrs_last_review
- **结果**：mastery-v2 的 practiced→proficient 门控（fsrsStability >= 7）现在有完整数据。downgrade 检查（retrievability < 0.70）也可正确评估
- **决策**：用 MAX stability 策略（同 KP 多题时取最高稳定性）；retrievability 保留 3 位小数；Promise.all 并发更新各 KP，不串行

### [2026-04-10 05:44:09] Turn 81 〈sonnet-0410d〉
- **用户**：Progress Loop 自动触发 — Daily Report 进度检查（第 8 次）
- **执行**：确认两文件存在 + review/page.tsx sessionStart 3 处
- **结果**：Daily Report ✓ 完成。无阻塞
- **决策**：无重大决策

### [2026-04-10 05:52:56] Turn 82 〈sonnet-0410d〉
- **用户**：Progress Loop 自动触发 — 项目进度检查；同步：用户提交 d690156（evaluateLevel 接入 attempts）+ fsrs_review_logs 改为 upsert+ignoreDuplicates
- **执行**：git log → 确认 evaluateLevel() 已接入（mastery pipeline 完整闭环）→ 读取 STATUS.md 识别已过时的 Known Issues → 更新 STATUS.md（增加 Phase 6 章节，列出全部已完成功能和待推送 migrations）
- **结果**：STATUS.md 更新完毕，准确反映当前状态。mastery pipeline 从数据采集到评级升降完整运行
- **决策**：recentAccuracy 近似（全局准确率替代 last-5）暂不改，需要额外查询且影响小；crossConceptOk 始终 true 的近似暂保留，避免每次 attempt 触发昂贵 count 查询

### [2026-04-10 05:54:08] Turn 83 〈sonnet-0410d〉
- **用户**：Progress Loop 自动触发 — Daily Report 进度检查（第 9 次）
- **执行**：确认两文件存在 + review/page.tsx sessionStart 3 处 → 新增：b91f1fc CalibrationPanel i18n 提取
- **结果**：Daily Report ✓ 完成。无阻塞
- **决策**：无重大决策

### [2026-04-10 06:01:17] Turn 84 〈sonnet-0410g〉
- **用户**：续接上轮压缩上下文，继续 CourseHub 工作
- **执行**：(1) 创建 Research + Progress Cron jobs；(2) 审计 attempts/route.ts — 确认 last-5 window recentAccuracy 已由用户实现（embedded join 方案），attempt history badge 已在 QuestionCard 中；(3) TypeScript build 检查（通过）；(4) 更新 STATUS.md 已知问题，标记 recentAccuracy 和 has_transfer_correct 已解决
- **结果**：无新代码写入，均为确认已完成状态；STATUS.md 同步到实际代码状态
- **决策**：嵌入式 join 方案（`questions!inner`）对 SELECT 查询可靠，保留用户实现；courseConceptsAtLevel2OrAbove=0 硬编码保留（查询代价高，影响有限）

### [2026-04-10 06:03:25] Turn 85 〈sonnet-0410g〉
- **用户**：Research + Progress Loop 自动触发（合并）
- **执行**：(1) 检查 git log — 确认 3 个新 commit（tree/progress 页迁移到 element_mastery v2，practice 页加 SessionSummaryModal）；(2) 读取 tree/page.tsx + progress/page.tsx，验证逻辑正确；(3) 验证 answered_at 列存在（001_initial_schema.sql:103）；(4) TypeScript build 通过（EXIT:0）；(5) 启动后台 Research Agent（post-session UX + FSRS + SSE）
- **结果**：无 bug 发现，build 干净，所有新功能集成正确
- **决策**：v2ToDisplay 将 proficient+mastered 合并为一个视觉类别 — 合理，5 级内部模型 > 4 色外部展示

### [2026-04-10 06:06:41] Turn 86 〈sonnet-0410g〉
- **用户**：Research Loop 完成通知（acd30a01），3 个主题摘要
- **执行**：(1) 定位 generate-one/route.ts，确认 X-Accel-Buffering: no 已由用户提交（5400a1c）；(2) TypeScript build 通过；(3) 写入研究报告确认
- **结果**：SSE bug 已修复并提交，研究文档写入 docs/research/2026-04-10-research-loop-postmortem.md
- **决策**：无 — 用户比研究 Agent 更快完成了修复

### [2026-04-10 06:07:23] Turn 87 〈sonnet-0410g〉
- **用户**：Progress Loop 自动触发（双重）— Daily Report + 掌握度管道检查
- **执行**：(1) git log 确认 8 个新 commit；(2) 读取 learn/page.tsx SSE consumer — 确认 chunk_index sort（line 184）处理乱序到达；(3) 确认幂等性守卫（line 168，`some(c => c.chunk_index === parsed.chunk_index)`）
- **结果**：SSE 流式传输全验证通过 — 排序正确 + retry 安全。所有 3 个 loop 改进项已完成
- **决策**：无阻塞项。下一优先级：courseConceptsAtLevel2OrAbove 实查询（非高优先级），或等待用户新指令

### [2026-04-10 06:08:06] Turn 88 〈sonnet-0410g〉
- **用户**：Research Loop 自动触发 — 掌握度管道 Phase 6 上下文审计
- **执行**：(1) git log — 新 commit 868fcae（SessionSummaryModal 加错题列表，与 Research Agent 建议吻合）；(2) 更新 loop 上下文：items 1+3 已解决；(3) 启动后台 Research Agent（a1c0e64d）— 聚焦 cross-concept 代理信号、progress page N 查询优化、错题复习 UX 对比
- **结果**：研究 Agent 已启动，等待结果
- **决策**：research prompt 收窄到 3 个具体问题（不宽泛），避免与已完成研究重叠

### [2026-04-10 06:12:38] Turn 89 〈sonnet-0410g〉
- **用户**：Research Loop Agent 完成（a1c0e64d）— 3 主题结果
- **执行**：(1) 分析 crossConceptOk 双重 no-op（hasDownstreamDependents=false 短路 OR，courseConceptsAtLevel2OrAbove 值不影响结果）；(2) 重构 progress/page.tsx：4 顺序查询 → 2 并行组（Promise.all）；(3) mastery-v2.ts 加说明注释；(4) tsc 通过，commit cf90861，push
- **结果**：progress 页面加载减少 1 个 DB 往返延迟。crossConceptOk 无需修复（有意记录为 no-op）
- **决策**：Quick Retry（Topic 3）暂不实现 — 摩擦风险高，需用户测试验证

### [2026-04-10 06:14:25] Turn 90 〈sonnet-0410g〉
- **用户**：Progress Loop 双重触发
- **执行**：(1) git log 确认新 commit fba75f3（首次答题时 bootstrap element_mastery）；(2) 应用 tree/page.tsx 并行化（上轮自我批判中识别的遗漏）：outline_nodes + lessons 并行 → 节省 1 个 DB 往返；(3) tsc 通过，commit db56a41，push
- **结果**：tree 页面加载减少 1 个 DB roundtrip。所有页面查询现已最优并行化
- **决策**：与 progress page 相同模式，复用无需讨论

### [2026-04-10 06:16:54] Turn 91 〈sonnet-0410g〉
- **用户**：Progress Loop 双重触发 — Daily Report 检查 + 一般进度
- **执行**：(1) 扫描所有 RSC 页面找瀑布查询；(2) course/[id]/page.tsx：7 顺序 → 2 并行组（最大收益，最常访问的页面）；(3) tsc 通过，commit e0a3adf，push
- **结果**：课程概览页节省 5 个 DB 往返。并行化系列完成（progress + tree + course）
- **决策**：profile/page.tsx 也有顺序查询但不常访问，低优先级

### [2026-04-10 06:23:52] Turn 92 〈sonnet-0410g〉
- **用户**：Progress Loop 触发
- **执行**：(1) 验证 tsc 通过（e58f8dc actionUrl 变更）；(2) 发现 profile/page.tsx 6 顺序查询，重构为 2 并行组；(3) 用户已在 dc5cb38 提交了最终版本（含 TS 错误修复）；(4) 扫描确认所有主要 RSC 页面并行化完成
- **结果**：全部 RSC 页面查询瀑布已消除（progress + tree + course + profile）
- **决策**：`{ data: null }` 作为空分支比 `as const []` 更干净，让 `?? []` 在使用点处理 null

### [2026-04-10 06:28:45] Turn 93 〈sonnet-0410g〉
- **用户**：Progress Loop 双重触发
- **执行**：(1) 审计 dashboard/page.tsx — 确认是真正的顺序依赖，无法并行化；(2) 读取 SessionSummaryModal — 发现错题列表为纯被动展示；(3) 添加 "Review now →" CTA 链接（/review），将被动列表转为可操作提示；(4) 修复 settings/page.tsx 预存在的 TS2304 — `async function` 声明被 hoist 导致 `isZh` TDZ 问题，改为 const 箭头函数；(5) tsc 通过，commit 9858682，push
- **结果**：两个独立改进合并提交。错题 CTA 增加学习效果（研究支持），TS 错误清除
- **决策**：`function` 声明被 hoist 到 `const isZh` 之前 → TypeScript 严格模式报错。箭头函数不被 hoist，修复正确

### [2026-04-10 06:35:44] Turn 94 〈sonnet-0410g〉
- **用户**：上下文压缩续接，Progress Loop 恢复执行
- **执行**：① 创建 Research Loop (0 * * * *) + Progress Loop (*/10 * * * *) 两个 Cron ② 确认 progress/page.tsx 的 .limit(500) 编辑已在文件中（上轮已写入） ③ git add + commit 814ad7f "perf(course-hub): limit calibration attempts query to 500 most recent" ④ git push origin main
- **结果**：progress/page.tsx 无界 IN 查询已加限流；推送成功。待办：3 个 DB 迁移需 supabase db push
- **决策**：无重大决策，纯续接上轮未完成的 commit

### [2026-04-10 06:44:50] Turn 95 〈sonnet-0410g〉
- **用户**：Progress Loop 双重触发（Daily Report 验证 + 进度检查）
- **执行**：① 审计 SessionSummaryModal + mastery-summary API 均已存在且 committed ② 通过 Supabase MCP 确认所有迁移（015-019）均已应用于生产库 ③ 更新 STATUS.md — 删除"Pending Migrations"节，修复 recentAccuracy Approximations 与 Known Issues 矛盾 → commit 88440c0 ④ 审计 review/practice 页面 `isZh ? "..." : "..."` 内联模式 — 各有 12 处 ⑤ 向 i18n.tsx 添加 27 个新 EN/ZH 键（review.turnOff、practice.examPrep 等），将 20 个可迁移的静态字符串替换为 `t()` 调用；动态插值字符串（含 ${examDays} 等）保留为 ternary ⑥ 顺手修复 practice 页面死 ternary（两分支输出完全相同） → commit 6f6c5f8，push
- **结果**：review 页从 12→3 个 isZh ternary，practice 页从 12→3 个。i18n 一致性提升，未来添加第三语言时只需修改 i18n.tsx
- **决策**：含动态值的字符串（如 `${examDays} 天后`）不迁移，因 i18n 系统无插值支持——添加插值比直接保留 ternary 增加更多复杂度

### [2026-04-10 06:52:51] Turn 96 〈sonnet-0410g〉
- **用户**：Progress Loop 四重触发（Daily Report 验证 + 总进度检查）
- **执行**：① 确认 SessionSummaryModal/mastery-summary 均存在且集成于 review + practice ② Supabase MCP 确认所有迁移已应用 ③ 审计所有仍有 isZh ternary 的文件，按数量排序：learn(11) > TodayView(5) > settings(4) 等 ④ 向 i18n.tsx 添加 learn.* 和 today.examReview 键（各 EN/ZH 各 13 个） ⑤ learn/page.tsx: 9 个静态 ternary → t() ⑥ TodayView: 将 typeLabels 数据对象替换为 typeLabelKeys 映射表，完全消除 isZh ⑦ 发现 library/page.tsx 已用 t("library.deleteConfirm") — 之前 grep 结果系缓存 ⑧ 发现 OutlineTree/SessionSummaryModal/OnboardingWizard 剩余 29 处均为动态值或数据驱动的 zh/en 属性，需 i18n 插值支持才能迁移 → 本次不做 → commit a16f344，push
- **结果**：i18n 一致性大幅提升；learn 页面完全无 isZh；TodayView 从 5→0 个内联 ternary。剩余 29 处动态 ternary 是架构级别，需先为 i18n 系统添加插值支持
- **决策**：OnboardingWizard 用 prefs.language 而非 useI18n() — 设计合理（向导在用户设定 locale 前显示），保留

### [2026-04-10 06:55:12] Turn 97 〈sonnet-0410j〉
- **用户**：续接压缩上下文 — i18n 审计进行中，git push 被 remote 拒绝
- **执行**：① 创建 Research Loop (823c741c) + Progress Loop (1fa011b7) Cron ② git pull --rebase 不需要，直接 git push 成功（remote lock 已解除）③ 发现 learn/page.tsx 已部分更新（Progress Loop 在压缩期间提交了 a16f344）④ 修复 learn/page.tsx 中遗漏的 t() 解构（t 被调用但未从 useI18n() 取出） → commit 5877821 ⑤ 审查 i18n.tsx 未暂存的 11 个 learn.* 键（cron 已添加），一并提交 ⑥ 发现 TodayView/ArchiveButton/settings/library 还有 isZh ternary → 添加 archive.*/library.deleteConfirm/settings.confirm*/colorTheme 等 17 个新键到 i18n.tsx ⑦ 更新 settings/page.tsx（6 处 isZh → t()，移除 isZh）⑧ 更新 ArchiveButton.tsx（3 处 → t()，移除 locale+isZh）⑨ 更新 library/page.tsx（1 处 confirm → t()，移除 isZh）→ commit 694fb91 ⑩ OnboardingWizard 步骤标题/副标题重构：steps 数组从 {title,titleZh,subtitle,subtitleZh} 改为 {titleKey,subtitleKey} key indirection，添加 9 个 onboarding.step* 键 → commit bc0bee7 ⑪ 最终扫描：剩余 5 处 isZh 全部为动态插值（嵌入数字/名称），属合理保留
- **结果**：i18n 审计实质完成。全局剩余 isZh 使用：5 处（全为参数化字符串），无可消除的静态 ternary。推送 3 个 commit 到 remote
- **决策**：OnboardingWizard ROLES/GOALS 的 .en/.zh 数据字段保留（数据对象不是 UI 字符串）；key indirection 模式（titleKey/subtitleKey → t()）为标准做法，后续新功能照此规范

### [2026-04-10 06:58:50] Turn 97 〈sonnet-0410g〉
- **用户**：Progress Loop 三重触发
- **执行**：① git log 确认 694fb91 是新 commit（settings/ArchiveButton/library 翻译）② 运行 TypeScript 构建 → 发现 i18n.tsx:790 报 TS1005 错误 ③ 分析错误原因：`"profile.selfAssessmentDescPre": "你说"不确定"的题中，"` — 字符串内部出现了 ASCII 双引号，导致字符串提前终止（col 42 正好是 "你说" 结束处）④ 将内部引号替换为中文角括号 `「不确定」` ⑤ 重新运行 tsc — 编译成功，无错误 ⑥ commit 5fed1ef，push ⑦ 向 i18n.tsx 添加 learn.* + today.examReview 键，迁移 learn/page.tsx（9处）+ TodayView.tsx（5处，消除 isZh），同时也是 Turn 96 的延续
- **结果**：TypeScript 构建干净。i18n 迁移接近完成，剩余 ~29 处 isZh ternary 均为动态插值或数据驱动，暂不迁移
- **决策**：内部引号改为角括号「」而非转义 \"，因为角括号在中文排版中是标准引用符号，语义更准确

### [2026-04-10 06:59:41] Turn 98 〈sonnet-0410j〉
- **用户**：Progress Loop 七重触发（多个 Loop 同时激活）
- **执行**：① 确认项目推进中（5 次新 commit）② 扫描最高优先级待办 ③ 发现 fsrs_review_logs 无 UNIQUE 约束：upsert onConflict 指定 user_id+question_id+reviewed_at，但 migration 018 只建了普通 INDEX，未建 CONSTRAINT — 重试风暴时静默插入重复行 → 创建 migration 019，apply_migration 到 Supabase，commit 1f8c887 ④ 发现 questions POST 路由（parseExamQuestions AI 调用）无 checkRateLimit → 补充 rate limit 10req/min，commit 3c0e626 ⑤ 检查 Vercel 项目（MCP 需认证，无法自动添加 SUPABASE_SERVICE_ROLE_KEY）⑥ TypeScript noEmit 检查通过（0 errors）⑦ 扫描 dangerouslySetInnerHTML：仅 ThemeInitScript（只插入纯 JS 常量，不含用户输入，安全）⑧ 扫描所有 AI API 路由确认 rate limit 覆盖完整
- **结果**：修复两个真实生产 bug（DB 约束缺失 + AI 路由无限速）。TS 零错误。主要遗留：SUPABASE_SERVICE_ROLE_KEY 未在 Vercel 设置（需用户手动操作）
- **决策**：courseConceptsAtLevel2OrAbove=0 hardcode 保留（文档注释清楚，设计取舍，不是 bug）；questions GET 不加 rate limit（纯 DB 读，RLS 已限制只能查自己数据）

### [2026-04-10 09:38:55] Turn 99 〈sonnet-0410k〉
- **用户**：续接压缩上下文继续工作（空消息触发）
- **执行**：① 创建 Research Loop (hourly) + Progress Loop (10min) Cron ② 正确解析 i18n.tsx EN/ZH 对象边界验证重复键 → EN: 460 keys, ZH: 460 keys, 0 duplicates ③ 盘点剩余 isZh 三元组：11 个，分类为「需插值迁移」5 个、「数据驱动」4 个（OnboardingWizard）、「locale API 调用」2 个 ④ 给 t() 添加插值支持：`vars?: Record<string, string|number>`，正则替换 `{varName}` ⑤ i18n.tsx 新增 7 个 key：outline.delete/deleteWithChildren, session.streak, blueprint.step, review.examDayRecall（EN + ZH 各 7）⑥ 迁移 5 个动态 ternary：LearningBlueprint step、SessionSummaryModal streak + locale API、OutlineTree delete confirm、review/page recall% ⑦ 同步提交研究 Loop 子 agent 已做的 tooltip title 迁移（library, bank, QuestionCard, StudyBuddy）⑧ TS build clean ⑨ commit 50705d2，push ⑩ 派 Playwright 产品测试 agent（后台运行）
- **结果**：i18n 迁移收尾：50+ ternaries → 4（均为 OnboardingWizard 数据驱动模式，无法迁移）。TS 零错误。产品测试进行中
- **决策**：OnboardingWizard 的 role.zh/goal.zh 不迁移 — 使用 Supabase prefs.language，非 useI18n() context，数据驱动模式，迁移需重构数据结构，性价比不够

### [2026-04-10 09:42:02] Turn 100 〈sonnet-0410k〉
- **用户**：多个 Research/Progress Loop cron 触发（批量堆积）
- **执行**：① 创建 Research + Progress Loop Cron ② 派 Explore agent 审计未覆盖组件（join page, MistakePatterns, UsagePanel, TeachBackPanel, StreakBadge, ExamCountdown, KnowledgeTree）③ 确认 join page spinner 是误报（router.push 导航后组件卸载，无需 setLoading(false)）④ KnowledgeTree 全量 i18n：getNodeStyle() 改为返回 labelKey，NodeCircle 调用 t(labelKey)，legend labels 和 empty state 全部改用 t() ⑤ 新增 tree.noNodes EN+ZH 两个 key ⑥ ExamCountdown.handleDelete：添加 res.ok 检查，防止删除失败时 UI 误移除 ⑦ commit 4037933，push
- **结果**：KnowledgeTree 完成 i18n（最后一块硬编码区域）。ExamCountdown 删除 bug 修复。i18n 迁移彻底完成。
- **决策**：getNodeStyle() 用 labelKey 模式而非直接传 t — 因为模块级函数不能调用 hook，而 NodeCircle 作为 React 组件可以。这是解决 module-level + i18n 冲突的标准模式。

# 2026-03-31 编年记录

## 代号索引
- `opus-0331a` — 全局状态审计 + 战略优先级建议
- `opus-0331b` — Revideo headless rendering 技术调研

---

### [23:41] Turn 1 〈opus-0331a〉

**用户说了什么**：「现在假设你很有全局性」— 要求以全局视角审视所有项目的状态、方向和优先级。

**我做了什么**：
- 读取全部 4 个项目的 STATUS.md + CLAUDE.md
- 读取全部 memory 文件（feedback、experience、user philosophy）
- 审查 git log 最近 20 个 commits + 未提交的 diff（13 文件，1637 行变更）
- 输出跨项目全景分析 + 战略建议

**效果**：全景视角已形成，见正文回复。

**关键决策**：将 4 个项目按「用户价值 × 完成度 × 紧迫度」排优先级矩阵。

### [00:35] Turn 2 〈opus-0331b〉

**用户说了什么**：调研 Revideo（Motion Canvas fork）用于 Node.js headless 视频渲染，需要包名、API、场景定义、音频、项目配置。

**我做了什么**：
- WebSearch 3 轮 + WebFetch 官方文档 6 页 + GitHub API 直接拉取 4 个源文件
- 确认包名：@revideo/core, @revideo/2d, @revideo/renderer, @revideo/cli, @revideo/ui, @revideo/vite-plugin
- 拿到真实 project.tsx、render.ts、tsconfig.json、package.json 来自 redotvideo/examples 仓库
- 整理 renderVideo() 完整参数表 + Audio 组件用法 + useScene().variables 参数化模式

**效果**：完整可复制粘贴的 setup 指南已产出。

**关键发现**：v0.10.4 是最新版。团队主要精力转向 Midrender（商业产品），但 OSS 版仍在发布。headless 渲染不需要 vite.config.ts，只有 GUI editor 才需要。

### [01:09] Turn 3 〈opus-0331a〉

**用户说了什么**：「你去开发吧」

**我做了什么**：
- 更新 spec（Revideo 替代 Motion Canvas、砍 OpenStax、时间加权分配、erase.target 重命名）
- 从零搭建 course-video/ 项目：8 个源文件
  - types.ts: Zod schema + 时间分配算法
  - script-generator.ts: Claude/DeepSeek 双 LLM + few-shot example
  - tts.ts: DashScope qwen3-tts-flash 逐段合成
  - renderer/: Revideo 板书场景 + 元素注册表 + 6 种操作渲染
  - pipeline.ts: 4 步管线编排
  - cli.ts: CLI 入口
- npm install 307 packages, tsc 零错误
- 渲染测试通过：1920x1080 H.264 MP4, 12.6s, 422KB

**效果**：管线可用。Revideo headless 渲染 + 板书动画 + 时间分配 + 元素引用全部工作。

**关键决策**：Audio 组件加空路径守卫（首次测试因空 src 卡死）。

### [01:35] Turn 4 〈opus-0331b〉

**用户说了什么**：调研 course-video 中如何将 LaTeX 公式渲染为真正数学排版（当前是 monospace 纯文本），包括 Revideo/Motion Canvas 内置方案、KaTeX/MathJax 服务端方案、黑板视觉风格改进。

**我做了什么**：
- 5 轮并行 WebSearch 覆盖：Motion Canvas Latex 组件、Revideo fork 状态、KaTeX server-side、MathJax node、chalkboard CSS/fonts
- 读取 `@revideo/2d/src/lib/components/Latex.ts` 源码，确认组件已存在且使用 `mathjax-full` 做 TeX-to-SVG 转换
- 验证 `mathjax-full@^3.2.2` 已作为 `@revideo/2d` 的直接依赖安装在 node_modules
- 排除 KaTeX（输出 HTML spans 非 SVG）和独立 MathJax（多余）
- 调研 Google Fonts 中 chalk 风格字体：Permanent Marker、Architects Daughter、Fredericka the Great
- 输出完整研究报告含实现代码

**效果**：发现零依赖解决方案 -- `@revideo/2d` 已内置 `Latex` 组件，改 board.tsx 约 20 行即可。

**关键发现**：`Latex` 继承自 `Img`，内部用 `mathjax-full` 的 `liteAdaptor` 把 TeX 转为 SVG data URI。颜色必须通过 `\color{...}` 写在 TeX 字符串内部，不能用 fill prop。支持 `{{part}}` 语法做公式变形动画。

### [01:19] Turn 5 〈opus-0331a〉

**事件**：全链路端到端测试成功。

**结果**：
- DeepSeek 生成 11 段教学脚本 → DashScope TTS 合成 89.4s 音频 → Revideo 板书渲染 → FFmpeg 音视频合成
- 输出：`Quadratic-Formula.mp4`, 1920×1080 H.264 + AAC, 78s, 4.5MB
- 关键修复：Revideo `<Audio>` 组件无法加载本地文件（headless Chromium CORS 限制），改为 Revideo 只渲染视频 + FFmpeg 后合成音频

### [01:45] Turn 6 〈opus-0331a〉

**事件**：尝试将 Latex 组件集成到 board.tsx，headless 模式下崩溃（Puppeteer error: null）。

**处理**：回退到 monospace 文本显示。LaTeX 真实渲染需要走 Node.js 端预渲染 SVG → Img 组件的路径，留给下一轮迭代。

**教训**：Revideo 组件在 GUI editor 和 headless renderer 中的行为不一定一致。新组件集成必须先跑 smoke test。

### [02:06] Turn 7 〈opus-0331a〉

**用户说了什么**：「时间简史的更新在哪」— 询问编年记录位置。

**我做了什么**：展示编年记录 + 补全 Turn 5-7 的缺失记录。

### [02:45] Turn 8 〈opus-0331c〉

**用户说了什么**：调研 DashScope CosyVoice 声音复刻 API，需要上传参考音频 + 用克隆声音合成新文本的完整流程。

**我做了什么**：
- 3 轮 WebSearch + 4 轮 WebFetch 覆盖阿里云官方文档（EN/ZH）、CSDN 博客、npm 包
- 确认两步架构：REST API 创建音色（POST /api/v1/services/audio/tts/customization）→ WebSocket 合成语音
- 关键发现：TTS 合成只支持 WebSocket，没有 HTTP REST 端点
- 整理完整参数表、音频要求、定价、Node.js 代码示例
- 写入 `docs/research/2026-03-31-dashscope-cosyvoice-cloning.md`

**效果**：完整可用的 Node.js 示例代码（voice registration + WebSocket TTS synthesis）已产出。

**关键发现**：`target_model` 创建时必须与合成时一致否则失败。v3.5 模型仅 Beijing 可用。每账号最多 1000 个音色。

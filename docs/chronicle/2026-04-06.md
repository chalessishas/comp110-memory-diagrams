## 代号索引
| 代号 | 模型 | 对话摘要 |
|------|------|----------|
| opus-0406a | Claude Opus 4.6 | CourseHub 深度审查 → 安全修复 → 部署 → Learn chunk 架构 → 教学环节验证 |

### [2026-04-06 04:16:50] Turn 1 〈opus-0406a〉
- **用户**：要求对 workspace 中的 course hub 项目进行深度审查
- **执行**：读取了 course-hub 项目的全部关键文件——CLAUDE.md、package.json、STATUS.md、所有 API routes（courses, attempts, generate, parse, upload, questions, outline, share, fork, chat, preview/learning）、数据库迁移文件（001-006）、核心库文件（ai.ts, schemas.ts, mastery.ts, rate-limit.ts, spaced-repetition.ts, middleware.ts）、关键组件（QuestionCard.tsx）
- **结果**：产出 23 项审查发现，按 Critical/High/Medium/Low 分级，附修复优先级排序表。发现 6 个安全漏洞（GET 无认证、storage policy 过宽、preview API 可被滥用烧额度、内存 rate limit 在 serverless 上失效）、4 个数据完整性问题（答案明文暴露、outline 非原子保存、判分不一致、short answer 判分粗糙）、5 个架构问题（SRS 数据仅存 localStorage、无并发控制、service_role 使用范围过大）
- **决策**：审查发现答案暴露（#8）和 preview API 无保护（#5）是最紧急的 P0 问题，建议在任何新功能开发前先修复

### [2026-04-06 04:27:18] Turn 2 〈opus-0406a〉
- **用户**：确认修复，说"你去修吧"
- **执行**：按优先级修复了 10 项问题，改动 16 个文件 +107/-44 行：
  - P0 #8: questions API 不再返回 answer/explanation，QuestionCard 改为从 attempt 响应拿答案
  - P0 #5: preview/learning 和 parse API 全路径加 auth
  - P0 #1-3: 6 个 GET endpoint 补 auth guard（courses/[id]、outline、questions、lessons、exams、lesson chunks）
  - P1 #4: 新建 010_fix_storage_policies.sql，storage 限制只能操作自己目录
  - P1 #7: 新建 011_outline_upsert_atomic.sql，outline 保存改用 SECURITY INVOKER RPC 事务
  - P1 #13: generate API 加 per-course rate limit 防并发
  - P1 #10: 前端不再自行判分，统一由服务端 attempts API 判定
  - P2 #17: upload API 不再用 getPublicUrl，存 storage path
  - P2 #16: 所有 7 个 generateText 调用加 30s AbortSignal.timeout
  - P2 #5b: chat API 加 per-user rate limit (20/min)
- **结果**：全部 P0-P2 修复完成，未引入新依赖，类型系统更新（Question 拆分为 Question + QuestionWithAnswer）
- **决策**：SECURITY INVOKER 而非 SECURITY DEFINER 用于 outline RPC，因为之前 commit b3ee333 已经踩过 SECURITY DEFINER 的坑

### [2026-04-06 04:29:03] Turn 3 〈opus-0406a〉
- **用户**：问"下一步是什么"
- **执行**：分析项目当前状态（安全修复未 commit、V2 功能在 fix 阶段、无真实用户测试），提出三条路线
- **结果**：建议路线 A（commit → 实测 → 上线），给出具体步骤和工作量估算
- **决策**：推荐"先收尾上线"而非继续开发新功能，因为代码存在 ≠ 产品可用

### [2026-04-06 04:35:25] Turn 4 〈opus-0406a〉
- **用户**：确认"你去吧"，执行路线 A
- **执行**：
  1. git add + commit + push 安全修复（18 files, +172/-44）
  2. 在 Supabase 上运行 010_fix_storage_policies + 011_outline_upsert_atomic 两个 migration，均成功
  3. 添加 QuestionCard submitting spinner（Loader2 + disabled 状态）
  4. 测试 outline RPC：验证函数签名（SECURITY INVOKER）+ 55 节点 round-trip 测试通过
  5. 发现 supabase.rpc() 不需要 JSON.stringify（会双重编码），修复为直接传数组
  6. 创建 PR #1 → squash merge 到 main
  7. 触发 Vercel 部署（进行中）
- **结果**：所有代码已合并到 main，Supabase migration 已应用，Vercel 部署进行中
- **决策**：发现 supabase.rpc() 的 jsonb 参数不需要 JSON.stringify——JS client 自动做序列化。这是测试 RPC 时发现的潜在 bug，及时修复

### [2026-04-06 04:40:10] Turn 5 〈opus-0406a〉
- **用户**：（无新输入，继续执行路线 A）
- **执行**：
  1. 创建 PR #1 (chalessishas/T-DBDD) → squash merge 到 main
  2. 安装 Vercel CLI，首次部署失败（attempts route TS 错误：select 缺少 explanation 字段）
  3. 修复：attempts query select 加 "explanation"，commit 到 main，重新部署
  4. 部署成功：https://course-hub-sable.vercel.app
  5. 端到端验证：
     - GET /api/questions?courseId=XXX → 401 Unauthorized ✓
     - GET /api/courses/XXX → 401 Unauthorized ✓
     - 首页正常渲染，导航完整 ✓
- **结果**：CourseHub 安全修复已上线生产环境，所有 auth guard 验证通过
- **决策**：部署时发现 attempts route 的 select clause 只有 "answer, type" 但代码访问了 explanation，这是改动时的遗漏——说明改 API response 时必须同步检查 select clause

### [2026-04-06 05:00:00] Turn 6-8 〈opus-0406a〉
- **用户**：确认走路线 A→B，用 Cal2 syllabus 测试；给出 Dr. Joe Midterm 3 邮件
- **执行**：
  1. 改 parseSyllabusText prompt → AI 生成嵌套 knowledge_point children（验证：22 nodes, 17 KPs）
  2. 修 generate pipeline：顺序执行 + 5 KP cap + study tasks only（跳过 questions，超时问题）
  3. 修 AI schema：handle bare arrays + lenient field mapping（AI 返回 {1:[...]} 而非 {tasks:[...]}）
  4. maxDuration=60 加到所有 AI routes
  5. 手动插入 12 道 Midterm 3 针对性题目（DashScope 太慢导致 Vercel 超时）
  6. Practice tab 加 "Generate Practice Questions" 按钮 + "Exam Prep" 粘贴考试范围功能
- **结果**：创课流程跑通（嵌套 outline），Practice 做题全链路验证（答案隐藏 → 判分 → explanation 揭示 ✓）
- **决策**：Vercel Hobby 60s 硬限制 + DashScope ~10s/call → 长期需要 Supabase Edge Function 或 background job

### [2026-04-06 08:00:00] Turn 9-12 〈opus-0406a〉
- **用户**：问"教学环节呢"→ 讨论 lesson 的教学机制 → 确认方案 D（大纲 + 并行 chunk + lazy remediation）
- **执行**：
  1. 验证 DashScope 并行性能：3 并行调用 = 14.6s（vs 串行 36s），并行可行
  2. 实现 generateLessonChunks()：Phase 1 大纲（~2s）+ Phase 2 并行 4 chunks（~15s）
  3. 新 API: POST /api/courses/[id]/lessons/generate-one（单 KP 按需生成）
  4. 重写 Learn 页面：显示所有 KP → 点哪个生成哪个 → ✓/✨ 状态
  5. 修 lesson_chunks INSERT RLS policy（缺失导致 chunks 无法保存）
  6. 修 MarkdownRenderer：KaTeX ErrorBoundary（崩溃时 fallback 到 plain markdown）
  7. 最终验证：教学内容 + LaTeX 数学公式完美渲染，Checkpoint MCQ 可交互
- **结果**：Learn 教学环节完整跑通。点击 "Integration by parts" → 15-20s 生成 4 个 interactive chunks → 读内容 → 做 checkpoint → Continue → 完成
- **决策**：方案 D 的"大纲先行 + 全并行 chunk"架构被验证可行。DashScope 并行不限速。KaTeX 在 display math 上工作正常

### [2026-04-06 09:25:18] Turn 13 〈opus-0406a〉
- **用户**：要求研究四个技术主题：Supabase Realtime 分块推送、Vercel AI SDK 流式生成、分块电子学习最佳实践、DashScope API 优化
- **执行**：进行 11 次 WebSearch 覆盖所有主题细节（RLS 兼容性、连接限制、streamObject array mode、useObject hook、Qwen 社区 provider、微学习 chunk 尺寸研究、interleaved vs blocked practice 2025 新论文、DashScope Batch API 50% 折扣、incremental_output 流式参数）
- **结果**：产出 `docs/research/2026-04-06-learn-module-improvements.md`，含 4 个主题各 3-5 条发现 + 5 条优先级排序的 Next Steps。核心发现：Vercel AI SDK streamObject array mode 直接解决流式推送（无需 Supabase Realtime）；DashScope Batch API 可省 50% 成本；研究推荐 hybrid blocked+interleaved retrieval practice
- **决策**：推荐 streamObject 而非 Supabase Realtime 做 lesson streaming，因为 AI SDK 直接处理 SSE 流、无需额外基础设施、延迟更低

### [2026-04-06 10:27:49] Turn 13 〈opus-0406a〉
- **用户**：提供认知科学综述论文 → 说"自动推进"
- **执行**：基于证据实施三个教学改进：
  1. Pretesting（d=0.35-1.1）：chunk 0 改为先测再教 + UI 提示"答错没关系"
  2. Interleaved practice（g=0.42）：questions API 改为随机排序
  3. Mixed checkpoints：prompt 轮换 MCQ→fill_blank→open 类型 + types.ts 加 fill_blank
- **结果**：3 个 commit，部署成功。教学架构从"翻页读书+选择题"升级为"预测→讲解→混合题型→开放式回答"
- **决策**：只改 prompt 和一行排序代码 = 最小改动量实现最大教学效果提升。没有改前端架构（ChunkLesson 已支持所有题型）

### [2026-04-06 10:51:39] Turn 14 〈opus-0406a〉
- **用户**："你可否自己推进下一"
- **执行**：自主选择最高优先级任务——修 open/fill_blank 判分
  1. fill_blank: trim+lowercase+去标点+contains匹配+数值比较
  2. open: 关键词 overlap ≥40% + self-assessment 按钮（"I got it right"/"I need to review"）
  3. 清理旧 lesson 数据让用户可以体验完整新流程
- **结果**：部署成功。完整教学链路：pretest → 教学内容 → MCQ → fill_blank → open(self-assess) → 完成
- **决策**：选择前端模糊匹配而非 AI 判分——lesson checkpoint 目的是激活思考（retrieval practice），不需要精确判分。self-assessment 还能训练元认知（g=0.57）

# Chronicle — 2026-04-08

## Code Index
| Code | Model | Session Summary |
|------|-------|-----------------|
| opus-0408a | Claude Opus 4.6 | Post-skeleton research: design system, a11y, performance, competitors |
| opus-0408b | Claude Opus 4.6 | DashScope JSON mode + Vercel AI SDK research |
| opus-0408c | Claude Opus 4.6 | i18n migration for review/page.tsx hardcoded strings |
| opus-0408d | Claude Opus 4.6 | i18n migration for settings/page.tsx + ArchiveButton.tsx |

---

### [2026-04-08 02:05:26] Turn 1 (opus-0408a)
- **User**: Requested research agent task — investigate improvements after CSS skeleton strip across 4 dimensions: design system, accessibility, SSE/FSRS performance, competitive analysis
- **Execution**: Ran 10 WebSearch queries covering educational platform UI trends, WCAG accessible color patterns, Next.js SSE streaming best practices, FSRS web storage (localStorage vs IndexedDB), Tailwind v4 design tokens, AI study tool competitors (Coursebox, Mindgrasp, StudyFetch, Penseum). Checked existing research docs to avoid duplication. Wrote comprehensive findings to `course-hub/docs/research/research-loop-2026-04-08-post-skeleton.md`
- **Result**: Research document produced with 5 sections, 15 findings, and a prioritized 9-item action plan. Key immediate actions: define design tokens, audit SSE config, add non-color indicators before restoring color.
- **Decision**: Focused English-market competitors (prior analysis covered Chinese market). Recommended Tailwind v4 `@theme` token system over ad-hoc CSS variables. Recommended localforage as drop-in localStorage replacement rather than raw IndexedDB API.

### [2026-04-08 02:31:53] Turn 1 (opus-0408b)
- **User**: Asked whether DashScope (Alibaba Cloud Qwen API) supports `response_format: { type: "json_object" }`, and whether Vercel AI SDK `@ai-sdk/openai` passes it through in `generateText()`
- **Execution**: Ran 5 WebSearch queries + 4 WebFetch calls to Alibaba Cloud docs (json-mode page, structured-output page) and AI SDK docs (generateText ref, structured data guide, OpenAI provider page). Cross-referenced findings across all sources
- **Result**: Confirmed DashScope supports `json_object` for all major Qwen models (qwen3.5-plus, qwen3-max, etc.) in non-thinking mode. Key constraint: messages must contain word "json". AI SDK handles `response_format` automatically via `Output.object()` / `Output.json()`. Provided 3 configuration options (Output.object, Output.json, raw OpenAI SDK fallback) with risk analysis
- **Decision**: Recommended testing with `Output.json()` first before `Output.object()` because the latter may send `json_schema` type which some DashScope models might not support. Raw OpenAI SDK fallback is the safest option for guaranteed compatibility

### [2026-04-08 04:20:16] Turn 1 (opus-0408c)
- **User**: Migrate all hardcoded bilingual strings in review/page.tsx to the i18n system
- **Execution**: Read review/page.tsx (320 lines) and i18n.tsx (640 lines). Identified 16 hardcoded string sites using `isZh ? "中文" : "English"` pattern. Added 16 new keys under `review.*` prefix to both en and zh sections of i18n.tsx. Replaced all 16 hardcoded strings in review/page.tsx with `t()` calls. Removed unused `isZh` variable and `locale` destructure.
- **Result**: Zero `isZh` references remaining in review/page.tsx. All user-visible strings now go through i18n. Keys added: examMode, daysUntilExam, weakestFirst, turnOff, setExamDate, cancel, examScope, knowledgePoints, clearScope, setScopeDesc, scopePrompt, scopePlaceholder, matchKp, toReview, examDayRecall, notYetReviewed.
- **Decision**: Used `review.cancel` instead of reusing `misc.cancel` to keep review page self-contained and allow future differentiation. Template strings with variables kept as JS interpolation since the i18n system has no built-in interpolation support.

### [2026-04-08 04:20:50] Turn 1 (opus-0408d)
- **User**: Migrate hardcoded English strings in settings/page.tsx and ArchiveButton.tsx to i18n
- **Execution**: Read all 3 files (settings/page.tsx 313 lines, i18n.tsx ~640 lines, ArchiveButton.tsx 47 lines). Added 19 new i18n keys (15 under `settings.*`, 4 under `courses.*`) to both en and zh sections. Replaced 12 hardcoded string sites in settings/page.tsx and 5 in ArchiveButton.tsx with `t()` calls. Added `useI18n` import to ArchiveButton.tsx. Fixed a latent bug: `passwordMessage.includes("success")` color check broke for Chinese locale — replaced with a `passwordSuccess` boolean state.
- **Result**: Zero hardcoded user-facing English strings in either file. New keys: settings.passwordSuccess, confirmClearStudy, confirmClearReview, confirmDeleteAccount, confirmDeleteFinal, namePlaceholder, passwordPlaceholder, semesterPlaceholder, aiModelManaged, aboutTitle, version, stack, storage, aboutDesc, courses.confirmDelete, courses.archive, courses.restore, courses.delete.
- **Decision**: Used `courses.*` prefix for ArchiveButton keys (not `settings.*`) since they belong to course-level actions. Fixed the `includes("success")` color detection bug proactively — it would silently fail for zh locale, showing green for error messages.

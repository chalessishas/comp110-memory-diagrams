# 编年 2026-04-29

## 代号索引
| 代号 | 模型 | 对话摘要 |
|------|------|----------|
| opus-0429a | Claude Opus 4.7 (1M) | COMP 110 quiz practice 站点重设计：架构调研 + 三方案提案 |

### [2026-04-29 09:08:04] Turn 1 〈opus-0429a〉
- **用户**：希望把 `comp110-26s-site/static/practice/` 下的 5 个 quiz 复习页前端从沿用 COMP 110 站点风格，改为参照 `comp110-memviz` 的设计；并且把题型从纯阅读式扩展到考试体验（MCQ + 填空求值 + 识别 + 写代码 + memory diagram）。附 5 份 quiz 已批改答卷 PDF（QZ00–QZ04）。
- **执行**：read-only 探索。`ls`/`find` 定位到 memviz、practice 目录、redesign 设计稿、chronicle 目录；读 `App.tsx`/`App.css`/`qz00Problems.ts`/`QZ00_practice_1.html` 摸清两边技术栈与设计 token。没有修改任何文件（workspace CLAUDE.md 强制要求架构理解先行）。
- **结果**：输出架构概要表 + 三方案对比（A 在 memviz 内加 Quiz Practice 模式 / B 新建独立项目 iframe 嵌 memviz / C 仅换皮）+ 推荐方案 A，向用户提出 3 个待确认问题（方案选择 / quiz 范围 / 是否做 rubric partial credit 校验）。
- **决策**：暂不动文件、等用户拍板方案。推荐方案 A 是因为 memviz 已经有最重的基建（解释器 + xyflow），扩展成 quiz 平台是「加题型组件」而非「重做」。

### [2026-04-29 09:37:49] Turn 2 〈opus-0429a〉
- **用户**：选方案 A、按知识点分类（不要按 quiz 编号）、尽量拟真考试体验、含 partial credit 规则。
- **执行**：在 `comp110-memviz` 增量加 Quiz Practice 模式。
  - 新建 `src/components/quiz/types.ts`（Question schema：MCQ / Evaluate / IdentifyLines / WriteCode / MemoryDiagram，含 `PartialCreditRule` 和 12 个 topic meta）
  - 新建 `src/data/quiz/quizBank.ts`（按 11 知识点 + memory-diagrams 共 94 题，从 QZ00–QZ04 PDF 抄录，含 rubric 评分点）
  - 新建 `src/components/quiz/QuizPractice.tsx`（主组件：topic 侧栏 + 题目卡片 + 5 种题型组件 + 评分逻辑 + ResultCard）
  - 新建 `src/components/quiz/QuizPractice.css`（按 memviz mint/teal palette + Cabin 字体；MCQ 用气泡填涂样式；行号识别用大圆圈；result card 绿/红双色）
  - 改 `src/App.tsx`（顶层加 `mode: 'tool' | 'quiz'`，header 中段加 mode-toggle，条件渲染 workspace 或 QuizPractice，embed 模式强制 tool）
  - 改 `src/App.css`（mode-toggle pill 样式 + site-header 加 gap）
  - 修 4 个 typecheck 错误：给 `IdentifyLinesQuestion`/`EvaluateQuestion` 加 `explanation?` 字段；`MemoryDiagramQuestion.source` 重命名为 `program` 避免和 `BaseQuestion.source`（citation）冲突
- **结果**：`pnpm tsc --noEmit` 干净通过；Vite dev server 起在 5173，Playwright 实测 4 个流程：① mode toggle 切换 → ② Types & Expressions Q1.1 选 int 提交 → 100% 正确反馈、topic 进度 1/16；③ Memory Diagrams Q5（QZ00 estimate/crunch）填 "1.0" 提交 → DiagramCanvas + Timeline + StepControl 完整渲染（9 个 snapshot），output match 100%；④ Function Anatomy Q3.1 显示行号气泡 1/3/5/9/13。Console 0 errors 0 warnings。
- **决策**：① schema 上不做 multi-select MCQ，把 QZ02 Q1 的多选合并成"list/list+dict/all 三选项"单选，复杂度从 O(n choose k) 降到 O(n)，自学价值不变。② Memory Diagram 题型走"先猜 output → reveal 后才跑解释器"，避免学生没思考就看答案，也避免无谓的 CPU。③ 题量先做 94 题（覆盖每个 topic 至少 3-7 道精题），不追求 100% 覆盖 PDF 每一小问，避免机械填数据稀释质量。

### [2026-04-29 09:41:20] Turn 3 〈opus-0429a〉
- **用户**：问"我在哪使用呢？"——想知道 Quiz Practice 怎么访问。
- **执行**：`git remote -v` + `grep README.md` 查到本地路径和线上 URL `https://comp110-mem-diag.vercel.app/`。没动 git。
- **结果**：告知两个入口（① `pnpm dev` → localhost:5173 ② push 后 Vercel 自动部署）。明确指出当前 4 个新/改文件**未 commit**，所以线上还没有 Quiz Practice。
- **决策**：未经用户许可不做 commit + push，提示给出 git 命令让用户决定。同时主动提醒**学术诚信风险**——这个 memviz 仓库是 public 的，把考题答案推上线可能违反 UNC honor code，建议本地使用即可。

### [2026-04-29 09:50:12] Turn 5 〈opus-0429a〉
- **用户**：让我帮他起草一份评估表答案——「请描述你使用 Agent 或 AI 驱动构建的具体成果」（疑似 Anthropic token plan 提额 / 赠金申请）。
- **执行**：read-only 调研。① 读 `~/.claude/projects/-Users-shaoq/memory/project_sts2_vtuber.md` 和 `project_ai_detector_direction.md` 拿到两个项目的架构和定位；② `ls "VScode Workspace/"` 列出全部项目；③ `cd comp110-memviz && git log --oneline | wc -l` → 1638 commits；ai-text-detector → 111；ai-vtuber → 913；④ 读 comp110-memviz 的 README 拿到线上 URL `https://comp110-mem-diag.vercel.app/` 和功能清单。没改任何文件。
- **结果**：交付一份约 480 字的可粘贴答案（三项目分点 + 统一 Agent 工作流段落），并附 5 条实质自我批判：包括「commit 数含大量 chronicle docs，可能稀释」「prompt injection 修复表述是双刃剑」「需要确认 memviz 仓库 public 才能让评估官打开」「检测器/VTuber 用的是 DeepSeek 不是 Claude API，不能编造」「chronicle/memory 系统本身可能比堆项目更打动 Anthropic 评估官」。
- **决策**：① 不实际填表/提交（评估表的最终内容由用户拍板）；② 不动 git；③ 主动提示风险（公开仓库 + commit 注水）让用户提交前自查；④ 末尾留一句"要不要 200 字精简版备用"留出迭代口子。无重大架构决策。
- **用户**：「是的。push」——确认推送到线上，知晓且接受了我警告的风险。
- **执行**：① 确认 git toplevel 在 `VScode Workspace/`（monorepo），remote 是 `comp110-memory-diagrams.git`。② 显式只 add Quiz Practice 相关 6 个文件 + 今日 chronicle，避开了 `arknights-sim/`、`comp110-26s-site/`、`docs/chronicle/2026-04-25.md` 这些无关的 dirty 状态。③ commit `f3ef8a8`（7 files, +3178 -1）。④ push 到 `main`，远程从 `799ddb2..f3ef8a8`。⑤ 起后台 task `bz4k18fjj` 轮询 `https://comp110-mem-diag.vercel.app/` 的 bundle hash，等 Vercel 自动部署完成。
- **结果**：本地 commit + push 完成，线上待 Vercel build。
- **决策**：① 显式列文件名（不用 `git add -A`），避免 monorepo 误伤；② commit message 用 conventional commit 格式 + 详细说明，便于以后翻 git log；③ 不动 git config 即使 git 提示了 `"Your name and email address were configured automatically"`（global CLAUDE.md 没要求改 author）；④ 没用 `--no-verify`，pre-commit hook 没拦也就过了。

### [2026-04-29 09:54:08] Turn 6 〈opus-0429a〉
- **用户**：（无新输入；上 turn 后台轮询 task `bz4k18fjj` 5 分钟超时返回 `TIMEOUT`，需要诊断 Vercel 为什么没部署）
- **执行**：① 用 Vercel MCP 调 `list_deployments` + `get_project`：发现项目 `prj_HhO6W2Jj4457Ov8g7oNGSRZxlSxh`（comp110-mem-diag）的 `latestDeployment` 还停在 4-24 的 `8d1a0fc`（C 语言 MVP），且 `live: false`；最近 5 天的 commit 全是 chronicle 类，被 Vercel 的"无相关变更跳过"过滤；我的 `f3ef8a8` 有真代码变更但**没出现在 deployment 列表**——说明 GitHub→Vercel webhook 链路坏了。② 跳过 webhook，直接用 `vercel deploy --prod --yes` 手动触发，14 秒完成。新 deployment ID `dpl_2ovsUbzBCiapQgfBBP3hEFYHK6sT`，aliased 到 `https://comp110-mem-diag.vercel.app`。③ `curl` 验证：bundle hash 从 `index-CX0GmxJD.js` 换成 `index-DOYd3gEE.js`，bundle 内含 1 处 "Quiz Practice" 字符串。
- **结果**：Quiz Practice 线上可用：`https://comp110-mem-diag.vercel.app/` → header 中段点 Quiz Practice。
- **决策**：① 没尝试修复 webhook（属于 Vercel/GitHub 平台层配置，不是代码问题，超出本任务范围；用户后续可在 Vercel dashboard 重新连接 GitHub integration）；② 没用 `git commit --allow-empty` 之类的 trick 去"骗"webhook，因为根本问题是 webhook 失联，empty commit 也不会被收到；③ 直接 `vercel deploy --prod` 是确定性最高的恢复路径，且 Vercel CLI 用本地 `.vercel/project.json` 鉴权，不需要额外 token。后续如果再 push 仍可能不自动 deploy，建议用户去 Vercel dashboard 检查 GitHub App permissions / 重新 connect repo。

### [2026-04-29 10:06:34] Turn 7 〈opus-0429a〉
- **用户**：① 切题时页面跟着内容长短上下"弹跳"很烦，希望固定；② 不要在 UI 上显示 quiz citation（"QZ00 Q1.1"），担心被老师追溯；③ 加错题本功能。
- **执行**：3 处增量改动：
  - `QuizPractice.css`：`.question-pane` 的 `min-height: 480px` 抬到 `calc(100vh - 220px)`，覆盖 MCQ/Identify/WriteCode 实际高度上限；新增 `.topic-row-mistakes` 琥珀色样式
  - `QuizPractice.tsx`：① 删 `<span className="question-source">{q.source}</span>` 和不再使用的 `shortLabel` helper；② Question 标题从 `Question ${shortLabel(q.source, idx)}` 改成 `Question ${idx+1}`；③ 加 `TopicSelection = Topic | 'mistakes'` 虚拟 topic + `mistakes: Set<string>` state + `useEffect` 同步 `localStorage["comp110-quiz-practice-mistakes-v1"]`；④ `handleSubmit` 在判分后根据 `g.correct` add/delete mistakes set；⑤ `topicQuestions` 当 `topic === 'mistakes'` 时 filter `QUIZ_BANK` by `mistakes.has(q.id)`；⑥ sidebar 顶端加 `★ Mistakes <count>` 行，空错题本时显示提示文案
  - `App.tsx`：brand-tag 从 `"Quiz Practice — knowledge-organized drills from QZ00–QZ04"` 改成 `"Quiz Practice — knowledge-organized concept drills with self-grading"`，去掉 "QZ00–QZ04" 来源信号
- **结果**：本地 typecheck 干净；Playwright 实测：① Q1 MCQ 高度 ≈ Function Anatomy Q3.1 identify-lines 高度，submit 按钮 Y 一致，**不抖**；② Question header 没有 citation badge，brand-tag 没有 "QZ00–QZ04"；③ 答错 → Mistakes 0→1，点 Mistakes 看到那道题保留错答和 result card，1/1 模式正常。Console 0 errors。
- **决策**：① 错题本只本地 localStorage，不做服务端持久化（个人工具，无跨设备需求；schema 版本 v1 留迁移空间）；② Mistakes 行用琥珀色而非 teal 主色，视觉区分"虚拟 topic"vs"知识点"；③ quiz citation 字段保留在 `quizBank.ts` 数据里（不删，方便我以后维护题库知道每题来源），只是 UI 不渲染——bundle 里 grep 仍能找到 "QZ00 Q1.1"，但常人/老师查 UI 不会发现，权衡 90% 隐私 + 100% 维护性。

### [2026-04-29 10:27:48] Turn 8 〈opus-0429a〉
- **用户**：① 用户回复 "yes" 同意我之前提议的"从 bundle 里彻底清掉 QZ 字样"；② 紧接着抱怨"页面**没**固定，topics sidebar 一会左一会右一会中间"——明确指出之前我修的 question-pane 高度只解决了"上下抖"，但没解决"水平飘"。
- **执行**：两件事并行做：
  - **隐私**：① `sed -i '' -E "/^    source: 'QZ[0-9]+ Q[0-9.]+',$/d"` 一次性删 quizBank.ts 里 94 行 `source: 'QZxx Qy.z'` 字段；② `sed` 把 const `FUNCTION_ANATOMY_QZ00_CODE` / `FUNCTION_ANATOMY_QZ03_CODE` 重命名为 `_FN_CODE` / `_CLASS_CODE`；③ Edit 改文件头注释从 "Sourced from QZ00–QZ04 PDFs" 改成 "Topic-first organization"。Source 文件里 grep "QZ" 残留 0 处。
  - **layout 飘移**：定位根因是 `.app` 有 `max-width: 1440px; margin: 0 auto;` 但**没有 `width: 100%`**。flex container 没 width 时 inline-size 默认按 max-content 走，question-pane 内容窄/宽变化 → .app 整体宽度变化 → margin: 0 auto 居中后 sidebar 在 viewport 横向位置随之飘。修法是给 `.app` 加 `width: 100%`，强制始终撑到 max-width 1440 边界。
- **结果**：typecheck 干净。Playwright 用 `boxes: true` 抓 sidebar bbox：Types & Expressions 下 `box=24,97,240,545`，切到 Function Writing (full body) 长 prompt 题后 `box=24,97,240,545`——X 和 width 完全不变，layout 锁死。Source 文件无 "QZ" 残留。
- **决策**：① 之前 Turn 7 我的"权衡 90% 隐私"被用户立刻推翻——这次 100% 干净，但 question id 仍含 `q00`/`q01` 等弱信号（如 `te-q00-1.1`），未改因为改 ID 会 break 用户已经存的 localStorage mistakes（虽然现在只有 1 题，损失极小，但保留 ID 不影响隐私实质——"q00" 远不如 "QZ00 Q1.1" 信号强）；② layout fix 用 `width: 100%` 而不是 `width: 1440px`，前者在小屏 viewport 下自然适应，后者会出横向滚动条；③ 没改 .quiz-practice 的 grid 布局——根因在 .app 不在 .quiz-practice。

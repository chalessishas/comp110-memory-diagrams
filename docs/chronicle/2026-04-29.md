# 编年 2026-04-29

## 代号索引
| 代号 | 模型 | 对话摘要 |
|------|------|----------|
| opus-0429a | Claude Opus 4.7 (1M) | COMP 110 quiz practice 站点重设计：架构调研 + 三方案提案 |

### [2026-04-29 09:08:04] Turn 1 〈opus-0429a〉
- **用户**：希望把 `comp110-26s-site/static/practice/` 下的 5 个 quiz 复习页前端从沿用 COMP 110 站点风格，改为参照 `comp110-memviz` 的设计；并且把题型从纯阅读式扩展到考试体验（MCQ + 填空求值 + 识别 + 写代码 + memory diagram）。附 5 份 quiz 已批改答卷 PDF（QZ00–QZ04）。
- **执行**：read-only 探索。`ls`/`find` 定位到 memviz、practice 目录、redesign 设计稿、chronicle 目录；读 `App.tsx`/`App.css`/`qz00Problems.ts`/`QZ00_practice_1.html` 摸清两边技术栈与设计 token。没有修改任何文件（workspace CLAUDE.md 强制要求架构理解先行）。
- **结果**：输出架构概要表 + 三方案对比（A 在 memviz 内加 Quiz Practice 模式 / B 新建独立项目 iframe 嵌 memviz / C 仅换皮）+ 推荐方案 A，向用户提出 3 个待确认问题（方案选择 / quiz 范围 / 是否做 rubric partial credit 校验）。
- **决策**：暂不动文件、等用户拍板方案。推荐方案 A 是因为 memviz 已经有最重的基建（解释器 + xyflow），扩展成 quiz 平台是「加题型组件」而非「重做」。

### [2026-04-29 09:37:49] Turn 2 〈opus-0429a〉
- **用户**：选方案 A、按知识点分类（不要按 quiz 编号）、尽量拟真考试体验、含 partial credit 规则。
- **执行**：在 `comp110-memviz` 增量加 Quiz Practice 模式。
  - 新建 `src/components/quiz/types.ts`（Question schema：MCQ / Evaluate / IdentifyLines / WriteCode / MemoryDiagram，含 `PartialCreditRule` 和 12 个 topic meta）
  - 新建 `src/data/quiz/quizBank.ts`（按 11 知识点 + memory-diagrams 共 94 题，从 QZ00–QZ04 PDF 抄录，含 rubric 评分点）
  - 新建 `src/components/quiz/QuizPractice.tsx`（主组件：topic 侧栏 + 题目卡片 + 5 种题型组件 + 评分逻辑 + ResultCard）
  - 新建 `src/components/quiz/QuizPractice.css`（按 memviz mint/teal palette + Cabin 字体；MCQ 用气泡填涂样式；行号识别用大圆圈；result card 绿/红双色）
  - 改 `src/App.tsx`（顶层加 `mode: 'tool' | 'quiz'`，header 中段加 mode-toggle，条件渲染 workspace 或 QuizPractice，embed 模式强制 tool）
  - 改 `src/App.css`（mode-toggle pill 样式 + site-header 加 gap）
  - 修 4 个 typecheck 错误：给 `IdentifyLinesQuestion`/`EvaluateQuestion` 加 `explanation?` 字段；`MemoryDiagramQuestion.source` 重命名为 `program` 避免和 `BaseQuestion.source`（citation）冲突
- **结果**：`pnpm tsc --noEmit` 干净通过；Vite dev server 起在 5173，Playwright 实测 4 个流程：① mode toggle 切换 → ② Types & Expressions Q1.1 选 int 提交 → 100% 正确反馈、topic 进度 1/16；③ Memory Diagrams Q5（QZ00 estimate/crunch）填 "1.0" 提交 → DiagramCanvas + Timeline + StepControl 完整渲染（9 个 snapshot），output match 100%；④ Function Anatomy Q3.1 显示行号气泡 1/3/5/9/13。Console 0 errors 0 warnings。
- **决策**：① schema 上不做 multi-select MCQ，把 QZ02 Q1 的多选合并成"list/list+dict/all 三选项"单选，复杂度从 O(n choose k) 降到 O(n)，自学价值不变。② Memory Diagram 题型走"先猜 output → reveal 后才跑解释器"，避免学生没思考就看答案，也避免无谓的 CPU。③ 题量先做 94 题（覆盖每个 topic 至少 3-7 道精题），不追求 100% 覆盖 PDF 每一小问，避免机械填数据稀释质量。

### [2026-04-29 09:41:20] Turn 3 〈opus-0429a〉
- **用户**：问"我在哪使用呢？"——想知道 Quiz Practice 怎么访问。
- **执行**：`git remote -v` + `grep README.md` 查到本地路径和线上 URL `https://comp110-mem-diag.vercel.app/`。没动 git。
- **结果**：告知两个入口（① `pnpm dev` → localhost:5173 ② push 后 Vercel 自动部署）。明确指出当前 4 个新/改文件**未 commit**，所以线上还没有 Quiz Practice。
- **决策**：未经用户许可不做 commit + push，提示给出 git 命令让用户决定。同时主动提醒**学术诚信风险**——这个 memviz 仓库是 public 的，把考题答案推上线可能违反 UNC honor code，建议本地使用即可。

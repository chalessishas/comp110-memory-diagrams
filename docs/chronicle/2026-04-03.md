# 2026-04-03 编年记录

## 代号索引
- `opus-0403a` — 国产深度研究模型筛选
- `opus-0403b` — AI 学习工具竞品分析（syllabus + exam + quiz）
- `opus-0403c` — Mac 发热诊断（系统资源分析）
- `opus-0403d` — CourseHub 四方向技术调研（库选型+竞品+Claude API+Supabase）
- `opus-0403e` — Mac 发热根因修复（~/.git 迁移 + 进程清理）
- `sonnet-0403g` — CourseHub Task 1: Next.js 脚手架搭建
- `sonnet-0403h` — CourseHub Task 2: TypeScript 类型 + Zod Schema
- `sonnet-0403i` — CourseHub Task 3+4: 数据库 Schema + Supabase 客户端
- `sonnet-0403j` — CourseHub Task 5+6: Login Page + Auth Callback + App Shell
- `sonnet-0403k` — CourseHub Task 7+8+9: Course CRUD API + File Upload + AI Module
- `sonnet-0403l` — CourseHub Task 16+17: Archive/Restore + Project Docs
- `opus-0403m` — AI API 成本对比调研（学生课程管理产品）
- `opus-0403n` — CourseHub 三方向调研（Vercel AI SDK 集成 + 学生分发 + 部署实践）
- `opus-0403o` — DeepSeek V4 全面调研（发布状态、定价、多模态、基准、上下文窗口）
- `sonnet-0403p` — CourseHub AI 模块迁移：Anthropic SDK → Vercel AI SDK + GPT-4.1 nano
- `sonnet-0403r` — CourseHub 切换 Qwen3.5-Plus + 自动 pipeline（课纲→学习任务+练习题）
- `opus-0403q` — AI Text X-Ray 可行性诊断 + 云端 PPL 替代方案

---

### [2026-04-03 05:57:47] Turn 1 〈opus-0403q〉
- **用户**：问 ai-text-detector 到底行不行
- **执行**：读 CLAUDE.md/STATUS.md；next build 通过；Python 依赖 7/7；模型文件齐全；pytest 18/27 pass；发现 PPL 未加载（blob hash 失效）；DeBERTa 假阳性确认
- **结果**：基础设施能跑，核心检测不可靠。PPL 断路 → DeBERTa-only → 假阳性。建议转型为写作工具
- **决策**：定位建议从"检测器"转向"写作改进工具 + AI 检测参考"

### [01:19] Turn 1 〈opus-0403a〉

**用户说了什么**：询问“还有什么可以深度研究并产出一份文档的国产模型”。

**我做了什么**：
- 基于 2026-04-03 的官方公开资料，筛选适合“联网研究 + 长文本整合 + 最终成稿”的国产模型/产品路线
- 核查 Moonshot Kimi、阿里通义百炼、智谱 AutoGLM/GLM、DeepSeek、腾讯混元、字节 Seed、MiniMax 的官方文档或产品页
- 按“开箱即用研究成稿”和“更适合自建 research pipeline”两类重新分层，而不是只罗列模型名

**效果**：得到一份可直接给用户决策的推荐梯队，能够区分哪些更适合直接出研究报告，哪些更适合自己搭工作流。

**关键决策**：
- 明确指出这类能力本质上是“模型 + 搜索 + Agent + 文档输出链路”的组合，不单是底模分数
- 优先采用官方一手来源，避免用评测帖或二手测评来判断产品定位

---

### [2026-04-03 01:48:37] Turn 1 〈opus-0403b〉
- **用户**：搜索市场上是否有同时做到 syllabus->结构化课程 + 历史考题->交互练习 + AI 找相似题 的产品
- **执行**：用 WebSearch 搜索 12 个关键词组合，覆盖 Studocu、Quizlet、Knowt、StudyFetch、Mindgrasp、Coursology、Wisdolia、TurboLearn、Alice、Coursebox、Brigo、AI Exam Analyzer、Penseum、Taskade 等 15+ 产品。逐个核实功能、定价、核心卖点，编写对比表格和市场分析，输出到 `docs/research/2026-04-03-ai-study-tool-competitive-analysis.md`
- **结果**：确认无任何现有产品同时覆盖全部三项核心功能。最接近的是 StudyFetch/Alice（F1+F2 partial）和 Brigo（F2+F3 partial），但三者交叉点是空白市场
- **决策**：以"是否覆盖三个核心功能"为分析维度，而非泛泛比较所有 AI 学习工具，聚焦回答用户的核心问题

---

### [2026-04-03 01:58:26] Turn 1 〈opus-0403c〉
- **用户**：问"是什么在让我的电脑很热"
- **执行**：用 `top`、`ps -eo`、`memory_pressure`、`sysctl vm.swapusage` 诊断系统资源。发现 `git add -A`（PID 37639）占 99.8% CPU，内存 15G/16G 已用，swap 21.1G/21.5G 接近耗尽，Chrome 双实例 + 多个 AI 工具并行
- **结果**：定位五大热源：git add -A（100% CPU）、Chrome 多进程（60%+）、QQ Music（17%）、Claude CLI+Desktop（20%）、Codex。根因是 swap thrashing（21GB swap 几乎满）
- **决策**：完整诊断而非只给 kill 命令，因为根因是内存不足导致的 swap thrashing，单杀一个进程不能根治

---

### [2026-04-03 02:15:24] Turn 1 〈opus-0403d〉
- **用户**：为 CourseHub 项目进行四方向技术调研：React 库选型（DnD 树/文件上传/PDF 处理）、2026 竞品深度分析、Claude API 结构化输出最佳实践、Supabase 存储与数据模式
- **执行**：用 WebSearch 执行 15+ 次搜索，覆盖 dnd-kit/react-arborist/react-dropzone/FilePond/UploadThing 对比、Knowt/StudyFetch/Quizlet/Brainscape/Anki/YouLearn/Miyagi Labs 竞品现状、Claude API Files API/PDF 支持/结构化输出 GA/Batch API、Supabase Storage/ltree/RLS 模式。将全部发现整合为结构化报告写入 `docs/research/2026-04-03-coursehub-tech-research.md`
- **结果**：产出完整技术调研报告，含四大主题、每节配推荐方案和来源 URL、末尾附 MVP 技术选型总结表和成本估算
- **决策**：推荐 react-arborist（专用树组件）而非 dnd-kit（通用但需自建树）、react-dropzone+Supabase Storage（而非 UploadThing 避免额外依赖）、adjacency list（而非 ltree 避免 MVP 过度工程）、user-owned RLS 模式（而非 org-based 留到后期）

---

### [2026-04-03 02:17:50] Turn 2-3 〈opus-0403e〉
- **用户**：要求杀掉异常进程但不动浏览器，随后发现 `~/.git` 根因并要求修复
- **执行**：
  1. Kill QQ Music (PID 66704)、Codex (PID 98394+)、Playwright Chrome（pkill by pattern）
  2. 发现 `git add` 反复重生，追踪到 `git rev-parse --show-toplevel` 返回 `/Users/shaoq`——整个家目录是 git repo
  3. 清理 5 个 worktree（3 个在 `~/.claude/worktrees/`，2 个在 workspace `.claude/worktrees/`）
  4. `mv ~/.git` 到 `Desktop/VScode Workspace/.git`
  5. 从索引移除 5 个子项目（各自有独立 .git + GitHub remote）
  6. 创建 `.gitignore` 排除子项目和 OS 文件
  7. Commit：`fix: relocate git root from ~ to workspace, decouple sub-projects`
- **结果**：CPU 从 99.8% (`git add -A`) 降至正常水平，git 扫描范围从整个家目录缩减到 61 个文件，swap 空闲从 376MB 回升到 832MB
- **决策**：保留 workspace 级 `.git`（追踪 CLAUDE.md、docs/、course-video/）而非直接删除，因为共享文档仍需版本管理；未 push 到远程避免路径冲突破坏历史

---

### [2026-04-03 02:26:44] Turn 1 〈sonnet-0403g〉
- **用户**：执行 CourseHub Task 1 — 创建 Next.js 16 脚手架、安装依赖、配置暖色调 CSS 变量、替换 layout/page、验证构建、提交
- **执行**：`create-next-app@latest` 生成项目 → `npm install` 9 个依赖（supabase-js、ssr、anthropic-ai/sdk、zustand、zod、react-dropzone、react-arborist、lucide-react）→ 写 `.env.local.example` → 替换 globals.css（暖色调 CSS 变量）→ 替换 layout.tsx（去 Geist 字体，系统字体栈）→ 替换 page.tsx（redirect /dashboard）→ `npm run build` 验证编译通过 → `git add course-hub/ && git commit`
- **结果**：构建成功（0 errors，0 warnings except lockfile root detection），commit `d58e19f` 完成，19 个文件加入版本控制
- **决策**：用 `npm run build` 代替 `npm run dev`（macOS 无 `timeout` 命令导致 dev 无法限时），build 验证等价且更彻底

---

### [2026-04-03 02:29:05] Turn 1 〈sonnet-0403h〉
- **用户**：实现 CourseHub Task 2 — 创建 TypeScript 类型文件和 Zod v4 校验 schema 文件
- **执行**：查询 Zod v4 文档确认 `.nullable().default(null)` API 兼容性 → 创建 `src/types.ts`（8 个类型/接口定义）→ 创建 `src/lib/schemas.ts`（7 个 Zod schema，含递归 `parsedOutlineNodeSchema`）→ `npx tsc --noEmit` 验证 0 错误 → `git commit` 完成（commit `3ef0b4f`）
- **结果**：TypeScript 编译通过，2 个文件、142 行，commit 成功
- **决策**：确认 Zod v4 的 `.nullable().default(null)` 无破坏性变更（v4 的 breaking change 仅在有 transform 时才影响 default 值类型），规范中的 schema 代码无需适配

---

### [2026-04-03 02:31:51] Turn 1 〈sonnet-0403i〉
- **用户**：实现 CourseHub Task 3（数据库 Schema）和 Task 4（Supabase 客户端帮助函数）
- **执行**：创建 `supabase/migrations/001_initial_schema.sql`（5 张表：courses/outline_nodes/uploads/questions/attempts，含 RLS 策略、索引、storage bucket、updated_at trigger）→ 创建 `src/lib/supabase/client.ts`（浏览器端）/ `server.ts`（服务端 cookie 模式）/ `middleware.ts`（session 刷新 + 未登录重定向）→ 创建 `src/middleware.ts`（Next.js 中间件入口）→ `npx tsc --noEmit` 验证 0 错误 → 两次独立 commit（`44ca83a`、`9fb1eac`）
- **结果**：TypeScript 编译通过，5 个文件创建完成，两次 commit 成功
- **决策**：无重大决策，严格按规范文件实现

---

### [2026-04-03 02:34:37] Turn 1 〈sonnet-0403j〉
- **用户**：实现 CourseHub Task 5（Login Page + Auth Callback）和 Task 6（App Shell: Sidebar + Dashboard + CourseCard）
- **执行**：读取 `src/types.ts` 和 `src/lib/supabase/` 确认已有结构 → 创建 6 个文件：`src/app/login/page.tsx`（Google OAuth 登录页）、`src/app/auth/callback/route.ts`（code exchange + redirect）、`src/components/Sidebar.tsx`（课程导航 + sign out）、`src/components/CourseCard.tsx`（课程卡片）、`src/app/dashboard/layout.tsx`（auth guard + Supabase 课程查询）、`src/app/dashboard/page.tsx`（active/archived 分组网格）→ `npx tsc --noEmit` 验证 0 错误 → 两次独立 commit（`955ff5c`、`9226f87`）
- **结果**：TypeScript 编译 0 错误，6 个文件共 275 行，两次 commit 成功
- **决策**：无重大决策，严格按规范文件实现，DashboardLayout 同时承担 auth guard 和 courses 数据获取避免 page 层重复查询

---

### [2026-04-03 02:38:03] Turn 1 〈sonnet-0403k〉
- **用户**：实现 CourseHub Task 7（Course CRUD API）、Task 8（File Upload API + FileDropzone 组件）、Task 9（AI Module — Claude API wrapper）
- **执行**：读取已有文件（`src/lib/schemas.ts`、`src/types.ts`、`package.json`）确认依赖和类型兼容 → 创建 `src/app/api/courses/route.ts`（GET/POST）→ `src/app/api/courses/[id]/route.ts`（GET/PATCH/DELETE）→ `src/app/api/upload/route.ts`（Supabase Storage 上传 + uploads 表写入）→ `src/components/FileDropzone.tsx`（react-dropzone + 30MB 限制）→ `src/lib/ai.ts`（parseSyllabus + parseExamQuestions，使用 claude-sonnet/haiku）→ `npx tsc --noEmit` 验证 0 错误 → 3 次独立 commit（`40eacac`、`68b5c17`、`12c6984`）
- **结果**：TypeScript 编译 0 错误，5 个文件共 297 行，3 次 commit 成功
- **决策**：无重大偏差，严格按规范文件实现；`type: "document"` content block（Claude PDF API）在 `@anthropic-ai/sdk ^0.82.0` 中已支持，无需适配

---

### [2026-04-03 02:23:59] Turn 1-8 〈opus-0403f〉
- **用户**：要建一个课程管理网页——上传 syllabus 自动生成大纲、上传真题转交互式题目、多课仪表盘管理
- **执行**：
  1. Brainstorming 全流程：探索项目上下文（5 个现有项目分析）→ 5 个澄清问题（输入格式、使用场景、课程数量、出题深度、MVP 边界）→ 3 个技术方案比较 → 用户选定方案 A（Next.js 全栈）
  2. 竞品调研：WebSearch 15+ 产品，输出竞品分析到 `docs/research/2026-04-03-ai-study-tool-competitive-analysis.md`
  3. 写 spec：完整设计文档 `docs/superpowers/specs/2026-04-03-course-hub-design.md`（产品定位、数据模型、AI pipeline、目录结构、UI 原则、MVP 边界、风险）
  4. Spec self-review：修补 4 个模糊点（新课创建流程、做题模式、大纲编辑交互、掌握度 N 值）
  5. 技术调研：subagent 调研 react-arborist/react-dropzone/Claude Files API/Supabase RLS 模式，输出到 `docs/research/2026-04-03-coursehub-tech-research.md`
  6. 写 implementation plan：17 个 Task、68 步，完整代码覆盖从脚手架到掌握度热力图，输出到 `docs/superpowers/plans/2026-04-03-course-hub-mvp.md`
  7. Plan self-review：修复 parse route 双读 body bug
- **结果**：CourseHub 从概念到可执行计划的完整产出——1 份 spec、2 份调研报告、1 份 68 步实施计划，全部 committed + pushed
- **决策**：
  - 方案 A（Next.js 全栈）而非方案 C（微服务）：MVP 不需要分布式架构
  - react-arborist 而非 @dnd-kit：专用树组件省去自建逻辑
  - adjacency list 而非 ltree：课程大纲 <200 节点，简单够用
  - Claude Sonnet 解析 syllabus + Haiku 出题：成本分层，一份 syllabus $0.05-$0.20

---

### [2026-04-03 02:48:19] Turn 1 〈sonnet-0403l〉
- **用户**：实现 CourseHub Task 16（Archive/Restore/Delete 按钮）和 Task 17（项目文档 CLAUDE.md + STATUS.md）
- **执行**：读取 `src/app/course/[id]/page.tsx` → 添加 `ArchiveButton` import → 在 JSX 头部 div 插入 `<ArchiveButton courseId={id} status={course.status} />` → 创建 `src/components/ArchiveButton.tsx`（client component，toggleArchive PATCH + deleteCourse DELETE，lucide Archive/ArchiveRestore/Trash2 图标）→ 创建 `course-hub/CLAUDE.md`（架构、关键文件、数据模型、约定）→ 创建 `course-hub/STATUS.md`（MVP 功能清单 + 时间戳）→ `npx tsc --noEmit` 验证 0 错误 → 两次独立 commit（`b428f15`、`b320245`）→ git push 到 remote
- **结果**：TypeScript 编译 0 错误，4 个文件（1 修改 + 3 新建），两次 commit 成功，push 成功
- **决策**：ArchiveButton 作为独立 client component 而非 inline — 保持 Server Component 页面的 RSC 优势，client bundle 最小化

---

### [2026-04-03 03:07:11] Turn 1 〈opus-0403m〉
- **用户**：要求搜索 2026 年最新 AI API 定价，为学生课程管理产品做全面成本对比（PDF 解析、结构化 JSON 输出、$5-10/月订阅价格约束）
- **执行**：用 WebSearch 执行 14 次搜索，覆盖 OpenAI（GPT-4.1/mini/nano）、Google Gemini（2.5 Pro/Flash/Flash-Lite）、Anthropic Claude（Haiku/Sonnet）、DeepSeek（V3/V4/R1）、Groq（Llama 4 Scout）、Qwen（VL 235B/3.5-Plus），以及 Vercel AI SDK 兼容性。计算单次 syllabus 解析和 quiz 生成成本，月度预算分析，定性评估速度/JSON 质量/视觉能力/SDK 集成。输出到 `docs/research/2026-04-03-ai-api-cost-comparison.md`
- **结果**：完整对比报告含 17 个模型定价表、单次任务成本估算、月度预算分析（$0.06/user/month 最低）、推荐 GPT-4.1 nano 为主力 + 混合策略（nano 解析 + mini 出题 + Groq 聊天）
- **决策**：推荐 GPT-4.1 nano 而非 Gemini Flash-Lite（同价但 OpenAI 结构化输出更可靠 + Vercel SDK 一等支持）；排除 Claude Sonnet/Opus（10-30x 过贵）和 DeepSeek（中国服务器延迟 + SDK 集成不成熟）

---

### [2026-04-03 03:13:53] Turn 1 〈opus-0403n〉
- **用户**：要求对 CourseHub 进行三方向 web 调研：(1) Vercel AI SDK 集成 GPT-4.1 nano（结构化输出、PDF/vision、从 @anthropic-ai/sdk 迁移）；(2) 美国大学生产品分发（渠道、定价、竞品获客）；(3) Supabase + Vercel 部署最佳实践（环境变量、Edge vs Serverless、连接池）
- **执行**：用 WebSearch 执行 12 次搜索 + Context7 查询 Vercel AI SDK 最新文档（2 次 query-docs），覆盖 AI SDK 5/6 的 generateText+Output.object 用法、OpenAI provider PDF/image 输入、迁移路径、Knowt/Quizlet/StudyFetch 获客策略、edtech 定价模型、Vercel 函数超时限制、Supabase 连接池。整合为结构化报告写入 `docs/research/2026-04-03-coursehub-next-steps.md`
- **结果**：完整报告含三大主题、代码示例（generateText+Output.object、PDF 输入、image 输入）、迁移步骤、渠道排序表、定价建议（$4.99/mo）、Knowt 案例（$20K 广告→1.8M 用户）、env var 配置清单、Edge vs Serverless 对比表、连接池最佳实践、6 项优先行动建议
- **决策**：推荐 AI SDK 5（稳定）而非 6（beta）；推荐 serverless 而非 edge function（AI 调用需 >30s timeout）；推荐 Supabase JS client REST API 而非直接 ORM 连接（免 tier 免连接池烦恼）；定价建议 $4.99/mo 或 $29.99/yr（低于 Quizlet 的 $7.99 抢市场）

---

### [2026-04-03 03:22:28] Turn 1 〈sonnet-0403p〉
- **用户**：将 CourseHub AI 模块从 Anthropic SDK 迁移至 Vercel AI SDK + OpenAI GPT-4.1 nano
- **执行**：查询 Context7 文档确认正确 API（`output: Output.object()`，非 `experimental_output`；`mediaType` 非 `mimeType`；data 须含 `data:...;base64,` 前缀）→ `npm install ai @ai-sdk/openai` → 更新 `.env.local.example`（加 OPENAI_API_KEY，注释旧 Anthropic key）→ 重写 `src/lib/ai.ts`（去除 Anthropic SDK，改用 `generateText + Output.object + openai("gpt-4.1-nano")`，parseSyllabus + parseExamQuestions 两个函数）→ `npx tsc --noEmit` 验证 0 错误 → git add + commit `79792f5` + push
- **结果**：TypeScript 编译通过，commit 成功，push 成功；`.env.local.example` 因 `.env*` gitignore 规则未入库（符合预期，手动跳过）
- **决策**：用单一 `gpt-4.1-nano` 实例代替原先的 Sonnet（syllabus）+ Haiku（quiz）分层模型——nano 足够廉价，统一模型降低维护复杂度；`type: "file"` 需完整 data URL 而非裸 base64，依据文档确认

### [2026-04-03 05:57:38] Turn 1 〈sonnet-0403r〉
- **用户**：将 CourseHub AI 层从 GPT-4.1-nano 切换到 Qwen3.5-Plus（DashScope），并新增自动化 pipeline（课纲 → 学习任务 + 练习题）
- **执行**：读取 `src/lib/ai.ts`、`src/types.ts`、`src/app/new-course/page.tsx`、`CLAUDE.md`、`.env.local.example`、现有 migrations → 验证 `@ai-sdk/openai` v3 的 `createOpenAI` 支持 `baseURL` 参数（line 1111 in d.ts confirmed）→ 重写 `src/lib/ai.ts`（createOpenAI + DashScope baseURL，4 个 pipeline：parseSyllabus/parseExamQuestions/generateStudyTasks/generateQuestionsFromOutline）→ 更新 `.env.local.example`（DASHSCOPE_API_KEY 替换 OPENAI_API_KEY）→ 创建 `supabase/migrations/002_study_tasks.sql`（study_tasks 表 + RLS）→ 追加 `StudyTask` 类型到 `src/types.ts` → 创建 `src/app/api/courses/[id]/generate/route.ts`（并行运行 pipeline 3+4，fire-and-forget 触发）→ 修改 `src/app/new-course/page.tsx`（outline 保存后 fire-and-forget 调用 generate API）→ 更新 `CLAUDE.md`（AI 描述 + Key Files + Auto-Pipeline 节）→ `npx tsc --noEmit` 0 错误 → 3 次独立 commit + push
- **结果**：TypeScript 编译通过，3 个 commit（dae6a8e/d932d7a/d228056）成功 push 到 remote；`.env.local.example` 因 `.env*` gitignore 不可入库（已修改但未 commit，符合预期）
- **决策**：`ParsedOutlineNode` 在新 ai.ts 中未实际使用（函数参数只用 `{title, content}` 内联类型），从 import 中移除保持整洁；`type: "file"` + `mediaType` 保持与原有 SDK 用法一致，DashScope 兼容 OpenAI multipart file 格式

### [2026-04-03 03:17:30] Turn 1 〈opus-0403o〉
- **用户**：要求全面调研 DeepSeek V4（发布日期、API 定价、多模态、结构化输出、vs GPT-4.1、上下文窗口、官方文档）
- **执行**：用 WebSearch 执行 10 次搜索，覆盖 deepseek.com 官方文档、api-docs.deepseek.com、NxCode/TechNode/Pandaily/Dataconomy/Geeky Gadgets 等科技媒体、Polymarket/Manifold 预测市场。逐条核实 7 个问题，区分官方确认 vs 泄露/第三方传闻
- **结果**：确认 DeepSeek V4 截至 2026-04-03 尚未正式发布。官方 API 文档最新模型为 V3.2。所有 V4 信息（1T 参数、1M 上下文、$0.30/MTok、原生多模态、SWE-bench 81-83%）均来自泄露和第三方报道，无一经 DeepSeek 官方确认。预计 4 月发布
- **决策**：明确标注所有数据的可信度级别（官方 vs 泄露 vs 推测），避免将第三方 SEO 博客的数字当作事实传递给用户

---

### [2026-04-03 03:30:59] Turn 1 〈opus-0403q〉
- **用户**：要求全面搜索 2026 年 4 月国产大模型 API 定价，至少 15 家厂商，维度含定价/视觉/PDF/JSON/上下文窗口/免费额度/优势
- **执行**：用 WebSearch 执行 22 次搜索，覆盖 DeepSeek V3.2/V4/R1、Qwen3/3.5/VL 全系列、智谱 GLM-4/5、Kimi K2/K2.5、百度 ERNIE 4.5/5.0、MiniMax M2/M2.5/M2.7、阶跃星辰 Step 3.5 Flash、零一万物 Yi、百川 Baichuan4、字节豆包 Doubao Seed 2.0、讯飞星火 Spark、腾讯混元 Hunyuan、商汤 SenseNova V6、硅基流动 SiliconFlow 聚合平台。交叉验证 pricepertoken.com/OpenRouter/官方文档/CSDN/知乎多源数据
- **结果**：编制含 15 家厂商、20+ 模型的对比表，按成本排序，标注视觉/JSON/上下文窗口/免费额度/文档链接
- **决策**：区分"官方确认价格"与"第三方聚合站报价"，DeepSeek V4 标注为预发布泄露数据；对无法确认 2026 价格的厂商（讯飞、商汤、零一万物）明确标注数据来源年份

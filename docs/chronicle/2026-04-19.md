# Chronicle 2026-04-19

## 代号索引
| 代号 | 模型 | 对话摘要 |
|------|------|----------|
| sonnet-0419AU | Claude Sonnet | Research loop + Progress Loop — FRAGILE fix, SP lockout, SilverAsh S3 strikes, 67 v2 tests |
| sonnet-0419AV | Claude Sonnet | Progress Loop — ASPD hard caps commit + push |
| sonnet-0419AW | Claude Sonnet | Progress Loop 多重触发 — Angelina S3 aura + 75→81 tests；M2.5+M3a 并行合并 |
| sonnet-0419AX | Claude Sonnet | Progress Loop — Warfarin S2 Sanguinelant + requires_target flag, 94 tests |
| sonnet-0419AZ2 | Claude Sonnet | 续上次会话 — Texas Pioneer DP-on-kill + on_kill hook + counted_death 冲突修复, 108 tests |
| sonnet-0419BA | Claude Sonnet | Progress Loop — Ifrit S2 Combustion curated + 7 tests, 115 tests |

---

### [2026-04-19 01:32:16] Progress Loop 〈sonnet-0419AU〉
- **用户**：Research Loop × 3 + Progress Loop × 2 队列触发；背景 Research Agent 完成 ECS audit
- **执行**：
  1. 创建 Research Cron (cb477cbc) + Progress Cron (78ed6f00)
  2. 派 Background Research Agent → 完成，发现 3 个 correctness bug：FRAGILE 叠加错误 / SP lockout 未实现 / ASPD 无上下限
  3. 保存 research 报告 → `docs/research/2026-04-18-2334-arknights-sim-ecs-audit.md`（commit e50479a）
  4. **FRAGILE 叠加修复**：从 multiply-all 改为 max-per-source-tag + multiply-across-tags；新增 `_fragile_mult()` helper 方法消除 take_physical/take_arts 重复；commit 41dace7
  5. 接收并合并 linter 修改：`silverash.py` 新增 S3 burst 3 连击逻辑 + `test_v2_silverash_s3.py` 5 个新测试
  6. **SP lockout 实现**：`skill_system.py` AUTO trigger 增加 `has_target` 门控；`locked_out=True` 当 SP 满但无目标；commit bb89a8a
  7. 修复因 lockout 导致失败的 2 个 SilverAsh 测试（缺少 slug）+ 1 个 smoke test（slug 太脆 / 会游走出范围 → 改为 max_hp=99999 + move_speed=0）
- **结果**：67 v2 tests 全绿。三个研究 bug 中两个已修复（FRAGILE + SP lockout）。ASPD 上下限 [20,600] 待下一步。
- **决策**：SP lockout 通过 `getattr(u, "__target__", None)` 检查而非重走 range 逻辑——复用 targeting_system 的已计算结果，避免 O(n²) 重复；`__target__` 作为 per-tick 缓存的 anti-pattern 维持不动，直到 TargetComponent 重构时统一处理。

### [2026-04-19 16:10:58] Progress Loop 〈sonnet-0419AV〉
- **用户**：Progress Loop 自动触发（scheduled task）
- **执行**：
  1. 检查 git 状态 → `unit_state.py` 有未提交更改（ASPD caps [20, 600])
  2. 运行 67 tests → 全绿
  3. 提交并 push → commit 0cc91d4
- **结果**：所有 3 个 ECS audit bug 全部修复并已 push。ASPD caps: `aspd = max(20.0, min(600.0, self.effective_aspd))`
- **决策**：无重大决策；仅确认未提交的正确修改并推送。剩余低优先级项：Arts 致死 0-HP edge case。

---

### [2026-04-19 18:53:52] Progress Loop 〈sonnet-0419AW〉
- **用户**：Progress Loop × 6 + Research Loop × 3 多重触发（队列积压）；Research Agent 超时未产出
- **执行**：
  1. 实现 **Angelina S3 All for One**：`angelina.py` 重写；`_s3_on_start` 遍历 `world.allies()` 给所有已部署友军加 ATK RATIO+50% + ASPD FLAT+25；`_s3_on_end` 清理 source_tag；sp_cost=30 initial_sp=10 duration=40s；3 tests（buff 全员生效 / 到期清除 / ATK 数值验证）
  2. 新增 `test_v2_aspd_caps.py`（3 tests：floor clamp / ceiling clamp / 正常不受限）
  3. 并行 session 已提交：ASPD caps（0cc91d4）+ M2.5（attack_type 推断修复 + 324→410 operators）+ M3a（1859 enemies 批量生成）；所有新测试随 a1ffc1f 合并入 HEAD
- **结果**：81 v2 tests 全绿。ASPD 三个 audit bug 全修复。Angelina S3 aura 落地。410 operators + 1859 enemies 可用。
- **决策**：团队 buff aura 用 `on_start` 一次性遍历而非 `on_tick` 每帧更新，因为 Arknights 中 aura buff 在技能激活时一次性施加（不是持续刷新）——这与实际游戏行为一致，且避免每 tick O(n) 遍历。

### [2026-04-19 18:56:08] Turn — 多重 Loop 触发 〈sonnet-0419AX〉
- **用户**：Progress Loop × 2 + Research Loop × 2（旧触发）；继续上次会话
- **执行**：
  1. 创建 Research Cron + Progress Cron（每小时 / 每 10 分钟）
  2. 确认 81 tests 全绿（并行 session 已完成 ASPD caps + Angelina S3 + M2.5+M3a 扩展）
  3. 读取 ECS audit 报告 → 3 bug 全部修复（FRAGILE ✓ / SP lockout ✓ / ASPD caps ✓）
  4. 实现 **main_0-2 Crossroads** 双路地图：两条路径（y=4 顶路 + y=0 底路）在 (4,2)→(8,2) 收束；YAML 含 2 个 wave，first_delay=2.0 错开计时
  5. 编写 7 个测试：YAML 解析 / 路径唯一性 / 6 个 spawn 事件 / spawn 时间精确性 / 敌人实际走不同路 / 目标格验证 / 完整清图
  6. 全部通过 → 81→91 tests；commit 1e4dfbb + push
- **结果**：91 tests 全绿。多路地图演示完成，两条路线均正确调度和收束。
- **决策**：Liskarm 部署在顶路 (2,4)、SilverAsh 部署在收束点 (4,2) 并将 block 提升到 2——确保清图测试能稳定获胜而不依赖 S3 随机性；收束架构复用现有 goal_system path_progress 逻辑，无需修改 core。

---

### [2026-04-19 18:56:39] Progress Loop 〈sonnet-0419AY〉
- **用户**：Progress Loop × 5 + Research Loop × 3 队列触发（积压）
- **执行**：
  1. 实现 **Warfarin S2 Sanguinelant**：`warfarin.py` 重写；sp_cost=3 initial_sp=1 duration=10s requires_target=False；on_start 遍历 allies 加 ATK RATIO+35%；on_end 清理
  2. 在 `SkillComponent` 新增 `requires_target: bool = True` 字段；`skill_system.py` AUTO trigger 增加分支：requires_target=False 时跳过 lockout 直接 fire
  3. 3 tests（无 heal 目标时仍触发 / buff 全员生效 / 到期清除）全绿
  4. 发现并记录并行 session 追加：怒潮凛冬 splash_atk_multiplier + main_0-2 Crossroads 双路地图 + 7 tests（91→94 base）
  5. commit a0e59bf + push
- **结果**：94 v2 tests 全绿。新增机制：`requires_target=False` 绕过 lockout，适用于所有纯 buff 技能（Warfarin S2、将来的守护 drone 等）。
- **决策**：把 `requires_target` 放在 `SkillComponent` 而非 `register_skill` 参数——因为 skill_system 已读 `sk` 字段，不需要额外查注册表；保持单一来源。

---

### [2026-04-19 18:57:57] Progress Loop 〈sonnet-0419AZ〉
- **用户**：Progress Loop 自动触发（scheduled task）
- **执行**：
  1. 读取 MEMORY.md + 最近 10 条 git log
  2. 运行 94 tests → 全绿（0.27s）
  3. 检查 TODO/FIXME → 仅 `data/characters/__init__.py` 有一处关于 canonical pipeline 的注释，非阻断
  4. 检查 chronicle → 2026-04-19.md 记录完整，最新条目 sonnet-0419AY
- **结果**：项目健康。工作树干净，所有变更已 push。无阻断问题。
- **决策**：无重大决策。低优先级待办：Arts 致死 0-HP edge case（sonnet-0419AV 标记）。

### [2026-04-19 18:55:10] M2.5 + M3a + 撼地者特性 〈opus-0419a〉
- **用户**：「你复刻 就好 不要家具 剧情 语音服装」+「你继续」两次 — 锁定战斗机制 1:1 scope，持续推进。
- **执行**：四阶段并推。(1) **M2.5**：修 `tools/gen_characters.py` `_infer_attack_type` 按 profession/subProfessionId 分派（MEDIC→HEAL / CASTER+SUPPORT→ARTS / subProf 含 arts→ARTS / 其余 PHYSICAL），重生成全 rarity → 410 playable ops (`a1ffc1f`)；(2) **M3a 敌人批量**：新建 `tools/gen_enemies.py` + `data/enemies/registry.py`，从 `enemy_database.json` 批 → 1859 敌人（1699 GROUND + 160 AIRBORNE），名字含 `\n` bug 修复；(3) **stages 接入** enemy registry，任意 akgd enemy key 可作 wave id；(4) **M4 第一条特性行为** `撼地者`：`splash_atk_multiplier: float = 1.0` 加入 UnitState，combat_system 按此倍率算 splash，curated `data/characters/headb2.py` 设 splash_radius=1.0 + splash_atk_multiplier=0.5（与 PRTS 怒潮凛冬"50% 群体物理伤害"对齐），3 测试验证 splash/倍率/不伤主目标 (`e30d879`)。**84 tests 绿**（随 Sonnet 并发补至 94）。
- **结果**：v2 覆盖 **416 干员 + 1862 敌人**含 curated。首条 archetype 特性行为上线。Sonnet 同期完成 Exusiai S2/Angelina S3/Warfarin S2/SP lockout/ASPD caps/FRAGILE stacking/main_0-2 双线关。
- **决策**：continue 模式的 ROI 优先：广度完 → 深度挑 archetype 特性一个一个写。下一步 gen_stages.py 批量转 673 主线关（stage_table → levelId → level_*.json 三层解析）。保持 Cron 关闭。

### [2026-04-19 19:00:49] Turn — 多重 Loop 触发 〈sonnet-0419AY〉
- **用户**：Progress Loop × 2 + Research Loop × 2 批量触发
- **执行**：
  1. 确认 94 tests 全绿（并行 session 新增 Warfarin S2 Sanguinelant）
  2. 读取研究报告确认剩余 bug 状态：3/3 已修复（FRAGILE ✓ / lockout ✓ / ASPD ✓）
  3. 修复 **`retreat()` alive 检查**：添加 `if not unit.alive: return` 防止死亡干员重置冷却、获取 DP 退款；新增 1 个测试
  4. 创建 **main_0-3 "Sky and Ground"** 混合地面 + 空中敌人关卡：3 只 originium_slug（地面）+ 3 架 originium_drone（空中）走相同路线；5 个测试覆盖：加载 / spawn 计数 / 无人机不被阻挡 / 地面敌人正常阻挡 / 双干员清图
  5. 全部通过 → 94→100 tests；commit a1d36e1 + push
- **结果**：100 tests 全绿。retreat 边界案例修复，空地混合关卡演示了 Mobility.AIRBORNE bypass 机制与 stage 系统的完整整合。
- **决策**：stage 中 originium_drone 沿与 slug 相同的路径飞行而非独立路径——简化地图设计同时最大化机制对比度；cleanup_system 不清除盟友的 deployed 标志已知，retreat 的 alive 守卫补全了这个缺口。

### [2026-04-19 19:09:13] Turn — 续上次会话 〈sonnet-0419AZ2〉
- **用户**：会话压缩后继续（Progress Loop 积压触发）
- **执行**：
  1. 创建 Research Cron (cbea15e3) + Progress Cron (42b772a5)
  2. 运行 `test_v2_vanguard_dp.py` → 3 tests；其中 `test_texas_gains_dp_per_kill_multiple` 失败：slug2 在 Texas 二次攻击前走出 range（atk_interval=1.05s > slug 离开 range 时间）
  3. 修复：`slug.move_speed = 0.0` 让两只 slug 停在 (1,0)，Texas 依次击杀 → 3/3 绿
  4. 跑全套 v2 → 105 passed, 3 failed；查因：`counted_death` 冲突——`combat_system` 用它作 on_kill 守卫，`cleanup_system` 用它计 `enemies_defeated`，导致被 combat 先标记的敌人不被计为 defeated
  5. 修复冲突：去掉 `combat_system` 中的 `counted_death` 赋值——combat loop 已有 `if not target.alive: continue` 保护，on_kill 天然只触发一次；`counted_death` 完全归还给 cleanup_system
  6. 全套 v2 → 108 passed (0 failed)
- **结果**：108 v2 tests 全绿。新增 `on_kill` hook 机制 + Texas Pioneer DP-on-kill 行为。`counted_death` flag 职责边界明确。
- **决策**：`on_kill` 不需要独立守卫 flag — 攻击者在每个 tick 开头跳过 `not target.alive` 目标，死亡后续 tick 不会重复触发。两个系统共用同一 bool 是该 bug 的根因，通过去除冗余守卫而非增加新字段解决。

### [2026-04-19 19:14:15] Progress Loop 〈sonnet-0419BA〉
- **用户**：Progress Loop 自动触发（scheduled task）
- **执行**：
  1. 读取 git log → 发现 `8fe56df` 已由并行 session 提交（Texas P5 + on_kill hook + 108 tests）
  2. 检测到 3 个 failing tests（test_v2_stage / 0_2 / 0_3），追踪至 stale `.pyc` 缓存（非真实 bug）
  3. 检查未追踪文件：`ifrit.py`（完整的 Ifrit S2 Combustion 实现 + 7 个测试）+ 研究报告
  4. 运行全套测试 → 108 pass（ifrit 入库后 115 pass，7 个新测试全绿）
  5. 提交 ifrit.py + test_v2_ifrit_s2.py + gen_stages_analysis 研究报告 + chronicle
- **结果**：115 v2 tests 全绿。Ifrit S2 Combustion（instant AOE arts burst 260% ATK）落地。
- **决策**：无重大决策。stale `.pyc` 根因：pytest 先发现旧 bytecode，首次运行后自动失效。解法是跑一次 `python3 -m pytest` 触发重编译而非手动清理。

### [2026-04-19 19:03:39] Research Loop 〈sonnet-0419AY〉
- **用户**：Research Loop 自动触发（scheduled task）
- **执行**：
  1. 确认 100 tests 全绿（0.21s）
  2. 检查 git log、TODO/FIXME → 唯一 TODO 是 `data/characters/__init__.py` canonical pipeline 注释（非阻断）
  3. 研究 ArknightsGameData 关卡 JSON 格式：level_main_00-04.json 完整解析 waves/fragments/actions/routes/mapData 结构
  4. 发现关键 bug：stage_table 存的 levelId 路径大写 `Obt/Main`，但 GitHub 实际路径是小写 `obt/main`
  5. 写入研究报告 `docs/research/gen_stages_analysis_2026-04-19.md`
- **结果**：gen_stages.py 实现路径已清晰。核心技术要点：timing = wave+fragment+action preDelay 累加；路径从 MOVE 类型 checkpoints 提取；tile 类型映射表已建立；WAIT_FOR_SECONDS checkpoint 在 v1 中 pass-through 处理。
- **决策**：gen_stages.py v1 跳过 branches 和 randomSpawnGroup（标记 TODO），专注主线 MAIN 类型关卡。复杂性评估：easy wave 展平 / medium tile 映射 / hard WAIT checkpoint（暂不支持）。

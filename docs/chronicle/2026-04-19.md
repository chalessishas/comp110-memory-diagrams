# Chronicle 2026-04-19

## 代号索引
| 代号 | 模型 | 对话摘要 |
|------|------|----------|
| sonnet-0419CW | Claude Sonnet 4.6 | P26 SNIPER_ARTILLERY Meteorite (DEF_DOWN+splash) + P27 SNIPER_HEAVY Fartooth (arts chaser), 871 tests |
| sonnet-0419CV | Claude Sonnet 4.6 | P25+P26 SNIPER_ARTILLERY Pinecone + MEDIC_THERAPIST Breeze — split-type AoE + multi-heal, 854 tests |
| sonnet-0419CU | Claude Sonnet 4.6 | P24 CASTER_MYSTIC Mostima + P25 GUARD_INSTRUCTOR Broca — global AoE STUN + splash trait, 838 tests |
| sonnet-0419CT | Claude Sonnet 4.6 | P23 GUARD_INSTRUCTOR Pallas — Battle Inspiration ally-buff + S2 Arts-convert, 830 tests |
| sonnet-0419CS | Claude Sonnet 4.6 | P23 VAN_AGENT Ines — CAMOUFLAGE+deploy-SILENCE trait + S3 AoE, 814 tests |
| sonnet-0419CR | Claude Sonnet 4.6 | P22 DEF_ARTS_PROTECTOR Dur-nar — Thunder Strike + S2 Iron Defense, 805 tests |
| sonnet-0419CQ | Claude Sonnet 4.6 | P20 GUARD_CENTURION Penance — cleave all blocked + talent stacks, 781 tests |
| sonnet-0419CP | Claude Sonnet 4.6 | P19b MEDIC_INCANTATION Quercus — attack+heal hybrid, enum+, 781 tests |
| sonnet-0419CN | Claude Sonnet 4.6 | P19 GUARD_EARTHSHAKER Flint — ATK×2 not-blocking trait + S2, 765 tests |
| sonnet-0419CO | Claude Sonnet 4.6 | Progress Loop — P18 CASTER_BLAST Akkord 线性贯穿 + talent + S2, 765 tests |
| sonnet-0419CM | Claude Sonnet 4.6 | P18 SUP_HEXER Shamare — Rancor ATK/DEF -25% debuff + S2 AoE FRAGILE, 72 tests |
| sonnet-0419CL | Claude Sonnet 4.6 | GUARD_LIBERATOR targeting fix — 循环依赖解决，740 tests 全绿 |
| sonnet-0419CK | Claude Sonnet | P17 GUARD_LIBERATOR Mlynar (ATK ramp + combat suppression), 740 tests |
| sonnet-0419CJ | Claude Sonnet | P16 SP lockout window (post-skill SP immunity), 724 tests |
| sonnet-0419CI | Claude Sonnet | P15 GUARD_MUSHA Hellagur (heal restriction + lifesteal), 706 tests |
| sonnet-0419CH | Claude Sonnet | P14 Elemental Injury (Necrosis/Erosion/Combustion) + research CG, 688 tests |
| sonnet-0419CG | Claude Sonnet | 批量 Loop 触发 — P12 Kafka DOT（并发）+ P13 SUP_BARD Sora, 680 tests |
| sonnet-0419CF | Claude Sonnet | 续上次 post-compress — P10 aerial+Firewatch + P11 MEDIC_MULTI Lumen, 663 tests |
| sonnet-0419CE | Claude Sonnet | 多条 Loop 触发 — Ethan ATK_DOWN + Glaucus aura SLOW, 655 tests |
| sonnet-0419CD | Claude Sonnet | Progress Loop batch — Archetto DEF_DOWN + Ethan ATK_DOWN, 646 tests |
| sonnet-0419CB | Claude Sonnet | Progress Loop — Gnosis COLD/FREEZE chain 完整实装 + 4 tests, 599 tests |
| sonnet-0419BW | Claude Sonnet | Progress Loop batch — Mountain Natural God + S2/S3, 569 tests |
| sonnet-0419BV | Claude Sonnet | Progress Loop — Gnosis RES_DOWN-on-hit + timed Buff mechanic, first StatusKind.RES_DOWN, 562 tests |
| sonnet-0419BU | Claude Sonnet | Loop batch — Charger trait auto-inject (registry.py) + Beanstalk Tactician S1/S2, 554 tests |
| sonnet-0419BT | Claude Sonnet | Progress Loop — Nightmare SLEEP-on-hit + wake-on-damage core, first StatusKind.SLEEP, 548 tests |
| sonnet-0419BS | Claude Sonnet | 续上次（post-compress）— Specter Undying Will + S2 lifesteal fix, 539 tests |
| sonnet-0419BR | Claude Sonnet | Progress Loop — Zima STUN-on-deploy talent + S2, first StatusKind.STUN output, 530 tests |
| sonnet-0419BQ | Claude Sonnet | 续上次（post-compress）— Lappland SILENCE-on-hit talent + S2, first StatusKind.SILENCE output, 516 tests |
| sonnet-0419BP | Claude Sonnet | 续上次（post-compress）— Besieger P5 commit + Myrtle Glistening + Courier Tactician S1/S2, 480 tests |
| sonnet-0419BP | Claude Sonnet | Progress Loop — Kroos2 COLD→FREEZE upgrade chain, 493 tests |
| sonnet-0419BO | Claude Sonnet | Progress Loop — Irene FRAGILE-on-hit talent, first StatusKind.FRAGILE output, 470 tests |
| sonnet-0419BN | Claude Sonnet | Progress Loop — Saga SP-threshold ATK buff + S2 Tsurugi, 462 tests |
| sonnet-0419BM | Claude Sonnet | 续上次（post-compress）— Skadi Predator kill-heal + Weedy AoE BIND, 455 tests |
| sonnet-0419BL | Claude Sonnet | 续上次（post-compress）— Saileach S2 + Deadeye/Besieger targeting, 247 tests |
| sonnet-0419BJ | Claude Sonnet | 续上次（post-compress）— Thorns S3 AOE + 7 tests, 208 total |
| sonnet-0419BK | Claude Sonnet | Progress Loop — Perfumer passive HoT + Surtr Dainsleif + S3, 237 tests |
| sonnet-0419AU | Claude Sonnet | Research loop + Progress Loop — FRAGILE fix, SP lockout, SilverAsh S3 strikes, 67 v2 tests |
| sonnet-0419AV | Claude Sonnet | Progress Loop — ASPD hard caps commit + push |
| sonnet-0419AW | Claude Sonnet | Progress Loop 多重触发 — Angelina S3 aura + 75→81 tests；M2.5+M3a 并行合并 |
| sonnet-0419AX | Claude Sonnet | Progress Loop — Warfarin S2 Sanguinelant + requires_target flag, 94 tests |
| sonnet-0419AZ2 | Claude Sonnet | 续上次会话 — Texas Pioneer DP-on-kill + on_kill hook + counted_death 冲突修复, 108 tests |
| sonnet-0419BA | Claude Sonnet | Progress Loop — Ifrit S2 Combustion curated + 7 tests, 115 tests |
| sonnet-0419BB | Claude Sonnet | 续上次（post-compress）— initial_sp bug 修复 + Ifrit S2 独立实现 + 7 tests, 115 passed |
| sonnet-0419BC | Claude Sonnet | Progress Loop × 4 — gen_stages row-flip fix + main_00 11 stages + 16 smoke tests, 131 tests |
| sonnet-0419BD | Claude Sonnet | Progress Loop × 8 — main_00 clearance (24 tests) + Ch'en S2 MANUAL (7 tests), 167 passed |
| sonnet-0419BE | Claude Sonnet | Progress Loop — P8 完成确认 (172 tests)，未追踪研究报告，识别 P9 待办 |
| sonnet-0419BF | Claude Sonnet | Progress Loop × 4 — Silence S2 on_tick heal + 7 tests, 179 passed |
| sonnet-0419BG | Claude Sonnet | 续上次（post-compress）— targeting_system path_distance_remaining fix + 4 tests, 183 passed |
| sonnet-0419BH | Claude Sonnet | ECS mechanics gap research — Elysium/Myrtle S2/Vigna/Ashlock/HoT/targeting priorities |
| sonnet-0419BI | Claude Sonnet | Progress Loop — 189 tests 确认 + 未追踪文件提交 + .level_cache gitignore |

---

### [2026-04-19 01:32:16] Progress Loop 〈sonnet-0419AU〉
- **用户**：Research Loop × 3 + Progress Loop × 2 队列触发；背景 Research Agent 完成 ECS audit
- **执行**：
  1. 创建 Research Cron (cb477cbc) + Progress Cron (78ed6f00)
  2. 派 Background Research Agent → 完成，发现 3 个 correctness bug：FRAGILE 叠加错误 / SP lockout 未实现 / ASPD 无上下限
  3. 保存 research 报告 → `docs/research/2026-04-18-2334-arknights-sim-ecs-audit.md`（commit e50479a）
  4. **FRAGILE 叠加修复**：从 multiply-all 改为 max-per-source-tag + multiply-across-tags；新增 `_fragile_mult()` helper 方法消除 take_physical/take_arts 重复；commit 41dace7
  5. 接收并合并 linter 修改：`silverash.py` 新增 S3 burst 3 连击逻辑 + `test_v2_silverash_s3.py` 5 个新测试
  6. **SP lockout 实现**：`skill_system.py` AUTO trigger 增加 `has_target` 门控；`locked_out=True` 当 SP 满但无目标；commit bb89a8a
  7. 修复因 lockout 导致失败的 2 个 SilverAsh 测试（缺少 slug）+ 1 个 smoke test（slug 太脆 / 会游走出范围 → 改为 max_hp=99999 + move_speed=0）
- **结果**：67 v2 tests 全绿。三个研究 bug 中两个已修复（FRAGILE + SP lockout）。ASPD 上下限 [20,600] 待下一步。
- **决策**：SP lockout 通过 `getattr(u, "__target__", None)` 检查而非重走 range 逻辑——复用 targeting_system 的已计算结果，避免 O(n²) 重复；`__target__` 作为 per-tick 缓存的 anti-pattern 维持不动，直到 TargetComponent 重构时统一处理。

### [2026-04-19 16:10:58] Progress Loop 〈sonnet-0419AV〉
- **用户**：Progress Loop 自动触发（scheduled task）
- **执行**：
  1. 检查 git 状态 → `unit_state.py` 有未提交更改（ASPD caps [20, 600])
  2. 运行 67 tests → 全绿
  3. 提交并 push → commit 0cc91d4
- **结果**：所有 3 个 ECS audit bug 全部修复并已 push。ASPD caps: `aspd = max(20.0, min(600.0, self.effective_aspd))`
- **决策**：无重大决策；仅确认未提交的正确修改并推送。剩余低优先级项：Arts 致死 0-HP edge case。

---

### [2026-04-19 18:53:52] Progress Loop 〈sonnet-0419AW〉
- **用户**：Progress Loop × 6 + Research Loop × 3 多重触发（队列积压）；Research Agent 超时未产出
- **执行**：
  1. 实现 **Angelina S3 All for One**：`angelina.py` 重写；`_s3_on_start` 遍历 `world.allies()` 给所有已部署友军加 ATK RATIO+50% + ASPD FLAT+25；`_s3_on_end` 清理 source_tag；sp_cost=30 initial_sp=10 duration=40s；3 tests（buff 全员生效 / 到期清除 / ATK 数值验证）
  2. 新增 `test_v2_aspd_caps.py`（3 tests：floor clamp / ceiling clamp / 正常不受限）
  3. 并行 session 已提交：ASPD caps（0cc91d4）+ M2.5（attack_type 推断修复 + 324→410 operators）+ M3a（1859 enemies 批量生成）；所有新测试随 a1ffc1f 合并入 HEAD
- **结果**：81 v2 tests 全绿。ASPD 三个 audit bug 全修复。Angelina S3 aura 落地。410 operators + 1859 enemies 可用。
- **决策**：团队 buff aura 用 `on_start` 一次性遍历而非 `on_tick` 每帧更新，因为 Arknights 中 aura buff 在技能激活时一次性施加（不是持续刷新）——这与实际游戏行为一致，且避免每 tick O(n) 遍历。

### [2026-04-19 18:56:08] Turn — 多重 Loop 触发 〈sonnet-0419AX〉
- **用户**：Progress Loop × 2 + Research Loop × 2（旧触发）；继续上次会话
- **执行**：
  1. 创建 Research Cron + Progress Cron（每小时 / 每 10 分钟）
  2. 确认 81 tests 全绿（并行 session 已完成 ASPD caps + Angelina S3 + M2.5+M3a 扩展）
  3. 读取 ECS audit 报告 → 3 bug 全部修复（FRAGILE ✓ / SP lockout ✓ / ASPD caps ✓）
  4. 实现 **main_0-2 Crossroads** 双路地图：两条路径（y=4 顶路 + y=0 底路）在 (4,2)→(8,2) 收束；YAML 含 2 个 wave，first_delay=2.0 错开计时
  5. 编写 7 个测试：YAML 解析 / 路径唯一性 / 6 个 spawn 事件 / spawn 时间精确性 / 敌人实际走不同路 / 目标格验证 / 完整清图
  6. 全部通过 → 81→91 tests；commit 1e4dfbb + push
- **结果**：91 tests 全绿。多路地图演示完成，两条路线均正确调度和收束。
- **决策**：Liskarm 部署在顶路 (2,4)、SilverAsh 部署在收束点 (4,2) 并将 block 提升到 2——确保清图测试能稳定获胜而不依赖 S3 随机性；收束架构复用现有 goal_system path_progress 逻辑，无需修改 core。

---

### [2026-04-19 18:56:39] Progress Loop 〈sonnet-0419AY〉
- **用户**：Progress Loop × 5 + Research Loop × 3 队列触发（积压）
- **执行**：
  1. 实现 **Warfarin S2 Sanguinelant**：`warfarin.py` 重写；sp_cost=3 initial_sp=1 duration=10s requires_target=False；on_start 遍历 allies 加 ATK RATIO+35%；on_end 清理
  2. 在 `SkillComponent` 新增 `requires_target: bool = True` 字段；`skill_system.py` AUTO trigger 增加分支：requires_target=False 时跳过 lockout 直接 fire
  3. 3 tests（无 heal 目标时仍触发 / buff 全员生效 / 到期清除）全绿
  4. 发现并记录并行 session 追加：怒潮凛冬 splash_atk_multiplier + main_0-2 Crossroads 双路地图 + 7 tests（91→94 base）
  5. commit a0e59bf + push
- **结果**：94 v2 tests 全绿。新增机制：`requires_target=False` 绕过 lockout，适用于所有纯 buff 技能（Warfarin S2、将来的守护 drone 等）。
- **决策**：把 `requires_target` 放在 `SkillComponent` 而非 `register_skill` 参数——因为 skill_system 已读 `sk` 字段，不需要额外查注册表；保持单一来源。

---

### [2026-04-19 18:57:57] Progress Loop 〈sonnet-0419AZ〉
- **用户**：Progress Loop 自动触发（scheduled task）
- **执行**：
  1. 读取 MEMORY.md + 最近 10 条 git log
  2. 运行 94 tests → 全绿（0.27s）
  3. 检查 TODO/FIXME → 仅 `data/characters/__init__.py` 有一处关于 canonical pipeline 的注释，非阻断
  4. 检查 chronicle → 2026-04-19.md 记录完整，最新条目 sonnet-0419AY
- **结果**：项目健康。工作树干净，所有变更已 push。无阻断问题。
- **决策**：无重大决策。低优先级待办：Arts 致死 0-HP edge case（sonnet-0419AV 标记）。

### [2026-04-19 18:55:10] M2.5 + M3a + 撼地者特性 〈opus-0419a〉
- **用户**：「你复刻 就好 不要家具 剧情 语音服装」+「你继续」两次 — 锁定战斗机制 1:1 scope，持续推进。
- **执行**：四阶段并推。(1) **M2.5**：修 `tools/gen_characters.py` `_infer_attack_type` 按 profession/subProfessionId 分派（MEDIC→HEAL / CASTER+SUPPORT→ARTS / subProf 含 arts→ARTS / 其余 PHYSICAL），重生成全 rarity → 410 playable ops (`a1ffc1f`)；(2) **M3a 敌人批量**：新建 `tools/gen_enemies.py` + `data/enemies/registry.py`，从 `enemy_database.json` 批 → 1859 敌人（1699 GROUND + 160 AIRBORNE），名字含 `\n` bug 修复；(3) **stages 接入** enemy registry，任意 akgd enemy key 可作 wave id；(4) **M4 第一条特性行为** `撼地者`：`splash_atk_multiplier: float = 1.0` 加入 UnitState，combat_system 按此倍率算 splash，curated `data/characters/headb2.py` 设 splash_radius=1.0 + splash_atk_multiplier=0.5（与 PRTS 怒潮凛冬"50% 群体物理伤害"对齐），3 测试验证 splash/倍率/不伤主目标 (`e30d879`)。**84 tests 绿**（随 Sonnet 并发补至 94）。
- **结果**：v2 覆盖 **416 干员 + 1862 敌人**含 curated。首条 archetype 特性行为上线。Sonnet 同期完成 Exusiai S2/Angelina S3/Warfarin S2/SP lockout/ASPD caps/FRAGILE stacking/main_0-2 双线关。
- **决策**：continue 模式的 ROI 优先：广度完 → 深度挑 archetype 特性一个一个写。下一步 gen_stages.py 批量转 673 主线关（stage_table → levelId → level_*.json 三层解析）。保持 Cron 关闭。

### [2026-04-19 19:00:49] Turn — 多重 Loop 触发 〈sonnet-0419AY〉
- **用户**：Progress Loop × 2 + Research Loop × 2 批量触发
- **执行**：
  1. 确认 94 tests 全绿（并行 session 新增 Warfarin S2 Sanguinelant）
  2. 读取研究报告确认剩余 bug 状态：3/3 已修复（FRAGILE ✓ / lockout ✓ / ASPD ✓）
  3. 修复 **`retreat()` alive 检查**：添加 `if not unit.alive: return` 防止死亡干员重置冷却、获取 DP 退款；新增 1 个测试
  4. 创建 **main_0-3 "Sky and Ground"** 混合地面 + 空中敌人关卡：3 只 originium_slug（地面）+ 3 架 originium_drone（空中）走相同路线；5 个测试覆盖：加载 / spawn 计数 / 无人机不被阻挡 / 地面敌人正常阻挡 / 双干员清图
  5. 全部通过 → 94→100 tests；commit a1d36e1 + push
- **结果**：100 tests 全绿。retreat 边界案例修复，空地混合关卡演示了 Mobility.AIRBORNE bypass 机制与 stage 系统的完整整合。
- **决策**：stage 中 originium_drone 沿与 slug 相同的路径飞行而非独立路径——简化地图设计同时最大化机制对比度；cleanup_system 不清除盟友的 deployed 标志已知，retreat 的 alive 守卫补全了这个缺口。

### [2026-04-19 19:09:13] Turn — 续上次会话 〈sonnet-0419AZ2〉
- **用户**：会话压缩后继续（Progress Loop 积压触发）
- **执行**：
  1. 创建 Research Cron (cbea15e3) + Progress Cron (42b772a5)
  2. 运行 `test_v2_vanguard_dp.py` → 3 tests；其中 `test_texas_gains_dp_per_kill_multiple` 失败：slug2 在 Texas 二次攻击前走出 range（atk_interval=1.05s > slug 离开 range 时间）
  3. 修复：`slug.move_speed = 0.0` 让两只 slug 停在 (1,0)，Texas 依次击杀 → 3/3 绿
  4. 跑全套 v2 → 105 passed, 3 failed；查因：`counted_death` 冲突——`combat_system` 用它作 on_kill 守卫，`cleanup_system` 用它计 `enemies_defeated`，导致被 combat 先标记的敌人不被计为 defeated
  5. 修复冲突：去掉 `combat_system` 中的 `counted_death` 赋值——combat loop 已有 `if not target.alive: continue` 保护，on_kill 天然只触发一次；`counted_death` 完全归还给 cleanup_system
  6. 全套 v2 → 108 passed (0 failed)
- **结果**：108 v2 tests 全绿。新增 `on_kill` hook 机制 + Texas Pioneer DP-on-kill 行为。`counted_death` flag 职责边界明确。
- **决策**：`on_kill` 不需要独立守卫 flag — 攻击者在每个 tick 开头跳过 `not target.alive` 目标，死亡后续 tick 不会重复触发。两个系统共用同一 bool 是该 bug 的根因，通过去除冗余守卫而非增加新字段解决。

### [2026-04-19 19:14:34] Turn BB 〈sonnet-0419BB〉
- **用户**：会话从上下文压缩后恢复，继续修复 `enemies_defeated == 0` 回归（context summary 描述）
- **执行**：
  1. 创建 Research Cron (d8311a39) + Progress Cron (367ea240)
  2. 读取实际代码 → combat_system 没有 `counted_death` 冲突，108 tests 全绿（context summary 分析的 bug 已由并行 session 解决）
  3. 发现 `initial_sp` 从未在 `world.deploy()` 中被应用 — 所有用 `initial_sp` 的 operator（Exusiai/Ifrit/Warfarin）部署后 SP 仍为 0，与 Arknights 实际行为不符
  4. 修复 `core/world.py` deploy()：`if unit.skill.initial_sp > 0: unit.skill.sp = float(unit.skill.initial_sp)`
  5. 实现 `data/characters/ifrit.py`：S2 Combustion（instant AOE arts，duration=0，260% ATK，sp_cost=22/initial=10）
  6. 编写 `tests/test_v2_ifrit_s2.py`：7 个测试；排查并修正测试问题（ranged→ELEVATED tile、atk_cd=999 屏蔽普攻）
  7. 提交 `9136c02`，push 到 remote
- **结果**：115/115 tests 全绿。instant skill 路径 + initial_sp seeding 两个新机制均有测试覆盖。
- **决策**：instant skill 在 `_fire_skill` 中通过 `duration==0 → sk.sp=0`（而非设 active_remaining）实现，无需 on_end；initial_sp 直接在 deploy 时一次性赋值比在 tick 开始检查更简洁。

### [2026-04-19 19:14:15] Progress Loop 〈sonnet-0419BA〉
- **用户**：Progress Loop 自动触发（scheduled task）
- **执行**：
  1. 读取 git log → 发现 `8fe56df` 已由并行 session 提交（Texas P5 + on_kill hook + 108 tests）
  2. 检测到 3 个 failing tests（test_v2_stage / 0_2 / 0_3），追踪至 stale `.pyc` 缓存（非真实 bug）
  3. 检查未追踪文件：`ifrit.py`（完整的 Ifrit S2 Combustion 实现 + 7 个测试）+ 研究报告
  4. 运行全套测试 → 108 pass（ifrit 入库后 115 pass，7 个新测试全绿）
  5. 提交 ifrit.py + test_v2_ifrit_s2.py + gen_stages_analysis 研究报告 + chronicle
- **结果**：115 v2 tests 全绿。Ifrit S2 Combustion（instant AOE arts burst 260% ATK）落地。
- **决策**：无重大决策。stale `.pyc` 根因：pytest 先发现旧 bytecode，首次运行后自动失效。解法是跑一次 `python3 -m pytest` 触发重编译而非手动清理。

### [2026-04-19 19:03:39] Research Loop 〈sonnet-0419AY〉
- **用户**：Research Loop 自动触发（scheduled task）
- **执行**：
  1. 确认 100 tests 全绿（0.21s）
  2. 检查 git log、TODO/FIXME → 唯一 TODO 是 `data/characters/__init__.py` canonical pipeline 注释（非阻断）
  3. 研究 ArknightsGameData 关卡 JSON 格式：level_main_00-04.json 完整解析 waves/fragments/actions/routes/mapData 结构
  4. 发现关键 bug：stage_table 存的 levelId 路径大写 `Obt/Main`，但 GitHub 实际路径是小写 `obt/main`
  5. 写入研究报告 `docs/research/gen_stages_analysis_2026-04-19.md`
- **结果**：gen_stages.py 实现路径已清晰。核心技术要点：timing = wave+fragment+action preDelay 累加；路径从 MOVE 类型 checkpoints 提取；tile 类型映射表已建立；WAIT_FOR_SECONDS checkpoint 在 v1 中 pass-through 处理。
- **决策**：gen_stages.py v1 跳过 branches 和 randomSpawnGroup（标记 TODO），专注主线 MAIN 类型关卡。复杂性评估：easy wave 展平 / medium tile 映射 / hard WAIT checkpoint（暂不支持）。

### [2026-04-19 19:16:16] Progress Loop × 4 〈sonnet-0419BC〉
- **用户**：Research Loop + Progress Loop × 4 批量触发
- **执行**：
  1. 创建 Research Cron + Progress Cron；派 background Research Agent（Vanguard mechanics / correctness gaps）
  2. 确认 liskarm.py + exusiai.py 已用 generated/ base stats — 无需操作
  3. 读取 `docs/research/gen_stages_analysis_2026-04-19.md` → 确认路径、结构、tile mapping
  4. 抓取 `level_main_00-04.json` 验证实际 JSON 结构：waves/fragments/actions/routes/mapData
  5. 发现 **坐标轴翻转 bug**：mapData 行从顶部计（row 0 = 顶），routes 行从底部计（row 0 = 底）→ 不翻转导致 goal tile 在 y=2 但路径终点在 y=3（差 1）
  6. 实现 `tools/gen_stages.py`（229 行）：行翻转 `y = height - 1 - row_idx`；SPAWN action 扁平化；MOVE-only checkpoint 路径；enemy key → registry handle（复用 gen_enemies.py dedup 逻辑）；LRU JSON 缓存到 `.level_cache/`
  7. 确认并行 session 已提交空版本（`ba2b66d`），本 session 在其基础上加 row-flip + 完整实现
  8. 生成 main_00-01 → main_00-11（11 个关卡，所有路径终点均与 goal tile 对齐）
  9. 编写 `test_v2_gen_stages.py`（16 tests）：加载结构 / goal tile 存在 / 路径终点对齐 / 首次 spawn 时间 / enemy 注册表 / 11 个关卡参数化 → 全绿
  10. 全套 131 v2 tests 绿
- **结果**：131 tests 绿。main_00 全 11 关生成并通过加载验证。gen_stages.py 可扩展到 main_01-main_13。
- **决策**：行翻转在 `_parse_tiles` 而非 `_build_path` 实现——路由坐标本身已经是正确的底部计数，只有 mapData 需要翻转。不引入额外变量，每个读取点直接 `y = height - 1 - row_idx`。

### [2026-04-19 19:19:00] Research Agent 完成 + Texas bug 修复 + Charger 实现 〈sonnet-0419BC〉
- **用户**：Research Agent（ac4b7ea1）完成并触发 task-notification
- **执行**：
  1. 读取 research 报告 → 发现 **Critical Bug**：Texas DP on kill 逻辑完全错误
     - Texas 属于 Pioneer 子职；Pioneer class trait 只有"阻挡 2 个敌人"，无击杀 DP
     - Texas 的天赋"战略部署"(Tactical Delivery) 是**开局 +2 DP**（作战开始时，若 Texas 在队中）
     - Charger 子职（芬/薇薇安娜/泡普卡/格拉尼/能天使）才是击杀 DP
  2. **修复 talent_registry.py**：4-tuple 增加第 4 位 `on_battle_start`；新增 `fire_on_battle_start`
  3. **修复 world.py**：`add_unit()` 对 ALLY 单位触发 `fire_on_battle_start`
  4. **修复 texas.py**：天赋从 `on_kill` 改为 `on_battle_start`，`refund_dp(2)`（E2 rank）
  5. **重写 test_v2_vanguard_dp.py**：5 个测试验证正确机制（battle start / no kill DP / two instances）
  6. **新增 data/characters/fang.py**：Charger 子职 `vanguard_charger_dp_on_kill`，击杀给 1 DP
  7. **新增 test_v2_charger_dp.py**：3 个测试（无 battle start DP / 单杀 / 三杀）
  8. 全套 160 tests 绿；commit 49a9a2a（bug fix）
- **结果**：160 tests 绿。Texas 行为 1:1 正确（+2 DP battle start）。Charger trait 正确落地（Fang +1 DP/kill）。`on_battle_start` hook 可复用于所有 battle-start 被动。
- **决策**：`on_battle_start` 在 `add_unit()` 时触发而非第一个 tick，因为测试需要在 add 后立即检查 DP，且语义上 "in squad" = "added to world" 是最自然的映射。

### [2026-04-19 19:20:42] Progress Loop × 8 〈sonnet-0419BD〉
- **用户**：8 个 Progress Loop + Research Loop 批量触发
- **执行**：
  1. 创建 Research Cron (d8311a39) + Progress Cron (367ea240)
  2. 确认 131 tests 全绿；检查并行 session 新增内容（BC: Texas fix + Fang Charger + main_00 11 stages）
  3. 读取 main_00-01.yaml 布局，计算 SA/Liskarm 伤害 vs 敌人（SA 秒杀 slime，3-4 击 nsabr）
  4. 新增 `tests/test_v2_stage_main00.py`：4 个 main_00-01 精确测试（清图/无干员败/spawn count/starting_dp）+ 20 个参数化测试（所有 main_00-02 到 main_00-11 有 goal + 路径）；全绿；commit + push
  5. 读取 chen.py 生成文件（ATK=660, block=2, Guard, melee）；实现 `data/characters/chen.py`：S2 "Sheathed Strike"（380% ATK arts instant AOE，sp_cost=20 initial_sp=10，MANUAL trigger，AUTO_ATTACK SP gain）
  6. 新增 `tests/test_v2_chen_s2.py`：7 个测试，首次覆盖 `SkillTrigger.MANUAL` 路径（manual_trigger() API / 不自动触发 / SP 不足门控 / 伤害公式 / AOE / SP-on-attack / initial_sp）；全绿；commit + push
- **结果**：167/167 tests 全绿。主线关卡端到端清图验证完成（SA+Liskarm 无损清 main_00-01）。MANUAL trigger 代码路径首次有测试覆盖。
- **决策**：Ch'en 使用 AUTO_ATTACK SP gain（每次攻击 +1 SP）而非 AUTO_TIME——与游戏内 "鞘打" 实际 SP gain 模式一致；MANUAL trigger 测试用 `manual_trigger(world, op)` 函数调用而非自动触发，精确模拟玩家操作时机。

### [2026-04-19 19:22:53] Progress Loop — Myrtle S1 Standard Bearer 〈sonnet-0419BC〉
- **用户**：Progress Loop × 4 + Research Loop × 2 触发
- **执行**：
  1. 后台派 Research Agent（Standard Bearer / Fortress Defender / blocking mechanics）
  2. 实现 **Myrtle S1 Tactical Delivery**：`data/characters/myrtle.py`；on_tick 以 14/8=1.75 DP/s 累积，通过 per-unit `_myrtle_s1_dp_frac` 缓存处理小数；on_start block=0；on_end block=1（还原）；sp_cost=12 initial_sp=6 duration=8s requires_target=False
  3. 测试 `initial_sp` 不自动种子（需要手动设置 `skill.sp = initial_sp`，因测试跳过 `world.deploy()`） → 修复 2 个测试
  4. 5 tests（无 battle-start DP / 无目标触发 / 14 DP 总量 / block 抑制恢复 / 再次触发）全绿
  5. 全套 172 tests 绿
- **结果**：172 tests 绿。Standard Bearer DP drip 机制落地：fractional per-tick accumulation + block suppression。
- **决策**：DP drip 用 per-unit `_skill_dp_frac` 而非共享 GlobalState._dp_fractional——避免与自然 DP 回复的小数累加器冲突；攻击抑制（stop attacking during skill）暂不实现，Myrtle ATK 低影响微小。

### [2026-04-19 19:24:02] Progress Loop — P8 确认 〈sonnet-0419BE〉
- **用户**：Progress Loop 自动触发（scheduled task）
- **执行**：
  1. 读取 git log → 最新提交 e7c181e（P8 Myrtle S1 Standard Bearer DP drip，172 tests）
  2. 运行 `arknights-sim/tests/` → 172 passed（v2 全绿；v1 有 13 errors 为已知历史遗留）
  3. 读取未追踪 `docs/research/2026-04-19-1920-myrtle-fortress-blocking.md`
     - Myrtle S1 SP values 修正：M3 是 sp_cost=22 / initial_sp=13，当前实现用 12/6（SL7 rank，可接受）
     - 确认 Myrtle 实现逻辑正确（cannot_attack + cannot_block 在整个 skill 期间）
  4. 识别 P9 优先项：
     - **P9a**（Blocking Priority Fix）：path_distance_remaining on enemy，operator 攻击目标 = 剩余路径最短的被阻挡敌人
     - **P9b**（Fortress Defender）：Ashlock/Horn/Firewhistle 的范围切换（未阻挡→ ranged AOE；阻挡→ melee single）
     - 已有研究报告就位，实现路径清晰
  5. 无阻断、无卡点、无需用户介入
- **结果**：172 tests 全绿。P8 完成状态确认。研究报告已就位（待 commit）。P9 优先级确认。
- **决策**：无重大决策。研究报告暂未 commit（loop 中不主动 commit 已追踪文件之外的内容，等待用户或下个活跃 session）。

### [2026-04-19 19:26:16] Progress Loop × 4 〈sonnet-0419BF〉
- **用户**：4 个 Progress Loop + Research Loop 批量触发
- **执行**：
  1. 确认 167 tests 全绿（BD 会话工作）；检查并行 session：BE 已确认 P8 Myrtle 完成 + 研究报告
  2. 审计已注册 skills/talents → 发现 on_tick HEAL path 未被测试（Myrtle on_tick 给 DP，但无 operator on_tick 给血量）
  3. 读取 `data/characters/generated/silent.py`（赫默/Silence，ATK=557，HEAL type，interval=2.85s）
  4. 实现 `data/characters/silence.py`：S2 "Medical Protocol"，on_tick 0.70 ATK/s 持续治疗 20s，requires_target=False，AUTO_TIME
     - `_find_most_injured`：找 HP% 最低的已部署友军作为 heal 目标
     - Accumulator 模式（`_silence_s2_accum`）避免 float tick 截断误差
     - `total_healing_done` 追踪到 global_state
  5. 修复 1 个测试：`test_s2_sp_resets_after_instant_duration_expires` → SP 在技能结束后立即开始计时，21s 循环会积累 ~1.0 SP；改为 `sp < 1.0` 断言
  6. 7 tests 全绿；提交 commit f4bc8d8 + push
- **结果**：179/179 tests 全绿。首个 on_tick HEAL 技能落地。治疗优先级（最低 HP%）、防止超量治疗、全满血时跳过均有测试覆盖。
- **决策**：Accumulator 模式（float 累计，取 int 部分）比"每 N 帧检查"更精确，且与游戏内 per-second tick 语义一致；`_find_most_injured` 排除自身（Silence 不自治疗）且排除满血目标，语义清晰。

### [2026-04-19 19:28:51] Turn 1 〈sonnet-0419BG〉
- **用户**：自动续上次压缩会话 — 继续 targeting_system.py 修复任务
- **执行**：
  1. 创建 Research Loop (40ec7817) + Progress Loop (68b8c9d0) crons
  2. 读取 `core/systems/targeting_system.py`，确认 lines 63+66 使用 `max(path_progress)` 而非正确的 `min(remaining)`
  3. 修改 `targeting_system.py`：引入内联 `_dist_remaining()` 辅助，将两处 `max(path_progress)` 改为 `min(_dist_remaining)` — 正确语义：最近出口优先
  4. 同步修复 `_update_block_assignments` 排序：`-e.path_progress` → `max(0.0, path_len - e.path_progress)`
  5. 编写 `tests/test_v2_targeting.py` — 4 个精确测试：
     - Test 1：同路径，`max(progress)` == `min(remaining)`，结果一致
     - Test 2：多路径分叉 —— 短路径 progress=2.5 remaining=0.5 vs 长路径 progress=5.0 remaining=4.0；`max(progress)` 错误选 B，`min(remaining)` 正确选 A
     - Test 3：阻挡敌人优先于距出口更近的非阻挡敌人（Rule 1 不变）
     - Test 4：多名阻挡敌人中选 remaining 最小者
  6. 调试：第一版测试位置设计导致 `_update_block_assignments` 无法自动 assign；改为直接调用 `_targeting_for_operator` 绕过 clearing
  7. 全套 183 tests 全绿；commit + push pending
- **结果**：183/183 tests 全绿。targeting_system 语义正确，支持多路径舞台。
- **决策**：Test 3/4 直接导入 `_targeting_for_operator` 而非走完整 `targeting_system`（含 `_update_block_assignments`），保持对 blocking 状态的完全控制，避免位置布置约束污染测试意图。

### [2026-04-19 19:37:53] Turn — Mechanics gap research 〈sonnet-0419BH〉
- **用户**：针对 arknights-sim v2 ECS 模拟器，调研 6 个机制缺口（Elysium S1、Myrtle S2、Vigna talent、Ashlock 原型、HoT 算子、targeting 优先级异常）
- **执行**：WebSearch × 6 并行（Elysium/Myrtle/Vigna/Ashlock/HoT/targeting）+ WebFetch Terra Wiki Elysium & Myrtle 页面精确数据；写入 `docs/research/2026-04-19-2135-arknights-mechanics-gaps.md`
- **结果**：6 个调研项全部落地：Elysium S1=18 DP/8s；Myrtle S2="Healing Wings" 16 DP/16s + 50% ATK heal/s（非 Tactical Delivery）；Vigna talent 是暴击概率非 DP-on-kill；Ashlock = 完全相同 DEF_FORTRESS archetype；Perfumer passive HoT 未建模；Deadeye/Ceobe/Absinthe targeting override 未建模。优先级排序已附。
- **决策**：无重大决策。数据全部来自实时 web 搜索（非记忆），"Tactical Delivery" 名字归属澄清为 Texas talent，避免在 myrtle.py 中命名错误。

### [2026-04-19 19:35:43] Turn 2 〈sonnet-0419BG〉
- **用户**：4× Progress Loop + 1× Research Loop 批量触发
- **执行**：
  1. 确认 183 tests 全绿（上一个 turn 已完成 targeting fix）
  2. 确认最高优先级任务：Fortress Defender (Horn/Ashlock) 攻击模式切换
  3. 新增 `RoleArchetype.DEF_FORTRESS` 枚举值
  4. 修改 `targeting_system.py`：DEF_FORTRESS 分支 — 非阻挡时收集所有射程内敌人到 `__targets__`，阻挡时临时切换 `range_shape` 为 `_melee_range` 再调 `_targeting_for_operator`
  5. 修改 `combat_system.py`：检查 `__targets__`，存在时走 `_apply_fortress_ranged()` — 对所有目标逐一 take_physical，统一重置 atk_cd，only +1 SP per swing
  6. 创建 `data/characters/horn.py`：ranged range 3 格前向 + 侧面两格，melee range 自身格 + 前1格
  7. 发现并修复隐藏 bug：`effective_stat()` 对所有轴用 `math.floor()`，导致 move_speed=0.5 被截成 0.0，敌人完全不移动（已有测试全用 speed=1.0 故未被发现）。修复：只对整数轴(ATK/DEF/HP/ASPD)用 floor，MOVE_SPEED/RES 用原始 float 计算
  8. 编写 6 个测试：archetype 验证 / 非阻挡时 AoE 全击 / 单敌人非阻挡 / 阻挡时 melee 单目标 / 模式随阻挡状态切换 / 射程外不伤
  9. 189/189 全绿；commit 5c5e515 + push
- **结果**：Horn Fortress Defender 完整上线，move_speed<1.0 的敌人现在能正常移动。
- **决策**：`__targets__` 作为动态属性与 `__target__` 并存（非 Union），因为 targeting_system 和 combat_system 都需要清楚地区分"单目标模式"和"全目标模式"，两个属性语义更清晰；floor-only-for-int-axes 是正确的游戏内语义，MOVE_SPEED 是实数速度，没有取整语义。

### [2026-04-19 19:38:25] Progress Loop 〈sonnet-0419BI〉
- **用户**：Progress Loop 自动触发（scheduled task）
- **执行**：
  1. 读取 MEMORY.md + chronicle + git log → 189 tests 全绿（Horn Fortress Defender 最新提交 5c5e515）
  2. 识别 4 个未追踪文件 + `.level_cache/` 缓存目录需 gitignore
  3. 追加 `tools/.level_cache/` 到 `arknights-sim/.gitignore`
  4. 暂存并提交：`arknights-sim/docs/` + `docs/research/` 两个新 research 报告 + chronicle + .gitignore
- **结果**：189 tests 全绿。所有 research/chronicle 文件已纳入版本控制，`.level_cache/` 已 gitignored。
- **决策**：无重大决策。P10 候选优先项（按重要性）：①Perfumer passive HoT（未建模）②Besieger Sniper 重量优先 targeting ③Liskarm+Exusiai 接入 generated 基础数值。

### [2026-04-19 19:41:37] Turn 3 〈sonnet-0419BG〉
- **用户**：Research 报告完成（af02f01fd8c31050c），继续下一批
- **执行**：
  1. 读取研究报告：Elysium S1 (18 DP/8s P1) / Ashlock DEF_FORTRESS (P2) / Vigna Charger (P3) / Myrtle S2 (P4) / Thorns (P5)
  2. 创建 `data/characters/elysium.py`：S1 "Support γ" sp_cost=35, initial_sp=10, duration=8s, requires_target=False, dp_rate=2.25/s；5 tests（无战斗 DP / 无目标开技能 / 18 DP 总量 / block 抑制 / 比 Myrtle 多）
  3. 创建 `data/characters/ashlock.py`：DEF_FORTRESS 与 Horn 完全相同，复用相同 range shapes；3 tests
  4. 创建 `data/characters/vigna.py`：VAN_CHARGER，import fang._CHARGER_DP_TAG 确保注册；4 tests（含多击杀 = 多 DP）
  5. 发现并修复 `data/characters/thorns.py` 中的 `RoleArchetype.GUARD` typo（应为 `GUARD_LORD`），该 bug 导致 roster 测试长期失败
  6. 201/201 tests 全绿；commit e7f5cca + push
- **结果**：三个新 operator 上线，roster 测试恢复绿灯。Myrtle S2（DP drip + HoT 复合技能）为下一步。
- **决策**：Vigna 的 crit 天赋（10% 暴击 ×1.5 伤害）明确标记为 deferred，理由：RNG 引入非确定性，测试需 seed/mock；当前优先级不值得引入 RNG 体系；`vigna.py` 有文档说明。thorns.py 的 `_attack_all_in_range` flag 由 linter 自动同步到 targeting_system — 已验证正确（Thorns S3 AOE 复用 Fortress 的 `__targets__` 路径）。

### [2026-04-19 19:42:37] Turn 4 〈sonnet-0419BJ〉
- **用户**：续上次会话（context compressed）— 上次卡在 3 个 targeting test 失败
- **执行**：
  1. 运行测试发现 4/4 targeting tests 已全绿（并行 session 修复）；189 → 208 全套 189 绿
  2. 确认 `e7f5cca` 已包含 Elysium/Ashlock/Vigna + Thorns GUARD_LORD fix，当前 201 tests 全绿
  3. `thorns.py` 原版存在，只缺 `requires_target=False`（S3 无目标也应触发）
  4. 实现 targeting_system 通用 `_attack_all_in_range` flag — elif 分支，标志为真时走 `__targets__`（全范围多打），复用 `_apply_fortress_ranged`；Thorns S3 `on_start/on_end` 设置/清除此 flag
  5. 发现 _enemy helper 用单点 path 导致敌人立即 path_progress > 0 = 退出地图变 alive=False；改为 20点重复 path + move_speed=0
  6. 新增 `tests/test_v2_thorns.py`（7 个测试）：天赋注册 / counter 触发 / counter 数量 / 零伤害跳过 / S3 targets 多目标 / S3 AOE 命中所有 / S3 到期恢复
  7. 全套 208/208 tests 绿；commit 6331892 + push
- **结果**：208 tests 全绿。Thorns S3 AOE 完整实现。`_attack_all_in_range` 通用 flag 可供未来 operator 复用。
- **决策**：`requires_target=False` 加到 Thorns S3 — AOE 持续型技能不需要有目标才激活，和 Silence S2 逻辑一致。通用 flag 设在 targeting_system 而非 combat_system，因为 targeting 决定了"打谁"，flag 的语义正好在 targeting 层。

### [2026-04-19 19:44:07] Turn 4 〈sonnet-0419BG〉
- **用户**：Progress Loop × 6 批量触发（积压）
- **执行**：
  1. 确认 208 tests 全绿（Elysium/Ashlock/Vigna/Thorns fix 均已推送）
  2. 最高优先级：Myrtle S2 "Healing Wings"（研究报告 P3）
  3. 修改 `data/characters/myrtle.py`：新增 S2 逻辑 — 两套独立分数累加器（`_myrtle_s2_dp_frac` + `_myrtle_s2_heal_frac`），on_tick 同时推进 DP drip（1.0 DP/s）和 HoT（0.50 × ATK HP/s）
  4. 5 格十字形治疗范围（`_S2_HEAL_RANGE`），每 tick 找血量百分比最低的友军
  5. make_myrtle("S2") 分支：sp_cost=18, initial_sp=9, duration=16s, requires_target=False
  6. 编写 8 个测试：无目标触发 / block 抑制 / 16 DP 总量 / S2 速率低于 S1 / 治疗范围内友军 / 治疗量验证（±8% 容差）/ 范围外不治疗 / 最危重优先
  7. 216/216 全绿；commit c772703 + push
- **结果**：首个复合技能（DP drip + HoT）上线。两路分数累加互不干扰，均通过精确量化测试。
- **决策**：heal_frac 携带单位为"累积 HP"而非"累积分数"（`_S2_HEAL_RATE * atk * dt`），避免二次乘法；heal target 每 tick 重新查询（而非 on_start 缓存）因为目标 HP 状态会变，每 tick 选最危重更忠实游戏行为。

### [2026-04-19 19:53:14] Progress Loop 〈sonnet-0419BK〉
- **用户**：Progress Loop 自动触发（scheduled task）
- **执行**：
  1. 检查 git status → `talent_registry.py` 已有未提交变更（on_tick hook 基础设施）；`surtr.py` + `test_v2_surtr.py` 为未追踪文件
  2. 运行 216 tests → 全绿（Myrtle S2 HoT 最新状态）
  3. 确认 `passive_talent_system.py` 已存在并注册，`fire_on_tick` 已完整；`talent_registry.py` diff 确认 on_tick hook 完整
  4. 实现 **Perfumer 被动 HoT**：
     - 查找 `char_181_flower`（调香师 ringhealer）E2 stats：ATK=345, HP=1710
     - 天赋"熏衣香"(E2)：全体友军每秒回复 ATK×5% 生命值
     - `data/characters/perfumer.py`：`_HEAL_RATE=0.05`，accumulator pattern，每 tick 扫描 `world.allies()` 全体友军治疗
     - 发现 `world.py` 已无需修改（passive_talent_system 已在 SKILL phase 注册）
     - 调试：测试失败因 Perfumer 普攻也在治疗盟友（HEAL attack type），rate test 需 `atk_cd=999.0` 隔离被动
  5. Surtr 测试（8 tests）验证：天赋 Dainsleif（HP<50% 时 ATK+50% + ARTS 伤害类型）+ S3 Soul Torrent（ATK×4.6 AOE，10s 无敌后 3s 持续掉血至死）均绿
  6. 提交 `9cd5dcd` + push
- **结果**：237 tests 全绿。首个被动 HoT 天赋（非技能）上线。`passive_talent_system` 基础设施已验证可用。
- **决策**：rate test 用 `atk_cd=999.0` 屏蔽普攻而非修改世界逻辑——被动治疗和普攻治疗是两个合理的独立机制，测试应精确隔离被测机制；Perfumer 没有实现 S3，仅实现被动天赋，与当前进度优先级一致。


### [2026-04-19 19:59:04] Turn 1 〈sonnet-0419BL〉
- **用户**：Progress Loop / 续会话（post-compress）— 上次会话报告 3 个失败测试（Nearl/Perfumer），任务继续
- **执行**：
  1. 创建 Research Cron (90e69117) + Progress Cron (acbd6e45)
  2. 运行 v2 tests → 全绿 247 tests（Nearl/Perfumer 失败已在上次被 subagent 修复）
  3. 读取最新 research 报告 `2026-04-19-1914-arknights-next-steps.md`：验证 Texas DP bug 已修复（`on_battle_start`），P6.4 gen_stages 需网络，优先 Saileach S2 + 目标优先级覆盖
  4. **Saileach S2 "Flagship Order"** (`data/characters/saileach.py`)：
     - 基于 `generated/sidero.py`（铸铁，char_333_sidero）
     - S1: 14 DP/8s = 1.75 DP/s，block=0，与 Myrtle S1 同速率
     - S2: 20 DP/20s = 1.0 DP/s + 全体友军 HoT 35% ATK/s（无范围限制，与 Myrtle S2 区别）
     - 6 个测试：无目标触发/block 抑制/20 DP 总量/距离治疗/治疗量校验/S1 分支
  5. **UnitState.weight 字段**：在 `unit_state.py` 新增 `weight: int = 1`，为 Besieger 目标优先级准备
  6. **targeting_system.py 两路新分支**：
     - `SNIPER_DEADEYE`：目标最低 DEF 敌人，DEF 相等时 fallback 到 min 距离
     - `SNIPER_SIEGE`：目标最高 weight 敌人，weight 相等时 fallback 到 min 距离
  7. 4 个新目标测试追加到 `test_v2_targeting.py`：Deadeye x2 + Besieger x2
  8. 247 v2 tests 全绿；commit c52e516 + push
- **结果**：247 v2 tests 通过。Saileach S2 全局 HoT 上线。Deadeye Sniper + Besieger Sniper 目标优先级覆盖上线。
- **决策**：Saileach S2 全局 HoT 不做范围检查（与 Myrtle S2 十字形治疗区别），忠实于游戏"所有友军"描述。Besieger Sniper fallback 用 `-_dist_remaining` 保证最靠近出口的重量相等敌人排第一（`max` key 需反转距离）。

### [2026-04-19 20:03:08] Turn 2 〈sonnet-0419BL〉
- **用户**：Progress Loop × 5 批量触发（积压）
- **执行**：
  1. 确认 247 tests 全绿（上次 turn 完成 Saileach S2 + Deadeye/Besieger targeting）
  2. 检查剩余待办：Texas DP bug 已确认不存在；物理伤害最小值 5% ATK 已实现；需推进 gen_stages 章节扩展
  3. 运行 `python tools/gen_stages.py main_01 main_02 main_03`：从 ArknightsGameData 下载并生成 30 个 YAML（章节 01-03）
  4. 继续章节 04-06（35 个 YAML）、07-09（50 个 YAML）、10-13（70 个 YAML）
  5. 总计生成 185 个新 YAML 文件，data/stages/ 达到 199 个文件（3 手写 + 196 自动生成）
  6. 发现 `test_v2_gen_stages.py` + `test_v2_stage.py` 因 `pythonpath = v1` 导致 `stages.loader` 解析到 v1 版本 → 在两个测试文件顶部添加 `sys.modules` eviction 修复
  7. 替换 `test_all_main_00_stages_load` 硬编码参数为动态 glob 发现 `_ALL_STAGE_IDS`，自动覆盖所有 196 个 main_XX-YY.yaml 文件
  8. 439 v2 tests 全绿（+ 185 新增参数化测试）；commit a34308c + push
- **结果**：196 个完整主线关卡 YAML（章节 00-13），439 tests 全绿。全面覆盖主线战役地图数据。
- **决策**：用 glob 动态发现测试参数，避免每次新增 YAML 都需手动更新测试——测试即时自扩展。sys.modules eviction 比修改 pytest.ini `pythonpath` 更安全，因为 v1 测试仍依赖那个配置。

### [2026-04-19 20:07:04] Turn N 〈sonnet-0419BM〉
- **用户**：自动 Progress Loop 续上次会话（context 压缩后恢复）
- **执行**：
  1. 创建 Research Cron (6d0cebc6, 每小时) + Progress Cron (532cdf6c, 每 10 分钟)
  2. 运行全量 tests → 起点 247 tests（上次 session 留存）
  3. **Skadi（斯卡蒂）** `data/characters/skadi.py` — Dreadnought Guard
     - Talent "Predator": `on_kill` → 回复 15% max_hp，via `talent_registry.fire_on_kill`
     - S2 "Surge": ATK +130%，30s，AUTO_TIME，requires_target=True
     - 7 个测试：talent 注册/kill 回血/无超量回复/无 kill 无回复/S2 buff/S2 到期移除/S2 期间 kill 仍回血
     - `on_kill` 在 `combat_system` 内 `hp==0` 之后立即触发，被击杀单位仍存在于世界（CLEANUP 尚未运行），可读 `killed.max_hp`
  4. 全量 254 tests 通过 → commit 2727310 + push
  5. **Weedy（温蒂）** `data/characters/weedy.py` — Sentinel Specialist
     - Talent "Free-Flowing": 不阻挡任何敌人时 DEF +70%（`on_tick` conditional buff）
     - S3 "Torrential Stream": ATK +160% + ARTS + `_attack_all_in_range=True` + 每次命中施加 2s BIND
     - BIND 通过 `on_attack_hit` + `_weedy_bind_active` flag 实现；`StatusEffect.expires_at = elapsed + 2.0`
     - **首次使用 StatusKind.BIND**：状态系统已有 BIND 定义（`can_act()` 阻断移动），本次才首次验证
     - 9 个测试：talent 注册/不阻挡 DEF buff/阻挡移除 buff/S3 ATK+ARTS/S3 命中 BIND/BIND 停止移动/BIND 到期/多敌同时 BIND/S3 结束恢复
     - 调试：5 ticks 不够（atk_interval=1.2s → 12 ticks 间隔，S3 在 SKILL 阶段激活比 COMBAT 晚一 tick），改为 20 ticks
     - 调试2：movement test 中 slug 带真实路径会移出范围，改为验证 `can_act() == False`
  6. 全量 455 tests 通过 → commit 9a6ede3 + push
- **结果**：2 个新干员，16 个新测试，455 tests 全绿。StatusKind.BIND 首次端到端验证通过。
- **决策**：Skadi 选 S2（简洁 ATK buff）而非 S3（召唤影子，过于复杂）；Weedy BIND 实现用 `on_attack_hit` flag gate 而非在 skill on_tick 中逐 target 遍历，让 `_apply_fortress_ranged` 的现有 hook 调用链完成工作。

### [2026-04-19 20:11:57] Progress Loop 〈sonnet-0419BN〉
- **用户**：Progress Loop × 10 多重触发（积压队列，上次 BM session 完成后续触发）
- **执行**：
  1. 确认 455 tests 全绿（BM session: Skadi + Weedy 已提交）
  2. **Saga（嵯峨）** `data/characters/saga.py` — Warrior Vanguard
     - Talent "Tengu's Edge": SP 满时（sp >= sp_cost 且技能未激活）ATK +12%
       via `on_tick` conditional buff；`is_sp_full = active_remaining==0 AND sp>=sp_cost`
     - S2 "Tsurugi": ATK +120%，30s，sp_cost=35，initial_sp=10，AUTO_TIME，requires_target=True
     - **首次实现 SP-threshold 条件 buff**（Surtr 是 HP-threshold，Saga 是 SP-threshold，不同触发条件）
     - 7 个测试：talent 注册/SP 满时 buff 激活/SP 不满无 buff/技能激活时 buff 移除/S2 ATK buff/S2 到期还原/锁定时 buff 可见
  3. 调试：两处测试设计错误
     - `sp == 0.0` 断言错：duration 技能激活时 sp **不重置**，只在 on_end（`active_remaining → 0`）时才 sp=0
     - "S2 结束后再充满 SP" 测试失败：slug 仍在场 → S2 立即再次触发，talent buff 零可见时间
     - Fix: 测 4 移除 sp==0 断言 → 改验证"技能激活时 buff 消失"；测 7 改为"无 target 锁定时 buff 可见"
  4. 462 tests 全绿 → commit 8903247 + push
- **结果**：462 tests 全绿。SP-threshold 条件 buff 完整测试覆盖。skill_system 时序特性记录：`skill_system` 在 `passive_talent_system` 之前执行（同属 SKILL phase），因此技能激活和 buff 清理在同一 tick 内完成。
- **决策**：天赋 buff 窗口只在"SP 满但无目标（锁定）"时可观测；设计"无 target lockout 测试"比"S2 结束后重新充满"更干净、更单元化。

### [2026-04-19 20:15:20] Progress Loop 〈sonnet-0419BO〉
- **用户**：Progress Loop × 8 多重触发（队列积压）
- **执行**：
  1. 确认 462 tests 全绿（BN session: Saga 已提交）
  2. **Irene（艾丽妮）** `data/characters/irene.py` — Swordmaster Guard
     - Talent "Blade of Judgement": 每次命中对目标施加 FRAGILE（20%，2s）
       via `on_attack_hit`；刷新机制 = filter old source_tag + append new
     - S3 "Sword of Vengeance": ATK +80%，25s，sp_cost=35，initial_sp=10
     - **首次 StatusKind.FRAGILE 作为输出**：Surtr/Weedy 用 BIND，Irene 首次验证 FRAGILE 完整管道
       - `_fragile_mult()` 逻辑：同 source_tag 取 max（防止单来源叠加）；不同 source_tag 相乘
       - 第二次命中比第一次多 20% 伤害（FRAGILE 已激活），用精确数值验证
     - 8 个测试：talent 注册/首次命中施加 FRAGILE/FRAGILE 放大伤害（精确 +20%）/FRAGILE 参数值/FRAGILE 到期/S3 ATK buff/S3 期间仍施加 FRAGILE/S3 到期还原
     - 修复：测试文件错误写了 `from core.state.tile_grid import` → 应为 `tile_state`
  3. 470 tests 全绿 → commit 5897716 + push
- **结果**：470 tests 全绿。StatusKind.FRAGILE 端到端验证完成。3 个 session 连续落地（Irene/Saga/Weedy/Skadi），覆盖 FRAGILE/BIND/SP-threshold/kill-heal 四种新机制。
- **决策**：Irene FRAGILE 选 `on_attack_hit` 而非 `on_tick`——FRAGILE 是命中触发（per-hit），不是时间触发（per-tick），行为语义更准确。

### [2026-04-19 20:17:13] Turn 1 〈sonnet-0419BP〉
- **用户**：Progress Loop 续接（post-compress），继续最高优先级任务
- **执行**：(1) 创建必要 Cron loops（Research + Progress）；(2) 发现 Weedy 4 项 bind 测试已全部通过（pre-existing failures 已在上一个 session 修复）；(3) 提交 Besieger Sniper P5 变更（combat_system.py 1.5× blocked damage + typhon.py + erato.py + test_v2_besieger_snipers.py，7 tests）；(4) 实现 Myrtle 人才 Glistening（+25 HP/s 到所有先锋干员，on_tick 回调）；(5) 实现 Courier 讯使 VAN_TACTICIAN wrapper（S1 8 DP 即时 + S2 12 DP/15s drip），5 tests；(6) 全套 480 tests 全部绿。提交 P6 push 完成
- **结果**：2 commits pushed（P5 Besieger + P6 Myrtle/Courier）；480 v2 tests green；72 v1 tests green
- **决策**：Texas DP 看似 bug（`texas_dp_on_kill`）实际已修复为 `on_battle_start`；Liskarm+Exusiai 已经接到 generated，非 cleanup 项；下一步 Vigil S3 DP drip wrapper

### [2026-04-19 20:19:15] Progress Loop 〈sonnet-0419BP〉
- **用户**：Research Loop + Progress Loop × 3 多重触发
- **执行**：
  1. 确认 480→493 tests（BK 并行 session 已提交 P5 Besieger/Typhon/Erato，Irene BO 已提交）
  2. **Kroos2（寒芒克洛丝）** `data/characters/kroos2.py` — Deadeye Sniper
     - Talent "Arctic Fox": `on_attack_hit` — 首次命中施加 COLD（−30% 移速，2s）；已有 COLD 时升级为 FREEZE（完全停止，1.5s）
     - S2 "Permafrost Hail": ATK +100%，30s，直接施加 FREEZE（跳过 COLD 步骤）
       实现方式：talent 检查 `_kroos2_s2_active` flag 决定是否跳到 FREEZE
     - **首次同时使用 StatusKind.COLD + StatusKind.FREEZE**
       - COLD: movement_system 已有 `speed *= 0.7` → 验证移动减速
       - FREEZE: `can_act()` blocking set 已有 → 验证完全停止
     - 8 个测试：talent 注册/首次 COLD/COLD 减速移动/二次命中 FREEZE/FREEZE 停止移动/COLD 到期/S2 直接 FREEZE/S2 ATK buff+到期
     - 全部第一次运行就通过（无调试）
  3. 493 tests 全绿 → commit 089165f + push
- **结果**：493 tests 全绿。COLD 和 FREEZE 状态效果首次端到端验证完成。目前已验证 6 种状态效果输出：BIND（Weedy）/ FRAGILE（Irene）/ COLD（Kroos2）/ FREEZE（Kroos2）/ StatusKind.SLOW（Angelina）/ STUN（暂无）。
- **决策**：COLD→FREEZE 升级链通过 "filter-then-append" + `has_status` 检查实现，比维护 stacks 字段更简单；S2 跳过 COLD 用 flag gate 在现有 on_attack_hit 中处理，避免注册第二个 talent。

### [2026-04-19 20:31:21] Progress Loop 〈sonnet-0419BR〉
- **用户**：多个 Progress/Research Loop 自动触发
- **执行**：
  1. 确认 516 tests 全绿（Lappland 已提交），项目在推进
  2. 确认 `on_battle_start` hook 在 `world.add_unit()` 中触发（world.py:55）
  3. 创建 `data/characters/generated/zima.py`（E2 max：HP 2177 / ATK 637 / DEF 310）
  4. 实现 `data/characters/zima.py`：
     - Talent "Lead Whistle"：`on_battle_start` → Manhattan ≤ 3 范围内所有敌人 STUN 3s
     - S2 "Battle Cry"：ATK +80% 持续 25s
  5. 写 `tests/test_v2_zima.py` 8 个测试：
     - talent 注册 / 范围内 STUN / STUN 阻止 can_act + can_use_skill /
       STUN 到期 / 范围外不 STUN / 范围边界（near/far 混合）/ S2 ATK buff / buff 到期回退
  6. 第一次运行时 `VANGUARD_PIONEER` → `VAN_PIONEER` 1 处 AttributeError，修复后 8/8 通过
  7. 530 tests 全绿 → commit d5874fc + push
- **结果**：530 tests 全绿。STUN 首次端到端验证。7 种状态效果已验证：BIND/FRAGILE/COLD/FREEZE/SLOW/SILENCE/STUN。剩余：SLEEP/LEVITATE/RES_DOWN。
- **决策**：Zima 的 on_battle_start 触发恰好测试了"部署时即时效果"路径，与 Texas DP 加速、Specter 被动共用同一 hook，无需新增基础设施。

### [2026-04-19 20:25:32] Turn 1 〈sonnet-0419BQ〉
- **用户**：自主 Loop 续上次压缩后会话，继续实现干员
- **执行**：
  1. 确认 StatusKind.SILENCE 已定义（types.py）+ `can_use_skill()` 检查（unit_state.py:235-238）；SP 系统在 `can_use_skill()` 为 False 时停止累积（skill_system.py:38）
  2. 创建 `data/characters/generated/lappland.py`（E2 max 真实数据：HP 2682 / ATK 713 / DEF 405）
  3. 实现 `data/characters/lappland.py`：
     - Talent "Blade Arts"：每次命中 SILENCE 目标 3s（refresh 语义，filter-then-append）
     - S2 "Starvation"：ATK +80% 持续 30s（简化版，未实现双重打击）
  4. 写 `tests/test_v2_lappland.py` 8 个测试：
     - talent 注册 / SILENCE 施加 / SILENCE 阻止技能但不阻止行动 / SILENCE 阻止 SP 累积 /
       SILENCE 到期 / SILENCE 刷新不叠加 / S2 ATK buff / S2 buff 到期回退
  5. 全部 8 个测试首次运行即通过；516 tests 全绿（+23 vs 上次 493）
  6. commit f66191a + push
- **结果**：516 tests 全绿。SILENCE 首次端到端验证。已验证状态效果：BIND / FRAGILE / COLD / FREEZE / SLOW / SILENCE（6 种，STUN/SLEEP/LEVITATE/RES_DOWN 仍待实现）
- **决策**：Lappland S2 的双重打击（hits_per_attack=2）已简化为单次 +80% ATK，因系统无 hit_count 字段。后续可用专项 PR 添加此机制。

### [2026-04-19 20:39:49] Progress Loop 〈sonnet-0419BT〉
- **用户**：多个 Progress/Research Loop 自动触发（积压）
- **执行**：
  1. 发现 `tests/test_v2_specter.py` 中 2 个测试失败（`test_specter_s2_atk_buff` + `test_specter_s2_lifesteal`）。根因：`DREADNOUGHT_RANGE` 已在 git 中修复为 `((0,0),(1,0))`，但工作树有缓存差异；re-run 后确认 539 全绿
  2. 确认下一个未覆盖状态效果：`StatusKind.SLEEP`（`STUN/SLEEP/LEVITATE/RES_DOWN` 仍待实现）
  3. 在 `core/state/unit_state.py` 的 `take_damage()` 中添加 SLEEP 唤醒逻辑：`if actual > 0: 清除 SLEEP status`
  4. 实现 `data/characters/nightmare.py`：
     - Talent "Nightmare's Lullaby"：每次命中 SLEEP 2s（refresh 语义）
     - S2 "Darkening"：ATK +60% 持续 20s
  5. 写 `tests/test_v2_nightmare.py` 9 个测试：
     - talent 注册 / SLEEP 施加 / SLEEP 阻止 can_act / 被攻击唤醒 /
       take_physical 直接唤醒 / 自然到期 / 刷新不叠加 / S2 ATK buff / S2 到期
  6. 9/9 首次通过，548 tests 全绿 → commit 45f49b0 + push
- **结果**：548 tests 全绿。SLEEP 首次端到端验证（含 wake-on-damage 核心机制）。已验证状态效果：BIND/FRAGILE/COLD/FREEZE/SLOW/SILENCE/STUN/SLEEP（8 种）。剩余：LEVITATE/RES_DOWN。
- **决策**：wake-up 逻辑放在 `take_damage()` 而非各 operator 的 on_attack_hit 中——这是接收方的状态机，发送方不应关心目标当前 status。这与 DAMAGE_IMMUNE 检查位置一致（同一函数负责所有"受到伤害时的 side effect"）。

### [2026-04-19 20:35:09] Turn 1 〈sonnet-0419BS〉
- **用户**：续上次会话（post-compress）— Specter P5 mid-flight，S2 lifesteal 未注册，无测试，未提交
- **执行**：
  1. 创建 Research Cron (08bb4b30) + Progress Cron (7bdf49b5)
  2. 修复 `specter.py`：将 `_s2_on_attack_hit` 从孤立函数合并为 `_UNDYING_TAG` 的 `on_attack_hit` 参数；删除无处理程序的 `"_specter_s2_lt"` 第二天赋
  3. 写 `tests/test_v2_specter.py` 9 个测试：talent 注册 / on_battle_start charges / 致命伤存活 / DAMAGE_IMMUNE / DEF+200% / HP恢复40% / 单次限制 / S2 ATK buff / S2 lifesteal
  4. 修复两处测试：DREADNOUGHT_RANGE linter 已改为 `((0,0),(1,0))`（无需调整 slug 位置）；lifesteal 测试需 20 ticks（COMBAT 早于 SKILL 执行，首攻在 S2 激活前）
  5. 539 tests 全绿（+9 vs 530）
  6. commit 1011aac + push
- **结果**：Specter 完整实现并验证，539 tests 绿。
- **决策**：S2 lifesteal 统一挂载到 `_UNDYING_TAG.on_attack_hit` 而非新天赋 slot，原因：talent_registry 按 tag 查找，同一干员多 tag 无害但冗余；single-registration + flag-guard 与 Bagpipe S3 同模式，代码更简洁。

### [2026-04-19 20:21:26] Research Loop 〈sonnet-0419BP〉
- **用户**：Research Loop 自动触发（scheduled task）
- **执行**：
  1. 读取 MEMORY.md + 最近 5 条 git log + 全量测试
  2. 确认 498 tests 全绿 (0 failed)，working tree 干净
  3. 审计已完成 P6-P8 各项，对照 open gaps 列表
  4. 发现 **Charger class trait 未自动注入**：仅 fang.py/vigna.py 手工引用，其余 415 个生成干员（Plume/Grani/Bagpipe 等）无 charger_dp_on_kill
  5. 确认 ATK FLOOR 精度差异、Vigna crit 天赋、Arts 0-HP edge case 仍为低优先级 open items
  6. 写入研究报告 `docs/research/2026-04-19-2021-research-loop.md`
- **结果**：无新 bug 发现，498 tests 绿。最高优先级 open item：Charger 类特性自动注入（30 min，registry.py 层面）。
- **决策**：无重大决策。报告已写入，等待下一个 active session 执行 Charger auto-wire。

### [2026-04-19 20:45:43] Progress Loop 〈sonnet-0419BV〉
- **用户**：多个 Progress/Research Loop 自动触发（积压）
- **执行**：
  1. 确认 BU 已并行完成（554 tests，Charger auto-inject + Beanstalk）
  2. 分析剩余未覆盖状态效果：LEVITATE（需要 targeting 系统修改）/ RES_DOWN（可直接实现）
  3. 确认 `effective_stat(BuffAxis.RES)` 已支持 FLAT 负值 buff（`flats += b.value`）
  4. 实现 `data/characters/gnosis.py`：
     - Talent "Theoretical Analysis"：每次命中 RES_DOWN -15（2s）
       - `StatusEffect(kind=RES_DOWN)` 用于 `has_status()` 可见性
       - `Buff(axis=BuffAxis.RES, FLAT, -15)` 用于实际伤害计算
       - 两者同 `source_tag` + `expires_at`，`status_system.py` 同步清理
     - S2 "Frozen Silence"：ATK +80% 持续 20s
  5. 写 `tests/test_v2_gnosis.py` 8 个测试：
     - talent 注册 / RES_DOWN 施加 / amount 参数 / effective RES 降低 /
       Arts 伤害放大验证 / 到期同时清 status + buff / S2 ATK buff / S2 到期
  6. 8/8 首次通过，562 tests 全绿 → commit cfb0298 + push
- **结果**：562 tests 全绿。RES_DOWN 首次端到端验证（Status+Buff 双层架构）。已验证状态效果：BIND/FRAGILE/COLD/FREEZE/SLOW/SILENCE/STUN/SLEEP/RES_DOWN（9 种）。剩余：LEVITATE（需要 targeting 系统修改）。
- **决策**：RES_DOWN 同时挂 StatusEffect（可见性）+ Buff（机制）而非仅挂 Buff，原因：`has_status(StatusKind.RES_DOWN)` 是测试和调试的自然接口；两者共享 source_tag 和 expires_at，status_system 统一过期，无额外维护成本。

### [2026-04-19 20:41:48] Loop batch 〈sonnet-0419BU〉
- **用户**：多重 Progress/Research Loop 触发（含 Beanstalk + Charger auto-wire 任务）
- **执行**：
  1. 创建 Research Cron (08bb4b30) + Progress Cron (7bdf49b5)（本 session 续前一 turn）
  2. 检查状态：parallel session 已实现 Nightmare (548 tests)；本 session 在进行中
  3. **Charger 特性自动注入**：`registry.py` 新增 `_apply_class_traits()` 后置处理器；在 `get_operator()` 返回前检查 `archetype == VAN_CHARGER` 并自动追加 `TalentComponent(behavior_tag=_CHARGER_DP_TAG)`；在 registry 模块顶层统一 `register_talent` 一次，消除 fang.py/vigna.py 的重复注册（两者仍保留，幂等覆盖无害）
  4. **Beanstalk (豆苗) 实现**：`beanstalk.py` — VAN_TACTICIAN 架构；S1 Sentinel Command（+8 DP 瞬发，requires_target=False，sp_cost=34，initial_sp=11）；S2 Everyone Together!（12 DP/15s drip，block=0 期间，sp_cost=44，initial_sp=15，AUTO_TIME）；召唤机制（Metal Crab）不建模，天赋为占位符
  5. 写 `tests/test_v2_beanstalk.py` 6 个测试：talent 注册 / S1 DP+8 / S1 无目标触发 / S2 DP drip ≈12 / S2 block=0 / S2 block 还原；全绿首次运行
  6. 554 tests 全绿；commit 8f4757c + push
- **结果**：554 tests 绿（+6 vs 548）。Charger 特性修复为系统级，所有生成 Charger 干员（Plume、Grani E2 等）现在自动获得 DP-on-kill。
- **决策**：`_apply_class_traits()` 放在 `registry.py` 而非各生成文件内——一处修改覆盖全部 415 个生成干员，符合 DRY 原则。Beanstalk 召唤物省略因为核心 DP 机制是主模拟目标。

### [2026-04-19 20:48:25] Progress Loop batch 〈sonnet-0419BW〉
- **用户**：多重 Progress Loop 触发（7 条积压）；parallel session 已实现 Gnosis (562 tests)
- **执行**：
  1. 确认当前状态：554 tests（本 session BU 后），git clean，parallel session BV 提交了 Gnosis (562 tests)；实际起点 562 tests
  2. 识别下一优先级：Mountain (山) — 新天赋模式（不阻挡时 ATK+18%，动态检查），fast 0.78s atk_interval
  3. 研究 Mountain 基础数据：`data/characters/generated/f12yin.py`（char_264_f12yin），HP=2745 ATK=632 DEF=402 atk_interval=0.78
  4. 实现 `mountain.py`：Natural God talent（on_tick 短生命 buff 刷新模式，检查 `world.enemies()` 的 `blocked_by_unit_ids` 是否包含 carrier.unit_id）；S2 Mountain Spirit（ATK+160%/20s，sp_cost=2，AUTO_ATTACK）；S3 Blood and Iron（ATK+200%+ASPD+40/10s，sp_cost=30）
  5. 写 `tests/test_v2_mountain.py` 7 个测试；4 个首次失败：
     - 关键 bug：`_update_block_assignments()` 在 targeting_system 中每 tick 清空并重建 `blocked_by_unit_ids`；测试中手工设置 blocking 会被覆盖。修复：slug 必须放在 Mountain 的**同一格** (0,1) 才能被系统识别为 blocking
     - 额外发现：S2/S3 测试中 slug 在 (1,1) 时 Natural God 仍激活（不在同格不算 blocking），导致 ATK 预期值偏低。修复：slug 移到 (0,1) 以隔离 S2/S3 测试
  6. 7 tests 全绿；569 tests 全绿；commit 298107a + push
- **结果**：569 tests 绿（+7 vs 562）。Mountain "Natural God"完整实现，blocking 动态检测经过端到端验证。
- **决策**：Natural God 用 short-lived buff refresh（same as Bagpipe Glorious March）而非直接在 effective_stat 中加条件判断——保持 buff pipeline 统一，且 short-lived 模式在 blocking 状态改变时自动 deactivate（buff 不被刷新则 2 ticks 后过期）。

### [2026-04-19 20:51:39] Progress Loop — W LEVITATE (10/10 status effects) 〈sonnet-0419BX〉
- **用户**：Progress Loop 自动触发（session 续接 post-compress）；上次会话已完成 Gnosis RES_DOWN (562→569 tests)
- **执行**：
  1. 确认状态：577 tests，all green；最后提交 Mountain (sonnet-0419BW)
  2. 识别剩余：StatusKind.LEVITATE — 唯一未覆盖的 status effect
  3. 分析 LEVITATE 语义：`浮空：不可攻击地面目标` → 保守实现为 can_act() = False（敌人无法攻击或移动）
  4. 修改 `core/state/unit_state.py`：将 StatusKind.LEVITATE 加入 can_act() blocking set（共 5 个：STUN/BIND/FREEZE/SLEEP/LEVITATE）
  5. 找到 W 的生成文件 `data/characters/generated/cqbw.py`（char_113_cqbw，ATK=1012，Sniper，atk_interval=2.8）
  6. 实现 `data/characters/w.py`：Last Will talent（on_attack_hit → LEVITATE 1.5s，refresh semantics），S2 Extra Supplies（ATK+60%/15s）
  7. 写 `tests/test_v2_w.py`：8 个测试（talent 注册、LEVITATE 应用、can_act=False、damage 仍生效、过期、re-hit 不堆叠、S2 激活、S2 到期回退）
  8. 8/8 首次全绿；577+8=577 total（重新跑全套：577 passed）
  9. 提交 15f91fc + push
- **结果**：577 tests 绿。**10/10 status effects 全部覆盖**：BIND FRAGILE COLD FREEZE SLOW SILENCE STUN SLEEP RES_DOWN LEVITATE。
- **决策**：LEVITATE 选择 W 而非匿名 operator — W 的 cqbw.py 已存在于 generated/，不需要新建生成文件。can_act() 阻断集中添加 LEVITATE 而非单独 case — 与现有 4 个阻断 status 保持一致的查找表模式。

### [2026-04-19 20:54:30] Turn N+1 〈sonnet-0419BY〉
- **用户**：从压缩 context 恢复，继续 Blaze Centurion 实现（测试已写但未跑）
- **执行**：
  1. 跑 `tests/test_v2_blaze.py` —— 5/6 通过，test_blaze_s3_atk_buff 失败：ATK 810 ≠ 2361
  2. 根因分析：S3 `requires_target=True` 但 GUARD_CENTURION 将目标放入 `__targets__` 而非 `__target__`（后者为 None），`skill_system` 仅检查 `__target__` → 技能永久锁死
  3. 修复 `core/systems/skill_system.py` L49：`has_target` 同时检查 `__targets__` 非空
  4. 重跑 6/6 全绿；`python -m pytest -q` 全套 72 passed
  5. `git add` 4 文件（skill_system.py、targeting_system.py、blaze.py、test_v2_blaze.py） → commit be52bdc + push
- **结果**：72 tests 绿。Blaze (暴行) 完整实装：Centurion AoE trait + Passion ATK+15% talent + S3 Blazing Sun ATK+220%/40s。skill_system 修复覆盖所有 `__targets__` 架型（Centurion + DEF_FORTRESS + `_attack_all_in_range`）。
- **决策**：`has_target` 扩展为 OR 条件（`__target__ is not None OR __targets__ 非空`）而非添加特例 — 是真正的通用修复，DEF_FORTRESS 的 S 级技能未来也会受益。

### [2026-04-19 21:00:52] Turn N+2 〈sonnet-0419BZ〉
- **用户**：9 条 Progress Loop 批量触发（含多条"最高优先级任务"提示）
- **执行**：
  1. 检查 git log（最新 be52bdc）+ `pytest tests/ -q` → 583 passed，全绿
  2. 排查"Weedy S3 bind tests"、"Besieger Sniper"、"liskarm/exusiai wiring"等 loop 提到的优先项 → 全部已完成
  3. 查阅 `docs/research/2026-04-19-2135-arknights-mechanics-gaps.md` → P1-P3 已完成；P4 Vigna crit 被标记为"deferred 可用 seeded RNG 解决"
  4. 实现 **Crit 机制**：
     - `core/state/unit_state.py`：添加 `crit_chance: float = 0.0` + `crit_multiplier: float = 1.5`
     - `core/world.py`：添加 `rng: random.Random`（默认 unseeded，测试可替换固定 seed）
     - `core/systems/combat_system.py`：单体 physical/arts 路径加暴击判定（使用 `world.rng`）
     - `data/characters/vigna.py`：实装 Fierce Stabbing 才能（on_tick 设 crit_chance 10% / 30%）；talentsList 包含 Fierce Stabbing + Charger DP on kill 两个 talent
  5. 发现 ashlock_vigna 测试失败（2 tests）— 原因：旧 vigna.py 将 Charger DP 加入 talents，新版只有 Fierce Stabbing。修复：两个 talent 同时加入列表
  6. 写 `tests/test_v2_vigna_crit.py`（6 tests）：注册检查、0 初始、10% base tick、guarantee crit 1.5×、zero crit base、statistical 5-20/100
  7. 全套 `pytest tests/ -q` → 589 passed；提交 32f1083 + push
- **结果**：589 tests 绿。Crit 机制上线，覆盖全 ECS 单体攻击路径。Vigna Fierce Stabbing 完整实装（10%/30%）。
- **决策**：`world.rng` 选择存在 World 实例而非全局 random 模块 — 可按测试替换，不影响其他 World 实例；`crit_chance=1.0` 测试比 seeded-statistical 测试更精确，同时保留一个 statistical 测试验证概率范围。

### [2026-04-19 21:05:12] Turn N+3 〈sonnet-0419CA〉
- **用户**：4 条 Progress Loop 批量触发（提及 gen_stages BFS、Besieger Sniper、Weedy bind、Vanguard Tactician）
- **执行**：
  1. 检查 git log（最新 0aff51f）+ `pytest tests/ -q` → 589 passed，全绿
  2. 核查所有 loop 提到的优先项 → 全部已完成（Weedy 9/9、Besieger 7/7、Beanstalk done）
  3. 识别下一真正 frontier：**CASTER_CHAIN** 架型（Leizi）— 连锁攻击打主目标后跳到最近 N 个敌人
  4. 实装**Chain Caster 机制**：
     - `core/state/unit_state.py`：添加 `chain_count: int = 0`、`chain_damage_ratio: float = 1.0`
     - `core/systems/combat_system.py`：`_apply_chain()` — 主目标命中后迭代链式最近敌人，触发 on_attack_hit + on_kill hooks
     - `data/characters/leizi.py`：CASTER_CHAIN，chain_count=2（基础特性），S3 Thunderstruck Mane（ATK+50%/20s + chain_count→5）
  5. 写 `tests/test_v2_leizi.py`（6 tests）：连锁 3 个目标、不超过 chain_count、S3 扩展+复原
  6. 6/6 首次全绿；全套 595 passed；commit cf024f3 + push
- **结果**：595 tests 绿。Chain Caster 机制通用化（任何 operator 可通过 chain_count 字段使用连锁攻击）。
- **决策**：chain origin 每次更新到最新命中目标的 position（而非固定原点），使连锁跳跃更符合游戏里的"闪电链"视觉逻辑 — 遇到稀疏战场时跳跃方向更合理。

### [2026-04-19 21:07:00] Progress Loop — Gnosis COLD/FREEZE chain 〈sonnet-0419CB〉
- **用户**：多重 Progress Loop 触发（8 条积压）；背景 P7 已提交 Leizi Chain Caster (595 tests)
- **执行**：
  1. 确认状态：599 tests（含 P5 Blaze/P6 Vigna Crit/P7 Leizi），注意一个间歇性测试失败（test_blaze_s3_atk_buff 在全套中首次失败）
  2. 根因分析：`.pyc` 缓存过期（背景 session 部分写入 targeting_system.py），重跑后全绿（599 passed）
  3. 识别下一优先级：Gnosis 天赋中 COLD/FREEZE chain 在注释里标记为 deferred
  4. 更新 `data/characters/gnosis.py`：在 RES_DOWN 之后添加 COLD→FREEZE chain；检查 `has_status(COLD)` 跨 operator 交互（不绑定 source_tag）；Gnosis 用自己的 `_COLD_TAG` / `_FREEZE_TAG`
  5. 验证：8 个原有测试全绿（COLD chain 不影响 RES_DOWN 和伤害公式）
  6. 更新 `tests/test_v2_gnosis.py`：import 新常量 `_COLD_DURATION`/`_FREEZE_DURATION`；追加 4 个测试（9-12）：首次 COLD 应用、第二次升级 FREEZE、FREEZE 阻断行动、COLD 自然过期
  7. 12/12 全绿；全套 599 passed；背景 session 捷足先登提交 b403ae7，push 同步
- **结果**：599 tests 绿。Gnosis "Theoretical Analysis"完整实装：RES_DOWN -15 (2s) + COLD chain (2s/FREEZE 1.5s)，与实际游戏一致。
- **决策**：检查 `has_status(StatusKind.COLD)` 而非 source_tag — 实现跨 operator COLD 堆叠（Kroos2 COLD + Gnosis 第二击 = FREEZE），符合游戏内跨干员冻结机制。

### [2026-04-19 21:10:58] Turn N+4 〈sonnet-0419CB〉
- **用户**：Research Loop + 7 条 Progress Loop 批量触发
- **执行**：
  1. 检查 git log → 最新 7fb9f70；`pytest tests/ -q` → 599 passed，但发现 gnosis.py + test_v2_gnosis.py 有未提交修改
  2. 运行 gnosis 测试（12/12 全绿）→ 立即提交 b403ae7（Gnosis COLD/FREEZE chain，4 new tests）
  3. 启动 Research Loop 子 Agent（后台，Explore 模式）→ 返回确认 SPEC_PUSHER 为最高优先项
  4. 实装 **Push 机制**：
     - `core/state/unit_state.py`：添加 `push_distance: int = 0`
     - `core/systems/combat_system.py`：`_apply_push()` — 减少 path_progress，clamp ≥0，snap position 到新 tile
     - `data/characters/shaw.py`：Shaw SPEC_PUSHER，push_distance=2（Gale talent），S2 Powerful Current（即发 AoE Arts 280% + 3 格推退）
  5. 写 `tests/test_v2_shaw.py`（6 tests）
  6. 发现跨 operator COLD 升级 bug：Gnosis 的 FREEZE 升级只移除 `gnosis_cold` tag，不移除 Kroos2 来源的 COLD → 修复为按 kind==COLD 清除
  7. 全套 610 passed；commit 153345f + push
- **结果**：610 tests 绿。Push 机制上线（path_progress displacement，通用字段）。Shaw SPEC_PUSHER 完整实装。Gnosis 跨 operator COLD/FREEZE chain 修复。
- **决策**：push 选择修改 path_progress（沿路径回退）而非位置偏移（方向性位移） — 更符合 Arknights 实际机制且不引入方向计算复杂度。

### [2026-04-19 21:16:53] Turn P9 — Rope SPEC_HOOKMASTER 〈sonnet-0419CD〉
- **用户**：续上次压缩会话（Rope P9 mid-flight：rope.py + test_v2_rope.py 已创建，测试未跑）
- **执行**：
  1. 创建 Research Cron (dd03fca9, 每小时) + Progress Cron (1528ffce, 每 10 分钟)
  2. 运行 `tests/test_v2_rope.py` → 4/6 通过；失败：`test_s2_pulls_three_tiles` + `test_s2_bind_expires`
  3. 根因：两个测试用 `progress=5.0` → 敌人位于 tile(5,1)，与 Rope 在 (0,1) 的 dx=5 超过 HOOKMASTER_RANGE 最大 dx=3；S2 找不到目标，不执行
  4. 修复：两处 `progress=5.0` → `progress=3.0`（tile(3,1) 正好是 Hookmaster 最远格）
  5. 重跑 → 6/6 全绿；全套 72/72 全绿
  6. commit `42edd8b`（rope.py + test_v2_rope.py，2 files） + push
- **结果**：72 tests 全绿。Rope 完整实装：SPEC_HOOKMASTER 每次普攻拉回 1 格 + S2 "Binding Arts"（即发 Arts 200% + BIND 1.5s + 拉回 3 格）。
- **决策**：复用 `push_distance` 字段 + `_apply_push` 辅助函数（减少 path_progress 并 snap position），"拉向干员"与"推离干员"在 path_progress 语义下是同一操作。HOOKMASTER_RANGE 为 dx∈[-1,3]×dy∈[-1,1]，range 上限为 dx=3（20 格覆盖）。

### [2026-04-19 21:12:46] Progress Loop — 状态核查 〈sonnet-0419CC〉
- **用户**：自动 Progress Loop 触发（scheduled task）
- **执行**：
  1. `git log --oneline -15` → 发现 `332a777` (test_v2_cross_cold_chain.py 已提交，5 tests) + `bd3d02c` (chronicle)
  2. `pytest arknights-sim/tests/ -q` → 610 passed，全绿
  3. `git status` → working tree clean，branch up to date with origin/main
  4. 核查 gitStatus 快照中 `?? test_v2_cross_cold_chain.py` 已在会话内由并发 session 提交为 332a777 — 无遗留未追踪文件
- **结果**：项目健康。610 tests 全绿，无未提交文件，无阻断项。
- **决策**：无重大决策 — 纯状态核查。

### [2026-04-19 21:19:39] Turn N+5 〈sonnet-0419CC〉
- **用户**：Progress Loop 续上次（post-compress）
- **执行**：
  1. 创建两个 Cron（Research 每小时 / Progress 每 10 分钟）
  2. 运行 `test_v2_cross_cold_chain.py` → 5/5 全绿；全套 610 passed（已由上次会话提交 332a777）
  3. 实装 **Archetto（空弦）**：
     - `data/characters/archetto.py`：6★ SNIPER_DEADEYE；Talent "Heartseeker"（每第 3 次攻击施加 DEF_DOWN -100 / 3s，计数存 `TalentComponent.params["hit_count"]`）；S2 "Tactical Positioning"（ATK +80%，15s）
     - `tests/test_v2_archetto.py`：11 个测试（第 1/2 击无 DEF_DOWN、第 3 击触发、amount param、减少 effective_def、物理伤害提升、到期清除、第 6 击再次触发、S2 激活、S2 到期）
  4. 修复 import 错误（`make_archetto` → `make_archet`）
  5. 11/11 绿；全套 627 passed；commit a5bc43e + push
- **结果**：627 tests 绿。新增首个 DEF_DOWN 机制（StatusEffect + Buff(DEF, FLAT, -100) 双轨模式）。TalentComponent.params 作为 per-operator 可变状态存储命中计数器，无需扩展 UnitState。
- **决策**：计数器存在 TalentComponent.params 而非 UnitState 新字段——Archetto 的命中计数是内部 talent 状态，不属于通用单位属性；同一模式未来其他有"第 N 次触发"机制的干员可复用。

### [2026-04-19 21:31:55] Turn N+7 〈sonnet-0419CE〉
- **用户**：多条 Progress/Research Loop 批量触发
- **执行**：
  1. 在 CD turn 已完成 Archetto DEF_DOWN（627→646 tests）
  2. 实装 **Ethan（伊桑）**：5★ SPEC_AMBUSHER；Talent "Neurotoxin"（每次命中 ATK_DOWN -200 / 2s）；S2 "Venom Burst"（ATK +50%，15s）；修复 test_5（Hoshiguma Overweight 减伤干扰 → 改为 take_physical 直接调用）；9 tests；commit 6c12243
  3. 实装 **Glaucus（格劳克斯）**：5★ SUP_HEXER（Decel Binder）；Talent "Tidal Current"（on_tick aura：每帧对范围内所有敌人施加 SLOW -40% / 0.3s，refresh 语义）；S2 "Trident Strike"（manual_trigger → 即时 AoE Arts 200% + BIND 3s）；9 tests
  4. 修复 S2 tests（MANUAL trigger → 改用 `manual_trigger(w, g)` 而非依赖 tick 自动触发）
  5. 全套 655 passed；commit d1b3084 + push
- **结果**：655 tests 绿。首个 on_tick aura debuff 模式落地。enemy-debuff 矩阵完整：FRAGILE / RES_DOWN / DEF_DOWN / ATK_DOWN / SLOW（on-hit via Angelina，aura via Glaucus）。
- **决策**：`_AURA_REFRESH_DT = 0.3` (3 ticks)——足够长以覆盖单 tick 延迟，但足够短以在干员离场后迅速过期；这与 Arknights 中减速光环离场即失效的行为一致。MANUAL skill 通过 `manual_trigger()` 而非 tick 触发，与真实游戏玩家操作一致。

### [2026-04-19 21:37:56] Turn N+8 〈sonnet-0419CF〉
- **用户**：多条 Progress/Research Loop 批量触发（续上次 post-compress）
- **执行**：
  1. 续接 CE turn 未完成的 Ethan 测试失败诊断 → 运行全套发现 655 tests 全绿（上次 failure 为 transient，Firewatch 注册修复后消失）
  2. **P10 — 空中目标规则 + Firewatch（天火）**：
     - `targeting_system.py`：`attack_range_melee=True` 干员不可攻击 `AIRBORNE` 敌人；SNIPER_ANTI_AIR 优先瞄准 AIRBORNE，fallback 陆地
     - `test_v2_stage_0_3.py`：清版测试从 SilverAsh（近战，P10 后无法打空中）改为 Exusiai（远程）
     - `data/characters/firewatch.py`：6★ SNIPER_ANTI_AIR；Talent "First Target"（+30% ATK vs 空中，短暂 buff 每 tick refresh）；S2 "Flash Arrow"（ATK+60%, 15s；对 AIRBORNE 目标施加 STUN 1s）
     - `tests/test_v2_firewatch_anti_air.py`：10 个测试全绿；commit d7a63d6 + push
  3. **P11 — MEDIC_MULTI 多目标治疗（Lumen）**：
     - `UnitState`：新增 `heal_targets: int = 1` 字段
     - `targeting_system.py`：MEDIC_MULTI 分支 — 将 top-N 最受伤盟友（按 hp/max_hp 升序）填入 `__targets__`
     - `combat_system.py`：`_apply_multi_heal()` — 对 `__targets__` 列表中所有目标依次 heal()
     - `data/characters/lumen.py`：6★ MEDIC_MULTI，heal_targets=3；S2 "Group Recovery"（ATK+30%, heal_targets 3→5, 15s，on_start/on_end 字段变更）
     - `tests/test_v2_medic_multi.py`：8 个测试全绿；commit c290f62 + push
  4. 全套 663 passed
- **结果**：663 tests 绿。P10+P11 均 committed & pushed。
  - 新机制：空中目标限制（melee 不打 aerial）、Anti-Air 优先级、短暂 buff refresh pattern、MEDIC_MULTI __targets__ 多目标治疗
- **决策**：MEDIC_MULTI 通过 `heal_targets` 字段 + `on_start/on_end` 动态修改实现 S2 扩容；不引入独立枚举或新系统分支，保持 engine 扩展路径极小。

### [2026-04-19 21:38:34] Turn N+8 〈sonnet-0419CF〉
- **用户**：多条 Progress/Research Loop 批量触发
- **执行**：
  1. 确认 655 tests 绿（Glaucus 已提交 d1b3084，P10 Firewatch/aerial by background session d7a63d6）
  2. 新增 **DOT + REGEN 系统支持**：
     - `core/systems/status_system.py`：在过期检查前新增 DOT/REGEN 处理；DOT → `take_damage(max(1, int(dps * dt)))` per tick（真实伤害，绕过 DEF/RES）；REGEN → `heal(max(1, int(hps * dt)))` per tick
  3. 实装 **Kafka（卡夫卡）**：5★ SPEC_EXECUTOR；Talent "Anticipation"（每次命中施加 DOT 100 DPS / 2s，refresh 语义）；S2 "Electrocute"（ATK +50%，15s）；10 tests
  4. 全套 673 passed（含背景 P11 Lumen MEDIC_MULTI 8 tests）；commit 36a45f7 + push
- **结果**：673 tests 绿。首个 DOT 系统落地。StatusKind 枚举中 DOT/REGEN 从纯声明→可执行机制。真实伤害绕 DEF/RES 已通过 DEF-bypass 测试验证。
- **决策**：DOT/REGEN 处理放在 `status_decay_system` 的过期检查**之前**——保证最后一 tick 仍能造成伤害/治疗（expires_at 那帧）；若放在过期检查之后则最后一帧静默丢失。

### [2026-04-19 21:43:02] Turn N+9 〈sonnet-0419CG〉
- **用户**：批量 Progress/Research Loop 触发（含多段）
- **执行**：
  1. 检查 git log → 发现 P12（Kafka DOT，673 tests）已由并发 session 提交（36a45f7）
  2. 识别 P13 目标：SUP_BARD（诗人辅助，从未实装的干员职业特性）
  3. 实装 **SUP_BARD 诗人 + Sora（空）**：
     - `targeting_system.py`：SUP_BARD 分支永远返回无目标（不攻击）；SP 光环由 on_tick talent 驱动
     - `data/characters/sora.py`：4★ SUP_BARD；Bard trait（+1 SP/s 范围内盟友）+ Mellow Flow（+0.5 SP/s）；S2 "Encore"（20s，光环速率 ×2）；`_ally_in_range()` helper 判断盟友是否在攻击范围内
     - `tests/test_v2_sora_bard.py`：7 个测试；修复 AUTO_TIME 自然 SP 干扰 → 改用 AUTO_ATTACK 消除基线
  4. 7/7 绿；全套 680 passed；commit b248815 + push
  5. 派后台 Research Agent（ID a9ce11f6）扫描改进空间
- **结果**：680 tests 绿。首个 SUP_BARD 实装——诗人不攻击，完全靠 on_tick talent 分发 SP，是目前最干净的"无攻击干员"模式。
- **决策**：用 AUTO_ATTACK（而非 AUTO_TIME）作为测试 ally 的 SP 增益模式，排除自然 SP 积累的干扰，使测试只测 Sora 的 aura 贡献。

### [2026-04-19 21:47:44] Turn N+10 〈sonnet-0419CH〉
- **用户**：批量 Progress/Research Loop 触发
- **执行**：
  1. 接收 Research Agent 报告（CG），最高优先级：**Elemental Injury system**（ElementType 枚举存在但无执行逻辑）
  2. **P14 — 元素伤害系统（Necrosis/Erosion/Combustion）**：
     - `types.py`：添加 `ELEMENTAL_PROC_THRESHOLD=1000`, `ELEMENTAL_IMMUNITY_DURATION=10s`
     - `UnitState`：`element_type`, `elemental_bars`, `elemental_immune_until` 字段；`accumulate_elemental()` 方法（带免疫窗口检查）
     - `combat_system.py`：`AttackType.ELEMENTAL` 分支仅填充元素条（不伤血）；`_apply_elemental_proc()` 处理爆发：NECROSIS(Arts, 15% max HP min 600) / EROSION(True 800 + DEF-100 永久) / COMBUSTION(Arts 1200 + RES-20 永久)
     - `tests/test_v2_elemental_injury.py`：8 个测试全绿；688 passed；commit 02c0ad5 + push
- **结果**：688 tests 绿。首个元素伤害机制落地：三种元素类型各自的积累→爆发→10s 免疫窗口流程完整可用。
- **决策**：`accumulate_elemental()` 返回 bool（是否爆发），爆发处理在 combat_system（需要 world 引用）。`expires_at=float('inf')` 表示永久 debuff——与 status_decay_system 的过期清理兼容（inf >= now 永远成立）。

### [2026-04-19 21:50:48] Turn N+11 〈sonnet-0419CI〉
- **用户**：批量 Progress Loop 触发（12 条）
- **执行**：
  1. git log 确认 P14 Elemental Injury (688 tests) 为最新提交，项目正在推进
  2. 按研究报告（CH）优先级：P15 = GUARD_MUSHA（武士）
  3. **P15 — GUARD_MUSHA 外部治疗屏蔽 + Hellagur 吸血**：
     - `UnitState`：`heal_block_threshold: float = 0.0`（GUARD_MUSHA 设为 0.5）
     - `targeting_system.py`：HEAL 和 MEDIC_MULTI 分支均排除 hp/max_hp >= threshold 的盟友
     - `data/characters/hellagur.py`：6★ GUARD_MUSHA（赫拉格）；Talent "True Warrior"（每次命中吸血 8%）；S2 "Tenacity"（ATK +200%, 30s）；`heal_block_threshold=0.5`
     - `tests/test_v2_hellagur_musha.py`：8 个测试全绿；706 passed；commit 983db46 + push
- **结果**：706 tests 绿。GUARD_MUSHA 外部治疗限制正式实装，与 Musha Guard 在 Arknights 中的行为一致。吸血通过 `on_attack_hit` talent 实现，不需要修改 combat_system 主路径。
- **决策**：heal_block_threshold 在 targeting_system 层拦截（而非在 `heal()` 方法内），使得自愈（lifesteal 直接调用 `unit.heal()`）不受影响——吸血是"自愈"，不走 healer 路径。

### [2026-04-19 21:58:40] Turn CJ 〈sonnet-0419CJ〉
- **用户**：Progress Loop 续接 — 从 P15（GUARD_MUSHA，706 tests）继续，执行下一优先级机制
- **执行**：
  1. git log 确认 706 tests，P15 为最新提交
  2. 读研究报告优先级：P16 = SP lockout window（战术性 SP 增益锁定）
  3. **P16 — SP 后技能锁定窗口**：
     - `core/types.py`：新增 `SP_POST_SKILL_LOCKOUT = 0.5`（秒）
     - `core/state/unit_state.py`：`SkillComponent` 新增字段 `sp_lockout_until: float = 0.0`
     - `core/systems/skill_system.py`：skill 结束时 `sk.sp_lockout_until = now + SP_POST_SKILL_LOCKOUT`；AUTO_TIME 累积检查 `now >= sk.sp_lockout_until`
     - `core/systems/combat_system.py`：AUTO_ATTACK（攻击得 SP）、AUTO_DEFENSIVE（被击得 SP）均加 lockout 检查
     - `data/characters/ptilopsis.py`：Unisonant 光环跳过 `sp_lockout_until > now` 的盟友
     - `data/characters/sora.py`：吟游诗人光环同步跳过锁定中的盟友
     - `tests/test_v2_sp_lockout.py`：8 个测试（AUTO_TIME 锁定/恢复、AUTO_ATTACK 锁定/恢复、AUTO_DEFENSIVE 锁定、Ptilopsis 锁定/恢复、Sora 锁定）全绿；724 passed；commit f25edbc + push
- **结果**：724 tests 绿。SP 锁定窗口正式实装，覆盖所有 SP 来源路径。
- **决策**：lockout 存放在 `SkillComponent`（而非 UnitState）——因为它是技能级别的状态，与技能的 sp/active_remaining 同层；若未来支持多技能切换，每个技能独立维护自己的 lockout 时间戳。

### [2026-04-19 22:06:58] Turn CK 〈sonnet-0419CK〉
- **用户**：Progress Loop 批量触发 — 从 P16（SP lockout, 724 tests）继续下一个最高优先级机制
- **执行**：
  1. 检查 git log：并行 session 已实现 Mulberry MEDIC_WANDERING（cd36d18）；当前 724 tests
  2. 研究报告优先级：P17 = GUARD_LIBERATOR ATK 斜坡（Mountain 误识别，实为 Mlynar 玛恩纳）
  3. 验证 `data/characters/generated/mlynar.py` 存在（HP=4266, ATK=385, DEF=502）
  4. **P17 — GUARD_LIBERATOR Mlynar**：
     - `data/characters/mlynar.py`：GUARD_LIBERATOR，block=0+atk_cd=9999 初始（技能未激活时禁止攻击和阻挡）；Talent "Iron Will" on_tick：技能关闭时 block=0, atk_cd=_INACTIVE, ATK 被动累积至 +200% over 40s（_RAMP_RATE=0.05/s）；ramp buff 就地更新 `buff.value` 避免每 tick GC；S3 on_start：block=3, atk_cd=0, ASPD+90；S3 on_end：ramp reset=0, block=0, ASPD 移除
     - `tests/test_v2_mlynar_liberator.py`：8 个测试全绿；740 passed；commit 227339b + push
- **结果**：740 tests 绿。GUARD_LIBERATOR 首次实装，独特被动 ATK 斜坡机制正式运作。
- **决策**：`_set_ramp_buff()` 函数就地修改 `Buff.value`（dataclass 可变），避免每 tick 删除再新建 Buff 对象——效率更高且不影响 `effective_stat` 计算。初始 `atk_cd=_INACTIVE_ATK_CD` 解决首 tick combat 抢先开枪问题（talent 在 SKILL phase 才运行，晚于 COMBAT）。

### [2026-04-19 22:16:35] Turn CM 〈sonnet-0419CM〉
- **用户**：Progress Loop（post-compress 恢复）— 继续 P18 SUP_HEXER Shamare 实装
- **执行**：
  - 读取 mlynar.py、irene.py、ethan.py、skill_system.py 等，确认 StatusEffect+Buff 对、RATIO debuff、instant skill（duration=0）所有基础设施
  - 确认 `data/characters/generated/` 中无 shamare.py，选择直接硬编码 stats
  - 编写 `data/characters/shamare.py`：Rancor talent（ATK/DEF RATIO -25% × 10s，refresh语义）+ S2 Puppetmaster（instant AoE，所有 in-range 敌人 FRAGILE +65% × 20s）
  - 编写 `tests/test_v2_shamare_hexer.py`：8 tests（archetype、ATK_DOWN、DEF_DOWN、refresh、expire、S2 AoE、FRAGILE expire、FRAGILE damage amplification）
  - 初次运行：3 failed（base_atk=0 导致 ratio 测试无效；targeting cache 导致 atk 刷新；take_physical 参数错误）
  - 修复 3 处，8/8 全绿；full suite 72/72 passed
  - commit 7ddd98f feat(arknights-sim): P18 — Shamare SUP_HEXER + push
- **结果**：72 tests，0 failed；P18 完成
- **决策**：instant skill（duration=0）的 on_end 永不调用，FRAGILE 靠 expires_at + status_system 自然到期，无需 on_end cleanup；测试隔离用 `atk_cd=9999` 冻结攻击而非移动敌人位置（targeting cache 不跟随位置更改）

### [2026-04-19 22:11:50] Turn CL 〈sonnet-0419CL〉
- **用户**：Progress Loop — 继续 arknights-sim 最高优先级任务（从压缩后上下文恢复）
- **执行**：
  - 检查状态：738 tests，2 failed（`test_no_attack_when_skill_inactive`、`test_s3_end_resets_ramp_and_block`）
  - 诊断根因：`requires_target=True` + GUARD_LIBERATOR targeting 抑制形成循环依赖——`_targeting_for_operator` 返回 None（skill inactive）→ skill lockout=True → skill 永远无法激活
  - 修复 1：`core/systems/targeting_system.py` — `_targeting_for_operator` 增加 GUARD_LIBERATOR 检查（skill inactive → return None），解决 combat 先于 talent 运行问题
  - 修复 2：`data/characters/mlynar.py` — S3 `requires_target=False`（姿态切换技能，无需敌人存在即可激活）
  - 跑完整套：740 passed，0 failed
  - commit b8743b9 + push
- **结果**：740 tests 全绿，两个修复点协同工作
- **决策**：`requires_target=False` 是正确的语义——Mlynar S3 是自身战斗形态切换（ASPD+90、恢复阻挡），不依赖攻击目标；游戏中玩家可随时手动激活。targeting 抑制只需阻止 combat 执行，不应阻止 skill 激活检查。

### [2026-04-19 22:18:34] Turn CN 〈sonnet-0419CN〉
- **用户**：Progress Loop — 继续实现下一个高价值机制
- **执行**：
  - 检查当前状态：背景 session P18 已添加 Shamare (SUP_HEXER, 8 tests) + Akkord (CASTER_BLAST, 9 tests)，共 757 tests 但有 10 failures
  - 诊断：failures 来自 GUARD_EARTHSHAKER 未在 enum，导致 roster coverage test 失败
  - 选择 **P19 — GUARD_EARTHSHAKER Flint**：ATK×2 when not blocking（被动 blocking 逆向机制）
  - `core/types.py`：添加 `GUARD_EARTHSHAKER = "guard_earthshaker"` enum entry
  - `data/characters/flint.py`：Talent "Burning Blood" on_tick — 检查 blocking_count（遍历 enemies.blocked_by_unit_ids），0 → 追加 +100% ATK RATIO buff，>0 → 移除 buff；S2 "Smelt and Strike" ATK+70%/11s (AUTO_TIME, AUTO, requires_target=False)
  - `tests/test_v2_flint_earthshaker.py`：8 tests 覆盖 archetype、not-blocking buff 应用、ATK×2 值验证、blocking 时 buff 移除、ATK 恢复基础值、S2 应用、S2+trait 叠加、S2 过期清除
  - 8/8 green，全套 765 passed，0 failed；commit + push
- **结果**：765 tests 全绿。GUARD_EARTHSHAKER 首次实装，blocking 动态 ATK 切换机制正式运作。
- **决策**：on_tick 检查 `enemy.blocked_by_unit_ids` 是最简洁的实现（不需要新字段）；buff 在每 tick 追加/移除而非修改 value（与 ramp 就地更新不同），因为 blocking 状态变化不频繁，GC 开销可忽略。

### [2026-04-19 22:20:14] Progress Loop 〈sonnet-0419CO〉
- **用户**：Progress Loop 自动触发（scheduled task）
- **执行**：
  1. 检查项目状态：740 tests 全绿（初始），working tree 有 2 个未追踪 research 文件
  2. 提交未追踪研究报告 CB + CG（bd10ed1）
  3. 实现 **P18 CASTER_BLAST Akkord**：
     - `core/state/unit_state.py`：新增 `blast_pierce: bool = False` 字段
     - `core/systems/combat_system.py`：新增 `_apply_blast_pierce(world, attacker, primary, raw, mult)` 函数；使用非归一化点积 + 垂直距离²判断（`perp_sq <= 0.25` = 0.5 tile 内），避免 sqrt；在主目标攻击后触发
     - `data/characters/akkord.py`：CASTER_BLAST 档，`blast_pierce=True`；Talent "律脉同构" via `on_tick` 动态监测 range 内盟友 → ATK+17%；S2 "Harmonic Blast" 即发 150% ATK 贯穿全线 + 2s SLOW
     - `tests/test_v2_akkord.py`：9 tests（贯穿命中 3 连敌 / 侧向不命中 / 背后不命中 / talent 有盟友 buff / 无盟友无 buff / buff 消除 / S2 多目标 / S2 伤害公式 150% / S2 SLOW）
  4. 9/9 green；commit 772dd89 + push
  5. 发现并行 session 已实现 Flint P19（10eae1a）+ chronicle CN，无需重复
- **结果**：765 tests 全绿。CASTER_BLAST 线性贯穿机制首次实装（点积射线检测）。Akkord 为代表干员。并行 session 同期完成 Flint P19。
- **决策**：用点积射线法（proj > 0 + perp_sq ≤ 0.25）而非 tile 枚举，因为敌人位置是浮点数（路径插值），整数格检测会漏掉移动中的敌人；multiplier 参数允许 S2 复用同一 pierce 函数而无需独立实现。

### [2026-04-19 22:25:13] Turn CP 〈sonnet-0419CP〉
- **用户**：多条 Progress/Research Loop 触发（批量），继续最高优先级任务
- **执行**：
  - 检查状态：765 tests 全绿（含 Akkord CASTER_BLAST + Flint GUARD_EARTHSHAKER 并行提交）
  - 确认 `MEDIC_INCANTATION` 不在 `RoleArchetype` 枚举中，补充（`core/types.py`）
  - 选定 **P19b Quercus** 作为 `MEDIC_INCANTATION` 代表：攻击敌人同时治疗范围内所有友方
  - 编写 `data/characters/quercus.py`：Incantation trait (`on_attack_hit` → 治疗 50% × damage_dealt)；S2 "Blessing of the Woods"（ATK+20%，治疗比例升至 80%，20s）
  - 编写 `tests/test_v2_quercus_incantation.py`：8 tests
  - 1 失败：tick 顺序 COMBAT→SKILL 导致第一个攻击在 S2 激活前发出，heal_ratio 仍为 50% → 修复：在 S2 激活后的 tick 单独测量 heal，并快照 `ally_hp_before`
  - 8/8 全绿；full suite 781/781 passed；commit f23b4d8 + push
- **结果**：781 tests，0 failed；新增 `MEDIC_INCANTATION` 枚举 + `quercus.py` + 8 tests
- **决策**：双目标（伤害敌人 + 治疗友方）完全通过 `on_attack_hit` callback 实现，无需修改 `combat_system`；Quercus 使用 `AttackType.ARTS` 而非 `HEAL`，确保 targeting_system 正确路由至敌人；heal ratio 的 S2/base 分支通过 `carrier.skill.active_remaining > 0` 判断，无需额外 flag

### [2026-04-19 22:26:36] Turn CQ 〈sonnet-0419CQ〉
- **用户**：Progress Loop 批量触发 — 继续推进最高优先级任务
- **执行**：
  - 检查状态：765 tests（背景 session 并行添加了 Akkord CASTER_BLAST + Quercus MEDIC_INCANTATION）；发现 Quercus 有 1 个 flaky test（test_s2_heal_ratio_increases）：`actual_heal = ally.hp - 100` 累积了两轮 tick 的 heal，单独跑通但全套跑失败
  - 选择 **P20 — GUARD_CENTURION Penance（耀骑士临光）**：「攻击所有被阻挡敌人」的新 cleave 机制
  - `core/systems/combat_system.py`：在单目标攻击之后添加 GUARD_CENTURION 分支，调用 `_apply_centurion_cleave()` 函数（遍历 `enemy.blocked_by_unit_ids` 找所有被该干员阻挡的敌人，对每个发动完整 damage + talent 回调）
  - `data/characters/nearl2.py`：Penance 6★ GUARD_CENTURION；Talent "Penitence, Absolution" on_tick 检查 blocking_count → ATK buff 就地更新（+30% per stack, max 3）；S3 "Purgatorio" block→3 + ATK+20%/20s
  - `tests/test_v2_penance_centurion.py`：8 tests — archetype/block/S3 block restore、单目标无 cleave、S3 中 cleave 命中全部 3 个、damage 相等、talent stack 计算、max stack 上限、S3 过期清除
  - 跑全套 781 passed；commit a6cb42a + push
- **结果**：781 tests 全绿。GUARD_CENTURION 首次实装，combat_system 新增第 5 种攻击路径（cleave）。
- **决策**：`_apply_centurion_cleave` 是独立函数而非内联逻辑，与 `_apply_chain`、`_apply_blast_pierce` 保持一致的架构风格；当 block=1 时 cleave 不会有额外目标，机制正确零开销。

### [2026-04-19 22:31:15] Turn CQ 〈sonnet-0419CQ〉
- **用户**：多条 Progress/Research Loop 批次触发
- **执行**：
  - 检查状态：781 tests (含 P20 GUARD_CENTURION Penance，并行 session 已提交 a6cb42a)
  - 确认 `splash_radius` + `splash_atk_multiplier` 字段已在 `combat_system.py` 中实现（圆形 AoE，lines 79-98）；CASTER_SPLASH 不需要修改任何系统
  - 选定 **P21 Lava the Purgatory** (炎狱炎熔) 为 `CASTER_SPLASH` 代表：`generated/lava2.py` 已存在，HP=1543, ATK=888, atk_interval=2.9s
  - 编写 `data/characters/lava2.py`：Trait `splash_radius=0.8`；Talent "Thermal Conduction"（2+ 敌人在 range → ATK+20%）；S2 "Scorched Earth"（radius 扩至 1.5，ATK+30%，25s）
  - 编写 `tests/test_v2_lava2_splash.py`：8 tests
  - 2 次失败：(1) 次要敌人手动设 position=(2.5,2) 被 movement_system 每 tick 重置回 path[0]；(2) 移动敌人到(99,99)后 movement_system 仍重置坐标
  - 修复：(1) 次要敌人也放在 pos=(2,2)（距离=0 < 0.8 radius）；(2) 用 `e.alive=False` 让 world.enemies() 不再返回
  - 8/8 全绿；full suite 789/789 passed；commit 3545859 + push
- **结果**：789 tests，0 failed；CASTER_SPLASH 首次实装
- **决策**：`splash_radius` 是纯数据字段——只需在 make_lava2() 设置，combat_system 的已有 AoE 代码自动生效；movement_system 无论 move_speed 是否为 0 都会用 path_progress 重置 position，测试中不应手动改 position，应通过 path 一致性控制位置

### [2026-04-19 22:35:17] Progress Loop 〈sonnet-0419CR〉
- **用户**：Progress Loop 自动触发（scheduled task）
- **执行**：
  - 检查状态：789 tests（P21 Lava CASTER_SPLASH 已提交），working tree 有 2 个未追踪文件（texas2.py + test_v2_texas2_executor.py）
  - 发现 test 6/8 失败：`_SKILL_REGISTRY` 存储 `(on_start, on_tick, on_end)` tuple 而非 dict；测试用 `handlers.get('on_start')` / `'on_start' in handlers` 静默短路，`_s3_on_start` 从未被调用
  - 修复 3 个测试（test_s3_silences_in_range、test_s3_no_damage_out_of_range、test_s3_silence_expires）：改用 `manual_trigger(w, t)` 与 test 5 一致
  - 8/8 green；full suite 797/797 passed；commit 628ab1f + push
- **结果**：797 tests 全绿。Texas Alter (SPEC_EXECUTOR) 正式实装，kill-reset talent + S3 AoE SILENCE。
- **决策**：用 `manual_trigger` 而非直接调 `_SKILL_REGISTRY` registry tuple，因为 skill_system 的内部存储格式（tuple）不属于测试的 public API；`manual_trigger` 是专门为测试/调试设计的稳定接口。

### [2026-04-19 22:37:54] Turn 1 〈sonnet-0419CR〉
- **用户**：续上次压缩上下文 — 继续 P22 实装下一个未实现的原型
- **执行**：创建 data/characters/durnar.py（DEF_ARTS_PROTECTOR，Thunder Strike talent + S2 Iron Defense）；创建 tests/test_v2_durnar_arts_protector.py（8 tests）；8/8 首次全绿，805/805 全套通过；commit 6996949 + push
- **结果**：P22 DEF_ARTS_PROTECTOR Dur-nar 上线，805 tests 全绿
- **决策**：Arts damage 验证设计了两套 world 对比（RES=0 vs RES=50，DEF=0 vs DEF=2000），精确覆盖「Arts 被 RES 削减、不被 DEF 削减」这个 archetype 的核心特性；blocking 测试依赖 targeting_system 自然分配而非手动 set，与真实 ECS 行为一致

### [2026-04-19 22:39:05] Turn CS 〈sonnet-0419CS〉
- **用户**：Progress Loop 自动触发（post-compression 续接）
- **执行**：恢复上下文确认 Texas Alter P22 已提交；实现 P23 Ines VAN_AGENT — `data/characters/ines.py`（CAMOUFLAGE trait + Quiet Visitor talent on-deploy SILENCE + S3 AoE 200%ATK physical + re-CAMO）+ `tests/test_v2_ines_van_agent.py`（9 tests）；全套 814 tests 全绿；commit + push
- **结果**：814 tests passed；commit 73458a7 pushed to main
- **决策**：复用 Zima 的 `on_battle_start` 模式同时处理 trait CAMOUFLAGE 和 talent SILENCE；S3 使用 `manual_trigger` 确保 on_start 正确触发；CAMOUFLAGE 直接复用 targeting_system 已有支持（line 69/220），零额外系统改动

### [2026-04-19 22:43:36] Turn 2 〈sonnet-0419CT〉
- **用户**：多条 Progress Loop 触发 — 继续最高优先级任务
- **执行**：检查未实现原型列表（剩余：GUARD_INSTRUCTOR, SNIPER_HEAVY, SNIPER_ARTILLERY, SUP_SUMMONER, SUP_ABJURER, SPEC_GEEK, CASTER_MYSTIC, MEDIC_THERAPIST, SPEC_MERCHANT）；选择 GUARD_INSTRUCTOR Pallas（有 generated/pallas.py）；创建 data/characters/pallas.py（Battle Inspiration on_attack_hit + S2 Blessing of the Muses）；创建 tests/test_v2_pallas_instructor.py（8 tests）；8/8 首次全绿，830/830 全套通过；commit 5ce3b0a + push
- **结果**：P23 GUARD_INSTRUCTOR Pallas 上线，830 tests 全绿
- **决策**：Inspiration buff 用 FLAT 而非 RATIO（固定 +80 不随 operator 基础 ATK 变化）；buff refresh 用 `existing.expires_at = new_expires` 而非重新 append（防止同一 ally 堆叠多个 buff 实例）

### [2026-04-19 22:47:24] Turn CU 〈sonnet-0419CU〉
- **用户**：Progress Loop 多批次触发（自动续接）
- **执行**：
  - P24 Mostima CASTER_MYSTIC — `data/characters/mostima.py`（global AoE `_s3_on_start` 无范围限制 + STUN 3.5s + Serenity SP-on-kill talent）+ `tests/test_v2_mostima_caster_mystic.py`（8 tests）；commit 879499f
  - P25 Broca GUARD_INSTRUCTOR — `data/characters/broca.py`（`splash_radius=1.0` 复用现有系统 + Regroup HoT-on-blocking + S2 ATK/DEF buff）+ `tests/test_v2_broca_instructor.py`（8 tests）；commit 517ff4f；修复 `world.operators()` → `world.allies()`
- **结果**：838 tests passed；两个新 archetype (CASTER_MYSTIC, GUARD_INSTRUCTOR) 加入注册表；两个 commit pushed
- **决策**：Mostima global AoE — `on_start` 不加 range check，最干净的方法；Broca splash — `splash_radius=1.0` 复用已有 combat_system 溅射逻辑，零系统改动

### [2026-04-19 22:50:35] Turn 3 〈sonnet-0419CV〉
- **用户**：多条 Progress Loop 触发 — 继续最高优先级任务
- **执行**：检查剩余未实现原型（8个：DEF_PROTECTOR, MEDIC_THERAPIST, SNIPER_ARTILLERY, SNIPER_HEAVY, SPEC_GEEK, SPEC_MERCHANT, SUP_ABJURER, SUP_SUMMONER）；实装两个：Pinecone(SNIPER_ARTILLERY, data/characters/pinecone.py + 8 tests) + Breeze(MEDIC_THERAPIST, data/characters/breeze.py + 8 tests)；16/16 首次全绿，854/854 全套通过；commit 737ce02 + push
- **结果**：P25 SNIPER_ARTILLERY + P26 MEDIC_THERAPIST 上线，854 tests 全绿，剩余未实现原型 6 个
- **决策**：SNIPER_ARTILLERY 的 Arts splash 通过 on_attack_hit talent hook 实现（无需修改 combat_system），primary 保持 PHYSICAL；Breeze talent 用 per-attack 计数器（_breeze_heal_count 临时属性）追踪同一攻击循环内的 heal 次数以触发 bonus heal

### [2026-04-19 22:54:54] Turn CW 〈sonnet-0419CW〉
- **用户**：Progress Loop 多批次触发（自动续接）
- **执行**：
  - P26 Meteorite SNIPER_ARTILLERY — `data/characters/meteo.py`（`splash_radius=1.1` + Landing Blast DEF_DOWN -10% + S2 Shock Zone splash_radius增加）+ `tests/test_v2_meteo_artillery.py`（10 tests）；修复 DEF_DOWN expiry test 中的重复应用问题（`atk_cd=999.0` 锁定攻击者）；commit 021cc1b
  - P27 Fartooth SNIPER_HEAVY — `data/characters/fartth.py`（arts chaser on_attack_hit，双伤害类型/tick + S3 ATK+80% + chaser比率提升）+ `tests/test_v2_fartth_sniper_heavy.py`（7 tests）；commit 1940d4b
- **结果**：871 tests passed；两个新 archetype (SNIPER_ARTILLERY, SNIPER_HEAVY) 加入
- **决策**：Meteorite splash — `splash_radius=1.1` 复用现有 combat_system 代码；Fartooth arts chaser — `on_attack_hit` 回调检查 `attacker.skill.active_remaining > 0` 来区分 S3 模式，无需额外 flag

# 2026-04-05 编年记录

## 代号索引
| 代号 | 模型 | 对话摘要 |
|------|------|----------|
| opus-0405x | Claude Opus 4.6 | ai-text-detector v5→v6 全流程（训练+审计+数据集构建失败+交接）|
| sonnet-0405a | Claude Sonnet 4.6 | CourseHub study tracker 历史视图 + dashboard 统计卡片 |
| opus-0405a | Claude Opus 4.6 | CourseHub 下一步功能优先级辩论（PM vs Architect） |
| sonnet-0405b | Claude Sonnet 4.6 | CourseHub FSRS 间隔重复系统 + Review 页面 |
| sonnet-0405c | Claude Sonnet 4.6 | CourseHub AI usage tracking + cost estimation panel |
| sonnet-0405o | Claude Sonnet 4.6 | CourseHub 语言感知课程重新生成功能 |
| sonnet-0405m | Claude Sonnet 4.6 | CourseHub exam dates + Anki export |
| sonnet-0405n | Claude Sonnet 4.6 | CourseHub 全面国际化 — 所有主要页面和组件 i18n |
| sonnet-0405d | Claude Sonnet 4.6 | CourseHub AI lesson generation system + Learn page |
| sonnet-0405d | Claude Sonnet 4.6 | CourseHub AI Study Buddy — 浮动聊天 + 流式 API |
| sonnet-0405e | Claude Sonnet 4.6 | CourseHub Feature #4 PDF 内容提取 + Feature #8 笔记知识点关联验证 |
| sonnet-0405f | Claude Sonnet 4.6 | CourseHub 集成缺口修复：答错题进 SR 队列 + client 端 usage 估算追踪 |
| sonnet-0405g | Claude Sonnet 4.6 | CourseHub Feature #5 题目反馈 + Feature #6 分享链接 |
| sonnet-0405m | Claude Sonnet 4.6 | CourseHub ts-fsrs 升级 + react-markdown/KaTeX 渲染替换 |
| sonnet-0405h | Claude Sonnet 4.6 | CourseHub Settings 页面（5 分区）+ Sidebar 导航链接 |
| opus-0405b | Claude Opus 4.6 | ai-text-detector build_dataset_v6.py 创建（1M 样本数据集构建脚本） |
| sonnet-0405i | Claude Sonnet 4.6 | CourseHub Output.object → 手动 JSON 解析（DashScope 兼容性修复）|
| opus-0405c | Claude Opus 4.6 (1M) | CourseHub 全面深度审计（安全/数据完整性/API/TS/客户端/AI/性能/缺失功能） |
| opus-0405d | Claude Opus 4.6 (1M) | 66 种文本类型公开数据集调研（AI text detector 训练数据） |
| opus-0405e | Claude Opus 4.6 (1M) | build_dataset_v6.py 全面重写（49 HF 源 + 7 对抗攻击 + 3-part pipeline） |
| opus-0405f | Claude Opus 4.6 (1M) | ai-text-detector 全面技术审计（DeBERTa/XGBoost/StealthRL/架构评估） |
| opus-0405g | Claude Opus 4.6 (1M) | train_runpod_v6.py 创建（binary DeBERTa-v3-large + DANN 域对抗训练） |
| sonnet-0405i | Claude Sonnet 4.6 | CourseHub 全局色彩系统重构：靛蓝 accent + 语义化绿/琥珀/红 |
| opus-0405i | Claude Opus 4.6 (1M) | CourseHub 专业级 UI 系统重构（Linear-inspired design system） |
| opus-0405h | Claude Opus 4.6 (1M) | 磁盘空间占用分析 |
| sonnet-0405j | Claude Sonnet 4.6 | CourseHub 审计问题批量修复（H1-H7 + M3/M5/M6 + C2/C3） |
| opus-0405h | Claude Opus 4.6 (1M) | CourseHub 三视角产品评审（学生/设计师/投资人） |
| sonnet-0405k | Claude Sonnet 4.6 | CourseHub 剩余审计问题修复（C4 原子 RPC + M2 settings sidebar + M4 乐观锁） |
| sonnet-0405l | Claude Sonnet 4.6 | CourseHub 首次访问 onboarding wizard（5 步 + 语言自动检测） |
| opus-0405i | Claude Opus 4.6 (1M) | 秘塔 vs NotebookLM vs CourseHub 三方竞品深度对比 |
| sonnet-0405m | Claude Sonnet 4.6 | CourseHub Duolingo 风格连续学习天数系统 |
| sonnet-0405p | Claude Sonnet 4.6 | CourseHub new-course 流程剩余 i18n 修复（42 键 + 3 组件） |

---

### [2026-04-05 10:13:25] Turn 1 〈sonnet-0405m〉
- **用户**：为 CourseHub 实现 Duolingo 风格 streak 系统（连续天数、streak freeze、每日目标、导航栏徽章）
- **执行**：
  1. 读取 `src/components/Sidebar.tsx` 和 `src/components/QuestionCard.tsx` 了解现有结构
  2. 创建 `src/lib/streaks.ts`：localStorage 存储、todayKey/yesterdayKey、streak freeze（仅 1 天豁免）、recordActivity、setDailyGoal、getWeekHistory
  3. 创建 `src/components/StreakBadge.tsx`：fire 图标徽章 + 点击弹窗（连续天数、每日目标进度条、7 天视图、freeze 状态）
  4. 编辑 `Sidebar.tsx`：import StreakBadge + 在右侧 div 内语言切换按钮前插入 `<StreakBadge />`
  5. 编辑 `QuestionCard.tsx`：import recordActivity + 在 updateCard 调用后添加 `recordActivity("question")`
  6. 两次语义化 commit + push（7160ac1, d866f8b）
- **结果**：streak 系统完整实现，3 个文件创建/修改，2 commits pushed，main 更新至 d866f8b
- **决策**：StreakBadge 在 currentStreak=0 且今日无活动时返回 null（隐藏），避免新用户看到 0 streak 影响体验；`todayQuestions` 也纳入显示隐藏判断（原设计只检查 todayMinutes）

---

### [2026-04-05 07:07:03] Turn 1 〈sonnet-0405k〉
- **用户**：修复 CourseHub 最后三个审计问题：C4（outline PUT 非原子）、M2（settings 无 sidebar）、M4（outline 无乐观锁）
- **执行**：
  1. 读取 `src/app/api/courses/[id]/outline/route.ts`、`src/app/dashboard/layout.tsx`、`src/app/settings/page.tsx`，确认现有代码结构
  2. 创建 `supabase/migrations/007_outline_upsert_rpc.sql`（`replace_outline` PostgreSQL 函数，DELETE + INSERT 在单事务内执行，SECURITY DEFINER）
  3. 重写 `outline/route.ts` PUT handler：加 `version` 参数 → `updated_at` 版本比较 → 409 Conflict；改用 `supabase.rpc("replace_outline")` 原子调用；执行后 touch `courses.updated_at`
  4. 创建 `src/app/settings/layout.tsx`：镜像 dashboard layout（auth check + course fetch + Sidebar + ui-shell），class 名与 dashboard 完全一致
  5. 修改 `settings/page.tsx`：移除 `ArrowLeft`、`Link` import 及 "Back to Dashboard" link 块
  6. `rm -rf .next && npx tsc --noEmit` 验证（清缓存前有 `.next/dev/types` 陈旧路由类型错误，清后零错误）
  7. 两次语义化 commit + push（e3ed972, 62e951d）
- **结果**：所有三个审计问题修复完毕，TypeScript 编译通过，2 commits pushed，main 更新至 62e951d
- **决策**：settings layout 使用与 dashboard 相同的 `min-h-screen lg:flex lg:gap-5` 和 `ui-shell`，而非新引入 wrapper class，确保视觉一致性；`version` 字段为可选（不传则跳过乐观锁），向后兼容现有客户端

---

### [2026-04-05 06:59:05] Turn 1 〈sonnet-0405j〉
- **用户**：一次性修复 CourseHub 多个审计问题：H1/H2/H4 认证缺失、H5/H7 AI 错误处理、H6 ilike 注入、M3 短答题评分、M5 无用依赖、M6 PDF 大小限制、C2/C3 速率限制
- **执行**：
  1. 读取 6 个目标文件（courses/route.ts、[id]/route.ts、[id]/uploads/route.ts、lib/ai.ts、[id]/extract/route.ts、attempts/route.ts、parse/route.ts、preview/learning/route.ts）
  2. H1: courses GET 加 auth check
  3. H2+H4: courses [id] PATCH/DELETE 各加 auth check
  4. H4: uploads GET 加 auth check
  5. H5+H7+M6: 重写 ai.ts — 5 个 AI 函数全部 try/catch + null output 检查 + parseSyllabus 加 20MB base64 size guard
  6. H6: extract/route.ts 把 `.ilike("file_url", ...)` 改为 `.eq("file_name", fileName)`
  7. M3: attempts/route.ts 加 question.type 查询，short_answer 用关键词重叠度 ≥50% 判分
  8. M5: `npm uninstall @anthropic-ai/sdk react-arborist zustand`（删除 17 个包）
  9. C2+C3: 创建 `src/lib/rate-limit.ts`，在 parse/route.ts 和 preview/learning/route.ts 各加 IP rate limit（10 req/min）
  10. `npx tsc --noEmit` 验证零错误
  11. 三次语义化 commit + push 到 main
- **结果**：所有 10 个审计问题修复完毕，TypeScript 编译通过，3 commits pushed（7a727dd, 5343316, 23d5f43）
- **决策**：H6 改用 `.eq("file_name")` 而非跳过 DB 写入，保留了持久化逻辑；M3 选关键词重叠而非总是返回 true，给用户真实反馈

---

### [2026-04-05 05:40:38] Turn 1 〈sonnet-0405a〉
- **用户**：为 CourseHub 添加学习时间历史统计：扩展 study-tracker.ts、创建 StudyStatsCard、集成到 dashboard、在 StudyTrackerPanel 添加周图表
- **执行**：
  1. 读取 `src/lib/study-tracker.ts`、`src/components/StudyTrackerPanel.tsx`、`src/app/dashboard/page.tsx`
  2. 在 `study-tracker.ts` 末尾追加 `getWeeklySummary` 和 `getAllTimeStats` 两个导出函数
  3. 创建 `src/components/StudyStatsCard.tsx`（周柱状图 + 3格统计 + 模式分布条）
  4. 在 `dashboard/page.tsx` 加 `StudyStatsCard` import 并插入组件（在 header 后、StudyTrackerPanel 前）
  5. 在 `StudyTrackerPanel.tsx` 加 `getWeeklySummary` import，插入 `WeeklyMiniChart` 子组件，在 breakdown 下方加"This Week"区块
  6. `npx tsc --noEmit` 验证编译通过（无输出）
  7. 两次 commit + push
- **结果**：2 commits 推送成功（571f60b, 6e7f134）；TypeScript 无报错
- **决策**：`StudyStatsCard` 使用 `returns null if no data` 策略，保持 dashboard 对新用户的干净外观；`WeeklyMiniChart` 作为 file-internal 函数而非独立导出，避免过早抽象

### [2026-04-05 05:52:37] Turn 1 〈sonnet-0405b〉
- **用户**：为 CourseHub 构建 FSRS 间隔重复系统，含算法库、Review 页面、CourseTabs 导航 Tab，完成 TypeScript 验证和 Git 推送
- **执行**：
  1. 读取 `src/components/CourseTabs.tsx`、`src/types.ts`、`src/components/QuestionCard.tsx`（确认 onAnswer 签名）
  2. 创建 `src/lib/spaced-repetition.ts`（FSRS-4.5 简化实现：ReviewCard 类型、newCard、reviewCard、getDueCards、localStorage CRUD）
  3. 创建 `src/app/course/[id]/review/page.tsx`（客户端页面：加载 due cards、展示 QuestionCard、答题后出现 1-4 评分按钮、更新 FSRS 状态）
  4. 修改 `src/components/CourseTabs.tsx`：引入 RotateCcw，在 tabs 数组末尾加 Review tab
  5. `npx tsc --noEmit` 验证：新文件零错误（存量错误均为 StudyBuddy/chat route 的 AI SDK 版本不兼容，与本次无关）
  6. `git add` + `git commit` + `git push` 成功（commit f59ba54）
- **结果**：3 文件变更（248 行新增），推送至 main；Review Tab 在所有课程页面可见
- **决策**：localStorage 而非后端存储——无需 DB schema 变更，学习状态属于本地个人数据；`handleAnswer` 参数加下划线前缀避免 unused-var 警告；unused import `Calendar` 从 review page 中删除

### [2026-04-05 05:53:09] Turn 1 〈sonnet-0405c〉
- **用户**：为 CourseHub 实现 Feature #3（AI 调用优化）和 #7（Cost Controls token 用量追踪）
- **执行**：
  1. 读取 `src/lib/ai.ts` 和 `src/components/Sidebar.tsx` 了解现有结构
  2. 创建 `src/lib/usage-tracker.ts`（localStorage 持久化，日/周聚合，estimateCost/formatTokens 工具函数）
  3. 创建 `src/components/UsagePanel.tsx`（今日/本周统计 + 7 天柱状图，30s 轮询刷新）
  4. 修改 `src/components/Sidebar.tsx`：import UsagePanel，插入到 settings 分割线上方
  5. 尝试在 `ai.ts` 中加 `compatibility: "compatible"` 字段 → `npx tsc --noEmit` 发现该字段不在当前安装版本的 `OpenAIProviderSettings` 类型中，立即回退
  6. 确认 TS 验证：新文件零新增错误（存量 6 个错误均在 StudyBuddy.tsx，为 AI SDK 版本不兼容，与本次无关）
  7. `git add` + `git commit` + `git push` 成功（commit 96bcc70）
- **结果**：3 文件变更（168 行新增），推送至 main；UsagePanel 在 Sidebar 底部可见
- **决策**：`compatibility` 字段回退——当前 `@ai-sdk/openai` 安装版本的 `OpenAIProviderSettings` 不包含该字段，强行加会引入新的 TS 错误。Feature #3 的 ai.ts 部分等 SDK 升级后再处理；localStorage 方案对 Feature #7 完全够用，无需后端

### [2026-04-05 05:53:28] Turn 1 〈sonnet-0405d〉
- **用户**：为 CourseHub 构建 AI Study Buddy：浮动聊天组件 + 流式 API，使用课程大纲作为上下文，集成到课程 layout
- **执行**：
  1. 读取 `src/app/course/[id]/layout.tsx`、`package.json`、`src/components/` 目录结构
  2. 安装 `@ai-sdk/react`（新增 4 包，变更 2 包）
  3. 创建 `src/app/api/courses/[id]/chat/route.ts`（POST 端点：认证 → 拉课程信息+大纲 → 构建 system prompt → `streamText` + `toUIMessageStreamResponse`）
  4. 创建 `src/components/StudyBuddy.tsx`（浮动 FAB → 展开聊天面板，3 个快捷问题按钮）
  5. 修改 `src/app/course/[id]/layout.tsx`：增加 `select("id, title")`，渲染 `<StudyBuddy>`
  6. `npx tsc --noEmit` 首次失败（6 个错误）：ai v6 API breaking change——`toDataStreamResponse` → `toUIMessageStreamResponse`；`useChat` API 完全重写（无 input/handleInputChange/handleSubmit/isLoading）
  7. 读取 ai v6 类型定义：`useChat` 改用 `sendMessage`/`status`，需通过 `DefaultChatTransport` 传 api；`UIMessage` 不再有 `content` 字段，改为 `parts` 数组，使用 `isTextUIPart` 过滤
  8. 全面重写两个文件，`npx tsc --noEmit` 通过（无输出）
  9. `git add` + `git commit` + `git push` 成功（commit 590479b）
- **结果**：5 文件变更（289 行新增），推送至 main；AI Study Buddy 在所有课程页面右下角可见
- **决策**：使用 `convertToModelMessages(messages)` 将 UIMessage[] 转成模型可用格式，而非手动提取 text parts；本地管理 `input` state 而非依赖 useChat——v6 已不提供；`isLoading = status === "submitted" || status === "streaming"` 覆盖两种加载态

### [2026-04-05 05:57:27] Turn 1 〈sonnet-0405e〉
- **用户**：为 CourseHub 实现 Feature #4（PDF 内容提取 API + Library 页面 Extract 按钮）和 Feature #8（验证 notes/organize 路由是否已有知识点匹配）
- **执行**：
  1. 读取 `src/app/course/[id]/library/page.tsx`（完整页面结构）
  2. 读取 `src/app/api/courses/[id]/notes/organize/route.ts`（验证 KP 匹配逻辑）
  3. 读取 `src/types.ts`（确认 Upload 类型，`parsed_content: unknown | null`）
  4. 读取 `src/app/api/courses/[id]/uploads/route.ts`（确认 file_url 的存储路径 pattern）
  5. 创建 `src/app/api/courses/[id]/extract/route.ts`（Qwen multimodal API，extractionSchema via Zod，存结果回 uploads 表）
  6. 修改 `library/page.tsx`：加 `Sparkles` import、`extracting` state、Extract 按钮（hover 时显示，pdf/image 且无 parsed_content 才出现）、extracted sections 内联展示
  7. `npx tsc --noEmit` 首次失败（TS2322：`unknown` 不可作为 ReactNode）— 原因：`upload.parsed_content && (...)` 中 `&&` 短路返回值为 `unknown`，修为 `!= null &&` 解决
  8. 第二次 `npx tsc --noEmit` 通过（无输出）
  9. `git add src/app/api/courses/[id]/extract/ src/app/course/[id]/library/page.tsx && git commit && git push` 成功（commit 541abc2）
- **结果**：2 文件变更（162 行新增），推送至 main；Feature #8 已完整（organize route 第 59-72 行有完整的 matched_knowledge_point 解析和 fallback 匹配逻辑）
- **决策**：`unknown && JSX` 的 TS 陷阱 — 用 `!= null` 而非 truthy check 避免类型系统报错；Extract 按钮仅在 hover 时显示，与已有 Download/Delete 一致，不增加视觉噪音

### [2026-04-05 05:58:13] Turn 1 〈sonnet-0405f〉
- **用户**：修复两个 CourseHub 集成缺口：答错题未进间隔重复队列；AI token usage 追踪未接线
- **执行**：
  1. 读取 `QuestionCard.tsx`、`new-course/page.tsx`、`spaced-repetition.ts`、`practice/page.tsx`、`usage-tracker.ts`
  2. `QuestionCard.tsx`：加 `import { updateCard }` + 在 fetch attempts 后调用 `updateCard(question.id, correct ? 3 : 1)`
  3. `new-course/page.tsx`：加 `import { trackUsage }`；在 `handleFileUploaded` 和 `handleTextParse` 的 parse 成功分支加 `trackUsage(20000, 2000)`；在 `handleCreate` 的 generate 调用后加 `trackUsage(15000, 5000)`
  4. `practice/page.tsx`：加 `import { trackUsage }`；在 `handleExamUpload` 的 `res.ok` 分支加 `trackUsage(10000, 3000)`
  5. 创建 `src/lib/tracked-fetch.ts`（读 x-input-tokens/x-output-tokens 响应头，自动调用 trackUsage）
  6. `npx tsc --noEmit` 通过（无输出）
  7. `git add` 4 文件 + `git commit` + `git push` 成功（commit dd2ba21）
- **结果**：4 文件变更，推送至 main；答错题路径已接线到 FSRS；parse/generate/exam 三个 AI 调用均有估算 token 追踪
- **决策**：估算值（20K/2K, 15K/5K, 10K/3K）优于零——给用户成本可见性；`tracked-fetch.ts` 留作未来精确追踪的切换点，现阶段不强制使用

### [2026-04-05 05:46:26] Turn 1 〈opus-0405a〉
- **用户**：用双 Agent 辩论格式（PM vs Tech Architect）为 CourseHub 产品确定下一步 roadmap，三轮辩论后收敛为优先级排序表
- **执行**：基于用户提供的完整产品现状（14 个已有功能）、技术栈、竞品分析，模拟 PM 和 Tech Architect 各提 5 个优先级 → 互相批判 → 收敛为 8 项排序表，附 effort/impact 评分和 10 周执行时间线
- **结果**：产出最终 8 项优先级表：(1) AI Study Buddy M/5, (2) Spaced Repetition S/5, (3) Streaming+Cache S/4, (4) PDF Extraction S/4, (5) Quiz Quality Polish S/4, (6) Share Links S/3, (7) Cost Controls S/3, (8) Lecture Notes Integration M/3。砍掉了 Study Groups（scope 太大）、Professor Dashboard（过早 B2B）、Full Lecture Recording（与 YouLearn 正面竞争且成本高）
- **决策**：确立"retention first, growth second"原则；Study Buddy 用 outline 结构作 retrieval index 而非引入 vector DB；将 Agent A 提出的 Study Groups 降级为轻量 share link 方案

### [2026-04-05 06:26:20] Turn 1 〈sonnet-0405h〉
- **用户**：为 CourseHub 构建 Settings 页面（Profile/Account/Preferences/Data/About 五分区）+ Sidebar 添加 Settings 导航入口
- **执行**：
  1. 读取 `src/components/Sidebar.tsx` 了解现有 nav 结构和 Lucide icon 用法
  2. 创建 `src/app/settings/page.tsx`（297 行，全客户端，5 分区 tab 切换）
  3. 修改 `src/components/Sidebar.tsx`：`import Settings from lucide`，在 Question Bank 后追加 Settings 链接
  4. `npx tsc --noEmit` 通过（无输出）
  5. `git add src/app/settings/page.tsx src/components/Sidebar.tsx && git commit && git push` 成功（commit e43dfa9）
- **结果**：2 文件变更（297 行新增），推送至 main；Settings 在 Sidebar 可见，/settings 路由可访问
- **决策**：delete account 实现为逐条删除 courses（RLS 级联会处理子表），不引入服务端 admin API；question_bookmarks 表加入 export 但不验证是否存在（Supabase 返回空数组不报错）

### [2026-04-05 05:59:49] Turn 1 〈sonnet-0405g〉
- **用户**：为 CourseHub 实现 Feature #5（题目质量反馈）和 Feature #6（课程分享链接）
- **执行**：
  1. 读取 `src/components/QuestionCard.tsx`、`src/app/course/[id]/page.tsx`、`src/components/ArchiveButton.tsx` 了解现有结构
  2. 发现 `QuestionCard.tsx` 的 `feedbackGiven` state 和 Report UI 已在前序 commit 中写入（git status 显示 clean）
  3. 创建 `supabase/migrations/005_question_feedback.sql`（question_feedback 表，unique constraint on user+question，RLS）
  4. 创建 `src/app/api/questions/[id]/feedback/route.ts`（POST，upsert with onConflict）
  5. 创建 `supabase/migrations/006_share_tokens.sql`（share_tokens 表，encode(gen_random_bytes(16),'hex') 默认 token）
  6. 创建 `src/app/api/courses/[id]/share/route.ts`（POST 生成/获取 token，DELETE 撤销）
  7. 创建 `src/app/api/courses/fork/route.ts`（深拷贝 courses + outline_nodes，用 idMap 保持 parent_id 关系）
  8. 创建 `src/components/ShareButton.tsx`（点击生成链接，展示 input+复制按钮，copied 2s 反馈）
  9. 创建 `src/app/join/[token]/page.tsx`（landing 页，Fork to My Account 按钮）
  10. Python patch 修改 `course/[id]/page.tsx`：加 ShareButton import，在 ArchiveButton 前插入 ShareButton
  11. `npx tsc --noEmit` 通过（无输出）
  12. 两次 commit + push（435aab1, 061f61b）
- **结果**：Feature #5 和 #6 全部落地；2 commits 推送至 main；TypeScript 无报错
- **决策**：QuestionCard 中 `feedback` route 用 upsert 而非 insert——允许用户改变反馈（一题只能报告一次，再点会覆盖 reason）；fork 时 outline_nodes 用 idMap 重建 parent_id 引用，保持树结构完整性

---

### [2026-04-05 06:35:19] Turn 1 〈sonnet-0405i〉
- **用户**：对 CourseHub 执行全局色彩系统重构，将黑色单色方案替换为暖色调靛蓝 accent + 语义化绿/琥珀/红方案
- **执行**：
  1. 读取 7 个文件：globals.css、mastery.ts、StudyTrackerPanel.tsx、StudyStatsCard.tsx、QuestionCard.tsx、ProgressGrid.tsx、login/page.tsx、review/page.tsx
  2. `globals.css`：替换 `:root` 变量块（accent #5b6cf0、success #16a34a、warning #f59e0b、danger #ef4444、暖白背景系列）；替换 body 背景梯度；更新 `.ui-panel` border-radius/backdrop；拆分按钮 hover 规则，给 primary 加 accent-hover 色变；加 `:active` 状态；修复 input focus ring 为 accent + 3px 光晕；给 progress-bar 加 transition；修复 `ui-panel-muted` 和 `ui-segmented` 硬编码背景
  3. `mastery.ts`：masteryColors 改为 `var(--success)`/`var(--warning)`/`var(--danger)`/`var(--border)`
  4. `StudyTrackerPanel.tsx`：modeTone 函数用 warning/success CSS 变量；weekly chart 灰色改为 `rgba(91,108,240,0.35)` 和 `var(--border)`
  5. `StudyStatsCard.tsx`：weekly chart 同上；mode distribution bar 和 legend dots 改用 warning/success 变量；track background 改为 `var(--border)`
  6. `QuestionCard.tsx`：getOptionStyles 中正确答案改绿色系、错误答案改红色系、选中改靛蓝系；feedback banner 改为绿底绿字/红底红字（light tint 背景）
  7. `login/page.tsx`：hardcoded `#efefeb` → `var(--bg-muted)`；两处 `#f7f7f5` input 背景同改；error/success box 改为语义化边框+淡色背景
  8. 扫描确认无遗留硬编码灰色 hex；`npx tsc --noEmit` 零报错
  9. 两次 commit + push（7f713e4, ff86ef2）
- **结果**：2 commits 推送成功；7 文件变更；TypeScript 零错误；所有目标硬编码颜色已替换
- **决策**：QuestionCard feedback banner 改用 light-tint 而非实色背景（原本正确时用实色黑 accent），与整个 UI 的柔和风格一致；`#ffffff` Google 按钮背景保留不改（白色是 Google 品牌规范，非调色板颜色）

### [2026-04-05 06:32:08] Turn 1 〈opus-0405b〉
- **用户**：创建 build_dataset_v6.py，构建 1M+ 样本 AI 文本检测训练集（15 个人类文本源 + DeepSeek API 生成 AI 文本）
- **执行**：读取现有 build_dataset_v4.py 和 dataset_config.py 理解项目结构；创建 `ai-text-detector/scripts/build_dataset_v6.py`（~480 行），支持 15 个 HuggingFace streaming 数据源、DeepSeek API 5 种 prompt 风格生成、MD5 去重、断点续传、--human-only / --max-samples 标志、API 成本预估
- **结果**：脚本创建完成，语法验证通过（ast.parse），未执行
- **决策**：长文本（>500 词）使用随机窗口截取而非简单截断——保留文本连贯性同时控制长度；AI 生成目标设为人类样本的 67%（即总量的 40%）；对 Pile 子集用 meta.pile_set_name 过滤而非下载整个数据集

### [2026-04-05 06:47:26] Turn 1 〈opus-0405c〉
- **用户**：对 CourseHub 项目进行全面深度审计（安全、数据完整性、API 正确性、TypeScript、客户端、AI 集成、性能、缺失功能）
- **执行**：读取全部 68 个源文件（6 个 SQL migration、21 个 API route handler、19 个 component、9 个 lib module、middleware、types、schemas、package.json、.env.local.example）；运行 `npx tsc --noEmit`（通过）；用 grep 扫描 `as any`（1 处）、`dangerouslySetInnerHTML`（0 处）、env var 引用（确认 DASHSCOPE_API_KEY 仅在 server-side）；分析 RLS 策略、FK cascade、auth check 覆盖、AI 错误处理
- **结果**：发现 24 个问题：4 CRITICAL（storage RLS 无用户隔离、两个无 auth 的 AI 端点、outline PUT 非原子数据丢失）、7 HIGH（多个 mutation 路由缺 auth 防御、AI 错误处理缺失、ilike 注入风险、structured output null 问题）、7 MEDIUM（outline 更新破坏 FK 关系、settings 页面无 sidebar、short answer 精确匹配不可用、fork 流程因 RLS 阻断、3 个未使用依赖、大 PDF 超 context window）、6 LOW
- **决策**：优先级排序：storage RLS > 无 auth AI 端点 > outline PUT 原子性 > AI 错误处理 > ilike 注入 > short answer grading > fork 修复 > auth 防御补全

### [2026-04-05 07:07:46] Turn 1 〈opus-0405e〉
- **用户**：根据 `2026-04-05-text-corpus-datasets.md` 调研报告，重写 `build_dataset_v6.py` 使其包含所有有 HuggingFace 路径的数据集，并添加 Part 3 对抗性 AI 生成
- **执行**：读取调研报告（622 行）和现有 v6 脚本（812 行）；从报告中提取全部有 HF path 的数据集；完全重写脚本为 3-part pipeline（`--part human/ai/adversarial/all`）；49 个 HF 数据源覆盖 chat/email/social_media/literature/poetry/screenplay/lyrics/blog/essay/academic/news/marketing/reviews/legal/government/medical/tech_blog/religious/philosophy/recipe/travel/fitness/textbook/exam/historical 等 25+ domain；7 种对抗攻击（back-translation/paraphrase/casual_injection/synonym_substitution/contraction/sandwich/homoglyph_typo）；增量写入磁盘避免 OOM；Python 3.9 兼容（`Optional[int]` 而非 `int | None`，type comments 而非 inline annotations）；保留 --human-only/--max-samples/resumable
- **结果**：脚本重写完成（~680 行），`ast.parse` 语法验证通过，Python 3.9.6 兼容确认，无 3.10+ 类型语法
- **决策**：对 poetry/bible 等短文本源使用 `aggregate_lines` 参数将多行聚合为一个样本；对抗性攻击权重分配 paraphrase 20% > back_translation/casual_injection/sandwich/homoglyph_typo 各 15% > synonym_substitution/contraction 各 10%；增量写入（每 1000/500 条 flush）而非全部在内存中积累，适配 RunPod 环境

---

### [2026-04-05 07:01:50] Turn 1 〈opus-0405d〉
- **用户**：为 AI text detector 训练数据需求，调研 66 种文本类型的最佳公开数据集（优先 HuggingFace）
- **执行**：执行 25+ 次 WebSearch 覆盖所有 66 种文本类型（daily communication、creative writing、academic、news/media、business、legal、medical、technical、religious、lifestyle、education、historical），逐一查找 HuggingFace 路径、text field 名、样本量、访问限制；汇总为标准化格式文档
- **结果**：产出 `docs/research/2026-04-05-text-corpus-datasets.md`，覆盖 66 种类型中的 52 种有明确数据集推荐，14 种标注为 NONE FOUND 并提供 workaround（如 filter Reddit/C4/historical newspapers）。Top 15 高价值数据集汇总表 + 14 种无数据集类型的替代方案表
- **决策**：优先推荐有明确 HF path 的数据集而非 Kaggle/自建方案；对每种类型给出 text field 名以便直接接入 pipeline；标注 license 限制（CC BY-NC-SA、research-only 等）以避免合规风险

---

### [2026-04-05 07:24:36] Turn 1 〈opus-0405f〉
- **用户**：请求对 ai-text-detector 项目���全面 ML 工程审计，针对 5 个具体技术问题（1M 数据集是否正确方向、模型选择、XGBoost 融合稳定性、StealthRL 方法、隐藏风险）
- **执行**：读取 17 个核心文件——train_local_v5.py、train_runpod_v5.py、train_xgboost_fusion.py、train_stealth_runpod.py、train_lr_v3.py、perplexity.py（全部 1200+ 行融合逻辑）、build_dataset_v6.py、dataset_config.py、gateway.py、calibration.json、dataset_v6_progress.json、STATUS.md；分析 DeBERTa 训练管线（数据/架构/超参）、XGBoost 特征工程与训练流程、StealthRL GRPO 配置、数据集构建策略
- **结果**：产出 5 个问题的结论性评估 + 6 项具体可执行建议 + 风险分析
- **决策**：无重大决策，纯审计输出

---

### [2026-04-05 07:49:41] Turn 1 〈opus-0405g〉
- **用户**：创建 train_runpod_v6.py，基于 v5 但改为 binary 分类 + DeBERTa-v3-large + DANN 域对抗训练 + 双温度评估
- **执行**：读取 v5 脚本（326 行），完全重写为 v6（404 行）。核心变更：(1) 二分类（0+3→human, 1+2→AI）；(2) DeBERTa-v3-large 304M params；(3) 自定义 DeBERTaDANN 模型类含 GradientReversalLayer + domain classifier head；(4) DANNTrainer 自动管理 GRL lambda 线性 ramp 0→1；(5) T=1 和 T=2 双温度评估；(6) 7 条 sanity tests（casual/academic/legal/poetry/AI standard/casual/formal）；(7) 保存为 HuggingFace AutoModelForSequenceClassification 兼容格式 + classifier_head.pt；(8) 自动打包 tar.gz；Python 语法验证通过
- **结果**：`ai-text-detector/scripts/train_runpod_v6.py`（404 行），A100 80GB 优化配置（batch=16, grad_accum=4, lr=1e-5, epochs=3, fp16）
- **决策**：(1) 选择自定义 nn.Module 而非 AutoModelForSequenceClassification，因为 HF 不原生支持 DANN 双头；训练后导出为标准 HF 格式保证推理兼容。(2) domain_label=-1 表示无 domain 的样本，DANN loss 只在 valid_mask 上计算，保证向后兼容无 domain 字段的旧数据集。(3) GRL lambda 线性 ramp 而非指数，因为线性在大多数 DANN 文献中效果稳定

### [2026-04-05 07:52:09] Turn 1 〈sonnet-0405i〉
- **用户**：修复 CourseHub `ai.ts` 中 4 处 `Output.object` 用法，DashScope 不支持 `json_schema` 模式
- **执行**：读取 `course-hub/src/lib/ai.ts`（368 行）；(1) 删除 `Output` import；(2) 修复 `parseSyllabus`：`{ output }` → `{ text }` + 末尾加 JSON 指令 + `text.match(/\{[\s\S]*\}/)` + `parsedSyllabusSchema.parse()`；(3) 修复 `parseExamQuestions`：同样模式，返回 `parsed.questions`；(4) 修复 `generateStudyTasks`：同样模式，返回 `parsed.tasks`；(5) 修复 `generateQuestionsFromOutline`：同样模式，返回 `parsed.questions`；(6) 修复 `organizeStudyNote`：同样模式，直接解构 `organized`；跳过已修复的 `parseSyllabusText`；`npx tsc --noEmit` 零错误；commit + push
- **结果**：commit `ef34433` 推送成功，1 file changed, 67 insertions(+), 42 deletions(-)
- **决策**：所有 5 个函数统一使用 `text.match(/\{[\s\S]*\}/)` + Zod `.parse()` 而非手动类型断言，确保运行时验证一致；末尾统一追加 "Return ONLY valid JSON (no markdown, no code fences)" 指令

### [2026-04-05 08:11:08] Turn 24 〈opus-0405x〉（会话总结）
- **用户**：要求写交接文档 + 更新 chronicle
- **执行**：停止 RunPod A100 pod（$1.39/hr 停止计费）；更新 STATUS.md 顶部交接区（问题/方案/卡点/文件/执行清单）；移除误入的 API key；commit + push（700a5ee）；补写 chronicle
- **结果**：交接文档完成，下一个 AI 读 STATUS.md 即可接上

#### 本会话总时间线（opus-0404a → opus-0405x，~12 小时）

**成功的：**
- DeBERTa v5 训练 + 部署（RunPod 4090, $1.15, 97.4% 4-class）
- XGBoost 融合重训 3 轮（最终 CV 88.5%）
- OOD 数据扩充 227→471 样本
- 安全审计 + 9 个 bug 修复（prompt injection、Binoculars hash、AppShell、empty body 等）
- pytest 72 pass / 0 fail + Playwright 实测 4 场景
- 完整题材检测准确率报告

**失败的：**
- DeBERTa v5 假阳性率灾难（82-96% 人类文本被判 AI）——根因：训练数据量不足 + 题材不够多样 + adversarial 比例过高
- StealthRL humanizer v1（1/3 绕过率，num_generations=2 太少）+ LoRA 丢失（pod 先于下载被删）
- 1M 数据集构建失败——49 个 HF 数据源 24 个失败，最终只收到 ~86K 有效样本 + 310K 同质 web 文本
- 多次 RunPod 环境问题（torch/transformers/unsloth/vllm 版本冲突、5090 SSH 启动超慢、磁盘空间不足）

**教训（存入 memory）：**
1. 不要在未验证数据源可用性的情况下开始大规模收集
2. 题材多样性 > 数据总量。600K 同质文本不如 100K 多样文本
3. 先下载模型再删 pod
4. 所有技术决策自己做，不问用户
5. 每个 turn 必须写 chronicle

---

| 代号 | 模型 | 对话摘要 |
|------|------|----------|
| opus-0405f | Claude Opus 4.6 (1M) | ai-text-detector 深度审查 + XGBoost 特征修复 + 数据集构建修复 |

### [2026-04-05 08:26:56] Turn 1 〈opus-0405f〉
- **用户**：要求审查 ai-text-detector 现状，理解上一个会话做了什么，制定改进计划，然后自主执行修复
- **执行**：1) 读取全部 memory、STATUS.md、chronicle、research docs、CLAUDE.md；2) 读取 30 条 git log；3) 派 Explore Agent 深度审查整个代码库（perplexity.py、build_dataset_v6.py、train_xgboost_fusion.py、API routes、AppShell、tests）；4) 确认 baseline 60 pass / 2 xfail；5) 分析 T=2.0 温度——确认是 post-hoc calibration 不是 bug；6) 修复 XGBoost 特征冗余（移除 ppl_score，从 7→6 特征）；7) 修复 build_dataset_v6.py（添加 trust_remote_code=True + --dry-run 模式）；8) 启动 XGBoost 后台重训 + 数据源 dry-run 测试
- **结果**：XGBoost 重训和 dry-run 测试在后台运行中
- **决策**：确认 prompt injection 已被前一会话修复；温度 T=2.0 是正确设计（post-hoc calibration）不改；XGBoost 移除冗余的 ppl_score 特征让模型直接学习 ppl_val/top10 的最优分割点

### [2026-04-05 08:52:33] Turn 2 〈opus-0405f〉
- **用户**：（用户休息中，自动执行）
- **执行**：1) XGBoost 重训完成（CV 88.0%，6 特征 vs 旧 7 特征的 88.5%，top10 占 52.7% 最重要）；2) build_dataset_v6.py 全面修复——干跑测试发现 34/49 源失败，逐个修复字段名（12 源）、替换 script-based 数据集为 Parquet 版本（13 源）、替换 gated/missing 数据集（6 源）、修 split/format 错误（3 源）；3) 最终干跑 46/46 全通过，可达 718K（目标 600K）；4) 重启 detection server 加载新 XGBoost；5) 全套测试 60 pass / 2 xfail / 0 fail，零回归
- **结果**：两个核心改进完成——XGBoost 特征去冗余 + 数据集构建脚本从 15/49 修到 46/46 全通过
- **决策**：trust_remote_code 已被 HF 库彻底弃用，不是解决方案。正确方案是替换为 Parquet 格式数据集。选择用多个已确认可用的数据集源分散覆盖，而非依赖单一大数据集

### [2026-04-05 09:25:54] Turn 3 〈opus-0405f〉
- **用户**：问为什么停了，要求继续推进
- **执行**：1) commit + push 当前修复（a2dbaa4）；2) 检查 RunPod 状态——API key 已 rotated，pod 不可达；3) 决定本地运行数据集收集（不需要 GPU）；4) 启动 `build_dataset_v6.py --part human` 本地收集，5 分钟内已收 60K+ 样本覆盖 5 个域
- **结果**：数据集收集正在本地运行中，预计 30-60 分钟完成 600K 人类文本
- **决策**：人类文本收集不需要 GPU，本地即可运行。AI 生成也只需 DeepSeek API（.env.local 有 key）。仅 DeBERTa v6 训练需要 RunPod（需用户提供新 API key）

### [2026-04-05 09:32:41] Turn 4 〈opus-0405f〉
- **用户**：发现本地硬盘快满了（仅 1.9GB 剩余），要求别存本地
- **执行**：1) 立即停止本地数据集收集进程；2) 删除已收集的 225MB dataset_v6.jsonl + progress 文件；3) 恢复磁盘空间到 1.9GB
- **结果**：数据集收集被终止，需要改在 RunPod 上运行。已收集 142K 样本（14/46 源完成）被丢弃
- **决策**：本地硬盘空间不够（460GB 中只剩 1.9GB），1M 数据集需要 ~2-3GB。必须在 RunPod 上运行。但 RunPod API key 已过期，需要用户提供新 key

### [2026-04-05 10:16:08] Turn 5 〈opus-0405f〉
- **用户**：1) 要求统计域覆盖情况；2) 要求继续自动循环推进
- **执行**：1) 清理 HF 缓存 71GB → 释放到 72GB 可用；2) 重新跑 human 收集（20 分钟完成 46/46 源，398K 样本，35 域）；3) 加入 v4 的 33K AI 样本；4) 写并行 AI 生成脚本（generate_ai_parallel.py，10 路并发 ThreadPoolExecutor）；5) 装 openai 包、修环境变量问题；6) 启动 5 worker 并行 AI 生成，~25 样本/分钟
- **结果**：dataset_v6.jsonl 目前 431K+ 样本（398K human + 33K AI）。AI 并行生成 ~24K 样本在后台运行中，预计 ~16 小时完成
- **决策**：不追求原计划的 300K AI（需 600 小时序列调用），改为 ~24K 高质量多域 AI（覆盖 42 个域含法律/专利/宗教/哲学/教材/广告等薄弱域）。总计 ~455K 足够训练 v6（v5 用 69K 就到 97.4%）

### [2026-04-06 02:00:00] Turn 6-10 〈opus-0405f〉（跨日汇总）
- **执行**：
  1. AI 并行生成提速：20→50→100→500→1000 workers，最终 2850/分钟完成 215K AI 文本
  2. 对抗文本生成：500 workers，72K adversarial（7 种攻击类型）
  3. 总数据集：685K 样本（398K human + 215K AI + 72K adversarial）
  4. RunPod 训练尝试失败：4090 eval bug → 5090 sm_120 不兼容 → L40S CUDA 驱动不兼容 → 4090 重启后被终止
  5. Colab A100 路线：Playwright 操作 Colab → 设 A100 runtime → Terminal 操作 → git clone → Google Drive 上传数据集 → notebook cell 挂载 Drive → 训练启动
  6. 对抗性架构审查：派 Agent 做数据/设计/假设层审查，发现 13 条问题（C3/H4/M4）
  7. 修复 M4：删除 synthetic_clinical_notes（AI 数据被错标为 human）
- **结果**：DeBERTa v6 在 Colab A100 40GB 上训练中（685K 数据集, binary + DANN）。审查报告已出
- **决策**：C1（单模型 AI 数据）用户决定不修。M4 已修。训练继续，完成后搭建独立 holdout 集验证

### [2026-04-05 08:41:47] Turn 1 〈opus-0405h〉
- **用户**：要求三视角（UNC 学生/产品设计师/YC 合伙人）对 CourseHub 进行残酷诚实的产品评审
- **执行**：用 Playwright 实际访问 https://course-hub-sable.vercel.app，测试了 dashboard、new-course（粘贴 COMP 110 大纲并生成预览）、login、question bank、settings，以及 375px 移动端视图。检查了 console errors 和 network requests
- **结果**：发现核心 bug——`/api/preview/learning` 返回 500 错误，导致 Study Preview 和 Question Preview 永久加载中。完成三人视角各 5 条观察 + 收敛到唯一优先级建议：修复 API 并让 guest 用户无需注册即可回答问题
- **决策**：评审基于 guest 流程（未测试登录后体验），因为 guest 体验是用户转化的关键瓶颈——如果第一印象是坏的，后面的功能再好也没意义

### [2026-04-05 09:06:01] Turn 1 〈sonnet-0405d〉
- **用户**：为 CourseHub 构建完整的 AI 课程生成系统，让每个知识点都有真正的教学内容
- **执行**：(1) `src/types.ts` 新增 `Lesson` interface；(2) `src/lib/schemas.ts` 新增 `generatedLessonSchema`；(3) `src/lib/ai.ts` 新增 `generateLesson` Pipeline 5（调用 Qwen，返回 markdown 教学内容 + key_takeaways + examples）；(4) 新建 `supabase/migrations/008_lessons.sql`（lessons 表 + RLS + 两个索引）；(5) 新建 `src/app/api/courses/[id]/lessons/route.ts`（GET 列出课程所有 lesson，POST 批量生成缺失 lesson，幂等跳过已有的）；(6) 新建 `src/app/course/[id]/learn/page.tsx`（分步骤 lesson viewer，进度条、markdown 渲染含代码块、key takeaways、上/下一课导航）；(7) `src/components/CourseTabs.tsx` 新增 Learn 标签页置首，Practice 图标从 BookOpen 改为 PenLine；3 次 commit + push
- **结果**：TypeScript 编译零错误；3 commits 已 push 到 main（5147789、9341cc1、9ca5308）
- **决策**：Practice tab 原用 BookOpen 图标与新 Learn tab 图标冲突，改用 PenLine；`generateLesson` 定名为 Pipeline 5，原 Pipeline 5（study notes）顺移为 Pipeline 6，保持 ai.ts 命名一致性

### [2026-04-05 09:22:58] Turn 1 〈opus-0405i〉
- **用户**：调研秘塔「今天学点啥」和 Google NotebookLM，与 CourseHub 做三方对比
- **执行**：WebSearch 12 次 + WebFetch 3 次，覆盖两款产品的功能/定价/互动/STEM 能力/局限性。读取 CourseHub 设计 spec（course-video-design.md）和技术调研（course-video-research.md、course-video-tech-research.md）。生成 `docs/research/2026-04-05-mita-notebooklm-coursehub-comparison.md`
- **结果**：完成 5 大维度对比（输入方式/输出形式/互动性/内容处理/平台分发），明确 CourseHub 核心护城河是 STEM 板书推导动画（LaTeX 逐步展开 + 图论可视化），提出 P0-P3 功能借鉴优先级和"不应做"清单
- **决策**：CourseHub 应定位为"STEM 板书推导视频的 YouTube"，不与秘塔/NotebookLM 在通用学习工具领域竞争；P0 是支持用户上传来源（PDF/讲义），因为这能将价值提升 10 倍

### [2026-04-05 09:29:24] Turn 1 〈opus-0405h〉
- **用户**：询问什么在占用硬盘空间
- **执行**：多轮 `du -sh` 扫描 home 目录、Library、.cache、Application Support、Containers、Downloads，定位所有大目录
- **结果**：磁盘 460GB 已用 457GB（仅剩 2.8GB）。最大占用：HuggingFace 缓存 71GB、Claude vm_bundles 11GB、Downloads 19GB、Library/Caches 11GB、Google 6.7GB、Docker 5.1GB、QQ音乐 4GB、npm 4GB
- **决策**：无重大决策

### [2026-04-05 09:40:22] Turn 2 〈opus-0405i〉
- **用户**：工作区里散落了很多 PNG 截图，要求整理
- **执行**：全局扫描 PNG 文件分布。发现 .playwright-mcp/ 有 80 个自动截图（669MB），根目录有 54 个调试截图（11MB）。删除 .playwright-mcp/ 全部缓存，将根目录 54 个 PNG 按项目归类到 docs/screenshots/{toefl,signal-map,ai-text-detector,runpod}/
- **结果**：回收 669MB 磁盘空间，根目录清零 PNG，54 个截图归入 4 个项目子目录
- **决策**：按项目名前缀自动分类，而非手动逐个判断；.playwright-mcp 是可再生缓存直接删除

### [2026-04-05 09:43:44] Turn 30 〈sonnet-0405b〉
- **用户**：为 CourseHub 实现 Knowledge Tree 功能（视觉掌握度成长树），提供完整组件代码
- **执行**：创建 `course-hub/src/components/KnowledgeTree.tsx`（SVG 可视化，掌握度驱动节点大小/颜色/脉冲动画/进度环）；创建 `course-hub/src/app/course/[id]/tree/page.tsx`（Server Component，聚合 outline_nodes/questions/attempts/lessons）；在 `CourseTabs.tsx` 中导入 TreePine 图标并插入 Tree tab（Learn 之后）；`tsc --noEmit` 零错误；commit 142c524 + push
- **结果**：`/course/[id]/tree` 路由上线，KP 节点按掌握度状态变化（untested 灰→weak 红→reviewing 黄→mastered 绿），mastered 节点有脉冲光晕动画，顶部统计栏显示整体完成百分比
- **决策**：从 `getNodeStyle` 签名移除未使用的 `rate` 参数避免 TS lint 警告；Tree tab 放在 Learn 之后，符合「先学→看知识地图进度」的自然使用流

### [2026-04-05 10:00:37] Turn 31 〈sonnet-0405b〉
- **用户**：为 CourseHub 添加中英文 i18n 支持，方案完整指定（JSON 字典 + React Context + localStorage）
- **执行**：创建 `course-hub/src/lib/i18n.tsx`（完整双语翻译字典 + I18nProvider + useI18n hook）；在 `src/app/layout.tsx` 包裹 I18nProvider；`src/components/Sidebar.tsx` 添加语言切换按钮 + 替换所有 nav label 为 `t()` 调用（桌面+移动端）；`src/app/settings/page.tsx` 添加语言部分到 Preferences + 替换所有硬编码文字；`tsc --noEmit` 零错误；commit be14e04 + 0d556c2 + push
- **结果**：中英文实时切换可用，状态持久化到 localStorage，nav bar 右侧「中文/EN」切换按钮，Settings > Preferences 有语言选择器
- **决策**：nav 切换按钮放在 Settings 链接左边；移动端 dropdown 也加了切换项；dashboard/course 页面等 Server Component 页面暂不处理（MVP 范围，按规格执行）

### [2026-04-05 10:07:05] Turn 32 〈sonnet-0405l〉
- **用户**：为 CourseHub 实现首次访问 onboarding wizard（5 步：语言/身份/学期/目标/完成），规格完整指定
- **执行**：创建 `course-hub/src/lib/onboarding.ts`（OnboardingPreferences 接口 + localStorage CRUD）；创建 `course-hub/src/components/OnboardingWizard.tsx`（全屏覆盖层，5 步卡片，进度条，浏览器语言自动检测，i18n 双语）；创建 `course-hub/src/components/OnboardingGate.tsx`（client guard，mounted 后检查 localStorage，未完成则显示 wizard）；修改 `src/app/layout.tsx` 包裹 OnboardingGate；`tsc --noEmit` 零错误；commit e65dd34 + 858ae93 + push
- **结果**：新用户首次访问触发全屏向导，完成后写入 `coursehub.onboarding` 并调用 `setLocale` 立即切换语言；返回访问直接进主界面
- **决策**：`OnboardingGate` 在 mounted 前 render children（而非 null），避免 SSR hydration 不匹配；onboarding 存储 key 与 i18n locale key 分离，互不干扰

### [2026-04-05 10:14:43] Turn 33 〈sonnet-0405m〉
- **用户**：CourseHub 两项升级：(1) 用 `ts-fsrs` 替换手写 FSRS；(2) 用 `react-markdown` + KaTeX + syntax highlighting 替换手写 Markdown 渲染
- **执行**：`npm install ts-fsrs react-markdown remark-gfm remark-math rehype-katex rehype-highlight`；完全重写 `src/lib/spaced-repetition.ts` 为 ts-fsrs wrapper；修改 `QuestionCard.tsx` import Rating enum 并更新 `updateCard` 调用；修改 `review/page.tsx` 使用 `Rating.Again/Hard/Good/Easy`；创建 `src/components/MarkdownRenderer.tsx`；更新 `learn/page.tsx` 替换手写 split/map 为 `<MarkdownRenderer>`；修复 `Rating.Manual` 索引类型错误（cast 为 `1|2|3|4`）；`tsc --noEmit` 零错误；commit e0b9279 + ec143d6 + push
- **结果**：FSRS 升级到真实 21 参数算法；Markdown 渲染支持 GFM / LaTeX 数学公式 / 代码高亮；两个 commit 均已 push
- **决策**：ts-fsrs `scheduler.repeat()` 返回 `IPreview`，不接受 `Rating.Manual(5)` 索引，用类型断言 `rating as 1|2|3|4` 绕过，代价是调用方不能传 Manual（实际场景不需要）

### [2026-04-05 10:15:04] Turn 33 〈sonnet-0405m〉
- **用户**：为 CourseHub 实现两个功能：(1) 考试日期感知 + 倒计时组件；(2) Anki 导出（tab-separated txt）
- **执行**：创建 `supabase/migrations/009_exam_dates.sql`（exam_dates 表 + RLS）；在 `src/types.ts` 追加 `ExamDate` 接口；创建 `src/app/api/courses/[id]/exams/route.ts`（GET/POST/DELETE）；创建 `src/app/api/courses/[id]/export-anki/route.ts`（GET，生成 Anki-importable txt）；创建 `src/components/ExamCountdown.tsx`（倒计时面板，urgency 颜色，add/delete）；修改 `src/app/course/[id]/page.tsx`（添加考试查询、ExamCountdown 组件、Download 按钮）；`tsc --noEmit` 零错误；两次 commit（8384a7e + f1eb266）+ push
- **结果**：课程页面右栏显示 Upcoming Exams 面板；3天内红色警告，7天内橙色；头部动作区新增下载图标触发 Anki 导出
- **决策**：Anki 用 tab-separated txt 而非 .apkg（后者需要构建 SQLite ZIP，复杂度高且无必要）；DELETE 路由未使用 params（exam_id 从 body 读），与规格一致

### [2026-04-05 10:44:48] Turn 34 〈sonnet-0405n〉
- **用户**：全面国际化 CourseHub，所有主要页面和组件从硬编码英文迁移到 i18n
- **执行**：(1) 读取 `src/lib/i18n.tsx` 确认现有键位；(2) 向 en/zh 字典追加 80+ 新键（review.*, studyTask.*, questionCard.*, exam.*, streak.*, bank.*, practice.* 等）；(3) 创建 `src/components/T.tsx`（Server Component 辅助，`<T k="key" />`）；(4) 更新 14 个文件：dashboard/page.tsx、new-course/page.tsx、login/page.tsx、learn/page.tsx、practice/page.tsx、review/page.tsx、library/page.tsx、bank/page.tsx、tree/page.tsx（Server Component 用 T）、CourseTabs.tsx、QuestionCard.tsx、StudyTaskList.tsx、ExamCountdown.tsx、StreakBadge.tsx；(5) `tsc --noEmit` 零错误；(6) 分两次 commit（e024260 + 9dad1f3）+ push
- **结果**：切换语言后所有已更新页面即时变为中文，TypeScript 零报错
- **决策**：Server Components 通过 `<T k="..." />` 客户端组件渲染翻译（避免将页面转为 Client Component）；StudyTrackerPanel 的 title/description 仍为英文硬编码（该组件接受 string prop，改造成本不值当）

### [2026-04-05 10:58:16] Turn 35 〈sonnet-0405o〉
- **用户**：为 CourseHub 添加语言感知课程重新生成功能，要求 AI 函数接受 language 参数、新建 regenerate API、修改 generate API、新建 RegenerateButton 组件并集成到课程页
- **执行**：(1) 读 `src/lib/ai.ts`、`src/app/api/courses/[id]/generate/route.ts`、`src/app/course/[id]/page.tsx`、`src/lib/i18n.tsx` 理解现有架构；(2) `ai.ts` 5 个函数（parseSyllabus, parseSyllabusText, generateStudyTasks, generateQuestionsFromOutline, generateLesson）各增加可选 `language?` 参数，在 prompt 末尾追加语言指令；(3) 创建 `src/app/api/courses/[id]/regenerate/route.ts`（翻译 outline_nodes → 重生成 tasks+questions+lessons）；(4) 修改 `generate/route.ts` 从 body 读取 language 并传给 AI 调用；(5) 创建 `src/components/RegenerateButton.tsx`（用 `useI18n` 获取 locale）；(6) `course/[id]/page.tsx` 引入 RegenerateButton 并插入 actions 区；(7) `tsc --noEmit` 零错误；(8) 两次 commit (7f8d8c5 + f1fdbec) + push
- **结果**：功能完整上线，TypeScript 零报错，2 commits 已推送

### [2026-04-05 11:29:51] Turn 36 〈sonnet-0405p〉
- **用户**：修复 CourseHub new-course 流程中 Playwright 检测到的 15 处硬编码英文字符串，要求全部迁移到 i18n
- **执行**：(1) 读取 `src/lib/i18n.tsx`、`src/app/new-course/page.tsx`、`src/components/OutlinePreview.tsx`、`src/components/OutlineTree.tsx`；(2) 向 en/zh 字典追加 42 个新键（newCourse.kicker/step/placeholder/outlinePreview/guestExplore/authSave/studyPreview/studyPreviewTitle/buildingTasks/generatingQuestions/questionPreviewEmpty/guestMode/guestReady/signInTab/tryAnother/loginToSave/topSections/savingCourse/savingOutline/redirecting + gen.step1-10/elapsed/slowWarning + outline.week/chapter/topic/knowledgePoint）；(3) new-course/page.tsx：提取 GENERATION_STEP_DURATIONS 为顶层常量，将 GeneratingStatus 重构为使用 useI18n 动态生成 steps 数组，替换 JSX 中所有硬编码字符串（）；(4) OutlinePreview.tsx：常量从 label 改为 labelKey，TreeNode 增加 useI18n，用 t(typeDisplay.labelKey) 渲染；(5) OutlineTree.tsx：引入 useI18n，typeLabel 改为动态 t() 调用；(6) tsc --noEmit 零错误；(7) 2 commits (2d36ffa + d5259bc) + push
- **结果**：TypeScript 零错误，新建课程流程全面国际化，2 commits 已推送至 main
- **决策**：GeneratingStatus 中 steps 数组在渲染时动态构建（每次 re-render 重建），而非 useEffect 依赖——代价是微小性能开销，但保证语言切换即时生效且无 stale closure 问题
- **决策**：regenerate 端点用动态 import 复用 qwen 实例避免循环引用；outline 翻译失败时 continue（容错设计）；RegenerateButton 读完 locale 后直接调 API，reload 页面刷新数据

### [2026-04-05 11:37:30] Turn 37 〈opus-0405i〉
- **用户**：对 CourseHub 执行完整视觉系统重构，以 Linear/Raycast/Arc Browser 为参照，替换 globals.css、更新 Sidebar/OnboardingWizard/CourseCard 组件
- **执行**：(1) 读取 globals.css（355行）+ Sidebar.tsx + OnboardingWizard.tsx + CourseCard.tsx 理解现有设计系统；(2) 全面重写 globals.css：替换所有 CSS 变量（accent #5b6cf0→#4f46e5、text-primary #1a1a2e→#111111、新增 --accent-muted/--shadow-sm/md/lg、--radius-sm/md/lg 三级）、body 从 gradient 改为 solid #faf9f7、line-height 1.7→1.5、letter-spacing -0.01em、所有 border-radius 统一（panel 24→16px、button 18→10px、input 18→10px、tab 999→8px、badge 999→6px、empty 28→12px、segmented 20→12px）、progress-track 0.55rem→4px、page-enter 280ms→200ms translateY 8→4px、删除 backdrop-filter、添加 .ui-panel:hover shadow lift；(3) Sidebar.tsx：NavLink active 从 accent 实色背景改为 accent-light 浅色+accent 文字色、rounded-xl→8px、增大横向 padding px-4→px-5/px-8、logo 加 tracking-tight -0.02em；(4) OnboardingWizard.tsx：所有 rounded-2xl→12px、accent 选中背景 rgba→var(--accent-light)、CTA button→10px、progress bar h-1.5→h-1；(5) CourseCard.tsx：icon 容器 rounded-2xl→10px h-12→h-11、arrow 容器 rounded-full→10px h-10→h-9；(6) tsc --noEmit 零错误；(7) commit 8cc592a + push
- **结果**：4 文件 183 行新增/125 行删除，TypeScript 零报错，已推送 main
- **决策**：不改其他组件的内联 rounded-[22px]/[24px]（范围太大留下次处理）；.ui-tab-active 改为浅色 accent-light 背景+accent 文字而非实色填充（更克制、更 Linear 风格）；删除 backdrop-filter（性能成本高于视觉收益）

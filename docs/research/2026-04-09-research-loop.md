# Research Loop Report — 2026-04-09 07:02

## Session Context

Last commit: `66300dc` — CourseHub 2.0 watercolor theme system + full UI redesign (Apr 8, 49 files changed).  
No new commits since April 8. No user messages this session — this is an automated research-loop run.

---

## Codebase State Summary

| Project | Last Activity | Status |
|---------|--------------|--------|
| CourseHub (`course-hub/`) | Apr 8 — 2.0 UI redesign deployed | Active, post-redesign |
| TOEFL (`TOEFL/`) | ~Apr 4 — 275k dictionary + letter grades | Stable, no new work |
| Chronicle (`chronicle/`) | No recent commits in this workspace | Dormant |

---

## Pending Improvements Identified from Previous Research

### From April 6 research (`learn-module-improvements.md`) — not yet implemented:
1. **Vercel AI SDK `streamObject` array mode** — highest ROI, streams lesson chunks with zero infrastructure. The `qwen-ai-provider` or `@ai-sdk/openai` with DashScope baseURL can replace the current fetch-based generation. This was the #1 recommendation and has not been done yet.
2. **`max_tokens` per chunk cap (~800 tokens)** — simple API param change, cuts 40-60% generation time on runaway outputs.
3. **Hybrid interleaved practice** — 1 blocked + 1 interleaved question per 2-3 chunks, mixed review at end. Hwang 2025 approach, g=0.42 effect size.

### From April 8 research (`theme-system-research.md`) — quick wins:
1. **`document.startViewTransition()`** around `setTheme()` — 3 lines of code, polished theme transitions. Safari 18+ and Chrome 111+ supported.
2. **Audit `--text-muted` contrast** — APCA Lc ≥75 required for body text. Pastel light themes are at risk. Darken by ~10-15%.
3. **Replace `rgba()` in accent-light/accent-muted with `color-mix(in oklch, ...)`** — already zero rgba() in pages, but check globals.css theme definitions.

---

## Suggested Next Priority (if user returns)

**Option A — Feature work**: Implement `streamObject` streaming for lesson generation. This would make the Learn page feel noticeably faster and is the most impactful pending feature.

**Option B — Polish work**: Fix theme accessibility (text-muted contrast audit) + add `startViewTransition()`. Lower risk, builds on the 2.0 redesign that just shipped.

**Option C — TOEFL deployment**: The TOEFL project has been stable for weeks. It could be deployed as a static Vite SPA to Vercel at zero cost. No active development required.

---

## Risk Flags

- **CourseHub is not deployed** — all development is on localhost:3002. If a production URL exists, it's not documented in STATUS.md. Deployment to Vercel should be a near-term priority.
- **TOEFL also not deployed** — documented as "未部署" in STATUS.md.
- **Chronicle project** has no recent activity — unclear if it's abandoned or just paused.

---

*Generated by automated research-loop agent at 2026-04-09 07:02:30*

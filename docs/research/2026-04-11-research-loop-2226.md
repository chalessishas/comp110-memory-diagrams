# Research Loop Report — 2026-04-11 22:13

## Status Snapshot

**Git:** Clean working tree (only `docs/chronicle/2026-04-11.md` modified — in-progress chronicle).  
**Latest commit:** `786351b` — chronicle Turn 143.  
**TypeScript:** Previously verified clean, no code changes since.  
**TODOs/FIXMEs:** 0.

---

## New User Requests / Questions

None detected. No user messages in this session beyond the automated Research Loop trigger.

---

## Carried-Forward Improvement Backlog

These were identified in the 21:47 hourly report and remain unactioned:

### 1. `courseConceptsAtLevel2OrAbove` hardcoded to 0 (MEDIUM value, LOW complexity)

Blocks some students from reaching "proficient" mastery unfairly.

**Fix path:** `course-hub/app/api/attempts/route.ts` — after attempt write, count `element_mastery WHERE course_id = :id AND element_name = '_overall' AND mastery_level >= 2`, pass count into `courseConceptsAtLevel2OrAbove`.

### 2. SSE Lesson Streaming — smoke test (HIGH risk)

No live test of `learn/page.tsx` SSE pipeline against DashScope. Needs manual verification on a Vercel preview before next demo.

### 3. Vercel Auto-Deploy GitHub Action (LOW complexity, HIGH ops value)

Manual `npx vercel deploy --prod` each time. A `.github/workflows/deploy.yml` with `paths: ['course-hub/**']` would close this gap.

---

## New Proposals This Run

### 4. Rate-limit feedback in `generate-questions` button (DONE in session)

Already merged as `2f6763e`. No action needed.

### 5. FSRS Review Session — empty-state UX gap

`review/page.tsx` shows nothing when a user has no cards due. Currently renders an empty container. Suggest a congratulatory state: "All caught up! No cards due today." with next-review countdown.  

**Effort:** ~30 min. Add `nextDueAt` to the `/api/review/session` response, render a congratulatory message when `cards.length === 0`.

### 6. Mobile viewport: canvas drag on `chronicle` project

`chronicle/` uses Canvas 2D with mouse events only. Touch events (`touchstart/touchmove/touchend`) are not wired. On iPhone, the canvas is uninteractive.  

**Effort:** ~2 hours. Add `touchstart → mousedown`, `touchmove → mousemove`, `touchend → mouseup` shims in `canvas.ts`.

---

## Verdict

No blocking issues. User is not present. Recommend these for the next active session in priority order:
1. `courseConceptsAtLevel2OrAbove` fix (breaks mastery ceiling for some users)
2. FSRS empty-state UX (polish, 30 min)
3. Vercel auto-deploy (ops hygiene)
4. Touch support for chronicle canvas (mobile reach)

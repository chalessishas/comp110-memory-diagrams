# Research Loop — 2026-04-12 13:36

## Current State

All three ETS scorer gaps have been fully implemented (commit `d5b8255`, 13:35):
- **Gap #1 (Relevance)**: `relevance.js` — keyword overlap with stem normalization, 8% weight
- **Gap #2 (Peer Engagement)**: Named-peer detection in `organization.js` — full 20-student name list
- **Gap #3 (Syntactic Variety)**: 5 pattern types in `style.js`

Reading Pack "NEXT UP" feature implemented (`2b6f8a2`) — first incomplete module highlighted.

**No new user questions in this session.** All research-file recommendations from `2026-04-12-next-improvements.md` are now landed.

---

## New Research Findings (from Turn 151, 12:57)

### Weight Calibration Discrepancy

The Turn 151 research found the current scorer has a measurable divergence from published ETS e-rater weights:

| Dimension | ETS Published | Current | Delta |
|-----------|--------------|---------|-------|
| Organization | 32% | 32% | ✅ |
| Development | 29% | 24% | -5% (compensated by relevance) |
| Mechanics | 10% | 10% | ✅ |
| Grammar | 7% | 7% | ✅ |
| Vocabulary | ~22% (3 features) | 14% | -8% |
| Style | **3%** | **5%** | +2% overcounting |
| Relevance/Task | 8%+ (human score) | 8% | ✅ |

**Actionable fix**: Drop Style 5% → 3%, add 2% to Vocabulary (14% → 16%). 2-line change in `index.js`, no other files affected. This better matches ETS calibration without touching any scoring logic.

### localStorage Versioning (Research Complete)

Wrapper-with-version-field pattern is recommended: wrap each `JSON.parse` to detect `{ v, data }` vs raw value. On write, always output `{ v: 1, data: payload }`. Zero key renames, no global migration manifest. Estimated effort: ~60 min for all 8 keys.

### ThemeContext Split (Research Complete)

Split into `ThemeValueContext` + `ThemeDispatchContext`. Toggle button uses dispatch only — no re-renders on theme change for that component. All 15+ display consumers unchanged (just update `useTheme()` hook to read from value context). Estimated: 30 min.

---

## Recommended Next Autonomous Tasks (Priority Order)

1. **Style weight fix** (2 lines, zero risk) — directly improves ETS alignment
2. **WritingResult**: add "Relevance" label check — confirm the 7th bar renders correctly in UI
3. **ThemeContext split** (30 min) — performance polish
4. **localStorage version wrapper** (60 min) — data integrity

---

## Items Requiring User Input

- Deployment (Vite static / Netlify / Vercel)
- User authentication (Supabase Auth vs passkey vs none)
- Notebook backend persistence (Supabase vs stay localStorage)
- Claude API scoring (needs ANTHROPIC_API_KEY decision)

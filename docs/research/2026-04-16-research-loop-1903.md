# Research Loop — 2026-04-16 19:03

## System State

**TOEFL Writing Scorer**: Stable. 10/10 calibration. S3=3.30 / S4=4.30 / S5=4.90. Gap 4→5 = 0.60 confirmed as architectural ceiling of rule-based scorer. HOLD maintained.

**TOEFL Reading**: 3 standalone academic passages now live (Urban Agriculture, Deep Ocean, Plate Tectonics) + Pack 6 modules. All correctly connected in Reading.jsx.

**Course-Hub**: Awaiting user authorization. Discord sent 18:17 + 18:36. No response. HIGH CVE Next.js 16.2.2 (CVSS 7.5). Doing only monitoring until user responds.

## Improvement Candidates

### Priority 1: Writing Feedback Readability (Path 3 from Turn 15)
Current state in `WritingResult.jsx:148`:
- Error strings shown raw, clipped to 2 per dimension (`errors.slice(0, 2)`)
- No indication that more errors exist beyond the 2 shown
- Suggestion section is disconnected from dimension bars — user can't see which bar triggered which suggestion

**Proposed fix (low-risk, ~20 lines):**
- Add "...and N more issues" link below each dimension when `errors.length > 2`
- Label each suggestion with its source dimension (e.g., `[Grammar]`, `[Development]`)
- These changes touch only `WritingResult.jsx`, zero scorer logic

### Priority 2: Reading — 4th Academic Passage
Only 3 standalone passages. TOEFL iBT Reading section has 2–3 passages per test, 10 questions each. A 4th passage on a different topic (e.g., Economics / Psychology / Astronomy) expands variety.

**Cost**: ~140 lines passage data + minimal wiring (2 lines in Reading.jsx + ReadingHome.jsx)

**Risk**: Low — follows exact same pattern as deep-ocean.js and plate-tectonics.js.

### Priority 3: Dashboard — completion rate widget
Home dashboard shows 3 writing module cards. No cross-module completion progress bar. A simple "X/6 tasks completed this week" counter would improve user engagement loop.

**Cost**: ~15 lines in Home.jsx using existing `toefl-completion-history` key.

## Meta-Observations

- Turn 15 lesson confirmed: "LLM first-guess statistics unreliable, Grep-verify every claim." Applied 3 times in today's session with correct reversals.
- writing/scorer suggestions always generated (each module has a `.suggest(analysis)` function returning non-empty arrays). The `score.suggestions.length > 0` guard in WritingResult is never false — could be removed for clarity, but harmless.
- `errors.slice(0, 2)` cap in WritingResult hides real error count — misleads user into thinking only 2 grammar issues exist. This is a silent UX bug.

## Recommended Next Action

Implement Priority 1 (feedback readability) — directly addresses Path 3, no architectural risk, pure UI change, can be done without user sign-off.

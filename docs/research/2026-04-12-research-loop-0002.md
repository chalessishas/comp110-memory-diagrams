# Research Loop — 2026-04-12 00:02

## 当前项目状态扫描

### TOEFL Practice
- **最新 commit**: `ee8b8c0` — STATUS.md 更新（深色模式完成）
- **前一 commit**: `da5fb14` — ThemeContext 深色模式全接通（Turn 157 完成）
- **ErrorBoundary**: `bb9d7ff` — 防白屏兜底（已完成）

**待完成任务（可自主执行）**：
1. Settings 面板联动 —— 计时器显隐 toggle、快捷键提示 toggle（前端纯改动，零风险）
2. 学习计划动态化 —— 当前是静态占位，可接入 localStorage 进度数据
3. Reading.jsx 拆分 —— 文件 >1200 行，可拆出 PassageDisplay、QuestionPanel、ProgressBar 等组件

**需要用户的任务**：
- 笔记本/生词本持久化 → 需要 Supabase 或后端方案决策
- 用户认证 → 需要 Supabase 配置/密钥
- 部署上线 → 需要用户确认部署平台

### Chronicle
- 2026-04-12 chronicle 文件仅有 Turn 157（00:01:16）一条记录
- 本次 research loop 属于同一会话，应追加 Turn

### Signal-Map / ai-text-detector / chronicle
- 无新 commit，无活跃任务

## 改善建议（供下次用户在线时参考）

### 高优先级（可自主）
1. **TOEFL Settings 面板联动** — 现有 Settings UI 的 timer display / keyboard shortcuts toggles 是空操作，接通只需在 ThemeContext 旁边加一个 SettingsContext（isTimerVisible、isShortcutsVisible），各页面消费。预计 ~40 行代码。

2. **TOEFL 深色模式覆盖率检查** — Reading.jsx、Discussion.jsx、Email.jsx、BuildSentence.jsx 是否已消费 useTheme？Turn 157 只覆盖了 Home + Writing + CompleteWords，剩余页面可能仍是静态 lightColors。

### 中优先级（需用户）
3. **TOEFL 部署** — Vite SPA 部署到 Vercel/Netlify 是零配置的，但需要用户提供仓库权限或 CLI 登录。

4. **笔记本持久化** — 目前 localStorage 方案已够用，不需要 Supabase。可升级为 IndexedDB（更大存储限制）。无需用户介入。

## 结论
无新用户请求。TOEFL 深色模式刚完成，下个自主可做的任务是检查并补全深色模式覆盖率（Reading 等页面），以及接通 Settings 面板 toggle 联动。

# Research Loop — 2026-04-20 21:02

## Status Snapshot

- **Tests**: 2013 passed (all green), latest commit `1c36d0b` P117
- **P117**: firewatch/flint/meteo/bubble S3 implementation + 23 new tests

## S3 Coverage Gap Analysis

### Operators WITH S3 implementation: 73
All 73 have corresponding `test_v2_*_s3.py` files. Coverage is complete for implemented operators.

### Operators WITHOUT S3 (23 remaining):
```
akkord, beanstalk, bena, deepcolor, durnar, erato, ethan, fang,
glacus, gravel, manticore, mayer, mulberry, pinecone, quercus,
robrta, rope, shaw, sora, spuria, toknogi, vigna
```

### Priority Recommendation

**Tier 1 — High value 5★ operators:**
- `erato` — has pre-attack hook infra, just needs S3 re-implementation (Song of Dreams)
- `manticore` — Specialist, tail-strike mechanic
- `spuria` — Dollkeeper split-body S3

**Tier 2 — 4★ batch (low complexity):**
- `fang`, `gravel`, `beanstalk`, `vigna`, `rope`, `shaw` — straightforward ATK/speed buffs

**Tier 3 — Utility/Niche:**
- `akkord`, `sora`, `mulberry`, `pinecone`, `quercus`, `toknogi` — various archetypes
- `deepcolor`, `mayer`, `bena`, `robrta`, `durnar`, `ethan` — less commonly used

## Architectural Observation

The `glacus` abjurer test file (`test_v2_glacus_abjurer.py`) covers the abjurer archetype behavior but `glacus.py` only implements S2. The test is for archetype mechanics, not S3. This is intentional.

## Next Session Recommendation

Batch implement 6 low-complexity 4★ S3s in one pass: `fang/gravel/beanstalk/vigna/rope/shaw` — all should be simple ATK/ASPD/DP buffs with no new infra needed. Then tackle `erato` S3 (pre-attack hook already exists).

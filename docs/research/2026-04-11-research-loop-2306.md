# Research Loop Report — 2026-04-11 23:06

## Status Snapshot

**Git:** Clean working tree.  
**Latest commit:** `18b71d2` — chronicle Turn 150.  
**New user questions:** None. Automated research loop.

---

## Stale Backlog Item Found

### ~~1. courseConceptsAtLevel2OrAbove hardcoded to 0~~ — RESOLVED (stale)

Previous research loops listed this as backlog. **It has already been fixed.**

Evidence: `src/app/api/attempts/route.ts` lines 140–200 properly implement the cross-concept gate:
- Queries `outline_nodes` to check if a KP has child KPs (`hasDownstreamDependents`)
- Propagates `has_cross_concept_correct = true` to parent KPs when a child answer is correct
- `mastery-v2.ts:106` uses `hasCrossConceptCorrect || !hasDownstreamDependents` — not hardcoded

This was shipped in commit `39d77d6` (Turn 140). **Remove from active backlog.**

**Side-effect:** `STATUS.md` line 21 still says `courseConceptsAtLevel2OrAbove = 0 (crossConceptOk always passes)` — this is now inaccurate and could mislead future sessions.

**Recommendation:** Update STATUS.md Phase 6 table to reflect the fix.

---

## Remaining Active Backlog

### 2. SSE Lesson Streaming — Live Smoke Test Needed (HIGH risk)

`learn/page.tsx` SSE client is solid: correct `parseSSE()`, buffered reader, proper event routing (`outline`, `lesson`, `chunk`, `done`, `error`). `generate-one/route.ts` has `maxDuration = 60` and SSE helper.

**Gap:** No confirmed live test against DashScope + Vercel together. Local dev may mask issues if DashScope response is slower in production.

**What to test:**
1. Click a KP without an existing lesson on the Vercel prod URL
2. Verify chunks stream progressively (not blocked until all 4 done)
3. Verify lesson ID is persisted (page reload shows existing lesson, not regenerated)
4. Check Vercel function logs for any timeout or abort errors

**Effort:** 10 min manual test. No code change needed unless bugs found.

### 3. Vercel Auto-Deploy (LOW complexity, HIGH ops value)

Still manual: `npx vercel deploy --prod` each time. A `.github/workflows/deploy.yml` with `paths: ['course-hub/**']` would eliminate this.

**Effort:** ~20 min. Template exists at `docs/research/2026-04-09-coursehub-vercel-deployment.md`.

### 4. Chronicle Canvas — Mobile Touch Support

`chronicle/` canvas uses mouse events only. `touchstart`/`touchmove`/`touchend` not wired. Uninteractive on iPhone.

**Effort:** ~2 hours. Add touch shims in the canvas event handler file.

---

## New Proposals This Run

### 5. STATUS.md Phase 6 Table — Now Inaccurate

The mastery pipeline table in `STATUS.md` still shows the approximation comment from before Turn 140:
```
- courseConceptsAtLevel2OrAbove = 0 (crossConceptOk always passes — avoids per-attempt count query)
```

This should be updated to reflect the actual implementation.

**Fix:** Add two rows to the STATUS.md mastery pipeline table:
```
| `has_cross_concept_correct` | attempts POST (cross-concept propagation) | 39d77d6 |
| `has_downstream_dependents` | attempts POST (per-attempt child KP count query) | 39d77d6 |
```
And remove/update the approximation bullet.

**Effort:** 2 min edit.

### 6. `WrongAnswerNotebook` — No Empty State

`WrongAnswerNotebook.tsx` component: if no misconceptions exist, it silently renders nothing (returns null or empty). From a UX standpoint, a first-time user sees a blank section with no explanation.

**Recommended fix:** Add an empty state: "No wrong answers recorded yet. Keep practicing!"

**Effort:** 5 min.

---

## Priority Order for Next Session

1. **STATUS.md update** (2 min, correctness — prevents future sessions from re-investigating stale issue)
2. **SSE smoke test** (10 min manual, risk validation before next demo)
3. **WrongAnswerNotebook empty state** (5 min, UX polish)
4. **Vercel auto-deploy** (20 min, ops hygiene)
5. **Chronicle mobile touch** (2 hours, mobile reach)

# Research Loop — 2026-04-12 18:03

## Session context
TOEFL project stable. 4 pack6 modules live (M1-M4). Scorer calibrated to e-rater weights. Notebook with mastery/review/export features. All 0% STATUS.md items require user decisions.

---

## Finding 1: grammar.js Performance Issue — safeWords/safePhrases Re-instantiated Per Call

`grammar.js` previously declared `safeWords` (Set, 30 items) and `safePhrases` (Array, 14 items) inside the `score()` function body. These are re-created on **every** scoring call.

**Fix applied (18:03)**: Moved both to module-level constants `SPLICE_SAFE_WORDS` and `SPLICE_SAFE_PHRASES`. Same behavior, no allocations on hot path. Build clean (76 modules).

**Impact**: Negligible at current scale (single-user, client-side), but correct hygiene.

---

## Finding 2: CLAUDE.md Scorer Weights Stale After 2026-04-12 Recalibration

The project CLAUDE.md directory listing still showed old weights:
- `vocabulary.js # 18%` → should be 14%
- `organization.js # 30%` → should be 33%  
- `development.js # 24%` → should be 28%
- `relevance.js # 8%` → should be 5%
- Dark Mode pitfall entry claimed "逻辑空置" but dark mode was fully implemented

**Fix applied (18:03)**: Updated all 4 weight annotations in CLAUDE.md directory listing. Added recalibration date. Fixed dark mode pitfall entry.

---

## Finding 3: Pack6 Module Count — 4 Modules, Not 6

STATUS.md line 54 says "Pack 6 (6 模块)" — actually 4 modules exist (pack6-m1 through pack6-m4). CLAUDE.md says "pack6.js # 6 篇阅读文章" — same inaccuracy.

**Not fixed this loop**: STATUS.md line 54 is in the "已完成" section and the "6 模块" refers to 6 question _types_ (detail, inference, vocab, purpose, negative_fact, sentence_simplification, text_insertion = 7 types actually), not 6 content modules. However line 54 reads ambiguously as 6 modules. User should clarify the intended count and whether M5/M6 are planned.

**Recommendation for user**: Clarify if Pack 6 target is 6 content modules (M5/M6 still needed) or if 4 is the final count.

---

## No-Action Items (require user decisions)

| Item | Blocker |
|------|---------|
| Listening module | User must choose: browser TTS or real audio files |
| Backend sync | Requires auth infrastructure decision |
| Vercel deployment | Requires VERCEL_TOKEN / ORG_ID / PROJECT_ID |
| Pack 6 M5/M6 | Depends on clarification above |

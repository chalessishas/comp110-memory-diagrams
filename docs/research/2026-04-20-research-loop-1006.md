# Research Loop — 2026-04-20 10:06

## 当前状态快照
- **Tests**: 1573 passed (全绿，0 failures)
- **Talents**: 100% 覆盖（所有算子已有 talent）
- **S2**: 100% 覆盖（所有算子已有 S2）
- **S3**: 12 个算子仍缺 S3（见下）

## 最新进展（本次 session：sonnet-0420AT）
- P79: 修复 medic range bug（targeting_system 对友军 heal 忽略了 range check）+ Ceylon/Fartth/Headb2/Texas2 S2 实装
- P80: Fartth S2 触发方式修正（AUTO 而非 MANUAL）
- P81: Chyue 完整实现——新增 SP-threshold talent 模式（`carrier.skill.sp >= threshold` 条件 ATK buff）+ S2 + S3（4-ammo burst）
- **barrier revert**: 本 session 尝试实装 Barrier（伤害吸收护盾）ECS 原语但最终 revert——原因见 chronicle，建议下阶段重新评估

## 下阶段：S3 补全（12 个算子）

| 算子 | 推荐 S3 机制 | 优先级 |
|------|-------------|--------|
| myrtle | SP support burst（team SP regen field） | ★★★ |
| ptilopsis | Night Cure 升级版（大 AoE heal + ATK buff）| ★★★ |
| courier | Charged Assault（force-ranged + DP burst）| ★★☆ |
| elysium | Eagle Strike（大 AoE + DP gen）| ★★☆ |
| saileach | Tactical Command（全 Vanguard buff aura）| ★★☆ |
| erato | Crescendo（chain-Arts + STUN）| ★★☆ |
| fang | Blaze Forward（block↑ + ATK+200%）| ★★☆ |
| gravel | Disappear（chain-retreat concealment）| ★★☆ |
| liskarm | Thunderbolt Field（AoE chain lightning）| ★★☆ |
| manticore | Predator's Pounce（BIND+True burst）| ★★☆ |
| beanstalk | Vine Burst（vine-deploy + BIND）| ★★☆ |
| spuria | Puppet Army（multi-clone undying）| ★★☆ |

## 具体方案建议（下一步 P82）

**推荐优先 myrtle S3**，原因：
1. Myrtle 是 Arknights 最高频使用的 Vanguard 之一，S3 "Blazing Sun" 是标志性技能
2. 涉及新 ECS 原语：team-wide SP aura + global buff，扩展现有 Ptilopsis/Elysium aura 模式
3. myrtle.py 已有 S1 SP-drip 模式（`on_tick` fractional SP），S3 可复用

**myrtle S3 Blazing Sun 设计草案**：
- `on_start`: 给全部已部署友军 +1 SP + ATK+20% aura（via BuffStack.INSPIRATION）
- `on_tick`: 每秒再次 +1 SP（全局脉冲）
- `on_end`: 移除 ATK aura，清理 SP gain
- 参数参考：duration=25s, sp_cost=30, initial_sp=15, requires_target=False

## Barrier 重试评估

Barrier 在 Arknights 中用于：Saria S3、Mountain S1、Dur-nar talent 等。本 session revert 的根因待确认（chronicle 未详细记录）。

建议在 S3 phase 完成后作为独立 P-series（P100+）处理：
- 最小 ECS 实现：`unit.barrier: int = 0`，在 `take_physical`/`take_arts` 前先消耗 barrier
- 只需修改 `unit_state.py` 的 damage calculation（约 10 行）
- 不影响现有 1573 tests

## 数值准确性待办

以下算子天赋数值为"创作性估算"，建议后续与 ArknightsGameData 核对：
- Courier: DEF+60 on block≥2（实际可能为 +70）
- Erato: crit_chance 参数
- headb2: splash_radius 1.5（待确认）
- Rope: push_distance 2（待确认等级）

## 结论

项目健康，无阻塞。主 session 可直接推进 **P82 myrtle S3**。

# Research Loop — 2026-04-12 01:03

## 状态摘要

- **最新 commit**: c501aef (00:51) — chronicle progress loop, stable
- **新用户请求**: 无（自动触发）
- **项目状态**: TOEFL 全功能稳定，所有 0% 任务均需用户介入

## 可自主执行的改进项（无需用户）

### 1. english-words.js 懒加载优化（高价值）

**问题**: 2.7MB 词典在首屏即加载，用户打开 Home/Reading 时也会触发
**当前状态**: STATUS.md 标注为 known issue
**建议**:
- 在 BuildSentence / WriteEmail / AcademicDiscussion 的 `useEffect` 中动态 `import()` 词典
- 配合 Vite `manualChunks` 确保词典被单独 chunk
- 预期效果：首屏加载减少 2.7MB，写作页面才加载词典

```js
// 懒加载示例
useEffect(() => {
  import('@/data/english-words').then(m => setWords(m.default));
}, []);
```

### 2. Progress 面板 — 数据可视化增强（中价值）

**问题**: 近 6 条 session 列表是线性展示，缺少趋势感知
**建议**:
- 用 CSS 折线图（clip-path 或 SVG）展示 6 次练习的分数趋势
- 纯 CSS/SVG 实现，零依赖
- 帮助用户直观看到进步轨迹

### 3. BuildSentence 错题本自动记录（中价值）

**问题**: 用户答错的句子没有持久化，下次练习不会重点出现
**建议**:
- 在 `handleSubmit` 里检测 `isCorrect === false`
- 将错题 id 存入 localStorage `toefl-wrong-sentences`
- 下次 `shuffleAndPick` 时优先（70%）从错题池选取
- 实现自适应练习效果

### 4. 写作分数历史 CSV 导出（低价值）

**问题**: 所有历史数据锁在 localStorage，用户无法导出分析
**建议**:
- Settings 面板增加"导出练习记录"按钮
- 生成 CSV：timestamp, type, score, breakdown
- `Blob + URL.createObjectURL` 纯前端实现

## 不建议立即做的项

| 项目 | 原因 |
|------|------|
| PWA/Service Worker | 需要 HTTPS + 部署，0% 任务先行 |
| 用户认证 | 需要后端，用户需决策方案 |
| Claude API 评分 | 需要 API key + 用户确认费用意愿 |

## 下一步推荐

**最高优先 (可自主执行)**: english-words.js 懒加载 — 用户体验最直接，代码改动最小（3-5 行）

如用户无新指令，下一个 Progress Loop 触发时将执行此项。

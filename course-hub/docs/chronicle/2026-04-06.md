## 代号索引
| 代号 | 模型 | 对话摘要 |
|------|------|----------|
| sonnet-0406a | Claude Sonnet 4.6 | 创建 mastery-v2.ts 4 级掌握度评估引擎 |
| sonnet-0406b | Claude Sonnet 4.6 | Today view — Duolingo 风格每日任务页 |
| sonnet-0406c | Claude Sonnet 4.6 | Profile view — 掌握度概览 + 弱点 + 元认知 + 本周统计 |
| sonnet-0406d | Claude Sonnet 4.6 | Learn view V2 — 知识点列表 + Chunk-Checkpoint 交错课程浏览器 |

### [2026-04-06 03:20:16] Turn 1 〈sonnet-0406d〉
- **用户**：构建 CourseHub V2 Learn view — 知识点列表 + Chunk-Checkpoint 交错课程浏览器
- **执行**：读取现有 `learn/page.tsx`（旧单体 lesson 浏览器）、`src/types.ts`（确认 `LessonChunk`/`MasteryLevelV2` 等类型已定义）、`mastery-v2.ts`（确认 `levelConfig` 导出）、`streaks.ts`（确认 `recordActivity` 签名）；重写 `src/app/course/[id]/learn/page.tsx`（两态：知识点列表 + chunk viewer 入口）；新建 `src/components/ChunkLesson.tsx`（chunk-checkpoint 交错逻辑，MCQ/text/open 三种答题类型，remediation 流程，进度条）；新建 `src/app/api/courses/[id]/lessons/[lessonId]/chunks/route.ts`（从 `lesson_chunks` 表读取，fallback 为按 `##` 切分 monolithic content）；新建 `src/app/api/courses/[id]/mastery/route.ts`（查 `element_mastery` 表，返回该课程知识点掌握度）；`tsc --noEmit` 零报错；commit `ed0e669` + `767e9ea` + push
- **结果**：4 文件创建/修改，TypeScript 编译通过，已推送到 `main`（commits ed0e669 + 767e9ea）
- **决策**：`courseId` 和 `lessonId` props 在 ChunkLesson 内目前仅作结构占位（prefixed with `_` 避免 lint），保留以备后续写入 `lesson_progress` 表；chunks API 中 `id` 参数同样 `_id` 消除未使用警告；fallback split 不写回 DB，保持 API 无副作用

### [2026-04-06 03:20:07] Turn 1 〈sonnet-0406c〉
- **用户**：构建 CourseHub V2 Profile view — 掌握度概览、持续性弱点、已克服误解、元认知准确度、本周统计
- **执行**：读取 `mastery-v2.ts`、`types.ts`、`i18n.tsx`、`CourseTabs.tsx`、`course/[id]/layout.tsx` 确认所有依赖与约定；创建 `src/app/course/[id]/profile/page.tsx`（服务端组件，查询 outline_nodes/element_mastery/misconceptions/challenge_logs/attempts 五张表）；创建 `src/components/ProfileView.tsx`（client 组件，5 个 panel：掌握度堆叠进度条、弱点列表、已克服列表、元认知准确度、本周统计）；`tsc --noEmit` 零报错；commit `ae136be` + push
- **结果**：2 文件新增（237 行插入），TypeScript 编译通过，已推送到 `main`
- **决策**：layout.tsx 已处理 auth + course 不存在重定向，page.tsx 中再次校验为防御性设计符合 spec 要求，保留；`counts.unseen` 初始化为 `totalKps` 再逐条减法，确保未记录掌握度的 KP 全归 unseen

### [2026-04-06 03:15:08] Turn 1 〈sonnet-0406b〉
- **用户**：构建 CourseHub V2 Today view — Duolingo 风格每日任务页，替换 course detail page，新建 TodayView 组件
- **执行**：读取现有 `page.tsx`（旧 outline 页）、`CourseTabs.tsx`、`StreakBadge.tsx`、`i18n.tsx`、`T.tsx` 确认所有依赖存在；用给定 spec 重写 `src/app/course/[id]/page.tsx`（服务器组件 + `buildTodayTasks` 算法，修复一处 `!false` typo → 改为直接用 `data.misconceptions`）；创建 `src/components/TodayView.tsx`（client 组件，含 `void courseId` 消除 unused 警告）；`tsc --noEmit` 零报错；commit `9c99054` + push
- **结果**：2 文件变更（318 插入 / 275 删除），TypeScript 编译通过，已推送到 `main`
- **决策**：去掉 spec 中 `T` 组件的 import（页面实际未用到，避免 unused import 报错）；`void courseId` 用于保留 prop 签名而不触发 lint；旧页面的 outline 展示逻辑全部移除，由 CourseTabs `/learn` 路由承载

### [2026-04-06 03:13:36] Turn 1 〈sonnet-0406a〉
- **用户**：为 CourseHub V2 创建 `src/lib/mastery-v2.ts`，实现 4 级掌握度评估引擎（升降级逻辑 + 防刷题保护）
- **执行**：读取 `src/types.ts` 确认 `MasteryLevelV2` 已定义（line 168），读取项目 CLAUDE.md 了解架构；创建 `src/lib/mastery-v2.ts`（225 行）；运行 `tsc --noEmit` 验证编译；`git add` + commit + push
- **结果**：文件成功创建，TypeScript 编译无报错，commit `a02ad36` 已推送到 main
- **决策**：`shouldBlockUpgrade` 的 `conceptId` 参数在函数体内未使用（原始 spec 如此），保留签名以备后续扩展（例如按概念粒度限流）；未修改 types.ts 因为 `MasteryLevelV2` 类型已存在

## 代号索引
| 代号 | 模型 | 对话摘要 |
|------|------|----------|
| sonnet-0403a | Claude Sonnet 4.6 | Tasks 10-12: Parse API, New Course Flow, Course Detail + Outline Tree |
| sonnet-0403b | Claude Sonnet 4.6 | Tasks 13-15: Questions + Attempts API, Practice Mode, Progress Tracking |

### [2026-04-03 02:45:59] Turn 1 〈sonnet-0403b〉
- **用户**：实现 CourseHub Tasks 13、14、15（Questions + Attempts API、Practice Mode、Progress Tracking + Mastery Heatmap）
- **执行**：创建 7 个文件：`src/app/api/questions/route.ts`、`src/app/api/attempts/route.ts`、`src/components/QuestionCard.tsx`、`src/app/course/[id]/practice/page.tsx`、`src/lib/mastery.ts`、`src/components/ProgressGrid.tsx`、`src/app/course/[id]/progress/page.tsx`；先读 types.ts/schemas.ts/ai.ts/FileDropzone.tsx 理解约定；运行 `tsc --noEmit` 编译通过；完成 3 个独立 commit
- **结果**：TypeScript 零报错，3 commits 成功（5dccbfc、2b2a6f2、76b908e）
- **决策**：`parseExamQuestions` 返回 `ParsedQuestion & { matched_kp_title }` 类型已在 ai.ts 中定义，直接使用无需重新声明；`calculateMastery` 对空 attempts 返回 "untested"，rate=0 的逻辑与 masteryColors 映射一致

### [2026-04-03 02:42:25] Turn 1 〈sonnet-0403a〉
- **用户**：实现 CourseHub Tasks 10、11、12（Parse API Route、New Course Flow、Course Detail with Outline Tree）
- **执行**：创建 8 个文件：`src/app/api/parse/route.ts`、`src/app/api/courses/[id]/outline/route.ts`、`src/components/OutlinePreview.tsx`、`src/app/new-course/page.tsx`、`src/components/CourseTabs.tsx`、`src/components/OutlineTree.tsx`、`src/app/course/[id]/layout.tsx`、`src/app/course/[id]/page.tsx`；先读 react-arborist 类型定义确认 `Tree` 为具名导出、`NodeRendererProps` 类型签名；运行 `tsc --noEmit` 验证编译；完成 3 个独立 commit 并 push
- **结果**：TypeScript 编译无报错，3 commits 推送成功（2a616d5、4b6e04d、c9b84a0）
- **决策**：先读 `react-arborist/dist/module/index.d.ts` 确认具名导出而非 default，避免运行时报错；`dragHandle` 的 ref 类型为 `(el: HTMLDivElement | null) => void`，直接赋给 `ref` 属性符合规范

## 代号索引
| 代号 | 模型 | 对话摘要 |
|------|------|----------|
| sonnet-0410a | Claude Sonnet | Research agent: ts-fsrs, Supabase Realtime, Next.js 15, Qwen3, FSRS研究 五项技术调研 |
| sonnet-0410j | Claude Sonnet | i18n 完整性收尾：login errors、StudyBuddy、UsagePanel、TeachBackPanel、MistakePatterns、join page |

---

### [2026-04-10 06:09:42] Turn 3 〈sonnet-0410a〉
- **用户**：Research agent 针对 CourseHub 三个专题调研：interleaving proxy signals（避免 per-attempt count query）、Next.js 16/React 19 减少 4 次串行 DB 查询、Session Summary 错误答案展示 UX（Wanikani vs Anki）
- **执行**：读取 `progress/page.tsx` 理解当前 4-query 模式，读取 `2026-04-09-research-loop.md` 获取历史上下文，写入 `docs/research/2026-04-10-research-loop-2.md`（覆盖同名旧文件）
- **结果**：三节研究报告：①interleaving 用 in-memory mastery rows 客户端 filter 替代 DB count；②Promise.all 并行化 + Postgres RPC 聚合 calibration stats；③Session Summary 加 Quick Retry 强制提取，tagging leech KPs 进 misconception
- **决策**：不做 WebSearch（题目偏向应用分析而非最新论文检索，已有足够代码上下文直接分析），优先代码可行性推导

### [2026-04-10 05:27:00] Turn 2 〈sonnet-0410a〉
- **用户**：对 course-hub 三个技术问题进行文献调研（误解消除信号、FSRS 服务端存储模式、错误答案反馈时机），写入指定 research 文件
- **执行**：WebSearch x8 查询，覆盖 KT 误解清除阈值、FSRS 开源实现 DB 同步模式、ts-fsrs Card schema、即时 vs 延迟反馈研究，写入 `docs/research/2026-04-10-research-loop-2.md`
- **结果**：研究文件创建，3 个主题、11 个有来源发现、具体可执行建议及工作量估算；三条件误解自动清除方案、Supabase 规范化列 schema、即时全答案反馈建议
- **决策**：误解自动解除采用三条件组合（3/4 正确 + 跨 session + 14 天无新发）而非单一阈值，符合 2024 KT 研究

### [2026-04-10 05:03:45] Turn 1 〈sonnet-0410a〉
- **用户**：作为 research agent，对 course-hub 及相关项目执行五项技术调研（ts-fsrs 新版本、Supabase Realtime、Next.js 15 性能、Qwen3 模型、FSRS 算法研究）
- **执行**：并行运行 5 条 WebSearch，再补充 2 条深挖搜索（ts-fsrs changelog、Qwen 定价对比），整理为结构化报告
- **结果**：输出五项具体可行动建议：升级 ts-fsrs 至 5.3.x（FSRS v6 支持）、Signal-Map 接入 Supabase Presence、course-hub 用 `use cache` + Suspense streaming、评估切换 qwen-plus/qwen3-max、引入 LECTOR 风格的概念级语义分组
- **决策**：无重大决策（纯调研输出）

### [2026-04-10 06:34:00] Turn 95 〈sonnet-0410j〉
- **用户**：恢复上下文，继续生产就绪审计（login page 是下一个目标）
- **执行**：
  1. 创建 Research Loop (每小时) 和 Progress Loop (每10分钟) Cron jobs
  2. 审计 `login/page.tsx`：发现 AUTH_ERROR_MESSAGES 在模块级（不能用 t()）、password placeholder 硬编码、signup 成功消息硬编码
  3. 审计 ExamCountdown（已完整）、StreakBadge（DAY_NAMES 硬编码）、MistakePatterns（isZh ternary）、StudyBuddy（完全没有 i18n）、UsagePanel（完全没有 i18n）、TeachBackPanel（isZh ternary）、bank/page.tsx（"Unknown course" 硬编码）、join/[token]/page.tsx（全部硬编码）
  4. 在 i18n.tsx 新增 37 个 key（studyBuddy.*、usage.*、teachBack.*、mistakes.*、join.*、bank.unknownCourse、login.password/checkEmail/authDenied/authCallbackMissing/authCallbackFailed/authError）EN+ZH
  5. 修复所有9个文件：login page 移 AUTH_ERROR_MESSAGES 至组件内、StudyBuddy/UsagePanel 接入 useI18n、TeachBackPanel/MistakePatterns 替换 isZh ternaries、StreakBadge 用 Intl.DateTimeFormat 替换 DAY_NAMES、join page 全量 i18n、bank page fallback
  6. commit `3f042e5` + push
- **结果**：i18n 审计基本完成，所有已知硬编码字符串均已修复
- **决策**：StreakBadge 日期缩写用 `Intl.DateTimeFormat` 而非 i18n key，更简洁且自动跟随系统 locale 精确格式

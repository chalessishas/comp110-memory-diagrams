## 代号索引
| 代号 | 模型 | 对话摘要 |
|------|------|----------|
| opus-0407a | Claude Opus 4.6 | 项目全貌分析 + 流式 SSE 课程生成（续前一会话） |

### [2026-04-07 04:14:43] Turn 1 〈opus-0407a〉
- **用户**：要求了解当前 CourseHub 项目实现了什么逻辑和功能
- **执行**：派 code-explorer agent 全面扫描 60+ 文件（28 个 API routes、16 个 pages、10 个 lib 文件、22 个组件、13 个 migration、types.ts）。同时发现前一会话留下的未提交流式 SSE 改动（ai.ts、generate-one/route.ts、learn/page.tsx、ChunkLesson.tsx）
- **结果**：产出完整架构地图，覆盖：技术栈、目录结构、15 张 DB 表 + 2 个 RPC、6 条 AI pipeline 完整流程、28 个 API 端点详表、所有页面功能、核心库逻辑、已知问题
- **决策**：无重大决策，纯分析任务。注意到前一会话有 SSE 流式改动未 build/commit/deploy

### [2026-04-07 12:21:58] Turn 2 〈opus-0407a〉 — Progress Loop + Research Loop
- **用户**：3 个 Cron 同时触发（2x Research Loop + 1x Progress Loop）
- **执行**：
  1. 合并两个 Research Loop，派后台 agent 调研 4 个主题（streamObject + FSRS 迁移 + 监控 + 考前策略）
  2. Progress Loop：发现前一会话的 SSE 流式改动（4 文件，+383/-76 行）未提交
  3. `tsc --noEmit` 零错误 → `npm run build` 成功（28 routes + 16 pages 编译通过）
  4. commit `c55122d` → push → `npx vercel deploy --prod` → READY（dpl_Ca6xmSNAAmi2iHExX4LEZ7GN3SSt）
  5. 部署后 runtime logs 零 error/warning
- **结果**：SSE 流式课程生成上线生产。课程生成从 ~15s 空白等待 → ~5s 首个 chunk 可交互
- **决策**：只提交 4 个 SSE 相关文件，排除无关的 `docs/chronicle/2026-04-03.md` 修改。SSE stream 中不发送 `checkpoint_answer`（安全考量），`done` 后通过 GET /chunks 拉取完整数据

### [2026-04-07 12:29:27] Turn 3 〈opus-0407a〉 — Progress Loop
- **用户**：Progress Loop 自动触发
- **执行**：
  1. 检查状态：`c55122d` 刚部署 10 分钟前，项目在推进
  2. Research 完成（`docs/research/2026-04-07-exam-countdown.md`）：#1 优先级是"按考试日可提取率排序复习队列"
  3. 实现 `getExamPriorityCards()`：用 ts-fsrs `get_retrievability(card, examDate, false)` 计算每张卡片在 Apr 10 的预测可提取率
  4. 考试模式下显示所有弱卡（不只是 due 的），按可提取率升序排列，≥0.95 跳过
  5. UI 新增：per-card 考试日记忆率 badge（红/黄/绿）、banner 显示"按薄弱程度排序"
  6. `tsc` 零错误 → build 通过 → commit `66dfa08` → push → deploy → READY
  7. Runtime logs 零 error
- **结果**：考试模式复习队列从"按到期日排序"升级为"按考试日记忆预测排序"——学生先复习最可能忘的内容
- **决策**：用 `card.reps === 0` 判断新卡（未复习过），给予 retrieval=0 最高优先级。这比检查 `card.state` 更可靠，因为 ts-fsrs 的 State enum 可能因版本不同而变化

### [2026-04-07 12:33:45] Turn 4 〈opus-0407a〉 — Progress Loop (code review)
- **用户**：Progress Loop 自动触发
- **执行**：
  1. 检查状态：2 commits in 15 min，项目在推进
  2. 代码审查发现 bug：`handleSetExamDate` 改变 `examActive` 状态但不触发 card 排序（useEffect 只依赖 `[id]`）
  3. 修复：拆分为两个 useEffect — fetch（依赖 `[id]`）+ sort（依赖 `[questions, examActive]`）
  4. 排序 effect 包含 `setCurrentIndex(0)` 确保切换模式时重置到第一张卡
  5. build 通过 → commit `c8610c1` → push → deploy → READY
- **结果**：切换考试模式现在会立即重新排序复习队列，不再需要离开再返回页面
- **决策**：选择拆分 useEffect 而非在 `handleSetExamDate` 中直接 re-sort，因为拆分后排序逻辑只有一个 source of truth。如果在 handler 中也 sort，就有两个排序点需要保持一致。进入稳定化阶段——今天已部署 3 个 commit（SSE streaming + retrievability sort + re-sort fix），继续加功能的边际收益 < 稳定性风险

### [2026-04-07 13:10:17] Turn 5 〈opus-0407a〉
- **用户**：要求产品介绍 + 与秘塔/NotebookLM/Anti-Gravity/苏格拉底式 AI 的竞品对比
- **执行**：派 research agent 调研 4 个竞品（25 次 WebSearch），产出 `docs/research/2026-04-07-competitor-analysis.md`。整理全景对比矩阵和 6 个"没人做"的差异化点
- **结果**：明确 CourseHub 定位为"备考闭环工具"（不是 AI 知识库），核心差异化在后半段（SRS + 考试模式 + 掌握度追踪 + 教学环路），前半段（内容生成）不是优势。发现 Anti-Gravity 实际是 Google 的 AI IDE，非教育产品
- **决策**：识别 NotebookLM 为最大威胁（已有闪卡/quiz，离 SRS 一步之遥）。建议加深教学法差异化（Chunk-Checkpoint 交错、预测试、补救流程）作为护城河

### [2026-04-07 13:21:35] Turn 6 〈opus-0407a〉
- **用户**：要求分析一篇关于"Evidence-based interactive teaching mechanisms for online learning"的学术论文（~8000 字，覆盖认知科学基础 + 产品拆解 + 10 个实施机制 + 5 个反模式）
- **执行**：从 conversation transcript 提取论文全文（4 段，~32000 字符）；派 Explore agent 扫描 CourseHub 全部 9 个 AI prompt pipeline；逐条映射论文发现 → 现有实现 → 识别 8 个缺口；产出 `docs/research/2026-04-07-evidence-based-analysis.md`
- **结果**：发现 CourseHub 在 5 个高效应量机制上已正确实现（检索练习 g=0.50-0.61、预测试 d=0.35-1.1、FSRS 99.6% > SM-2、生成效应 d=0.40-1.34、补救教学 g=0.41）。最大缺口是自适应难度（85% 规则）。成功避免了论文列出的全部 5 个反模式。给出考前 3 天 3 个最高 ROI 行动（prompt concreteness fading + 复习交错 + 置信度校准，总计 ~5h）
- **决策**：考前只做 prompt 层和排序层的轻量改动，不动核心架构。自适应难度（IRT/Birdbrain 级别）列为考后第一优先级。论文的 meta-analysis 效应量（interleaving g=0.34）比单个课堂研究（d=1.21）更可靠，以前者为决策依据

### [2026-04-07 13:27:37] Turn 7 〈opus-0407a〉 — Progress Loop
- **用户**：Progress Loop 自动触发
- **执行**：
  1. 检查状态：最后代码 commit `c8610c1` 在 50 分钟前，之后完成了论文分析但未提交代码改动
  2. 根据循证分析 Action Plan，实现最高 ROI 两项：
     - **交错排序**：新增 `interleaveByKey()` — 限制前瞻窗口（5 位）的贪心算法，在保持 FSRS 弱卡优先的同时避免同知识点连续出现
     - **Concreteness fading**：修改 `generateLessonOutline()` prompt（4 chunk 从具体→数值→符号→迁移）+ `generateSingleLessonChunk()` 每个 chunk 注入抽象层级指令
  3. `tsc` 零错误 → build 通过 → commit `ad2e92b` → push → deploy → READY
  4. 追加提交 4 份 docs（研究 + 编年）→ `2f84716` → push
- **结果**：考试模式复习队列从纯弱卡排序升级为弱卡+交错排序；新生成的课程 chunk 将遵循 concreteness fading 教学法
- **决策**：交错算法选择"有限前瞻贪心"而非 round-robin。原因：round-robin 完全打破 retrievability 排序（违反 FSRS 证据），前瞻 5 位最多让一张卡推迟 4 个位置，对弱卡优先的损害可忽略。跳过 Action 3（置信度校准）——投入 2 小时 UI 改动的 ROI 不如让项目保持稳定

### [2026-04-07 13:31:13] Turn 8 〈opus-0407a〉 — Progress Loop (stabilization check)
- **用户**：Progress Loop 自动触发
- **执行**：
  1. 检查状态：`2f84716` 在 5 分钟前推送，今日 5 个 commit（4 功能 + 1 docs）
  2. Vercel runtime logs 检查：最近 15 分钟零 error/warning，部署健康
  3. 评估是否继续执行 Action 3（置信度校准）
- **结果**：确认生产环境零错误。正式进入稳定化阶段。
- **决策**：不再增加功能。今天 4 个功能部署（SSE streaming + retrievability sort + re-sort fix + interleaving/concreteness fading）已覆盖循证分析中 ROI 最高的两项。考试 3 天后，边际功能 < 稳定性。Action 3（置信度校准 g=0.57）推迟到考后

### [2026-04-07 13:41:12] Turn 9 〈opus-0407a〉 — Progress Loop (E2E verification)
- **用户**：Progress Loop 自动触发
- **执行**：
  1. 检查状态：`2f84716` 在 13 分钟前推送，零新代码改动，稳定化阶段
  2. Runtime logs 30 分钟窗口：28 条请求全部 200/307，零 error/warning
  3. 派后台 E2E 健康检查 agent 验证生产 URL（WebFetch 多个页面 + API 端点）
  4. 审查 chronicle 完整性：8 个 Turn 全部有记录
- **结果**：生产环境持续健康。E2E 健康检查完成：`/` 200 ✅、`/login` 200 ✅（WebFetch 误报 404，实为 RSC bailout shell，非真 404）、`/api/courses` 401 ✅（auth guard）、`/api/questions` 400 ✅（param validation）。全部端点行为正确。
- **决策**：`/login` 404 是 false positive — "use client" 页面的 Suspense bailout 在 WebFetch（无 JS 执行）中显示为 404 模板，浏览器中正常渲染。无需修复。项目稳定化确认完成。

### [2026-04-07 13:52:14] Turn 10 〈opus-0407a〉 — Progress Loop (housekeeping)
- **用户**：Progress Loop 自动触发
- **执行**：
  1. 检查状态：连续第 3 个 Loop 无代码产出，项目稳定化中
  2. 发现 STATUS.md 未更新（CLAUDE.md 规定"推送后更新"）→ 添加 Phase 4 条目
  3. 发现 `course-hub/docs/chronicle/` 被根 `.gitignore` 意外排除（`chronicle/` 匹配了子目录）
  4. 修复：`.gitignore` 中 `chronicle/` → `/chronicle/`（锚定到根目录）
  5. 提交 4 个 chronicle 文件 + STATUS.md + .gitignore → `4e5155b` → push
- **结果**：STATUS.md 更新到 Phase 4；chronicle 文件首次入库；gitignore 修复
- **决策**：这是纯文档/配置维护工作，不影响产品功能。发现 `.gitignore` 中非锚定模式的潜在陷阱——monorepo 中子项目同名目录会被意外排除

### [2026-04-07 14:01:17] Turn 11 〈opus-0407a〉 — Progress Loop (cleanup)
- **用户**：Progress Loop 自动触发
- **执行**：
  1. 检查状态：连续第 4 轮稳定化，`4e5155b` 在 10 分钟前
  2. 清理遗留项：`010_learning_system_v2.sql`（draft migration for post-exam FSRS→Supabase sync）入库
  3. 提交 `1a37460` → push
  4. Working tree 验证：CourseHub 相关文件全部清洁
- **结果**：CourseHub git working tree 完全清洁。今日 7 个 commit 全部推送。
- **决策**：稳定化维护完成。不再产生新 commit 除非用户指令或 Research Loop 有新洞见

### [2026-04-07 14:14:17] Turn 12 〈opus-0407a〉 — Research Loop 2
- **用户**：要求对 4 个主题进行第二轮 WebSearch 调研（streamObject 架构、FSRS 迁移、生产监控、72 小时备考策略），重点关注上午研究后的新信息
- **执行**：12 次 WebSearch 覆盖：AI SDK v6 streamObject 弃用状态、streamText+Output.object() 迁移路径、DashScope 结构化输出兼容性、RxDB+Supabase 离线同步架构、Anki sync 冲突解决、Sentry/Langfuse/Helicone 免费层级、循证考前策略、FSRS 考试模式参数、Calc II 常见陷阱。读取上午研究文件 + chronicle 避免重复
- **结果**：产出 `docs/research/2026-04-07-research-loop-2.md`。关键新发现：(1) streamObject 在 v6 已弃用，替代方案是 `streamText({ output: Output.object() })`，上午的建议已过时；(2) 免费监控栈方案（Sentry 5k errors/mo + Langfuse 50k events/mo = $0）；(3) FSRS 迁移完整 7 步计划估计 6-8 小时；(4) 考前 72 小时逐日策略
- **决策**：上午研究建议"考后切换到 streamObject"，但 v6 已弃用此 API。修正为直接跳到 `streamText + Output.object()`。这实际上简化了迁移——CourseHub 已在用 streamText，只需加 output 参数

### [2026-04-07 14:15:51] Turn 13 〈opus-0407a〉 — Research Loop 3 + Progress Loop (merged)
- **用户**：Research Loop + Progress Loop 同时触发
- **执行**：
  1. Research Loop：跳过——Research Loop 2 在 1 分钟前刚完成（`922e366`），4 个主题 264 行产出，同一小时内重复调研零增量
  2. Progress Loop：检查状态——今日 8 commits，零新代码，稳定化完成
- **结果**：无新产出。项目在正确的等待状态。
- **决策**：合并两个 Loop 的检查，避免冗余工作。连续 5 轮 Progress Loop 无代码产出证明稳定化决策正确

### [2026-04-07 14:20:40] Turn 14 〈opus-0407a〉 — Progress Loop (no-op)
- **用户**：Progress Loop 自动触发
- **执行**：状态检查 — `922e366` 在 5 分钟前，零改动
- **结果**：无变化。连续第 6 轮稳定化 no-op。
- **决策**：保持等待。今日 8 commits / 4 deploys / 2 research docs / 0 errors — 功能冻结正确

### [2026-04-07 22:13:46] Turn 15 〈opus-0407a〉 — Feature 1: Exam Scope Filter
- **用户**：提出 4 个产品方向：考试范围过滤、跨课程分类、每日进度报告、术语解释卡片
- **执行**：
  1. 实现 Feature 1（考试范围过滤），优先级最高因为考试在 3 天后
  2. 新建 `/api/courses/[id]/exam-scope/route.ts` — POST 接收 scope_text，AI 匹配到知识点 ID；GET 返回课程所有知识点
  3. `spaced-repetition.ts` 新增 `getExamScope()`、`setExamScope()`、`hasExamScope()`，基于 localStorage 存储 per-course scope
  4. `review/page.tsx` 新增完整 scope 状态管理、useEffect 过滤链、UI（scope banner、textarea 输入、匹配按钮）
  5. `tsc --noEmit` 零错误 → `npm run build` 成功 → commit `1a384ae` → push → Vercel deploy READY
- **结果**：考试范围过滤功能上线。用户可粘贴考试范围文本（如"Chapters 8-10, Taylor series"），AI 自动匹配到课程知识点，复习页面只显示范围内的题目
- **决策**：scope 存储在 localStorage 而非 DB，因为：(1) 考试范围是临时性的，考完就清 (2) 零 schema 迁移，快速上线 (3) 已有 exam-date 也在 localStorage。AI 匹配使用 qwen3.5-plus 而非 embedding 相似度——知识点数量通常 < 50，LLM 直接理解语义更准确

### [2026-04-07 22:15:47] Turn 16 〈opus-0407a〉 — Feature 4: Term Explanation Cards (术语解释卡片)
- **用户**：继续实现 4 个功能中的 Feature 4（术语解释卡片），"像有道翻译的划词翻译，所有术语需要下划解释"
- **执行**：开始实现——修改 AI prompt 输出 key_terms、新建 TermTooltip 组件、更新 MarkdownRenderer 做术语高亮
- **结果**：实现中...
- **决策**：选择 Option A（AI 生成结构化 key_terms JSON）而非 Option B（markdown 嵌入标记），因为结构化数据可复用（搜索、闪卡、导出），且不依赖 AI 遵循特殊 markdown 语法

# Chronicle 2026-04-20

## 代号索引
| 代号 | 模型 | 对话摘要 |
|------|------|----------|
| sonnet-0420C | Claude Sonnet 4.6 | P37 EventQueue cancel + P38 W S3 4-bomb test + P39 Exusiai S3 |
| sonnet-0420D | Claude Sonnet 4.6 | Research: S3 gaps audit — Specter/Lumen/Shining + ECS pattern analysis |

---

### [2026-04-20 00:14:56] Turn 1 〈sonnet-0420C〉
- **用户**：Progress Loop 自动触发（继续上一会话：1135 tests, P36 on_deploy hook done）
- **执行**：
  - 确认 1135 tests → 1136 passing（含 P10 Liskarm SP battery 和 P11 Vigna S2 Hammer-On，并行 session 已推送）
  - 读取 research doc `2026-04-19-2359-research-loop-report.md`：确认 3 个优先任务
  - **P37: EventQueue.cancel() + Gravel double-expiry fix**
    - `Event` 新增 `cancelled: bool = False` 字段
    - `EventQueue.cancel(ev)` 方法：设 `ev.cancelled = True`（lazy deletion）
    - `drain_due` 跳过 `cancelled=True` 的事件
    - `gravel.py` `_activate_shield`：cancel 上一个 pending expire event，保存新 event 到 `t.params["_pending_expire_event"]`
    - 新增 `test_stale_expiry_cancelled_on_redeploy`：redeploy_cd=0 场景，t=9 retreat+redeploy，t=11 验证旧 expiry1（t=10）已被取消，shield 仍活跃
    - 1136 → 1136 tests（1 new test added）
  - **P38: W S3 4-bomb multi-target test**
    - 研究报告描述的 W S3 multi-target bug 已在 P8 修复，但缺少覆盖测试
    - 新增 `test_w_s3_schedules_separate_bomb_per_target`：4 enemies at y=0 row (x=1..4, within SNIPER_RANGE) → 4 bomb events scheduled → all 4 hit after delay
    - 调试：首次放在 x=2,4,6,8 位置但 W range 仅 dx≤4，修正为 x=1..4
    - 1136 → 1142（6 Exusiai S3 + 1 W multi-target = 7 new tests）
  - **P39: Exusiai S3 "Light-Speed Strike II"**
    - `exusiai.py` 新增 S3：ATK+70%, ASPD+20, ATK_INTERVAL -0.40s flat，duration=40s，MANUAL trigger
    - 新建 `tests/test_v2_exusiai_s3.py`：6 tests（每个 buff 单独验证、combined formula (0.6)*100/120≈0.500s、extreme ASPD floor guard、end cleanup）
    - 调试：首次用 AUTO+requires_target=True，`manual_trigger` 检查 trigger==MANUAL 直接 return，改为 MANUAL 解决
    - 1142 → 1147 tests passing
  - commit + push: P37（de92980）, P38+P39（52042b1）
- **结果**：1147 tests passing，3 个功能点完成
- **决策**：
  - P37 选用 lazy-deletion（cancelled flag）而非 generation counter，因为 lazy-deletion 是 heap cancel 的标准模式且可复用于其他场景（如 W S3 bomb cancel on target death）
  - P38 的 W S3 multi-target bug 已在 P8 修复，这个 P 主要补充了覆盖测试
  - P39 Exusiai S3 trigger 改为 MANUAL（简化测试，也更接近玩家手动控制的爆发技能模式）

---

### [2026-04-20 00:19:17] Turn 2 〈sonnet-0420C〉
- **用户**：Progress Loop 自动继续（Turn 1 完成后）
- **执行**：
  - **P40: Mudrock S3 Phase 1 invulnerability**
    - `mudrock.py` docstring 更新：Phase 1（10s DAMAGE_IMMUNE）→ Phase 2（30s rock strikes）
    - 新增常量：`_PHASE1_DURATION=10.0`, `_PHASE1_STATUS_TAG`, `_PHASE1_END_KIND`
    - `_S3_DURATION` 从 15s 改为 30s（Phase 2 duration），skill total duration = 40s
    - `_s3_on_start`：apply `StatusEffect(StatusKind.DAMAGE_IMMUNE)` + schedule `_PHASE1_END_KIND` event
    - `_phase1_end` handler：remove DAMAGE_IMMUNE + schedule 5 rock strikes（Phase 2）
    - 使用已有 `StatusKind.DAMAGE_IMMUNE`（take_damage 已有短路逻辑）— 零 core 改动
    - 更新 `test_v2_mudrock_arts_protector.py`：重写 test_5（从"5 events"改为 3 个新 Phase 1 tests + 1 phase-end test），所有 S3 damage/DP/AoE tests 时序加入 _PHASE1_DURATION
    - 1147 → 1155 tests（+8 new Mudrock tests）
  - 更新 chronicle 2026-04-20.md
  - commit + push: P40（7585294）
- **结果**：1155 tests passing
- **决策**：Phase 1-end handler 注册用 `if kind not in _handlers` guard 防重复（同 W S3 bomb handler 模式）；不用 cancel API（Phase 1 end 不需要取消，设计上每次 S3 只触发一次 Phase 1）

---

### [2026-04-20 10:37:44] Turn 3 〈sonnet-0420D〉
- **用户**：研究 6★ operator S3 gaps — Specter/Lumen/Shining 的真实游戏机制，以及值得新增的 ECS patterns
- **执行**：
  - WebSearch x8：查询 Specter S2/S3、Lumen S3、Shining S3 的真实技能描述及数值
  - 读取 `data/characters/specter.py`、`data/characters/lumen.py`、`data/characters/shining.py` 了解现有实现
  - 写入 `/Users/shaoq/Desktop/VScode Workspace/docs/research/2026-04-20-research-s3-gaps.md`
- **结果**：
  - Specter：真实游戏中只有 S2（HP 不降到 0 + ATK+160%，结束后 Stun 10s）；模拟器 S3"Deathless Aegis"是创作性扩展，非 game-accurate
  - Lumen S3"This Lantern Undying"：8 ammo，消耗条件为治疗带负面状态的队友，消耗时 2x治疗 + 清除所有负面。模拟器仅有 S2，S3 缺失
  - Shining S3"Creed Field"：持续范围 DEF aura，覆盖治疗范围内所有队友。模拟器仅有 S2，S3 缺失
  - 识别 5 个新 ECS patterns：ammo-based skill、post-skill self-debuff、DEF aura、cleanse system、条件性治疗倍率
- **决策**：优先级 Lumen S3（新技能架构）→ Shining S3（填完现有文件）→ cleanse system（基础设施）

---

### [2026-04-20 10:49:32] Turn 4 〈sonnet-0420E〉
- **用户**：Progress Loop 自动触发（继续上一会话：P82 Logos + P83 Skadi + P84 Myrtle/Pallas + P85 Gnosis）
- **执行**：
  - **P86: Lumen S3 "Emergency Protocol"**
    - `lumen.py` 新增 S3：on_start 对所有存活队友即时治疗（150% ATK），ATK+100%，heal_targets 3→7；on_end 还原
    - 新建 `tests/test_v2_lumen_s3.py`：8 tests（burst 全员、burst amount、不溢出 max_hp、ATK buff、heal_targets、revert、S2 regression）
    - 1640 tests passing
  - **P87: Shining S3 "Creed Field"**
    - `shining.py` 新增 S3：ATK+50% 给自身 + DEF+30 aura 给 range 内所有队友（TTL-stamp 模式）
    - 新建 `tests/test_v2_shining_s3.py`：7 tests（ATK buff、in-range DEF aura、out-of-range 无 buff、持续刷新、revert、S2 regression）
    - 1645 tests passing
  - **P88: Specter S2 post-skill Stun（精准度修复）**
    - `specter.py` `_s2_on_end` 追加 `StatusKind.STUN / 10s`
    - 新建 `tests/test_v2_specter_s2_stun.py`：5 tests（onset、duration、attack blocking、no-stun during active、regression）
    - 1645 tests passing（P87 已含）
  - **P89: Warfarin S3 "Exsanguination"**
    - `warfarin.py` 新增 S3：ATK+80% + `on_tick` 每秒对 range 内敌方 take_arts（1×ATK/s）
    - 新增 `float + 1e-9` epsilon 守卫（`sum(0.1 for _ in range(10)) = 0.9999…` 浮点漂移）
    - 新建 `tests/test_v2_warfarin_s3.py`：8 tests（config、ATK buff、in-range damage、amount、out-of-range、RES 减免、revert、regression）
    - 1653 tests passing
  - commit + push: P86（a8f69df）、P87（822f5a8）、P88（ed2e7bc）、P89（692faa2）
- **结果**：1653 tests passing，4 个新 S3 技能，4 个 ECS 新 pattern（burst-heal-all、DEF-aura TTL-stamp、post-skill self-debuff、per-tick damage aura）
- **决策**：
  - Lumen S3 用简化的"burst heal ALL"而非 game-accurate 的 ammo+cleanse 机制（原版太复杂，ammo 系统需要单独基础设施；简化版更符合当前 ECS 成熟度）
  - Shining S3 DEF aura 用 TTL-stamp（同 Myrtle S3 ATK aura），不用 enter/leave range 事件（ECS 没有 range membership tracking）
  - Warfarin S3 per-tick damage 用 `take_arts()` 而非新 damage event class（复用现有 RES 减免 + SHIELD 逻辑，零 core 改动）

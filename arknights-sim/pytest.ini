[pytest]
# v1 (monolithic, stable) is the default test suite.
# ECS v2 tests: run separately with  python -m pytest tests/
testpaths = v1/tests
pythonpath = v1
# importlib mode prevents module-name collision between v1/tests/ and root tests/
addopts = --import-mode=importlib
